# 交付履历台账（Ledger）

> 位置：**工作区根目录下的 `Ledger.md`**（每个工作区一份，跨任务累积）。
> 规则：**只追加，不重写、不删改历史条目**；每完成一个 L2 任务（L1 可选）追加一行。
> **确需更正时的例外口径**（记录与事实不符时）：允许更正，但必须**内联保留原表述**（写成"原写…，更正为…"）+ 在**备注区**给出更正依据的出处，**不得静默改写**。更正不等于删除历史。
> 用途：追溯"谁在什么时候交付了什么、验收结论如何、独立审核结论、是否发生过越界授权"。

## 明细

| 日期 | 入口类型 | 任务 | 交付物 | 验收结论 | 独立审核 | 越界授权 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-10 | 正式需求 | ai-workflow v2.4.3 清理三项遗留 | `scripts/data_query.py`、`scripts/http_fetch.py`、`references/ops.md` | 达成（skill 18/18、plan 6/6） | 未做（规则生效前） | 有：读 `E:\ChatGPT\量化分析与数据分析` 作性能基线（用户「都做」授权） |
| 2026-09-10 | 正式需求 | ai-workflow v2.4.4 工作区边界成为红线③ | `SKILL.md`、`assets/plan-template.yaml`、7 个任务模板 | 达成（skill 18/18、plan 7/7，负向测试 FAIL 生效） | 未做（规则生效前） | 无 |
| 2026-09-10 | 正式需求 | ai-workflow v2.5.0 按标准流程图补齐四环节 | `SKILL.md`、`assets/ledger-template.md`、`references/playbook.md` | 达成（14/14 环节落点命中；skill 18/18、plan 8/8） | **通过**（独立子代理复核，发现 4 项问题已逐条返修） | 无（工作区根 = 技能目录，属基础设施例外） |
| 2026-09-10 | 正式需求 | ai-workflow v2.5.0 用户体验评估（体验报告 + 可优化方向） | `tasks/体验评估-ai-workflow-v2.5.0-2026-09-10/` 下：`体验报告.md`（v3）、`可优化方向.md`（v3）、`体验官A-冷启动.md`、`体验官B-可执行.md`、`独立审核意见.md`、`复核意见-回归.md`、`复核意见-第二轮回归.md`、`plan.yaml` | 达成（plan 7/7；skill 19/19） | **经两轮独立审核＋两轮返修复查，第三轮判「通过（有条件）」**：首轮判不通过（9 条，2 高）；返修后第二轮回归仍 2 条未解决；再返修后第三轮 6 条全清，仅余 3 条标注问题（已修）。**主代理自身 1 条错误降级被审核证伪并撤回** | 无（工作区根 = 技能目录，属基础设施例外；`~/.workbuddy/cache/ai-workflow/` 为缓存例外） |
| 2026-09-10 | **Bug/缺陷** | ai-workflow v2.5.1 修复 2 处真实代码缺陷 + 4 处主干部问题 + 10 项文档收口 | `SKILL.md`、`scripts/setup_env.ps1`、`scripts/checks.py`、`scripts/run_stage.ps1`、`scripts/office_io.py`、`scripts/data_query.py`、`references/ops.md`、`references/playbook.md`、`assets/report-template.md`，回归证据见 `tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/` | **未达成**（本行原写"达成"，2026-09-11 **更正**）：`skill 19/19`、`plan 7/7` 属实，但**不足以支撑"达成"**——对抗式互审坐实 2 处未闭合缺陷（路径候选两条假 PASS 通道）与 3 处虚假自述，10 个文件改动**保持未提交、未被冻结为已完成**。必改项 1–5 已在 **v2.5.2** 修复（见下行） | **两轮独立审核均判「不通过」**（首轮含 1 条「变更日志描述了从未存在的实现」的虚假自述；第二轮为「只改被点名处、未做全库同类清扫」）。2 轮返修用尽后按规则于 2026-09-11 转**对抗式互审**：两子代理**独立同判「不可提交」**并在质证后收敛到同一批必改项（严重 3 / 中 2），主代理逐条复现坐实 → **升级用户**。原此处写"未执行"，已更正为实际结论 | 无（工作区根 = 技能目录，属基础设施例外；已向用户报告 venv 临时改名实验并全部复原） |
| 2026-09-11 | 正式需求 | ai-workflow v2.5.2 流程末梢熔断机制 + v2.5.1 遗留必改项（路径候选收敛 / 记录类更正）一次提交 | `SKILL.md`、`scripts/checks.py`、`assets/plan-template.yaml`、`assets/熔断报告模板.md`、`references/orchestration.md`、`references/quality-gates.md`；v2.5.1 任务目录记录类更正见 `tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/`；回归与对照见 `tasks/技能增强-ai-workflow-v2.5.2-2026-09-11/` | 达成（skill 19/19 FAIL=0；本任务 plan 8/8、熔断门禁 step4 7/7 + step4b 12/12、全量回归 21/21） | **通过**（批A 代码初不通过→修；批B 文档通过；批C 记录初不通过→修；**批D 代码初判不通过**（唯一实质缺陷 M1 残留，针对修复前旧版 `checks.py`）→ 批F 聚焦复核用批D 同套夹具对当前代码重跑确认**通过**；批E 记录重验通过） | 无（工作区根 = 技能目录，属基础设施例外；`~/.workbuddy/cache/ai-workflow/` 为缓存例外） |
| 2026-09-11 | 正式需求 | ai-workflow v2.5.3「独立审核」由强制门禁降级为抽查制（用户决定） | `SKILL.md`、`references/ops.md`、`references/quality-gates.md`、`assets/ledger-template.md`、`assets/templates/UI组件库搭建.yaml`；对照表与验证原始输出见 `tasks/技能增强-ai-workflow-v2.5.3-2026-09-11/`；附加落点（本会话工作区）口径同步 3 处：`工作流瓶颈分析与优化方案-2026-09-11.md`、`工作流实现代码性能分析-2026-09-11.md`、`tools/stage_router.py`；**计划外连带修复**：`scripts/checks.py`（`status` 两处回归，见备注区） | 达成（`checks.py skill` 19/19 FAIL=0；本任务 plan **7/7** FAIL=0；旧口径关键词扫描无**非预期**残留；`status` 修复后「解析失败」残留=0，跨工作区 `E:\ChatGPT\工作流` 的 ⚠ 分支被真实触发） | **抽查豁免**（按新口径未命中必送三类：本改动为流程/文档规则，不涉安全·资金·权限·凭据·不可逆动作；用户当天明示决定；已跑机器门禁兜底。⚠️ 结构性利益冲突见备注区） | 无（工作区根 = 技能目录，属基础设施例外；附加落点 `E:\ChatGPT\工作流` 为本会话工作区，仅同步口径 3 处，已在 plan 与对照表中显式声明） |
| 2026-09-11 | 正式需求 | ai-workflow v2.6.0 落地优化3(阶段路由)+优化2措施3(限流计数器)+优化5措施2(按需加载自检) | `SKILL.md`、`scripts/http_fetch.py`、`references/ops.md` | 达成（skill 19/19、http_fetch.py py_compile 通过、节流 smoke 通过、三调用点 grep 确认） | 抽查豁免（流程/文档/工具规则改动，不涉安全·资金·权限·凭据·不可逆动作；已跑机器门禁兜底） | 无（工作区根=技能目录，属基础设施例外；`~/.workbuddy/cache/ai-workflow/` 为缓存例外） |
| 2026-09-11 | 正式需求 | ai-workflow v2.6.1 把阶段路由接入真实调用（model_tiers.json + ai_call --tier/--ledger + plan 模型档位字段）+ 降本实测 | `scripts/model_tiers.json`、`scripts/ai_call.py`、`assets/plan-template.yaml`、`SKILL.md`、`references/ops.md`、`tasks/技能增强-ai-workflow-v2.6.1-模型路由接入实测-2026-09-11/降本实测.md` | 达成（`ai_call.py` py_compile 通过；路由单测全绿；`model_tiers.json` 合法；`checks.py skill`/`plan` 本任务末跑） | 抽查豁免（流程/工具/文档改动，不涉安全·资金·权限·凭据·不可逆动作；已跑机器门禁兜底。⚠️ 结构性利益冲突同 v2.5.3：改动者自判免审，反证已留 ops.md v2.6.1） | 无（工作区根=技能目录，属基础设施例外；`~/.workbuddy/cache/ai-workflow/` 为缓存例外） |
| 2026-09-15 | 正式需求 | ai-workflow v3.0.0 新增「自主决策层」（能力路由 + 自适应计划 + 决策留痕） | `SKILL.md`、`references/capability-routing.md`、`references/adaptive-planning.md`、`assets/plan-template.yaml`、`scripts/checks.py`、`references/ops.md`；回归/对照/审核见 `tasks/技能增强-ai-workflow-v3.0-2026-09-15/`（`回归证据.txt`、`改动对照表-v3.0.md`、`独立审核意见.md`、`返修复查-独立复核.md`） | 达成（`checks.py skill` 19/19 FAIL=0；23 个 plan **逐项零回归**——FAIL 数与改造前完全一致；门禁阴性对照 T1–T6 全通过） | **通过**（未命中必送三类，但**主动送审**：改动对象是流程本身 + 作者已知弱点「描述与实现脱节」。首轮判不通过、发现 5 项脱节；返修后复查又发现 2 项同类残留；第 2 轮返修后复核**通过**） | 无（工作区根 = 技能目录，属基础设施例外） |
| 2026-09-15 | 正式需求 | ai-workflow v3.0.1 阶段 6 治理工具化（`archive_tasks.py`）+ 任务目录归档 14→8 + 两处措辞脱节修正 | 新增 `scripts/archive_tasks.py`；改 `SKILL.md`、`references/ops.md`；归档清单见 `tasks/_archive/2026-09/README.md`；推送与验收证据见 `tasks/技能增强-ai-workflow-v3.0-2026-09-15/远端推送独立验收.txt` | 达成（`checks.py skill` **20/20** FAIL=0、本任务 plan 10/10；归档 28 文件移动前后 sha256 快照一致；门禁阴性对照：显式指定被引用目录 → 退出码 2 并打印引用出处）。**远端推送独立验收 PASS**：本地 169 blob「缺失 0 / 内容不符 0」，本次删除 22 项确认已消失、写入 39 项 sha 全对，远端与本地逐 blob 一致（本地 `1b75fed` → 远端 `e0937811`） | **抽查豁免**（流程/工具/文档改动，不涉安全·资金·权限·凭据·不可逆动作；已跑机器门禁兜底）＋ **同轮调用 `doc-impl-parity-audit` 做文档↔实现交叉自查**，抓出并修正 2 处存量脱节（`http_fetch.py` 被写成有 `fetch` 子命令（实为单层 argparse）；`data_query.py` 的是位置参数 `path`。）**精度如实记录：6 项命中仅 2 项为真** | 无（工作区根 = 技能目录，属基础设施例外） |

| 2026-09-16 | 正式需求（补记） | ai-workflow v3.2.0 两类资源的分流推送路由（本体 → `ai-workflow-skill`／自研工具 → `ai-workflow-tools`） | 新增 `scripts/push_ontology.py`、`scripts/push_router.py`；改 `SKILL.md`（新小节「两类资源的分流推送路由」四要素）、`references/ops.md`；证据见 `tasks/技能增强-ai-workflow-v3.2.0-2026-09-16/`（`验证输出.txt`、`远端推送验收.txt`、`远端推送独立验收.txt`） | 达成（`checks.py skill` **23/23** FAIL=0；本任务 plan 11/11；判定真伪用 6 个隔离 fixture 证明 **6/6**；两通道 dry-run 前后远端 HEAD 不变）。**远端推送独立验收 7 判据全 PASS**：本地 178 项 ⊆ 远端 244 blob（缺失 0 / 不一致 0），远端独有 66 项经推送前 `base_tree` 证明全部早于本次推送（本地 `2862ccd` → 远端 `db2a3e58`；工具仓 `aecbbe39`） | **抽查豁免**（流程/工具/文档改动，不涉安全·资金·权限·凭据·不可逆动作；已跑机器门禁兜底）。⚠️ 结构性问题同 v2.5.3：本次改动**新增了"改不改送审"之外的推送规则**，仍由改动者自判免审 | ⚠️ **有**：本轮遇到并行会话未提交的 v3.1.1 改动混在同批文件里，且其日志含量化项目内部信息（`BJ920176`、`indicators/volume_caliber.py`、`历史数据集/`、`.scratch/junit_*.xml`），而 `ai-workflow-skill` 是 **public** 仓 → **停下并向用户说明，取得「合并推 v3.1.1 + v3.2.0」明确同意后才推** |
| 2026-09-16 | 正式需求 | ai-workflow v3.2.1 把推送验收器正式化为 `scripts/verify_push.py`（并登记为自研工具） | 新增 `scripts/verify_push.py`；改 `SKILL.md`（分流小节新增 ⑤ 推送后的独立验收、④ 补 `--result-file` 一行）、`references/ops.md`、`scripts/push_ontology.py`、`scripts/push_router.py`；证据见 `tasks/技能增强-ai-workflow-v3.2.1-2026-09-16/`（`验证输出.txt`、`远端推送验收.txt` + `.ontology.txt`、`远端推送验收-轮1日志落盘事故.txt`、`远端推送独立验收.txt`） | 达成（`checks.py skill` **24/24** FAIL=0；本任务 plan 对账 FAIL=0；**阴性对照 5/5**——破损输入（错误期望 HEAD / 过老基线 / 错误版本号 / 脏工作区 / 不存在的工具仓）均真报 FAIL）。**独立验收 7 判据全 PASS**（OK 36 / FAIL 0 / SKIP 0 / WARN 1，WARN 为自指边界，见备注）：本地 183 项 ⊆ 远端 249 blob（缺失 0 / 不一致 0），4 个自研工具在**两仓 blob sha 逐一相等**且等于本地独立重算值（本地 `8455602` → 远端 `1f79c6f3`；工具仓 `2f5aa4fd`） | **抽查豁免**（工具/流程/文档改动，不涉安全·资金·权限·凭据·不可逆动作；已跑机器门禁兜底）＋ **本轮实质是"给验收环节本身补工具"**，故以 dogfooding 自证：用新验收器验它自己的推送 | 无（工作区根 = 技能目录，属基础设施例外）。⚠️ 一处越界**已清理**：实测 `--result-file ../tasks/x` 时工具在技能根之外**新建了目录并写入文件**，已删除（`C:\Users\<用户名>\.workbuddy\skills\tasks\`），并加「越出技能根即拒绝写入」防线 |
| 2026-09-16 | 正式需求 | ai-workflow v3.4.0 执行《性能瓶颈分析与提速方案》P1–P9 + 两仓推送同步（用户：「直接开始执行该任务，完成后…推送到 GitHub 仓库，确保远程仓库与本地保持一致」） | 新文件 7（`scripts/gen_skill_index.py`、`scripts/perf_baseline.py`、`references/changelog.md`、`references/push-routing.md`、`references/security-guide.md`、两份推送证据）；改文件 25+ | **达成**：`checks.py skill` **26/26 FAIL=0**；P3/P8 功能断言 **10/10**（阴性对照确实会响）；新旧 `checks.py skill` 输出**逐字节一致**（各 1148 B，sha1 同 `0d0d8905…`）；**4 份真实历史 plan 零误伤**；同树对照耗时 **−79.2%**（1114.8→232.0 ms）、`mark --batch` **−87%**；`ops.md` −70% tok | **通过**（`verify_push.py` 独立验收，按通道分跑）：通道 A **OK 49 / FAIL 0 / SKIP 0 / WARN 0**；通道 B OK 20 / FAIL 1（`.gitignore` 同路径撞车，结构性，非本次动作错误） | 无（技能生成所需越界属豁免；证据文件**先于**推送提交，未用 `--allow-dirty` 绕过门禁） |
| 2026-09-17 | 正式需求 | 修复 `.gitignore` 同路径撞车（v3.4.0 遗留的**结构性开放缺陷**，修法 2：工作区侧弃权）。用户裁决「修法**你定**」+ 越界授权「仅选定修法所需路径」 | 工作区仓 `E:/ChatGPT/工作流`：改 `.git/info/exclude`（忽略规则逐条迁入）、提交 `8fb80fc`（`.gitignore` 退出跟踪，1 file changed / 18 deletions）；证据见 `tasks/修复-gitignore同路径撞车-2026-09-17/`（`plan.yaml`、`tmp/修复前快照.txt`、`通道B-验收.txt`、`口径更新待办.md`） | **达成**：`verify_push.py --skill-dir E:/ChatGPT/工作流` 通道 B **FAIL 1 → FAIL 0**（OK 17 / FAIL 0 / SKIP 3 / WARN 1）。**阴性对照**：同一验收器对修复前 rev `7011bf0` 仍判 `[FAIL] 不一致 1 项`、对修复后 `8fb80fc` 判 `[OK] 不一致 0 项`，唯一变量即 `.gitignore` 是否在 rev 树内。忽略规则迁移**逐条无损**（把 `.gitignore` 临时移开后，原 12 条规则全部改由 `.git/info/exclude` 命中）。技能本体 `git status` 改动数 **0** | **抽查豁免**（改的是本机工作区仓的跟踪清单与忽略规则，不涉安全·资金·权限·凭据·不可逆动作；唯一对外面是 GitHub **只读** API 的验收查询，无写入。已跑机器门禁兜底） | **有（当次授权，具体到路径）**：读 `E:/ChatGPT/工作流/.gitignore`；读+写 `E:/ChatGPT/工作流/.git/info/exclude`；写工作区仓索引与 HEAD（`git rm --cached` + **本地** commit，**未推送远端**）。**未做目录级泛授权** |

| 2026-09-17 | 正式需求 | **ai-workflow v3.5.0 落地《5.12 改进建议优先级》六项**（P0-1 红线③机器化／P0-2 出站扫描第 4 项提升为推送前判据／P0-3 口径守卫／P1-1 自研工具漏登记检测＋文案订正／P2-2 阶段 2·4 最低成本门禁／P2-3 同步 `push-routing.md` 过时口径）。用户指令：「对 ai workflow 进行优化，按照你给出的优化建议」 | 改 `scripts/checks.py`（新增 `_check_scope`／`_check_parity`／WARN 通道／`_check_stage_gates`／`_selftool_ledger`）、`scripts/push_router.py`（出站扫描接入 + 收窄 `CODE_EXT`）、新增自研 **`scripts/outbound_scan.py`**（**待推送**）、改 `SKILL.md`／`references/security-guide.md`／`references/push-routing.md`／`references/changelog.md`／`assets/plan-template.yaml`；任务档案见 `tasks/技能增强-ai-workflow-v3.5.0-2026-09-17/`（`出站扫描-本机绝对路径-清单.md`、`对账输出.txt`、`技能自检-v3.5.0.txt`、`回归-历史plan-v3.5.0.txt`、`基线对照-历史plan.txt`、`tmp/verify-P0-1·P0-3·P1-1·P2-2.txt`） | **达成**：`checks.py skill` **33/33 FAIL=0**；本任务 `checks.py plan` **FAIL=0**（SKIP=1 = 自研工具链接仍为「待推送」）；**历史 plan 全量回归 21 份、新增 FAIL=0**（对照物 = git `3dfcea3:scripts/checks.py`，逐份比对 FAIL 数，不是靠归因推断）；四组正负对照共 **16 例**全部符合预期且**原始输出未做关键词过滤**。⚠️ **本轮未推送远端**（下详） | **抽查豁免**（改动为流程／门禁／文档规则，不涉资金·权限·凭据·不可逆动作；用户未点名；未发生返修触顶。已跑机器门禁兜底） | 有（**基础设施例外**，非越界）：读 `C:/Users/<用户名>/.workbuddy/binaries/python/envs/ai-workflow/`（技能自身 venv，差异是发现通用解释器缺 pyyaml 导致 21 条假绿）。**已登记但未使用**：`E:/ChatGPT/工作流/Ledger.md`（该任务工作区根即技能根，履历记入技能根 `Ledger.md`，避免把技能任务误记为他处任务） |

| 2026-09-21 | content（self-judge） | **P0 建模落地：plan 中心数据模型（枚举集中 + 结构 schema）／熔断·步骤状态机形式化**（承接「工作流建模分析-2026-09-21」的 P0 两项；用户授权 D1=A/D2=A/D3=A 并指令「直接开始执行，无需再确认」） | 技能本体 **5 文件**：新增 `references/data-model.md`、`references/state-model.md`；改 `scripts/checks.py`（`PARITY_ITEMS` 7 项源规格 → 多源）、`assets/plan-template.yaml`、`SKILL.md`。任务档案 `tasks/P0建模落地-2026-09-21/`（`P0执行计划.md`、`P0完成报告.md`、`plan.yaml`、`tmp/` 11 文件含 6 份原始门禁输出与 3 个探针脚本） | 达成（`checks.py skill` **43/43 FAIL=0 SKIP=0 WARN=0**；`guard_constants` **7 组、漂移 0 组** + `--selftest` **4/4**；状态机对拍 **6/6** —— 含 **C2 阴性对照**「仅复位 `meta` 而步骤仍熔断 → **仍 FAIL**」，证明复位两步缺一不可；既有 plan 对拍 **FAIL=0**）。**两项不得读成绿**：① 散文式枚举重述（能力四类名／模型档位名）**未逐一清除**，仅靠人审（已登记为 data-model §3.2 诚实边界）；② 状态**迁移合法性无机器校验**（D3=A 决策，留作 P1 候选） | **抽查豁免**（文档建模 + 技能判据类产出，不涉安全·资金·权限·凭据·不可逆动作；用户明示「无需再确认」；未发生返修触顶。已跑机器门禁 + 阴性对照兜底） | 有（**技能生成例外**，已登记 `meta.越界授权`）：写 `C:/Users/<用户名>/.workbuddy/skills/ai-workflow/`（新增 2 + 改 3）。全量备份 `.workbuddy/tmp/skill-backup-P0-20260921-155450/`（`_manifest.txt` 含 sha256）。**未推送任何远端** |
| 2026-09-21 | code（self-judge） | **P1/P2 建模落地：决策模型（决策表）／策略模型（gate.py+S1-S4）／流程模型（六阶段活动图+R/F/T 节点）／路由模型（阶段×档位+category 联动）**（承接「工作流建模分析-2026-09-21」的 P1/P2；用户授权「直接开始执行，无需再确认」） | 技能本体 **5 文件**：新增 `references/decision-tables.md`、`references/policy.md`、`references/process-model.md`、`references/routing-table.md`；改 `SKILL.md`（手册与脚本索引 +4 行；自主决策层／阶段感知模型路由／信息安全 三处散文转引用指针）。任务档案 `tasks/P1P2建模落地-2026-09-21/`（`plan.yaml`、`trace.jsonl`、`_metrics.jsonl`、完成报告、`tmp/` 门禁输出与交叉核对脚本 `verify_p1p2.py`） | 达成（`checks.py skill` **43/43 FAIL=0**；`verify_p1p2.py` 交叉核对 **R1-R6 / T1-T3 / 四类能力 / 终止策略 / M1-M5 与源手册逐项相等、新文档零本机绝对路径**；`checks.py plan` **FAIL=0**（修复前曾 FAIL=5，因交付物误用相对路径，已改绝对技能根路径）；`guard_constants` **7 组、漂移 0**；出站扫描 **FAIL=0**（WARN=1 为 `SKILL.md:99` 既有内部路径，非本任务新增）；`trace.jsonl`/`_metrics.jsonl` 已补记）。**两项不得读成绿**：① 决策表只做**集中映射、不重述枚举取值、不新增守卫解析器**（取值单一源 = `checks.py` 常量 + `data-model.md` 守卫镜像，沿用 P0-2 D2 零新正则）；② 流程模型为**纯文档/可视化**，未新增门禁 | **抽查豁免**（文档建模 + 技能索引类产出，不涉安全·资金·权限·凭据·不可逆动作；用户明示「无需再确认」；未发生返修触顶。已跑机器门禁 + 交叉核对 + 出站扫描兜底） | 有（**技能生成例外**，已登记 `meta.越界授权`）：写 `C:/Users/<用户名>/.workbuddy/skills/ai-workflow/`（新增 4 文件 + 改 `SKILL.md` 索引与 3 处引用）。**未推送任何远端**（推送须单独确认） |
| 2026-09-22 | content（self-judge） | **检索快判（retrieval-judge）落地：Jev 式 System 1 快判的第三块拼图**（用户：「请建立一个合理的模型来帮助 ai 在工作时进行合适的检索，可参考 jev 的设计理念」）。判「该不该查／查哪类源／能否断言『不存在』」，与既有 `self-judge`（认知轴）／`method-judge`（手段轴）构成三层正交 | 技能本体 **7 文件**：新增 `references/retrieval-judge.md`（契约 §1–§10 + 附录 A + 变更记录，与另两个 judge **严格同构**）；改 `scripts/checks_core.py`（+4 常量）、`scripts/checks_judges.py`（+`_check_retrieval_verdict`／`_check_retrieval_sample`）、`scripts/checks.py`（+2 调用）、`assets/plan-template.yaml`（+`检索判定` 字段块）、`SKILL.md`（v4.4.2→**v4.5.0**；description 补触发锚；手册索引 +1 行；阶段 3 第 5 条前置触发说明）、`references/decision-tables.md`（新增第六张「检索快判决策表」6.1–6.3，章节重编号，变更记录 v2）。任务档案 `tasks/技能增强-ai-workflow-v4.5.0-2026-09-22/`（`plan.yaml`、`验收报告.md`、`trace.jsonl`、`_metrics.jsonl`、`tmp/` 7 份门禁原始输出 + 3 个阴性对照夹具） | 达成（**从工作区根**调用：`checks.py skill` **47/47 FAIL=0 SKIP=0 WARN=0**；`checks.py plan` **FAIL=0**（2 WARN 为抽样指路，不计入退出码）；`guard_constants` 漂移 **0**（7 组中 3 组因 `members` 路径问题报漂移，**已核实为既有缺陷、非本次引入**）；`outbound_scan` FAIL=0）。**三组阴性对照全中**：缺 `needs_retrieval` → 退出码 1；`+` 组合源类 → 退出码 0；源类越界「搜索引擎」→ 退出码 1。**向后兼容**：既有 plan 2 份「检索判定」项均为 WARN，**新增 FAIL=0**。**同构自证（机器验证）**：三份契约 `^##` 级标题比对 → 各 12 节、**11 节完全相同**，唯一差异为附录 A 标题。**两项不得读成绿**：① §5.1「否定断言证据门槛」**不可机器强制**（判的是「有没有出示 0 命中输出」这一**证据存在性**，非取值合法性），仅靠人审 + 抽样指路；② 附录 A 未直读三份 JEV 设计文档原文（越界未授权），属**基于公开检索的复述**，已在文中显式声明 | **抽查豁免**（流程／门禁／文档规则改动，不涉安全·资金·权限·凭据·不可逆动作；用户未点名；未发生返修触顶。已跑机器门禁 + 三组阴性对照兜底）。⚠️ 结构性利益冲突同 v2.5.3：本次新增的正是「检索该不该发生」的判据，仍由改动者自判免审 | 有（**技能生成例外**，已登记 `meta.越界授权`）：写 `C:/Users/<用户名>/.workbuddy/skills/ai-workflow/`（新增 1 文件 + 改 6 文件）。**未推送任何远端**（推送须单独确认） |

| 2026-09-22 | content（self-judge） | **项目 Git 开发规范落地（v4.6.0）**：把用户给定的 5 条 Git 规则（`main` 稳定／`dev` 开发／`exp-xxx` 并行实验／commit 注释 4 类／五类禁提交 + `.gitignore`／五步开发流程）落成**唯一事实源手册 + 只读机器校验 + 口径守卫三源同源**。用户原话：「我需要你在 ai-workflow 工作流中添加项目 Git 开发规范……后续编写代码、修改方案、生成 commit 信息时，请严格遵守这套规则。」 | 技能本体 **9 文件**：新增 `references/git-conventions.md`、`assets/gitignore-template.txt`、`scripts/git_check.py`；改 `scripts/checks_core.py`（+`VALID_COMMIT_TYPES`）、`scripts/checks_parity.py`（+第 8 项守卫「提交类型」三源）、`references/data-model.md`（§2.1 索引 + §2.2 取值表）、`references/playbook.md`（+第六类「代码／Git 项目类」）、`SKILL.md`（新章节 + 手册索引 + description + 版本 4.5.0→**4.6.0**）、`.gitignore`（补密钥类）。任务档案 `tasks/技能增强-ai-workflow-v4.6.0-2026-09-22/`（`plan.yaml`、`确认表.md`、`门禁输出.txt`） | 达成（**从工作区根**调用：`checks.py skill` **49/49 FAIL=0 SKIP=0 WARN=0**，较 v4.5.0 的 47/47 **+2 项**＝新增守卫及其解析；`checks.py plan` FAIL=0）。**阳性对照 2 例全绿**（`feat:` 基础型／`fix(scope):` 带可选 scope 型）；**阴性对照 3 例**：缺类型前缀 → RC=1、`chore` 未登记 → RC=1、无信息描述 → **RC=0 但 WARN**（证明「格式判据」与「质量判据」分离，机器不越权判质量）。**口径守卫两组阴性对照全中**：手册侧注入 `chore` → FAIL、工具侧常量注入 `chore` → FAIL，恢复后复绿 49/49。⚠️ **一处设计缺陷由守卫在首次运行中自行报出、未流到交付**：`_p_commit_types` 首版沿用「全文件扫格式」范式，误把 §2.1 分支职责表首格的 `main` / `dev` / `exp-xxx` 并入提交类型集合（实测 `解析得 dev/docs/exp-xxx/feat/fix/main/refactor ≠ docs/feat/fix/refactor`）→ 改用**表头结构性锚定**（表头唯一，不靠"别处别用这种格式"的纪律）。**三项不得读成绿**：① 并集模型「**多写会 FAIL、少写不会**」是覆盖边界，非全向覆盖；② `branch` 判不出"是不是新功能"，只 WARN；③ `ignore` 是启发式，非标准路径名会误报。 | **抽查豁免**（规范／门禁／文档改动，不涉安全·资金·权限·凭据·不可逆动作；用户未点名；未发生返修触顶。已跑机器门禁 + 5 例对照 + 2 组守卫阴性对照兜底）。⚠️ 结构性利益冲突同 v2.5.3：本次新增的正是「commit 该怎么写」的判据，仍由改动者自判免审 | 有（**技能生成例外**，已登记 `meta.越界授权`）：写 `C:/Users/<用户名>/.workbuddy/skills/ai-workflow/`（新增 3 文件 + 改 6 文件）。**未推送任何远端**（推送须单独确认） |

| 2026-09-23 | content（self-judge） | **作业模式（v4.7.0）落地 + 第四块 Jev 式快判 `homework-judge`（模式选用·结构级）**。用户原话：「在 ai-workflow skill 中新增一个『作业模式』，专门用于处理作业类问题，需符合大学生解题流程（题目理解与已知条件提取／思路分析与知识点关联／分步骤推导或求解／关键步骤解释／最终答案呈现与验证），并针对不同题型（数学计算、编程实现、论述写作）提供相应的解题结构」；追加「**也要让 laya 决策模型和 jev 的理念层面可以判断是否选择此模式**」 | 技能本体 **新建 3**：`references/homework-mode.md`（判据语义源：C1–C3 识别三判据 / H1–H5 五阶段结构 / 三类题型骨架 / I1–I4 作答纪律）、`references/homework-judge.md`（第四份 judge 契约，与另三份严格同构）、`assets/templates/作业解题.yaml`（L1 模板）；**改 15+**：`SKILL.md`（4.6.1→**4.7.0**：新章节 + 阶段 0 作业识别 + description 触发锚 + 手册索引 +2 + 核心命令 +`homework-judge` + 决策表计数订正）、`scripts/checks_core.py`（+5 常量）、`scripts/checks_parity.py`（+第 10 项守卫「作业模式」三源）、`scripts/checks_judges.py`（+检查项 18/19）、`scripts/checks.py`（+2 调用）、`scripts/laya_client.py`（+`homework_judge` 分支 + **回环代理旁路修复**）、`scripts/judges.json`、`assets/plan-template.yaml`（+`作业判定`）、`references/data-model.md`、`references/decision-tables.md`（+第七张表，§七/§八 顺延）、`references/playbook.md`、`references/quality-gates.md`、`references/ops.md`、`references/laya-backend.md`（+§4.1）、三份既有 judge 手册的变更记录/时点注记。任务档案 `tasks/作业模式-ai-workflow-v4.7.0-2026-09-23/`（`plan.yaml`、`确认表.md`、`验收报告.md`、`自检报告.md`、`trace.jsonl`、`tmp/` 5 个自证/对照脚本 + 3 份原始输出） | 达成（`checks.py skill` **52/52 FAIL=0**，v4.6.1 为 49/49 **+3**；离线自证 `_offline_check.py` **FAILS=0**（6 节）；阴性对照 `_negative_controls.py` **FAILS=0** —— 口径守卫注入 `讲解` → **FAIL**、sha256 恢复后**复绿**，plan 判据 5/5 用例符合预期；CLI 端到端服务未运行 → **exit 3 + `{"degraded":true}`**，属设计内降级）。**三项不得读成绿**：① 作业识别是**启发式、无机器判据**；② `meta.作业判定` 缺省**只 WARN**、**不是门禁**；③ I1–I4 是**纪律项**，机器不校验。**另拦下两处真实缺陷（均未流到交付）**：① 本机 `HTTP_PROXY` 把**本地回环**请求答成 502 → 服务活着却被**静默降级**（修法：回环走 `ProxyHandler({})`，配连接类异常阴性对照）；② `homework-mode.md` 变更记录声称"`quality-gates.md` §二 补一行"而**实际未落**（grep 零命中）—— 已补，属本技能记录过 3 次的高发病灶「描述与实现不符」，本轮靠"写完变更记录回查原文"拦下 | **抽查豁免**（结构/门禁/文档改动，不涉安全·资金·权限·凭据·不可逆动作；用户未点名；未发生返修触顶。已跑机器门禁 + 离线自证 + 阴性对照兜底）。⚠️ 结构性利益冲突同 v2.5.3：本次新增的正是"该不该走作业结构"的判据，仍由改动者自判免审 | 有（**技能生成例外**，已登记 `meta.越界授权`）：写 `C:/Users/<用户名>/.workbuddy/skills/ai-workflow/`（新增 3 + 改 15+）。**未推送任何远端**（推送须单独确认） |

| 2026-09-23 | code（self-judge） | **模式选用聚合模型（v4.7.0 补）**：为 `homework-judge` 补上缺失的**聚合**一段 —— W1–W5 维度分按确定性公式聚合成 `mode`/`distribution`/`confidence`/档位/派生字段，即 **Realization A 的可执行半边**；顺带提供 A/B 一致率工具，**闭环契约 §7.3 一直无法闭环的出口条件**。用户原话：「如果需要的话请建立一个模型」（条件式授权，判断依据来自契约 §7.3 自己登记的「未闭环」） | 技能本体：**新增** `scripts/homework_model.py`（聚合内核 + 契约装配 `to_yaml_block` + 一致率 `agree` + `selftest` + CLI；**纯标准库**）；**改 4**：`references/homework-judge.md`（+**§5.1 聚合规则（唯一口径源）**、+**§5.2 一致率出口条件**、§7.3 加时点注记、变更记录 v1.1）、`references/ops.md`（速查表 +1 行）、`SKILL.md`（作业模式节 +聚合与一致率段、核心命令 +1 行）、`references/laya-backend.md`（§5 出口条件加时点注记）。任务档案 `tasks/模式选用模型-ai-workflow-v4.7.0-2026-09-23/`（`plan.yaml`、`确认表.md`、`自检报告.md`、`trace.jsonl`、`_metrics.jsonl`、`tmp/` 4 脚本 + 3 份原始输出） | 达成（`checks.py skill` **53/53 FAIL=0**，较 v4.7.0 的 52/52 **+1**＝新增脚本的 py_compile 项，**非判据变化**；本任务 `checks.py plan` **FAIL=0**，其中 `meta.作业判定 合法` 为 OK——**即模型产出的判定块确实能过检查项 18**；`homework_model.py selftest` **FAILS=0（18 项）**；独立进程**前后哈希**一致）。**三组阴性对照必须"有牙"**：注入分歧 → 一致率 0.0；**破坏锚点 → 锚例命中 8/8 掉到 1/8**；缺维度/越界 → 抛 ValueError（不静默）。**四条不得读成绿**：① selftest 全绿只证「实现与公式自洽」，**不证判得对**；② 锚例 8/8 命中的维度分是**本模型推定**、非真实标注；③ 一致率工具**不产生样本**（真机 Laya 样本 **0 条**）；④ `ANCHORS`/`TAU` 是**未拟合先验**，`distribution`/`confidence` 只可用于对照与回归 | **抽查豁免**（脚本/契约/文档改动，不涉安全·资金·权限·凭据·不可逆动作；用户未点名；未发生返修触顶。已跑机器门禁 + selftest + 3 组阴性对照 + 独立进程哈希兜底）。⚠️ 结构性利益冲突同 v2.5.3：本次新增的正是「怎么把维度分算成模式」的判据，仍由改动者自判免审 | 有（**技能生成例外**，已登记 `meta.越界授权`）：写 `C:/Users/<用户名>/.workbuddy/skills/ai-workflow/`（新增 1 脚本 + 新建 1 任务目录 + 改 4 文件）。**未 commit、未 push**（推送属不可逆动作，须用户单独确认） |

| 2026-09-23 | code（self-judge） | **作业判定判据加固（v4.7.0 补 2）**：用户问「有没有需要优化的内容」→ 审查发现 **4 处实质问题**并逐条处置。① **检查项 18 漏网 4 条**（实测复现）：负概率可配平通过（`{作业题:0.9, 讲解题:0.15, 非作业:-0.05}` 和=1 且其余全合法）、`dimensions[W1]=9.9` 通过（只查键不查值）、`confidence=0.99` 与 `max(distribution)=0.42` 自相矛盾通过、`mode=作业题+作业题` 重复组合通过 → 已补 **N1–N4**（均属**确定性不一致**）。② **派生字段取错源**（比漏网更隐蔽）：`confidence` 取自未 round 的 `probs[top1]` 而定稿 `distribution` 经 round(4)+归一化 ⇒ 差约 2e-4 的**纯噪声**与真漂移同形；修法是**把定稿提前、所有派生字段在定稿后取值**（实测 3000 组随机维度偏差 **= 0**，严格恒等）。③ **文档与实现不符（第 5 次同型）**：`_nh_of_mode` 注释写「三处同源」，实际有第四处 `laya_client.map_homework_judge`（B 侧独立实现）→ 更正为四处并登记为**已知边界**（不收敛为 import：laya_client 有第二份物理拷贝）。④ **自证循环嫌疑→实测为阴性**：留一法 8/8、全量均值锚点 8/8、随机锚点均值 3.0/8 ⇒ 锚点**未被过拟合**，8/8 反映样例可分；但锚例维度分仍为**推定值**，故**仍不是准确率证据** | 技能本体 **改 3**：`scripts/checks_judges.py`（检查项 18 +4 条判据；`bad` 显示由前 3 条改为前 4 条并报总数）、`scripts/homework_model.py`（`distribution` 定稿**提前**，`mode`/`confidence`/`route_hint`/`needs_homework`/`closest` 全部改为定稿后派生；docstring 补「留一法复核」与「第四处载体」）、`references/homework-judge.md`（§5.1 末 +2 条引用块：留一法结论 + 四处载体；§5.2 未闭环表 +1 行）、`_archive/changelog.md`（+v4.7.0 补 2 条目） | 达成（`_verify_gate18.py` **FAILS=0**：4/4 违规被拦、合规块与组合态块**均不误伤**、3000 组派生恒等、2000 组 `closest`/`needs_homework` 与 argmax 同源 0 不一致；`homework_model.py selftest` **FAILS=0**；`checks.py plan` 两份 **FAIL=0**；`checks.py skill` **53/53 FAIL=0**）。**先量影响面后落地**：全库 33 份 plan 中含 `作业判定` 者仅 2 份，补后**新增 FAIL = 0**。⚠️ **本轮自踩一坑（已记）**：审计首版用「重定向 stdout 搜 `[FAIL]`」判是否被拦，而 `checks_core.fail()` **只 append 不打印** ⇒ 违规与合规**一律判成放行**，4/4 全绿**全是空的**；修正为读 `checks_core.results` 后才真拦住。另：首版用例用 `confidence=1.5`，**旧判据就能拦**，属"旧版已覆盖"而非"漏网"，已改为**只违反新判据**的隔离用例 | **抽查豁免**（判据/脚本/文档改动，不涉安全·资金·权限·凭据·不可逆动作；用户未点名；未发生返修触顶。已跑机器门禁 + 4 组阴性对照 + 影响面审计兜底）。⚠️ 结构性利益冲突同 v2.5.3：本次加固的正是检查项 18，仍由改动者自判免审 | 有（**技能生成例外**，已登记 `meta.越界授权`）：写 `C:/Users/<用户名>/.workbuddy/skills/ai-workflow/`（改 3 文件 + 任务目录 tmp 落原始输出）。**未 commit、未 push** |

| 2026-09-23 | code（self-judge） | **把「靠人工双写」的两处变成有机器信号（v4.7.0 补 3）**：① 第四处派生载体（`laya_client.map_homework_judge`）从**无判据**变为**有判据** —— 在 `homework_model.py selftest` 第 ⑪ 项 import laya_client 并断言**行为等价**（同一分布下 `_combine` 与 `needs_homework` 必须一致，4 组实测 0/4 不一致），**任一侧改口径即 FAIL**；不引入生产依赖（收敛成 import 会把"一个文件双写"膨胀成两个）。② 新增第 ⑫ 项**双拷贝漂移探测器**（只报不 FAIL），**实测出部署侧副本已漂移**：技能侧 476 行 / 部署侧 385 行，共享口径函数 3 个**逐行一致**，但部署侧**缺 `map_homework_judge` 与 `_opener`**（代理修复未同步），`app/judges.json` **无 `homework_judge`**。③ 拟加「route_hint 与 mode 严格相等」判据 —— 影响面 **2/2 不匹配**（人工撰写指引），会 100% 误伤，**不做**（负结论已登记） | 技能本体 **改 3**：`scripts/homework_model.py`（selftest +第⑪行为对等 / +第⑫双拷贝探测；+`import ast`）、`assets/plan-template.yaml`（`作业判定` 注释补**值域四条**）、`references/laya-backend.md`（**+§8 客户端双拷贝漂移** + 排障表 +1 行）；`_archive/changelog.md` +1 条目 | 达成（`homework_model.py selftest` **FAILS=0**；`_verify_gate18.py` **FAILS=0**；`checks.py plan` 两份 **FAIL=0**；`checks.py skill` **53/53 FAIL=0**）。**影响边界（如实）**：部署侧副本仅冒烟测试引用、**服务进程不依赖** ⇒ **不阻塞真机 B 侧采样**；同步该副本**属技能目录外写入，须用户授权，本轮未改**。⚠️ **自踩一坑（第三次"取错形状"）**：探测器首版用 `Path.home()` 定位副本，沙箱下指到 `E:\WBHomes\A` ⇒ 找不到目标却打印"仅技能内一份"，**把真实漂移盖掉**；改为技能根反推。**被门禁当场拦一次**（§8 写了两个 `.py` 文件名 → 文档引用完整性 FAIL），改后复绿 —— 再次验证"新内容第一次被拦的是引入它的那次改动" | **抽查豁免**（脚本/文档改动，不涉安全·资金·权限·凭据·不可逆动作；用户未点名；未发生返修触顶。已跑机器门禁 + 行为对等判据 + 影响面审计兜底）。⚠️ 结构性利益冲突同 v2.5.3：本次新增的正是跨实现一致性判据，仍由改动者自判免审 | 有（**技能生成例外**，已登记 `meta.越界授权`）：写技能目录（改 3 文件）。**未 commit、未 push** |

## 备注区

- **2026-09-11 更正说明**：v2.5.1 行（2026-09-10）原记「验收结论=达成」「独立审核=…未执行，已上报用户待决」，两处均与事实不符，已按对抗式互审裁决逐处更正并保留原表述痕迹。更正依据：`tasks/对抗互审-ai-workflow-v2.5.1-2026-09-11/裁决.md` §5。这是**「描述与实现不符」第 3 次复发**——故 v2.5.2 起把熔断做成**机器门禁**（`checks.py plan`），不再依赖自述。
- 2026-09-10：v2.5.0 起「独立审核」为强制门禁，执行者不得自审；返修后必须逐条回归。首次实跑：本任务的独立审核发现 2 中 2 低共 4 项问题（质量把关与独立审核口径冲突、豁免条件不封闭、阶段 5/6 验收职责边界不清、手册索引描述过时），已全部返修并复查。
- 2026-09-11：**对抗式互审首次实跑**（`SKILL.md:120` 的第三道门禁）即产出常规审核抓不到的东西——`checks.py` 的 `base.parent` 假 PASS 通道（使交付物落在**工作区根之外**也判通过，撞红线③）**两轮常规独立审核都未发现**，本轮被两个子代理从不同路径分别发现、并由主代理复现坐实。同时坐实 v2.5.1 的两处**虚假自述**：变更日志与 `plan.yaml` 称跨技能兜底「**彻底移除**」，实为收窄为固定 `~/.workbuddy/` 前缀（同型通道仍在，实测可救活缺失交付物）；`Ledger` 下行所记「已全部修正」同属不实——**「描述与实现不符」至此第 3 次复发**。另一条硬教训：**审计证据文件被污染会摧毁整条回归链的可信度**——`回归证据.txt` 里安全策略拦截回执（`SAFE_DELETE_BULK_CONFIRM_REQUIRED` JSON）被当成命令输出写入，且「回归 8/9」两段各重复 6 次，该文件须整份重写。
- 2026-09-11：**批D 与批F 的「不同时点代码」教训**：批D 15:14 对 `checks.py` 的 M1 标题校验判「不通过」（fenced code block 包五节仍 5/5），但其测试针对的是**修复前的旧版代码**；当前代码（先剥 ```` ``` ```` 再 `^#+\s*{节名}(?![\w])` 锚定）经批F 用批D 同一套夹具（g2_b3/g2_b1）重跑，确认两处绕过均 FAIL 报「缺标题」。结论：独立审核结论的有效性**绑定于其运行时的代码版本**——若审核与修复并行发生，须以「同版本复跑」闭环，不能仅凭旧结论放行或否决。
- 2026-09-10：体验评估任务成为「独立审核」的**最强证据**——它拦下了 4 处错误，其中一处是**主代理自己的错误降级**（用不对口的命令反驳体验官指控），以及**16 条被静默丢弃的子代理发现**。结论：**作者自查对"改一处忘一处"和"手算错误"完全失效**，必须由未参与者逐条回归。相关教训见 `tasks/体验评估-ai-workflow-v2.5.0-2026-09-10/可优化方向.md` §四（原计划编号 #40/#41/#42 —— 该号段已由 v2.5.2 的熔断类反模式占用，故不再预留）。
- 2026-09-11（v2.5.3）：**「独立审核」由强制门禁降级为抽查制**——默认主代理自查 + 机器门禁（`checks.py plan` 对账与熔断门禁、`checks.py skill`、`--check-links` 探活、`office_io` 读回、py_compile/断言），仅「**高危产出／用户点名／返修触顶**」三类必送审；「执行者不得自审」禁令**未取消**，只在命中三类时生效。决策依据：每任务对每个关键产出另起干净上下文送审＝稳定发生的双倍人力税（工作区 `工作流瓶颈分析与优化方案-2026-09-11.md` B3）。
- 2026-09-11（v2.5.3）**验证阶段撞出的真缺陷（计划外连带修复）**：`checks.py` 的 `status` 子命令有**两处回归**，使**阶段 0 必跑**的任务总览长期失真（完成度与交付物两列全废），且因 `:283` 的宽 `except` 把它吞成「解析失败(XXX)」，**表面看像数据格式问题、不像代码 bug**：① `:272` `if args.light:` 访问的是 `main()` 的局部变量 → 恒 `NameError`；② `:279` 用步骤 dict 去索引审计结果键 `缺失` → 恒 `KeyError`。两处均系 N6「轻量模式」改动引入，已修（改用形参 `light`；改为 `zip(audited, steps)` 且只对「完成」步骤判缺失）。**过程失误如实登记**：我先修了 ①，**误以为已闭合**，是回归输出里继续显示「解析失败」才暴露 ②——**「修好一处就以为收工」在同一函数内同样会漏**，必须以下一次**原始输出**判定，而非以「我改了那行」判定；这也是 `checks.py plan` 的「交付物必须真实存在、不接受应该生成了」同一条纪律在调试上的投影。
- 2026-09-11（v2.5.3）**本次改动的自查盲区（如实登记，不掩盖）**：本任务的「独立审核」列按**新口径**判「抽查豁免」，而**新口径本身就是本次改动的产物**——等于由改动者判定「本次改动无需送审」，属**结构性利益冲突**（改动者给自己发了免审通行证）。缓解措施：① 该环节的**反证证据原样留在 `references/ops.md` v2.5.3 条目**（`base.parent` 假 PASS 通道、虚假自述、「描述与实现不符」3 次复发**均系该环节发现**），供日后回溯与推翻；② 本任务仍跑满机器门禁（`skill` 19/19、plan 7/7），且门禁**确实抓到 2 个真实问题**（ops.md 引用写坏被引用完整性拦下；`status` 两处回归由总览输出暴露）。**日后若出现「自查没抓到、事后被证明该送审」的案例，本条即为重新收紧的依据。**
- 2026-09-15（v3.0.0）：**主动送审的价值再次被实证**。本任务**未命中**「必送三类」（非高危产出、用户未点名、未返修触顶），按抽查制本可自查放行；但考虑到「改动对象是所有未来任务都要走的流程」＋作者自述弱点「描述↔实现容易脱节」，仍主动送审。首轮独立审核即抓出 5 项脱节，**其中 P2 最锋利：照文档写的 `decide` 用法执行，会被同一个工具的门禁判 FAIL**（文档未列 `--choice`，而门禁强制其非空）。这类"工具自相矛盾"只要按文档跑一遍示例就能发现，但**作者恰恰不会去跑——因为默认自己写的是对的**。复查又抓出 2 项同类残留（`adaptive-planning.md` 把必填字段写成「候选」、回归证据指纹漏收 `ops.md`），亦全部返修。**「抽查豁免」的门槛是"未命中三类"，不是"我觉得没问题"。**
- 2026-09-15（v3.0.0）：与外部参照 `obra/superpowers`（286,871 star，MIT）的**口径分界**已写入 SKILL.md「自主决策层」与 `references/ops.md` v3.0.0 条目——其 `using-superpowers` 是**无条件强制**（"1% 可能适用就必须调用" + 12 条反合理化红线），本技能**只取其机制（按需加载／反合理化表／声明制／优先级／子代理 STOP），不取其强制**。**日后若再出现"照搬强制口径"的倾向，以本条为依据拒绝**：那会推翻用户 2026-09-11 定稿的「最高准则」（流程是手段、不是目的）。
- 2026-09-15（收尾）**任务目录归档首次执行**（阶段 6 第 6 条）：`checks.py status` 报 14 个 > 阈值 10，把 2026-09-10 的 6 个任务移入 `tasks/_archive/2026-09/`（28 个文件 / 158,687 字节，**只移动不删除**，移动前后逐文件 sha256 快照比对一致），任务目录 **14 → 8 个**。清单与理由见 `tasks/_archive/2026-09/README.md`。**一个非显然的坑**：归档会**打断文档引用完整性** —— 最旧的 `技能增强-ai-workflow-v2.4.3-2026-09-10` 被 `references/ops.md` 以反引号路径引用（其下 `_test_server.py`），归档它会使 `checks.py skill` 的「文档引用完整性」直接 FAIL，故**保留 v2.4.3 原位**、归档其余。**结论：归档不是纯粹的文件移动，是有外部约束的破坏性动作，动手前必须先扫引用面。**
- 2026-09-15（收尾）**巡检发现：本台账自身的路径引用不受任何机械校验**。上行明细中 v2.6.1 行（2026-09-11）引用的 `tasks/技能增强-ai-workflow-v2.6.1-模型路由接入实测-2026-09-11/降本实测.md` 在**技能目录下不存在** —— 该任务的工作区根是 `E:\ChatGPT\工作流`，文件在其 `tasks/` 下，属**跨工作区相对路径**，非文件丢失（故不改历史条目，仅在此登记）。根因：`checks.py skill` 的「文档引用完整性」只扫 `SKILL.md` 与 `references/*.md`，**不扫本文件、也不扫 `tasks/`**，故台账里的失效引用可长期存在而无门禁发现。日后若要把台账当作"可核验的证据索引"，须显式扩展自检范围。
- 2026-09-15（v3.0.1）**推送独立验收暴露的两个「口径」坑 —— 问题在验收器，不在推送本身**。① **`core.quotePath`**：`git ls-tree` 默认把中文路径转义成 `"…\346\212\200…"`，与远端 API 返回的正常中文路径比对，会**凭空造出 101 项「假缺失」**；第一版验收脚本据此误报 FAIL。修正口径：本地侧一律 `git -c core.quotePath=false ls-tree`。② **混合仓不适用「集合相等」**：远端 = 技能本体 ∪ 工作区档案（`技术调研-*`／`调研-AI效率-*`／`调研-token优化-*`／`tools_bench*`／顶层报告等 **66 项本地从未包含**），本地只有前者。**正确口径是「本地 ⊆ 远端」+「远端独有项须机器证明其早于本次推送」**（取推送前 `base_tree` 逐项比对），而非求集合相等 —— 相等口径会把 78 项既有内容误判为异常。教训：**验收器与被验收对象同样需要被质疑；只有"两面都对不上"才是真异常。**
- 2026-09-15（v3.0.1）**`.gitignore` 的两条既有约定（本次确认，非疏漏）**：`tasks/*/plan.yaml` 与 `tasks/*/tmp/` 均不入版本控制 —— 即 **plan 文件与 tmp 中间产物刻意不进仓**，任务根目录下的正式证据（如 `远端推送独立验收.txt`）才进仓。故远端看不到任何任务的 `plan.yaml`，这是设计，不是丢失。
- 2026-09-15（v3.1.0）**新增「自研工具与技能：标注与公开留痕」（用户指定）**：起因是任务中常自写脚本补能力缺口，这类产物**既非第三方依赖也非用户既有资产**，不声明来源、不公开留痕则事后无人知道它是什么、从哪来，且易被当成成熟库承担不该有的信任。规则三条：① 代码内标注（自研标识 / 名称 / 用途 / 适用场景 / 仓库链接）② 输出中标注（交付说明 / 报告 / 台账凡引用处须出现「自研」+ 名称 + 链接）③ 同步推送 public 仓库且此后每次更新及时推送、保持版本一致。落四处接缝（阶段 3 第 1 步 / 阶段 5 第 8 条 / 阶段 6 第 4 条 / 阶段 6 台账）。配套 `checks.py selftool` + `_check_selftools()`（字段可选、填了必须合法）与自研脚本 `scripts/publish_tools.py`（`init` / `push` / `verify`）。**验证**：`checks.py skill` **21/21 FAIL=0**；6 个 fixture 证明门禁真伪（缺字段 / 占位符 / 非列表 → FAIL，待推送 → SKIP，合法 / 无该字段 → OK）；**27 份历史 plan 同体对照（monkey-patch 开关）零回归**。**本轮修掉 2 个真缺陷**：① `gh` 报错 `Not Found (HTTP 404)`，按 token `isdigit()` 提取时 `404)` 带右括号判不出 → 真实 404 被误报成 `HTTP 1`；② 失败分支只 `print` 不入 `results`，输出「0/0 通过，FAIL=0」却退出码 1（与 v2.3 修过的假绿同源）。**诚实边界**：门禁查不出「代码里写了自研工具却没登记」；目标仓 `Garvin666/ai-workflow-tools` 截至本条写入时**尚未创建**（创建 public 仓属不可逆对外动作，待用户单独确认），故本任务 plan 的仓库链接为『待推送』；按用户决定**不追溯存量**。
- 2026-09-15（v3.1.0）**规则自身的落地自证 + 两个踩坑（如实登记，不掩盖）**：`scripts/publish_tools.py` 是本规则产出后的**第一个**自研工具，已按规则在文件头标注五项、并登记进本任务 `plan.yaml` 的 `meta.自研工具`。坑①：写 fixture 时用**双引号**包 Windows 路径（`工作区根: "C:\Users\…"`）被 YAML 当成转义序列抛 `ScannerError`，首轮 6 个 fixture **全部在解析阶段挂掉、根本没走到新门禁** —— **阴性对照必须先自证能跑通，否则"全 FAIL"证明不了任何事**；且同一坑在**本任务自己的 plan.yaml** 里同时存在，已一并改为单引号。坑②：端到端测试 `selftool` 时把一条占位登记（`x-probe`）写进了**真实** `plan.yaml` —— 留痕里混进假条目比不留更糟，已删除并复跑对账确认仅剩 1 条真实登记。**两条同源教训：测试用的假数据不得落进真实台账；验证脚本本身也要先被验证。**
- 2026-09-16（v3.1.0 收尾）**规则首次落地：建仓 + 首推 + 实机跑出一个真缺陷**。用户授权后创建 **`https://github.com/Garvin666/ai-workflow-tools`（public，`private=false` 经 `gh api` 独立复核）**，并按其指定**只推自研的**——仅 `scripts/publish_tools.py`，**技能本体不推**（仍走既有 `Garvin666/ai-workflow-skill` 仓）；此范围收窄按 **R5（用户中途改需求）** 记入 `plan.yaml` 的 `meta.计划修订`。首推后 `verify` 报 2/2，且用**自带代码**（非 `publish_tools.py` 内逻辑）独立重算 blob sha 得 `6a2fac1a…` == 远端 API 返回值，**未复用脚本自证**。**实机跑出的真缺陷**：`init` 复跑直接吃 `422` 并报 FAIL —— 与规则里承诺的「幂等可重跑」自相矛盾（`push`／`verify` 幂等，唯独 `init` 不是）。**这正是"文档写了、代码没有"的又一例**，且只在**真去复跑一次**时才现形；已修为「建仓前先查仓库：已存在且 public → `[OK] …跳过创建（幂等）` 退出码 0；已存在但 private → FAIL + 改可见性指引」，另加 422 竞态兜底（POST 撞车后复查，public 则按幂等通过）。修后重推（blob `f90b6c16…` → `6a2fac1a…`），即**新规则自身的第一次「改动即推送、保持版本一致」闭环**。验收留痕：`tasks/技能增强-ai-workflow-v3.1.0-2026-09-15/远端推送验收.txt`（建仓 → 首推 → 幂等复跑 → 独立复核五节）。**结论沉淀：推送类工具的"幂等"要逐个动作验证，不能因为同一个脚本里 `push` 幂等就默认 `init` 也幂等；而这类缺陷只有实机复跑能抓到。**
- 2026-09-16（v3.1.0 收尾·续）**技能本体推送 + 规则自身的自洽性补齐**。用户 2026-09-16 追加「将 ai-workflow 推远端 ai-workflow-skill 仓，写 ai-workflow-skill」→ ① **补齐「技能包」标注**：`SKILL.md` frontmatter 加 `selfbuilt: true` + `repo: https://github.com/Garvin666/ai-workflow-skill`，正文首段（H1 之后）加 `[自研技能]` 标注块给出用途／适用场景／仓库链接 —— 因为 **ai-workflow 本身就是自研技能，此前并未遵守自己刚写的规则**（这是本条前半段留下的自洽性缺口，被核出后立即补上）；② 增量推送 v3.1.0 到 `ai-workflow-skill`（本地 `c020f17` → 远端 `dc2121d5`，8 文件；再 `8e9a014` → `c5b53c7d` 带验收留痕）。**两仓分工自此明确**：自研工具 → `ai-workflow-tools`，技能本体 → `ai-workflow-skill`。**方式仍走 Git Data API + `base_tree`**（远端是混合仓且历史与本地不互通，直接 `git push` 会因历史无关被拒）。**独立验收**：验收器自写、与推送脚本零代码共享，五判据 PASS（本地 173 文件 ⊆ 远端 239 blob 且 sha 全等；远端独有 66 项经 `base_tree` 证明全部早于本次推送；关键文件 sha 一致；远端 `SKILL.md` 确含 `selfbuilt`／`repo`／`[自研技能]`／3.1.0；删除项 0）。**一处顺带修掉的存量脱节**：`ops.md` 里仍写着「目标仓截至本条写入时尚未创建」——仓早已建好，属典型「改一处忘一处」，本次改为已修正并回填。**另一处口径细化**：`SKILL.md` 的「默认仓库」原先只写 `ai-workflow-tools`，易被误读为技能本体的归属，现补「两仓分工」条目。**跨仓交叉印证（本轮意外收获）**：`scripts/publish_tools.py` 在 `ai-workflow-tools` 与 `ai-workflow-skill` 两仓的 blob sha **完全相同**（`6a2fac1a…`）—— 同一份内容、两个远端，天然构成「版本一致」的独立证据。
- 2026-09-16（v3.2.0）**新增「两类资源的分流推送路由」（用户指定）**：本体（Ontology）→ `ai-workflow-skill`，自研工具 → `ai-workflow-tools`，并要求明确四要素（触发条件／判定依据／推送格式／冲突处理策略）。方案经用户拍板：① 本体范围 = 技能本体 + 任务档案（与远端混合仓现状一致）；② 判定依据 = **标注头（事实）× `plan.yaml` 登记表（意图）× 链接指向（归属）三者交叉校验**，不一致即 FAIL；③ 触发形态定为**单命令分流器**（默认 dry-run），不采用 git 钩子与计划任务 —— 它们会在非预期时刻对远端写入且失败易被静默忽略。
  **⚠️ 最易误解点（已写进规则正文）**：**分流不是互斥二选一** —— 本体 = 技能仓改动面的**全量**，自研工具 = 其中的**子集**；故 `scripts/` 下自研脚本属**双属**，两仓各推一份（同文件跨仓 blob sha 相同，即「版本一致」的天然证据）。
  **工具**：新增 `scripts/push_ontology.py`（本体通道，由 v3.0 任务遗留的 `tasks/技能增强-ai-workflow-v3.0-2026-09-15/tmp/push_incremental.py` **提升为正式脚本**）+ `scripts/push_router.py`（分流器：`classify` 离线分类与交叉校验／`push` 分流推两仓）。提升时修掉原脚本三个隐患：藏在 `tmp/`（按约定不入版本控制 → 本体推送的唯一通道竟不在版本控制内）、仓名硬编码、**默认就真推**（与同族 `publish_tools.py` 默认 dry-run 口径相反 → 分流器无法安全包装两条命令）。
  **验证**：`checks.py skill` **23/23 FAIL=0**；**判定真伪用 6 个隔离 fixture 证明**（本体普通文件→OK；自研工具→双属；技能包 `selfbuilt` 且登记指向本体仓→**只走本体通道、不进工具仓**；漏登记→FAIL；登记与实现不符→FAIL；标注头缺项→FAIL），**6/6 断言全过**；**两通道 dry-run 实测**远端 HEAD 前后一致（零写入）。证据见 `tasks/技能增强-ai-workflow-v3.2.0-2026-09-16/`。
  **⭐ 本轮最有价值的发现：一次真实的跨通道内容不一致事故。** 首轮推送时，`push_router.py` 的 `--repo/--branch` 位置修复**发生在 commit 之后** → **本体通道按 commit 内容推（修复前版本）、工具通道按工作区文件推（修复版）** → 同一文件在两仓 blob sha 不一致，**公开仓一度躺着会生成错误命令的代码**。根因是两条通道的**内容来源不同源**（commit vs 工作区），而此前没有任何检查覆盖这一点。修法：`push --apply` 前检查 `git status --porcelain`，非空即 FAIL 并逐条列出（`--allow-dirty` 显式放行），并把该行写入 `SKILL.md` 的冲突策略表；验收器同步新增**判据 6「跨仓一致性」**（同名自研工具在两仓 sha 必须相同）—— 这是能抓住该类事故的机器判据，此前没有。**教训：多通道分发系统必须校验「各通道内容同源」，否则「都成功」不等于「结果一致」。**
  **本轮撞出的其余 4 个真缺陷（均由实测暴露，非推演）**：① **技能包判定顺序陷阱** —— `scan_selftool_header` 先扫正文标记、后判 frontmatter，`SKILL.md` 正文首段的 `[自研技能]` **输出标注**抢先命中脚本分支，技能包分支（读 `repo:`）**永不生效**，真实目录下 `SKILL.md` 被误报「漏登记」；② **技能包映射依赖中文命名** —— 初版靠名称里是否含「技能本体/技能包」把登记项映射到 `SKILL.md`，一改命名即失效，改为用 frontmatter `repo` 与登记链接**同仓比对**；③ **标记扫描假阳性** —— `ops.md` 脚本文档表里为说明标注格式而写了 `[自研工具]` 这个字符串本身，被判定器当成「文档有自研标注头却没登记」；修法是**收窄判据**（只认注释行），**不是把文档改得躲开门禁**（躲避式修法会让人不敢在文档里引用这个标记）；④ **`publish_tools.py` 参数位置** —— 其 `--repo/--branch` 是**顶层**参数，分流器初版放在 `push` 子命令之后 → argparse 判 unrecognized、退出码 2，由 dry-run 阶段暴露而非真写远端才发现。
  **⑤ 验收器自身的断言 bug（同类第 5 例）**：判据 3 原写「远端 blob 数必须**增加**」，而某轮只**修改**文件内容（不新增路径）时 blob 数不变 → 误报 FAIL。改为「**不减少**」，是否新增由另一条断言单独负责。**与 v3.1.1 对照脚本把 `[ OK ]` 写成 `[OK]` 同型 —— 「用来验别人的脚本本身也要被验」在本技能已累计多例，不是偶发。**
  **与 v3.1.1 并存（重要过程事实）**：本轮排查一处自检 FAIL 时，发现**另一个并行会话已在此技能目录完成 v3.1.1**（「技能库卫生」四条硬约定 + `checks.py` 跨根引用前缀修复），改动**未提交**且与本次 v3.2.0 混在同批文件。此类情况不得静默处置 —— 已向用户说明「推送会把对方成果一并发上 public 仓」，并列出 v3.1.0 日志中含量化项目内部信息（标的代码／指标模块路径／数据集路径），**取得明确同意后合并推送**（远端 `ai-workflow-skill` 由 3.1.0 一次到 v3.2.0，两条变更日志都保留）。
  **推送与验收**：4 轮分流推送（每轮均由 `push_router.py` 分流两仓），本地 `e1d2a23` → `e60b5f7` → `0fcad52` → `abc13ec` → `d1d962d`；远端 `ai-workflow-skill` `8f14ac61` → `67f9b6b8`，`ai-workflow-tools` `7dd805e4` → `aecbbe39`。**独立验收器**（自写、与三个推送脚本**零代码共享**，参数化以便逐轮复用）**7 判据全 PASS**：本地 178 项 ⊆ 远端 244 blob 且 sha 全等；远端独有 66 项经 `base_tree` 证明全部早于本次推送；无意外删除；两个自研工具在**两仓 sha 相同**；远端 `SKILL.md` 自证 3.2.0 且含四要素；两仓 public。
  **诚实边界（未解决，如实登记）**：① 无跨会话基线时**无法检测「远端被第三方改写」**这类漂移（如需该保护，推送后记录 sha 并用 `--expect-remote` 显式约束）；② 工具仓删除**需人工在网页进行**（contents API 单文件通道未实现删除）；③ `push_router` 只管辖**技能根内**的文件，技能根外的自研工具请直接用 `publish_tools.py`；④ **「推送验收器」已连续三轮各写一份**（v3.1.0 一份、v3.1.1 的对照脚本一份、本次一份），每份都落在 `tmp/` 内、不进仓、不可复用 —— 这是明确的重复信号，**建议后续正式化为 `scripts/verify_push.py` 并登记为自研工具**（本次未做，以免在用户已授权的推送范围内掺入新产出）。
- 2026-09-16（v3.2.1）**上一条的「建议」已落地**（用户指令「补」）：推送验收器正式化为 `scripts/verify_push.py`，登记为自研工具，并随分流规则推送两仓。三条设计要点：① **消灭所有"人工记得改"的常量**——期望版本从本地 rev 的 `SKILL.md` 自动推导、自研工具清单从 `tasks/**/plan.yaml` 的 `meta.自研工具` 自动发现、默认分支从远端元数据取，调用方只给「本地 rev + 推送前远端 HEAD」；② **与三条推送通道零代码共享**，且与分流器构成**方向相反的第二实现**（分流器标注头驱动、验收器登记表驱动），两者同时通过才算交叉印证；③ **阴性对照 5/5** 证明判据有牙（破损输入均真报 FAIL）——**一个从不报错的门禁等于没有门禁**。
- 2026-09-16（v3.2.1）**本轮撞出 5 处判据缺陷、1 处跨通道真缺陷，全部由实测暴露，作者自查一项未发现**。判据侧：① 登记名带中文括号注释（`ai-workflow（技能本体）`）按字面匹配 → 误报；② 把 v3.2.0 的 4 个**测试替身**登记当真缺陷（它们落在 `tasks/*/tmp/`，按约定不入版本控制）→ 4 项误报，修法是「`tmp/` 下 plan 不读」+「映射不到时区分：只在工作区 → SKIP／两处都无 → FAIL（悬空登记）」；③ 标注头按字面找「名称：」三字 → 对两个**合规**脚本误报；④ 技能包只认带斜杠的 `…/SKILL.md`，根级 `SKILL.md` 漏匹配 → 技能本体自身被误报「悬空登记」；⑤ **验收器比门禁松＝自相矛盾**：本文件初版把标注只写在 docstring 里，分流器（只认注释行）判 FAIL **阻塞推送**，验收器却判 OK。**判据可以更严，但不能比门禁更松**——否则两个工具给出互相矛盾的结论，谁也不敢信。
- 2026-09-16（v3.2.1）**跨通道真缺陷：日志落盘失败把成功的推送掩盖成失败**。现象是 `push_router --apply` 报「本体通道退出码=1 → 中止工具通道」，而远端 HEAD 明明已前进、提交内容正是本次改动。根因两段：`run()` 以 `cwd=scripts/` 调通道 → 相对路径 `--result-file tasks/…` 被写到 `scripts/tasks/…`（父目录不存在）；而该写入发生在 `PATCH ref` **之后**（成功路径的最后一步）→ 异常直接把成功改写成退出码 1，工具通道还被顺带中止，`verify_push.py` 因此**第一轮根本没进工具仓**。修法：相对路径一律以技能根为基准、落盘失败**只 WARN 不改推送结论**（日志是旁证不是动作）、`resolve()` 归一化后**越出技能根即拒绝写入**。**连带教训**：触发集＝「本次 commit 的改动集」这一口径，在中止轮次后重推时会**漏掉从未成功推送的文件**（本轮即 `verify_push.py`）→ 重推须把 `--base` 回退到「远端最后一次成功推送所对应的本地 rev」；验收器判据 4 会兜底报 FAIL。
- 2026-09-16（v3.2.1）**自指边界（如实登记，不假装闭合）**：验收报告本身就是工作区的改动 → 「写报告」必然使工作区变脏 → 判据 0（工作区干净）会 FAIL。故记录该轮验收时用 `--allow-dirty`，并在报告内显式说明该脏项**不影响其余 6 类判据**（判据 2 比对 rev 与远端、判据 4/6 读 rev 内 blob），工作区真正干净时的收尾复核结论另记于本条与交付回复。另有一处**纯属表述**的小瑕疵：判据 4 对 `SKILL.md` 报告的"注释行命中"实际来自文档正文里为说明格式而写的示例行（判 OK 仍正确，因为技能包走 frontmatter `selfbuilt` 分支），**未为消除该表述瑕疵再推一轮**——记在此处，供日后顺手清理。
- 2026-09-16（v3.2.1）**收尾复核结论（工作区真正干净时）**：上一条写「另记于本条与交付回复」，本条即该结论 —— `scripts/verify_push.py --rev HEAD --prev-remote 1f79c6f3… --expect-remote <收尾轮远端 HEAD>` → **FAIL=0，OK 37 / FAIL 0 / SKIP 0 / WARN 0，退出码 0**；远端 `ai-workflow-skill` HEAD = `0a79e151c6`，`ai-workflow-tools` HEAD = `2f5aa4fd42`；本地 `e9060b7`，工作区干净。**自证链条到此终止**：本行自身的提交由下一次推送的「本地 rev ⊆ 远端」判据兜底，不再递归。

- 2026-09-16（v3.4.0）**执行方式：先补证据、再推送，让门禁自然通过而不是绕过它**。`push_router push --apply` 有一道「工作区干净」门禁，理由是本体通道按 **commit 内容**推（`git cat-file blob <rev>:<path>`）、工具通道按 **工作区文件**推（`publish_tools.py --file <路径>`）—— 树一脏，两条通道就可能推出**不同内容**，同一文件在两仓 sha 不一致、公开仓里躺着旧版本。本轮把 `分流器-交叉校验.txt` 等证据**先 commit 再推**，两次推送均**未加 `--allow-dirty`**，门禁是真生效的。
- 2026-09-16（v3.4.0）**本轮另修三个真缺陷，全都不在报告清单里、全是执行时撞出来的**（作者自查一项未发现，与 v3.2.1 同型）：① `push_router.py` 的自研标注头判据对**文档类文件**误命中（markdown 的 `#` 被当注释，而 `push-routing.md` 的标注模板必须写出 `# [自研工具] xxx.py`）→ 收窄为「文档类整体不走脚本分支」，**是收窄判据不是把文档改得躲开门禁**；② `push_ontology.py` 的 **`--dry-run` 会真写远端**（blob POST 落在 `if not a.apply` 之前）→ 违背文件头自称的"绝不写远端"，且 32 项要 POST 29 次、一次 dry-run 拖到 **2 分 37 秒**；修后 dry-run 零 POST 秒回（工作区侧 dry-run 现场复验）；同族 `publish_tools.py` 经查两处 `if not args.apply:` 均在写操作之前，同型缺陷不存在；③ `verify_push.py` 的 **`--skill-dir` 是"文档上有、实现上无"的选项**（`git()` 的 cwd 硬编码为脚本位置）→ 后果不是"少检了点"，而是**混合仓的另一条本地来源根本无法被本脚本验收**；修法是新增 `GIT_ROOT`，不指定时行为与改动前**逐字节一致**。**可迁移结论：文档里"绝不写""可指定"这类断言，必须由代码结构保证，不能靠注释与 help 文本承诺。**
- 2026-09-16（v3.4.0）**⭐ 新发现的开放缺陷（本轮未修，待用户拍板）：`.gitignore` 在混合仓里同路径撞车**。`ai-workflow-skill` 同一远端路径被**两条本地来源各自主张**，且两侧策略**方向相反**——技能仓要忽略 `tasks/*/plan.yaml`、`tasks/*/tmp/`（设计如此），工作区仓要**跟踪**它的 `tasks/*/plan.yaml`。远端只存一份 → **后推者覆盖先推者**，另一侧的判据 2 恒 FAIL（实测本地 `5138ffeea1` ≠ 远端 `3d53038d4d`，远端当前为技能侧版本、**未被覆盖**）。**现行约束：工作区通道的改动面不得包含 `.gitignore`**，已写进 `references/push-routing.md`。三种候选修法（两侧共用合并内容／工作区侧改用 `.git/info/exclude` 放弃该路径主张／验收器引入「共享路径」概念并对该集合断言「远端 = 两侧之一的版本」）**都涉及删除公开仓既有路径或改动验收判据，属高风险，本轮不自行选**。
- 2026-09-16（v3.4.0）**⭐ 独立验收暴露了验收器自己的建模前提，已作为口径写进 `push-routing.md`**：判据 3 假设「**一轮推送 = 一个本地仓 + 一次提交**」，而混合仓一轮里有两类本地来源。实测对照最有说服力——**同一批 6 份报告**，基线取 `5633f77038`（推送前）时判「本次新出现 6 项」FAIL，基线取 `977d53b630`（**本通道**推送前）时判「新出现 0 项」OK。规律：**后推送的通道**用自己推送前的基线可全绿；**先推送的通道**若等兄弟通道推完才复核就必然误报；改用更晚基线则判据 1「非空跑」又 FAIL → **不存在单次全绿的基线**。故口径定为**按通道分跑、一通道一次**。这是前提不成立，**不得靠放松判据掩盖**，也不应把文件挪来挪去以求一次全绿。
- 2026-09-16（v3.4.0）**收尾状态与自证终止**：远端 `ai-workflow-skill` main = `0fa7882f2ca794d346a5bedc986afab45e1d79f1`（`5633f77038` → `f83b1f8dee` → `977d53b630` → `0fa7882f2c`），`ai-workflow-tools` 5 个自研脚本全部更新且**跨仓 sha 逐一相等**；本地 `666bb5b2`；工作区仓 `7011bf0` 已推（6 份顶层报告）。**自证链条到此终止**：本条所在的这次「证据+履历」提交，其自身的验收结论不再回写（回写会再产生一次提交、再开一轮），按 v3.2.1 立下的「自指边界显式终止」规则处理 —— 不再递归、不假装闭合。
- **2026-09-17 上一条的开放缺陷已修（修法 2），但口径文档未同步（显式登记，非遗漏）**：`.gitignore` 撞车按**工作区侧弃权**落地——工作区仓 `git rm --cached .gitignore` 并本地提交（`7011bf0` → `8fb80fc`），原有忽略规则**逐条迁入 `.git/info/exclude`**，`.gitignore` 文件保留在磁盘上（非破坏性）仅退出跟踪。**为什么选修法 2 而不是另两条**：修法 1（两侧合并内容）抗漂移弱、且会使工作区仓新增 `plan.yaml` 不再被跟踪（静默丢档风险）；修法 3（验收器建模）只让门禁接受现状、不除根，与上一条自己立的「**不得靠放松判据掩盖**」相悖。**唯一变量证明**：同一验收器对修复前 rev `7011bf0` 判 `[FAIL] 不一致 1 项`、对修复后 `8fb80fc` 判 `[OK] 不一致 0 项`。
- **2026-09-17 `references/push-routing.md:169-174` 的口径已过时，本次未同步**：该节仍写「限制一…（**未解决，待拍板**）」与「候选修法…**勿自行选**」，而事实已变。**未同步是纪律不是疏忽**——改该文件会使技能仓变脏，而技能仓刚完成「远端与本地一致」的收尾，变脏后必须再推一次才能维持一致性；本任务受「**不推送远端**」约束，且已立「自证链条到此终止」。精确行号与建议替换文本见 `tasks/修复-gitignore同路径撞车-2026-09-17/口径更新待办.md`。**这是「文档与实现脱节」的又一次出现**——与前几次不同的是，本次它被**显式登记为待办**（含可直接套用的替换文本），而不是静默存在。
- **2026-09-17 连带发现（非本任务范围）**：工作区仓 `E:/ChatGPT/工作流` 有 **3 项未提交产出**（`Ledger.md`、`评估-ai-workflow工作流-2026-09-17.md`、`tasks/评估-ai-workflow工作流-2026-09-17/`，均来自 2026-09-17 的评估任务）。本次验收因此出现 `[WARN] 工作区无未提交改动 —— 3 项未提交`（已用 `--allow-dirty` 放行，并如实标注）。**影响**：`push_router.py` 的「工作区干净」门禁会被这 3 项阻塞，下一次工作区通道推送前须先处置。本次**未动**（超出当次授权范围）。
- **2026-09-17 本次工作的一处过程事实（如实登记）**：主代理在阶段 1 首次判断"本地与远端已一致"时，依据是 `git log origin/main..HEAD` 返回空——但那是因为命令把 stderr 丢进了 `/dev/null`，**报错被吞、不是空结果**；该仓**根本没有 `origin/main` 追踪引用**（`git rev-parse origin/main` 报 `unknown revision`）。后经 `curl` 直取 GitHub API 才得到真实远端 HEAD（`287d1dc`）。**教训：把 stderr 静默丢弃，会把"命令失败"伪装成"结果为否"**——这类误读与 v3.2.1「验收器比门禁松」同属"用来判断的工具本身没被判断"。
- **2026-09-17 v3.4.0 任务的「收尾」本身补记（同一任务的续跑，不另立明细行）**：该任务步骤 7/8 长期停在「进行中」，而**交付物其实早已产出、推送也早已完成**——断掉的是**状态回写**。本次按 R6（连续性断裂）留痕后回写，并订正了步骤 7/8 的 `交付物` 字段：原写 `远端推送验收.txt` / `远端推送独立验收.txt`，**两个文件都不存在**；实际为 `远端推送-分流通道.txt` / `远端推送-独立验收.txt`。**由此暴露一个门禁盲区（值得单独记住）**：`checks.py plan` 的 Anti-drop 对账**只核对「已完成」的步骤**——该任务当时报 `FAIL=0` 是因为它只看了 6 步（原文「已完成步骤 6 个，交付物确认 6 个」），**两个坏路径完全没被核对**；回写状态后才覆盖到 8 步。**阴性对照**：只回写状态、不订正路径 → `12/14 通过，FAIL=2`、`RC=1`；订正后 → `12/12 通过，FAIL=0`。**教训：门禁的覆盖面本身就是它报告里的一行（"已完成 N 个"），读 FAIL=0 时必须连带读这一行** —— 否则"未完成"的步骤会连门禁一起逃逸。
- **2026-09-17 v3.5.0 的六条经验（每条都由机器证据支撑，不是感想）**：
  1. **同一条理由必须对整个类别生效，不能只对上次被投诉的那个成员生效**。P1-1 的标注头判据沿用了 v3.4.0「缺陷一」写的收窄规则（排除 `.md`）却**漏了 `.yaml`**，于是 `assets/plan-template.yaml` 注释里的一句 `[自研工具]` 让 `checks.py` 出 WARN、`push_router.py` 出 **FAIL（会阻塞推送）**。→ 引入 `CODE_EXT`，只扫代码脚本；**按后缀逐个打补丁，就是等着下次换个后缀再犯一遍**。
  2. **查表范围错了，会把"已登记"读成"漏登记"**。自研工具的登记发生在**创建它的那次任务**里，后续任务**修改**它时不再登记 → 只查本任务表，`push_router.py`（v3.2.0 已登记）被判未登记。修法：**本 plan 登记表 ∪ 全工作区总账**，**两侧都不能少** —— 只查总账时，落在 `tasks/*/tmp/` 的夹具 plan 自己的登记会被总账排除，反过来报「刚补登记仍报警」（**被夹具的阴性对照当场抓到**）。
  3. **门禁改了，夹具必须跟着重做**。原夹具拿 `push_router.py` 当阳性样本；判据收窄后它不再报警 → **阳性对照会静默退化成空转**，而"对照通过"依旧显示为通过。**"对照通过"只有在夹具与判据同步演进时才成立。**
  4. **自检项数会随被测目录增减漂移，跨版本不可直接相减**。`checks.py skill` 按 glob 编译 `scripts/*.py`；v3.4.0 的脚本今天跑出 28 而条目记的是 26。本轮 26→33 的 +7 已**逐项名对齐**后才写入 changelog。**只报差值不报构成，就是在制造下一个"不可复算"。**
  5. **「工作树自检绿」≠「已提交树自洽」**。把 v3.4.0 的**已提交树**（`git archive HEAD`）取出来跑它自己的 `checks.py skill`，得 **25/26、FAIL=1** —— 缺的是 3 个 `tasks/*/tmp/*.py`，被 `changelog.md`／`ops.md` 引用、却被 `.gitignore` 排除而**不在版本控制内**。「文档引用完整性」这道**扁平检查**在本地工作树通过，不代表**对外发布出去的那棵树**自洽。
  6. **诊断报告不得成为新的泄露源**。第一版《对账输出》把扫描器的原始输出连本机路径一起抄了进去，于是它自己成了改动面里最大的一处 FAIL 级命中（13/22 条）；同一坑在《出站扫描清单》上已踩过一次（把路径原样抄一遍＝再泄露一次）。→ 只登记**判定结果**（计数＋文件名），原始命中行落 `tmp/`（不外发）；交付物自身做**写盘后定点复核**。**自我指涉的统计必须在写盘之后做**，否则得到的是上一版文件的数据。
  7. **核验脚本必须自证取数形状 —— 取错形状不会报错，只会安静地全绿**。收尾做「写盘后定点复核」时，**首跑报「13 个文件 FAIL=0」**，因与 §⑤ 记录的 `FAIL=5` **互相矛盾**才被察觉。根因：`outbound_scan.scan_file()` 返回的是 **`list[dict]`**（键 `行号/级别/片段/豁免`），而探针按**对象属性**取数（`getattr(res, "fail", [])`）→ 属性不存在 → **静默回落到空列表 → 全 0**。改用正确 API 并先打印 `type()`/`len()` **自证形状**后得真值：**FAIL=3**（本任务 `plan.yaml` 的 `工作区根`、`scripts/checks.py` 的 venv 提示串、技能根 `Ledger.md` 的越界追溯叙述），本交付物 `对账输出.txt` 仍 **FAIL=0/WARN=0**。**这与本条目 5、6 及已有的「缺 pyyaml 致 21 条 plan 全 `rc≠0` 零标记被误读成全绿」同属一类病：用来判断的工具本身没有被判断。**
- **2026-09-17 v3.5.0 的两处待拍板（本轮刻意不做决定，如实上报）**：
  - **门禁判据与处置建议口径不一致**：P0-2 的出站扫描对**全量**本机路径判 FAIL，而《出站扫描清单》推荐的**选项 ① 只处置本体面**。门禁要求改全部、建议只改本体面 —— 二者必居其一要改，属口径决策，不自行选。
  - **`meta.工作区根` 这类操作性绝对路径字段被判 FAIL**：它**必须是可解析的绝对路径**，否则 `checks.py --base` 与 P0-1 的越界判据一并失效 —— 即**无法靠「换成占位符」消解**。实测：本轮改动面 22 文件中 FAIL=5，其中 **3 条来自两个 `plan.yaml` 的 `工作区根`／交付物字段**；另 2 条为存量（`Ledger.md`、`scripts/checks.py` 的 venv 提示串）。
  - **后果（已在 `对账输出.txt` §⑤ 留档）**：新门禁**会阻塞本轮推送**（改动面 `FAIL=5`）。这不是误判 —— 命中路径真实存在、推送后确实会再进公开仓；但它也意味着**在口径拍板与存量处置之前，任何含任务档案的推送都会被拦**。故本轮**未推送远端**，`scripts/outbound_scan.py` 的仓库链接保持「待推送」，`checks.py plan` 该项判 **SKIP**（中间态，非完成态）。
- **2026-09-17 v3.5.0 收尾留痕（步骤 9 完成后补记，不另立明细行）**：
  - **门禁终态**：`checks.py skill` **33/33 FAIL=0 SKIP=0 WARN=0**；本任务 `checks.py plan` **12/13 FAIL=0 SKIP=1 WARN=0**（Anti-drop：已完成 9 / 交付物 9）；历史 plan 回归 **21 份新增 FAIL=0**。SKIP 的唯一来源是该自研工具的「待推送」链接 —— **是本轮未推送的直接后果，属预期中间态**。
  - ⚠️ **订正本条目上方一处计数**：本文件曾写工作区仓「**3 项**未提交产出」，以 `git -c core.quotePath=false status --porcelain` 实测为 **16 项**（含 8 个任务目录、5 份顶层报告/附件）。原因：该行只列了当时视野内的 3 项，**未从 `git status` 反推全量** —— 与「登记表 ≠ 全集」同一病根。工作区通道推送前须先处置这 16 项。
  - ⚠️ **一处漂移已登记**：本文件在出站扫描中的 WARN 由 §⑤ 时的 **5** 变为 **6**（FAIL 仍为 1）—— 因 §⑤ 之后本文件备注区又追加了内容。**不因"主结论未被推翻"就静默略过。**
  - **登记但未施工**：把第 7 条经验（核验脚本自证取数形状）写入 `references/quality-gates.md` —— 属改既有技能文档、超出本轮九项授权，**登记为候选，不擅自实施**。
  - **2026-09-21 补推同步（本动作）**：应需求把工作区 `Ledger.md` 的 P0+P1/P2 两条归档行（见上方明细表末两行）与 `tasks/P1P2建模落地-2026-09-21/` 任务档案（仅含 clean 交付物：完成报告 / `trace.jsonl` / `_metrics.jsonl`；`tmp/` 诊断输出与含本机路径的脚本已排除，不进公开仓）追加同步至本公开仓 `ai-workflow-skill@main`。`.gitignore` 已排除 `tasks/*/plan.yaml`，故任务目录 `plan.yaml` 不推送。出站扫描按推送面 FAIL=0。
  - **2026-09-21 补推同步（续）**：应需求把 `tasks/P0建模落地-2026-09-21/` 任务档案也同步进本公开仓（commit `P0补推`，ref 见上方推送明细）。同样仅含 clean 交付物：`P0完成报告.md` / `trace.jsonl` / `_metrics.jsonl`；`tmp/` 诊断脚本（共 21 个文件，均含本机用户主目录与盘符路径）与 `plan.yaml` 一律排除，不进公开仓。P0 归档行已于上一条同步动作写入本 Ledger 明细表（L31），本次仅补任务目录。

- **2026-09-22（v4.5.0）三条教训，均由机器证据支撑**：
  1. **⭐ 对照实验必须回退「全部」相关改动，否则 import 链断裂会制造假象**。为证明 `gate.py` 的 FAIL 与本次改动无关，我只回退了 `checks_core.py` 一处改动，而 `checks_judges.py` 的 import 仍在引用新常量 → `gate.py` 直接**报错退出** → 输出里 `[XX ]` 计数为 **0**，看起来像"没有不符合预期的项"。**这是与「缺 pyyaml 致 21 条 plan 零标记被误读成全绿」同型的病**：进程根本没跑起来，却读成了"结果为空/正常"。修法不是记住"下次小心"，而是**对照实验前先列出改动面全集，回退按集合走**。
  2. **⭐ 恢复动作不能挂在 `&&` 链上**。上一条的恢复命令写作 `grep -c … && cp /tmp/cc.bak scripts/checks_core.py`，而 `grep -c` 在**无匹配时返回退出码 1** → `&&` 短路 → **恢复动作从未执行**，技能本体一度处于 import 断裂的坏状态。所幸及时发现并单独执行恢复（`grep` 确认 `RETRIEVAL_*` 四常量已回到 93–96 行、`py_compile` 通过）。**教训：清理/恢复类动作必须与前置判断解耦，用 `;` 或独立调用，绝不挂链。**
  3. **⭐「发现缺陷」与「断言不存在」一样，都要先查证再下结论**。我在**技能根**下跑 `checks.py skill`，得 46/47、`gate.py` 4 项不符预期，据此写入验收报告 §4.1 判为"**既有缺陷**，根因是路径分隔符"。转折点是翻 `references/changelog.md` 时发现 **v4.4.2 条目早已明确记录**：「从技能根调用会因 `gate.py` 用 `Path.cwd()` 而产生 4 项**假红**」。改从**工作区根**调用 → **47/47 FAIL=0 全绿**。已按「不静默改写历史」口径在报告中**内联更正**（原写…，更正为…）。**这正是本轮交付物 §5.1 那条「否定断言证据门槛」的一个活例——我在写它的同一天就违反了它一次**：把"没查证过"当成了"不存在缺陷"的论据。**判据写在文档里容易，写进自己的动作序列难。**
- **2026-09-22（v4.5.0）两处待拍板（本轮刻意不做决定，如实上报）**：
  - **① `guard_constants.py` 的 `members` 漂移（真缺陷，非本次引入）**：从工作区根调用仍报 3 组漂移（`CODE_EXT` / `REF_PATTERN` / `SELFTOOL_KEYS`，均报"在 `scripts/checks.py` 中找不到模块级常量"）。根因：v4.4.2 拆分后这三组常量已搬到 `scripts/checks_core.py:49/60/72`，但 guard 的 `members` 仍写 `["scripts/checks.py", …]`（第 71/77/83 行）。**未擅自修复**——修它等于改判据覆盖面，应单独立项。
  - **② `checks.py skill` 的 gate 判据对 cwd 敏感（假红源）**：`gate.py` 用 `Path.cwd()` 定位，导致同一份技能树在技能根 vs 工作区根下得出**不同结论**（46/47 vs 47/47）。changelog 已记录过，但**只记录未消除**。属"验收器自身前提不成立"，与 v3.4.0 那条「不存在单次全绿的基线」同族。**在口径拍板前，本技能的一切门禁结论都必须显式注明"从哪个根调用"**——本行与明细表末行的 47/47 均指**从工作区根调用**。
- **2026-09-22（v4.5.0）一处口径边界（如实声明）**：`references/retrieval-judge.md` **附录 A** 的标题刻意写作「与前两个 judge 契约的对应关系」，而非「JEV 设计文档对应关系」——因为本轮**未直读**三份 JEV 原始设计文档（读取技能目录外的用户私有文件属越界未授权），附录 A 内容系**基于公开检索（WebSearch/WebFetch）的复述**。同理，`self-judge`／`method-judge` 契约中「哲学来源：Jev」的表述系其**原有内容**，本次未改动、亦未独立核验其原始出处。**同构性可机器验证**（三份契约 `^##` 级标题比对：各 12 节、11 节完全相同），但**"与 Jev 原文一致"不可由本次交付断言**。
