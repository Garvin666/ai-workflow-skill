# 对照分析：ai-workflow v3.2.1（现状） vs v4.0 升级提案

> 日期：2026-09-16 ｜ 类型：对照分析
> **对比对象**
> - **现状**：`ai-workflow` 技能 v3.2.1（技能根 `~/.workbuddy/skills/ai-workflow/`）
> - **升级后**：本文提出的 **v4.0 提案**（10 项机制升级，见 §4）
>
> **证据口径**：现状侧全部来自**技能本体原文与代码**，不引用记忆、不引用 SKILL.md 的自述（"文档与实现脱节"是本工作区反复出现的坑，故对每条关键判断都回到实现核对）。
> 主要证据源：`SKILL.md`（465 行）、`assets/plan-template.yaml`、`scripts/checks.py`（自查 `check_skill` 134-194 行 / `audit_step` 252-271 行 / `_check_fuse` 474-517 行 / `check_plan` 755-810 行）、`references/` 8 份手册。
>
> **本文性质**：**分析文档，不修改技能本体**。任何对技能的实际改动须另立任务、先落 plan.yaml。

---

## 0. 前提判断：两者解决的不是同一个问题

在逐条对比之前必须先说清这一点，否则会得出"谁落后谁先进"的错误结论。

| | 三层架构方案（上一轮对象） | ai-workflow v3.2.1 |
|---|---|---|
| **管控对象** | **一次请求**的完整生命周期 | **一个项目**的完整生命周期 |
| **时间尺度** | 秒到分钟 | 小时到天 |
| **执行主体** | 业务系统（Agent 调工具） | AI 模型（人机协作） |
| **最大风险** | 资金损失、状态不一致 | 返工、走偏、越界、无效劳动 |
| **强制力来源** | 代码（规则引擎、幂等表） | 提示词 + **机器门禁**（checks.py） |
| **失败代价** | 不可逆的财务后果 | 可重来的时间成本 |

**结论：ai-workflow 不应照搬三层架构方案的强制式机制。** 理由有二：
1. 它的哲学明确写着"**只取机制、不取强制**"（与 `superpowers` 的分界），强制式的 `fail_mode=closed` 全面套用会推翻用户「最高准则」；
2. 它的既有强项（熔断机制、证据分级、独立审核防偏差、能力路由）**已经超过**三层架构方案，盲目"升级"会退化。

**真正该借鉴的只有一件事**：把**已有的红线从"声明"变成"机器可判"**。因为 SKILL.md 自己写了「一旦决定走本流程，下面的红线在本流程内不可豁免」—— 既然不可豁免，就不该只靠模型自律。

---

## 1. 整体结构对比

### 1.1 现状结构（v3.2.1 实测）

```
入口分型（消息 / 正式需求 / Bug）
   ↓
任务分层（L0 轻操作 / L1 快速通道 / L2 全流程）  ← 开工第一件事判定
   ↓
三条红线（① 确认前不执行 ② 计划先行 ③ 工作区边界）
   ↓
阶段 0 → 1 → 2 → 3 → 4 → 5 → 6
   ↓
贯穿机制：自主决策层（能力路由 / 自适应计划 R1–R6 / 三层留痕）
         独立审核抽查制 · 返修复查（2 轮上限）· 熔断机制（F1–F5）
         自研工具标注与公开留痕 · 分流推送路由 · 技能库卫生
```

### 1.2 机器门禁的实际覆盖（★核心发现）

这是本次核对最重要的产出 —— **门禁强度在 7 个阶段上分布极不均匀**：

| 阶段 | 门禁强度 | 实现依据 | 说明 |
|---|---|---|---|
| 阶段 0 入口分型 | 弱 | `cmd_status` | 只读总览，不构成阻断 |
| 阶段 1 上下文与确认 | **无** | — | **红线① 完全靠模型自律** |
| 阶段 2 调研与方案评审 | **无** | — | 靠证据分级纪律（人审） |
| 阶段 3 计划与执行 | **强** | `check_plan` / `mark` / `decide` / `revise` | plan 对账 + 三层留痕 |
| 阶段 4 Office 产出 | **无** | — | **红线③ 靠"逐条人工确认路径"** |
| 阶段 5 测试交付与验收 | **强** | `check_plan --base` + `_check_fuse` | 熔断门禁为 **fail-closed** |
| 阶段 6 归档与复盘 | 弱 | `cmd_status` / Ledger | 追加留痕，不阻断 |

**同一份 SKILL.md 里，`_check_fuse` 是 fail-closed 的机器阻断（连"把节名写进代码块"这种绕过都专门防了），而红线③只有一句人工确认要求。** 这不是设计者疏忽，而是"哪些能机器化"的取舍未做完。

### 1.3 升级后结构（v4.0 提案）

**不新增阶段、不改变七阶段主链**，只做两件事：
- 在**已有的三个阶段缺口**上补机器门禁（阶段 1 / 2 / 4）
- 在阶段 3/5/6 上补**执行期留痕与度量**（trace、幂等指纹、副作用登记、指标沉淀）

```
入口分型 → 任务分层 → 三条红线（① ② ③ **均带可判定信号**）
   ↓
阶段 0 → 1 → 2 → 3 → 4 → 5 → 6
   ↓
贯穿（新增 4 项）：执行 trace · 步骤幂等指纹 · 不可逆副作用登记 · 任务级指标沉淀
贯穿（保持 8 项）：自主决策层 / 独立审核抽查制 / 返修复查 / 熔断机制 /
                自研工具留痕 / 分流推送 / 技能库卫生 / 证据分级
```

---

## 2. 各阶段步骤对比

### 2.1 阶段 0 · 入口分型

| | 现状 | v4.0 提案 | 变化 |
|---|---|---|---|
| 入口判定 | 消息 / 正式需求 / Bug 三型 | 同 | 不变 |
| 任务分层 | L0 / L1 / L2，L1 须查过模板才能确认 | 同 + **运行期升级通道**（见 2.5） | 补纠偏 |
| 工作区状态 | `checks.py status` 只读一瞥 | 同 + 显示**未闭合门禁**计数 | 增强可观测 |
| 熔断状态 | 读 `plan.yaml`，已熔断禁自动续跑 | 同 | 不变 |

**判断**：阶段 0 本身设计已成熟（L1 必须查过模板才能确认这条，就是一次自我修正）。唯一缺口是**层级只能在开工时定一次**。

### 2.2 阶段 1 · 上下文收集与任务确认

| | 现状 | v4.0 提案 |
|---|---|---|
| 上下文收集 | 相关文件 / 历史任务 / 台账，只读索引不整库 | 同 |
| 材料理解 | 产出 3-5 行理解摘要 | 同 |
| 追问轮次 | ≤ 2 轮，第 1 轮问全 | 同 |
| 六要素 | 目标 / 输入 / 交付物 / 验收 / 约束 / **工作区根** | 同 |
| **红线① 的判定** | **无任何可判定信号** | **新增 `用户放行` 字段**（时间 + 原话摘要） |

**关键差异**：现状能证明"我提问了"，不能证明"用户放行了"。plan.yaml 的 meta 里没有任何确认状态字段 —— 只读一遍 `PLAN_META_REQUIRED = ("任务", "验证信号", "工作区根")` 即可确认（`checks.py:71`）。

**诚实标注（重要）**：`用户放行` 字段**无法做到真正的机器强制** —— 模型可以自己填。它只能提高伪造成本（要求写入用户原话摘要），属于"纪律项 + 低成本留痕"，**不得宣称它是门禁**。这一点必须在实现时写在字段注释里，否则又是一条"写在提示里的门禁"。（参见 SKILL.md 自己的原则：「写在提示里的熔断不是熔断，是建议」。）

### 2.3 阶段 2 · 调研与方案评审

| | 现状 | v4.0 提案 |
|---|---|---|
| 查重分流 | 开发类 / 调研类分流 | 同 |
| 数据实测 | GitHub 指标走 `http_fetch --github-repo`，禁二手 star | 同 |
| 证据分级 | 六级标注，厂商倍数不算证据 | 同 |
| 并行硬规则 | 检索方向 ≥3 必须并行派子代理 | 同 |
| 引入前检查 | 许可证 + 基础安全 | 同 |
| 方案评审 | ≥2 候选，五维对比 | 同 + **候选方案的"不可逆性"单列一维** |
| **子代理回传保真** | **无校验** | **新增 `must_keep` 校验**（见 2.7） |

**关键差异**：现状对子代理的要求是"只回传浓缩结论（1-2k token）"，质量把关要求"未经核实不得进入下一环节（优先抽查可复现硬数据）"。但核实的对象是**硬数据**，**不覆盖"关键约束是否在浓缩中丢失"**。子代理回传时漏掉一句"仅限 X 目录"，主代理就会基于不完整信息做决策，且**没有任何机制会发现**。

### 2.4 阶段 3 · 计划与执行

这是现状最强的一环，也是改动最需要克制的一环。

| | 现状 | v4.0 提案 |
|---|---|---|
| 计划先行 | plan.yaml + 中文键 + 交付物必填 | 同 |
| 状态更新 | `checks.py mark`，不手工 sed | 同 |
| 三层留痕 | `mark` / `decide` / `revise` | 同 |
| 模型路由 | 阶段感知（strong/mid/cheap）+ 账本 | 同 |
| Token 纪律 | 热/温/冷三层压缩 40-60%，禁 90%+ | 同 + **可度量**（见 2.6） |
| 子代理编排 | Handoff 结构化六字段 | 同 + **`must_keep`** |
| **执行 trace** | **无** | **新增 `trace.jsonl`**（见 2.5） |
| **步骤幂等** | **仅推送工具层有**（`push --apply` / `init`） | **新增步骤级产物指纹** |

**两处关键差异**

**① 无执行 trace。** plan.yaml 记录的是**离散状态点**（做到哪了 / 为什么引入 / 为什么变了），**不含时序**：哪一步在什么时间执行、跑了什么命令、产出如何。后果是复盘四问（"哪个阶段返工最多"）只能靠回忆，无数据支撑。

**② 幂等只在工具层，不在步骤层。** `push --apply` 内容一致则 SKIP、`publish_tools.py init` 幂等 —— 这是**工具级**幂等。但**步骤级**没有：会话中断后重启，若某步被误判为未完成而重跑，会重复调外部 API、重复生成文件、重复推送。现状依赖 `mark` 的状态记录 + 模型自查，属声明式。

### 2.5 阶段 4 · Office 产出

| | 现状 | v4.0 提案 |
|---|---|---|
| 产出工具 | `office_io.py`，编码 `utf-8-sig`，Word 双层字体 | 同 |
| **红线③ 的执行** | **人工**："产出前先列将写入的文件路径清单，逐条确认落在工作区根内" | **机器**：`check_plan` 逐条比对 `交付物` 与 `meta.工作区根`，越界即 FAIL |
| 合法越界 | 靠记忆与授权记录 | `meta.越界授权` 登记（路径 + 依据 + 时间） |

**这是最确凿的一处缺口**，有直接代码证据：

```python
# checks.py:252-271  audit_step()
for token in tokens:
    if not _is_file_like(token):
        nonfile.append(token)
    elif not any(c.exists() for c in _candidate_paths(token, base)):
        missing.append(token)        # ← 只判断「存在性」，不判断「是否越界」
```

`audit_step` 只做 `c.exists()`；`_candidate_paths(token, base)` 负责解析路径，**但不做任何与 `meta.工作区根` 的前缀比对**。也就是说：**一个把交付物写到工作区外的任务，`checks.py plan` 会判 OK。**

### 2.6 阶段 5 · 测试交付与验收

| | 现状 | v4.0 提案 |
|---|---|---|
| 测试证据 | 出示原始输出，非"我认为没问题" | 同 |
| Anti-drop 对账 | `checks.py plan --base` | 同 + **越界检查** |
| 熔断门禁 | `_check_fuse`，fail-closed | 同（**现状已很强，不动**） |
| 审核闭合 | 三类必送审 / 抽查豁免留痕 | 同 |
| 交付自检表 | 9 项，含"references 按需加载"（人审） | 同 + **上下文用量估算**（度量替代人审） |
| 发布协作 | 不可逆动作单独确认 | 同 + **副作用登记核对** |

**现状的亮点（必须认可）**：`_check_fuse` 是整套技能里工程质量最高的部分 —— fail-closed、剥除 fenced code block 防"写进代码块绕过"、标题行独占校验防"前缀粘连绕过"、复位两步缺一不可且明确"不做自动复位"。**这套设计已经达到了三层架构方案里 `fail_mode=closed` 的水准**，甚至更细（它还防了具体的绕过手法）。

### 2.7 阶段 6 · 结果核验、归档与复盘

| | 现状 | v4.0 提案 |
|---|---|---|
| 结果核验 | 对照验收标准逐条给"达成/未达成 + 证据" | 同 |
| Ledger 归档 | 追加不重写 | 同 |
| 复盘四问 | 靠回忆 | **用指标回答** |
| 沉淀分流 | 流程→技能 / 配置→模板 / 坑→反模式 / 工具→推送 | 同 |
| 任务目录治理 | `archive_tasks.py`（带引用前置门禁 + sha256 自证） | 同 |
| **任务级指标** | **无** | **新增 `metrics.jsonl`** |

**关键差异**：现状没有任务级统计 —— 返修轮次分布、熔断触发频次、抽查豁免比例、门禁 FAIL 率、阶段耗时，一个都没有。这导致**技能自身的演进缺少数据依据**（本工作区那份《工作流瓶颈分析与优化方案》就是靠人工翻 10 个 plan.yaml 写出来的）。

---

## 3. 关键差异清单

### 3.1 现状的 9 处薄弱点（均有证据）

| # | 薄弱点 | 证据 | 性质 |
|---|---|---|---|
| **W1** | **红线③ 无机器门禁** | `checks.py:252-271` 只做存在性判断 | 可机器化，未做 |
| **W2** | **红线① 无任何可判定信号** | `checks.py:71` meta 必填项只有三项 | **不可完全机器化**（诚实标注） |
| **W3** | 无执行 trace | plan.yaml 只有离散状态点，无时序 | 可补 |
| **W4** | 幂等只到工具层，未到步骤层 | 幂等实现仅在 `publish_tools.py` / `push_router.py` | 可补 |
| **W5** | 无不可逆副作用登记 | 只有"执行前单独确认"，无"执行后清单" | 可补（对接熔断第 2 步"冻"） |
| **W6** | 子代理结论无保真校验 | 质量把关只覆盖硬数据，不覆盖约束丢失 | 可补 |
| **W7** | 上下文预算只有目标值、无度量 | "压缩 40-60%"是目标；自检项靠人审 | 可补（近似度量） |
| **W8** | 任务层级无运行期重分类 | L1/L2 在阶段 0 一次判定 | 可补 |
| **W9** | 无任务级指标沉淀 | 无任何统计产物 | 可补 |
| **W10** | 门禁自身失效时的行为未定义 | `checks.py` 异常时无规定 | 可补（借鉴 `fail_mode`） |

### 3.2 现状已强于三层架构方案的部分（公正对比的前提）

**这一节必须写，否则对比失去意义。**

| ai-workflow 独有 | 三层架构方案有吗 | 说明 |
|---|---|---|
| **熔断机制（F1–F5 + 五步动作 + 防绕过）** | ✗ | 三层方案没有任何"全局停机"机制 |
| **独立审核防六偏差**（位置交换双跑 / 异家族裁判 / 长度格式不计分 / pin 版本 / 容差带） | ✗ | 三层方案只有"复核"概念 |
| **证据分级六级**（RCT / 基准实测 / 大样本自评 / 官方推荐 / 实践观点 / 反例） | ✗ | 三层方案无证据分级 |
| **能力路由**（知识/算力/事实/手脚 + 反合理化红线） | ✗ | 三层方案无"该不该引入辅助"的决策机制 |
| **自适应计划 R1–R6** | ✗（只有朴素"重规划"） | — |
| **自研工具标注与公开留痕** | ✗ | 三层方案完全没有这个概念 |
| **分流推送路由 + 推送后独立验收（7 判据零代码共享）** | ✗ | — |
| **技能库卫生四条约定** | ✗ | — |

**判断**：ai-workflow 在"**长周期协作的治理**"上已经走在前面；三层架构方案在"**单次执行的一致性**"上更细。两者的交集是：**执行期的可验证性**（幂等、留痕、闭环）。v4.0 提案只在这个交集里做文章。

---

## 4. v4.0 升级提案（10 项）

| # | 升级项 | 落在哪 | 判据 | 优先级 |
|---|---|---|---|---|
| **U1** | **红线③ 机器化**：`check_plan` 逐条比对交付物与 `meta.工作区根` 前缀，越界 FAIL；新增 `meta.越界授权` 登记合法越界 | `checks.py` + `plan-template.yaml` | 故意把交付物写到工作区外 → 必须 FAIL | **P0** |
| **U2** | **红线① 留痕**：meta 增 `用户放行`（时间 + 原话摘要），非空校验 | `plan-template.yaml` + `checks.py` | 字段非空 | P1（**非门禁**，见 §2.2 诚实标注） |
| **U3** | **执行 trace**：`tasks/<任务>/trace.jsonl`，每步记 `{时间, 步骤id, 动作, 命令, 结果摘要, 用时}`；新增 `checks.py trace` 子命令 | `checks.py` + SKILL.md 阶段 3 | 任务完成后可回放出完整时序 | P1 |
| **U4** | **步骤级幂等指纹**：昂贵/不可逆步骤在 plan.yaml 增 `输入指纹` / `产物指纹`；重跑时指纹一致则跳过并在 trace 记 `[SKIP] 指纹一致` | `plan-template.yaml` + SKILL.md 阶段 3 | 中断重启后同步骤不重复调外部资源 | P1 |
| **U5** | **不可逆副作用登记**：meta 增 `不可逆副作用[]`（时间/动作/目标/可否回滚/回滚方式）；阶段 5 交付前核对 | `plan-template.yaml` + SKILL.md 阶段 5 | 有推送/发布的任务必须列出，熔断时可直接引用 | **P0** |
| **U6** | **子代理结论保真校验**：Handoff 增 `must_keep`（主代理指定必须保留的约束/数字/路径），回传后主代理逐项校验 | `references/orchestration.md` + SKILL.md | 故意让子代理丢一个约束 → 必须被发现 | P1 |
| **U7** | **上下文用量度量**：`checks.py` 增轻量 token 估算（字符数折算），写进阶段 5 自检表，标注为**估算值** | `checks.py` + SKILL.md 阶段 5 | 自检表出现数字而非"未超量" | P2 |
| **U8** | **任务级指标沉淀**：`metrics.jsonl` 记 `{返修轮次, 是否熔断, 门禁FAIL数, 豁免比例, 阶段耗时}` | `checks.py` + SKILL.md 阶段 6 | 复盘四问能用数据回答 | P2 |
| **U9** | **L1→L2 运行期升级通道**：命中（模板不适用 / 产出规模超预期 / 出现跨文件改造）→ 升级为 L2、补 plan.yaml、留痕升级事件 | SKILL.md 任务分层节 | 升级事件在 plan.yaml 可见 | P2 |
| **U10** | **门禁失效时的行为定义**：`checks.py` 不可用时 —— 不可逆动作 **fail-closed（拒绝）**，只读动作放行 | SKILL.md 新节 | 明确写出，不留空白 | P1 |

### 4.1 关于 U1 的实现要点

```
越界判定 = 交付物路径（解析为绝对路径后）
           ∉ meta.工作区根 的子路径
           且 ∉ meta.越界授权（当次授权登记项）
```

要处理的边界情况（否则会造出"假 FAIL"）：
- 平台差异：Windows 大小写不敏感、路径分隔符 `/` 与 `\` 混用
- 符号链接与 `..` 归一化
- 基础设施例外路径（技能目录、venv、cache）**不算越界**
- plan.yaml 中已存在的历史绝对路径写法（须兼容）

> ⚠️ 本项目有前车之鉴：`checks.py` 的"文档引用完整性"因**扁平匹配**（不区分"引用"与"举例提及"）而反复产生假阳性。U1 必须从一开始就设计**成对的阳性/阴性对照测试**，否则会重蹈覆辙。

---

## 5. 优势分析

| 维度 | 升级后得到什么 |
|---|---|
| **红线可验证** | 红线③从"人审一句要求"变成"越界即 FAIL"。这是本提案最实质的收益 —— 它让 SKILL.md 自己那句"红线不可豁免"第一次有了机器支撑 |
| **复盘有数据** | W9 + W3 解决后，复盘四问从"回忆"变"查账"，技能演进的依据从人工翻 plan 变统计 |
| **中断可恢复** | W4 解决后，长任务中断重启不重复烧外部资源（API 配额、推送、大计算） |
| **失败可收敛** | W5 解决后，熔断时能立即回答"已经发生了什么不可逆的事"，而不是靠回忆 |
| **约束不丢失** | W6 解决后，子代理浓缩结论的关键约束有校验，堵住一类静默失真的入口 |
| **不破坏既有强项** | 10 项全部是**增量**：不动熔断、不动独立审核、不动证据分级、不动能力路由、不动红线语义 |

---

## 6. 潜在影响与代价

### 6.1 与"只取机制不取强制"的张力（最需要警惕）

W1/W2 的机器化会**增加流程的刚性**。但要注意区分：

- 「只取机制不取强制」约束的是 **要不要调用本技能**（见 SKILL.md「与强制式机制的分界」）—— 这是**入口判断**；
- 本提案动的是 **技能内部的执行纪律** —— SKILL.md 已明确"一旦决定走本流程，红线不可豁免"。

**所以 U1 不违反该原则。** 但 U2 不同：它试图给"用户确认"加机器痕迹，注定做不到强制，**必须如实标注为纪律项**，否则就是自欺。

### 6.2 假阳性风险（最高）

机器门禁最大的敌人不是漏判，是**误判**。本工作区已有先例：`checks.py` 的引用完整性检查因扁平匹配反复误报，`push_router.py` 因 `[自研工具]` 字符串出现在文档演示格式里而假阳性。

U1（越界）、U6（保真）都容易踩。**缓解：每项门禁必须配阳性 + 阴性对照测试**（故意造一个越界案例验证会红，造一个合法案例验证不会误红）。

### 6.3 记录成本

U3（trace）+ U8（metrics）要求每步都记录。对**长任务这是纯增益**（可复盘），对**短任务（L2 但只有 3 步）是纯开销**。

**缓解**：按任务层级启用 —— L0 不记，L1 只记 metrics 不记 trace，L2 全记。

### 6.4 与已有机制的耦合

U4（步骤幂等指纹）+ U9（层级升级）都会改动 `plan-template.yaml`。而 plan-template 的字段被 `checks.py` 的 `PLAN_META_REQUIRED` / `PLAN_STEP_REQUIRED` 与 `checks.py skill` 的"plan-template 必填键"检查同时引用（`checks.py:174-178`）。

**改动一处要同步三处**，否则会出现"技能自检 FAIL"或"新字段不被校验"的脱节 —— 这正是本工作区踩过的"改一处忘一处"。

### 6.5 成本量级估计

| 升级项 | 实现量 | 风险 |
|---|---|---|
| U1 | 小（一个函数 + 边界处理） | 中（假阳性） |
| U5 | 小（字段 + 阶段 5 核对） | 低 |
| U3 / U8 | 中（新子命令 + 写入时机） | 低 |
| U4 | 中（指纹算法需按步骤类型定义） | 中 |
| U6 / U9 | 小（文档 + 流程） | 低 |
| U2 / U7 / U10 | 小 | 低（U2 须诚实标注） |

---

## 7. 结论与优先级

### 7.1 总判断

> ai-workflow v3.2.1 与"升级后"的差别，**不是能力差距，而是「同一份红线在两处的执行强度不一致」**：
> `_check_fuse` 能做到 fail-closed 并专门防绕过，说明**这套技能有能力做好机器门禁**；
> 而红线③至今没有一行代码校验 —— 不是做不到，是**没做完**。

### 7.2 落地优先级

| 档位 | 项目 | 理由 |
|---|---|---|
| **P0（建议先做）** | **U1 红线③ 机器化**、**U5 不可逆副作用登记** | U1 补的是"技能自己承诺过、但代码没实现"的门禁；U5 让熔断时能立刻回答"已发生什么" —— 两者都直接对接**不可逆动作**，错了最贵 |
| **P1（收益明确）** | U3 执行 trace、U4 步骤幂等、U6 保真校验、U10 门禁失效行为、U2 红线① 留痕（**诚实标注为纪律项**） | 补齐执行期可验证性 |
| **P2（锦上添花）** | U7 上下文度量、U8 指标沉淀、U9 层级升级通道 | 改善复盘与演进依据 |

### 7.3 一条必须先立的纪律

**每一项新门禁都必须配阴性对照测试**（故意造一个该被拦的案例，确认它会红；再造一个合法案例，确认它不会误红）。

理由是本工作区的实测教训：这里的门禁出问题，**绝大多数不是漏判，而是误判**（引用完整性的扁平匹配、`[自研工具]` 字符串假阳性，都是同一个病）。没有阴性对照的门禁，等于给流程加了一个随机 FAIL 源。

### 7.4 建议的下一步

本文是分析，**不动技能本体**。若要推进：
1. 先就 U1 的**边界情况清单**（§4.1）达成一致 —— 这是最容易出假阳性的地方；
2. 按 ai-workflow 自身流程立项（落 plan.yaml、走阶段 0-6），**把本提案当作阶段 2 的候选方案之一**，而不是直接实施；
3. U1 的验收信号建议直接用阴性对照：**故意把某个交付物路径写到工作区外 → `checks.py plan` 必须 FAIL**。
