---
name: ai-workflow
description: AI 标准化多阶段工作方法论，适用于量化分析、跨境电商选品、小说写作、学习资料生成、技术调研等项目：入口分型（消息 / 正式需求 / Bug）与任务分层（轻操作豁免 / 快速通道 / 全流程）→ 查可用 Skill → 上下文收集与任务确认 → 调研与方案评审 → 编排执行（**自主决策层**：能力路由选型 + 自适应计划与重规划 + 决策留痕，机器可校验；计划先行、精检索、子代理 Handoff、**阶段感知模型路由**降本 70–90%）→ 独立审核**抽查制**（默认自查 + 机器门禁；高危产出／用户点名／返修触顶三类必送审，送审时执行者不自审）与返修复查 → 测试交付与验收（含发布协作）→ 结果核验、履历归档与复盘，**自研工具与技能须标注（名称/用途/适用场景）并按「本体 → ai-workflow-skill ／ 自研工具 → ai-workflow-tools」两类规则分流推送 public 仓库、交付附仓库链接**，流程末梢设**熔断机制**（触发即冻结并升级，机器可判定）。内置工作区边界、Token 纪律、评审防偏差与反模式清单。触发词：AI 工作流 / 按流程干活 / 标准流程 / 工作方法论 / ai-workflow。
agent_created: true
selfbuilt: true
repo: https://github.com/Garvin666/ai-workflow-skill
version: 3.2.0
---

# AI 工作流 v3.2.0

> **[自研技能]** 名称：`ai-workflow` ｜ 用途：为多阶段任务提供标准化流程编排（入口分型 / 任务分层 / 计划先行 / 自主决策层 / 独立审核抽查制 / 熔断），并配一套**机器可校验**的工具与门禁 ｜ 适用场景：产出文件或代码或报告、多步且步骤间有依赖、数据分析、跨文件改造、调研查重、方案评审；纯问答 / 查询 / 翻译 / 单文件读取不适用 ｜ 仓库：https://github.com/Garvin666/ai-workflow-skill

按阶段顺序执行任务；先做**入口分型**（消息 / 正式需求 / Bug），Bug 类必须走「复现→根因→修复→回归」。

> **适用范围（v2.6.2 明确，v3.0.0 沿用）**：本流程**只在被调用时生效** —— 要不要调用，由模型按「**能否让结果更快更好**」自行判断（见用户级 `USER.md`／`MEMORY.md` 的「最高准则」）；日常任务里"要不要先出计划、要不要先等确认"按**风险分级**处理，与本流程无关。**一旦决定走本流程，下面的红线在本流程内不可豁免** —— 既然选了走流程，就把它走完，否则没有调用它的意义。

**三条红线（本流程内不可豁免）**：① **用户确认前不执行** ② **计划先行**（未落盘 plan.yaml 不得开始处理）③ **工作区边界**（读写与产出只允许落在本次任务指定的工作区根目录内，越界必须先问）。其余按「任务分层」裁剪。**脚本用法、故障排查、变更日志见 `references/ops.md`。**

## 任务分层（开工第一件事）

| 层级 | 判定标准 | 执行方式 |
| --- | --- | --- |
| **L0 轻操作豁免** | 纯问答/查询/翻译、单文件读取、改一行字或一个变量、临时状态检查 | **直接做**，不建任务目录、不走阶段 0-6 |
| **L1 快速通道** | 阶段 0 命中 `assets/templates/` 同类模板且无实质差异（**排除 `_` 开头的示例／字段说明文件**，如 `assets/templates/_示例-数据分析.yaml`）；**L1 需待阶段 0 查过模板后才能确认**，不能在"开工第一件事"时直接判 L1 | 提议"按上次配置开跑"→ 确认后跳过追问循环，直接引用模板缓存的查重结论 |
| **L2 全流程** | 产出文件/代码/报告、多步操作、数据分析、跨文件改造、方案评审 | 完整走阶段 0-6 |

判定存疑时**默认 L2**。用户说"直接做/别走流程"则从其指令——**对用户的明示授权可替代"确认前不执行"，但"计划先行"仍需满足**（可在同一消息内给计划并直接执行）。

**环境预检**（仅当要跑脚本时）：venv 存在且能 import requests/openpyxl/python-docx/pypdf/pyyaml/duckdb，且 `Scripts/ast-grep.exe` 存在；异常则 `powershell -File scripts\setup_env.ps1`。检测与排障命令见 `references/ops.md`。

## 自主决策层（贯穿全流程，v3.0.0 新增）

本技能不只是一条固定流程 —— 它在执行中自行判断**该不该引入外部辅助、该引入哪一种**，并在事实基础变化时**自行调整计划**。两份手册**按需加载，勿预读**：

| 机制 | 一句话 | 手册 |
| --- | --- | --- |
| **能力路由** | 这一步缺的是 **知识／算力／事实／手脚** 中的哪一类？走「识别缺口 → 估收益 → 估开销」三步；**说不出可验证收益就不引入** | `references/capability-routing.md` |
| **自适应计划** | 一步 = 一个**最小可独立验证单元**；命中 R1–R6 即**必须**重规划，且**先留痕再改动** | `references/adaptive-planning.md` |

**与强制式机制的分界（重要，防口径漂移）**：外部参照的 `superpowers`（其 `using-superpowers` 技能）采用**无条件强制**——"只要有 1% 可能某技能适用，就必须调用"，并用反合理化红线表封死"跳过"的念头。本技能**只取其机制（按需加载／反合理化表／声明制／优先级），不取其强制**：要不要走本流程、要不要引入某能力，一律按「**能否让结果更快更好**」自判（即用户「最高准则」）。**照搬强制口径会推翻该准则**，故在此明确排除。

**三层留痕（可追踪性的载体，全部落在 `plan.yaml`）**：

| 层次 | 字段 | 更新方式 | 回答什么 |
| --- | --- | --- | --- |
| 步骤状态 | `steps[].状态` | `checks.py mark` | 做到哪了 |
| 能力决策 | `meta.决策记录` | `checks.py decide` | 为什么引入这个能力 |
| 计划修订 | `meta.计划修订` | `checks.py revise`（或手工追加） | 计划为什么变了 |

三层均为**可选字段**：纯本地读写、无外部辅助的步骤不必产生决策记录；未发生重规划时无修订条目。**留痕只记录真实发生过的决策，不补造、不填空。**

**引入能力时的声明制**（一行，写在进入该步之前；它同时是决策记录的原始素材）：

```
[能力] <知识|算力|事实|手脚> → <具体手段> ← 依据：<一句话判据>
```

**执行期六动作**（阶段 3 每步循环，详见 `references/adaptive-planning.md`）：
重述目标与判定标准 → 能力路由 → 执行 → 跑验证信号并**出示原始输出** → 更新状态 → 判 R1–R6（命中则重规划）。

## 工作区边界（红线③，开工前必做）

- **工作区根由用户按任务指定**（每次任务一个根），写入任务确认表的 `workspace` 字段与 plan.yaml 的 `工作区根`；用户未指定时默认当前会话工作目录，并在确认表里显式标注供其纠正。
- **任务目录一律建在工作区根下的 `tasks/`**（含 plan.yaml、交付物、`tmp/`）——即 `checks.py status/plan` 的默认基准 `工作区根/tasks/`。**技能目录的 `tasks/` 只记录技能自身演进类的任务**，不得用来存别的项目的任务。
- **允许**：工作区根内的读取、写入、产出、脚本临时文件（放 `tasks/<任务>/tmp/`）。
- **越界（根目录之外的任何路径）**：先停下来说明「要访问 X、因为 Y、需要什么授权」，得到**当次明确同意**才执行，并在交付说明里记录该次授权。上一任务的授权不自动延续到本任务。
- **基础设施例外**（不算越界，仅限工具自身用途，不得存放用户数据与产出）：

  | 例外路径 | 允许用途 |
  | --- | --- |
  | `~/.workbuddy/skills/ai-workflow/` | 读取技能指令、改技能本体（改技能属独立任务）、**仅"技能自身演进"类任务**才可写 `tasks/` 记录 |
  | `~/.workbuddy/binaries/python/envs/ai-workflow/` | 调用 venv 解释器与依赖 |
  | `~/.workbuddy/cache/ai-workflow/` | http_fetch 缓存读写 |

- 用户显式给出的其他路径（如「去 E:\X 读数据」）= 该路径临时纳入本任务范围，但仍不扩展到其上级或兄弟目录。

## 阶段 0：入口分型 + 开工前查可用 Skill（约 1 分钟）

1. **入口分型**（先判定来源，不同来源走不同侧重）：

   | 入口 | 判定特征 | 侧重 |
   | --- | --- | --- |
   | **消息类** | 一句话需求、口头想法、信息不全 | 侧重阶段 1 **补上下文**（追问补齐来源/目标），允许先出草案再迭代 |
   | **正式需求** | 带明确目标/交付物/验收口径 | 走完整六阶段；阶段 2 方案评审必做 |
   | **Bug/缺陷** | 报告"某处不对/报错/数据错"，含现有产物 | 必须补 **复现 → 根因定位 → 修复 → 回归验证** 闭环（`references/playbook.md` 缺陷修复节）；**禁止未复现就改** |

   入口不明时按「正式需求」处理，并在确认表里向用户确认来源类型。
2. **列本地 skill**：`Glob ~/.workbuddy/skills/*/SKILL.md` 与 `.workbuddy/skills/*/SKILL.md`，读各自 frontmatter 的 `name`／`description` 判断是否匹配；无匹配则调用 **`find-skills` skill**，或用推荐市场检索。
3. 判定：匹配 → 复用；部分匹配 → 复用可复用部分；无匹配 → 全新执行。
4. 查 `assets/templates/`（字段规范见 `assets/templates/README.md`）→ 命中走 L1。
5. **工作区状态一瞥**：`checks.py status` 看已有任务与交付物完成度，**不逐个打开历史任务文件**；并留意各任务 `plan.yaml` 的 `熔断状态`——为 `已熔断` 的任务**禁止自动续跑**，只能等用户复位（见「熔断机制」）。
6. **确认工作区根**：确定本次任务的 `workspace`（用户指定 / 默认当前会话目录），写进确认表与 plan.yaml；发现所需输入在工作区外时，按红线③先申请授权。

## 阶段 1：上下文收集与任务理解（追问 ≤2 轮）

**红线①：未确认前禁止执行处理类脚本或产出文件**（`checks.py status`／`skill`、`Read`／`Grep`／`Glob` 等**只读检查不受限**——阶段 0 的工作区状态一瞥即属此类；L0/L1 除外）。

1. **上下文收集**（先收再问，避免拿空问题打扰用户）：收集**相关文件**（输入资料、上游产物）、**历史任务**（`tasks/` 同类任务的 plan 与结论，看 `checks.py status`）、**既有交付物与台账**（工作区 `Ledger.md`）；只读索引与摘要，禁整库加载。收集范围仍受限红线③。
2. **材料理解**：读输入资料后产出 3-5 行**理解摘要**（这份材料是什么、关键结构/口径、可疑点），随确认表一并给出——防止"没看材料就开问"。
3. **代码调查**（仅编码类任务）：定位相关文件与入口 → 读懂现有实现 → 列出**影响面**（谁调用它、改了会波及什么）与**最小改动点**；调查结论进方案评审。跨行结构用 `ast-grep`、符号用 `ast-grep outline`。
4. **第 1 轮就把缺口问全**（1-4 个问题，选择题优先并附推荐项），能推断的要素默认推断、在确认表里标注"（默认，如无异议按此）"——**禁止挤牙膏式多轮追问，追问总轮次上限 2 轮**。
5. 六要素齐备：① 目标 ② 输入 ③ 交付物（格式/路径/命名）④ 验收标准 ⑤ 边界约束 ⑥ **工作区根**（本任务允许读写的目录，红线③依据；未指定则默认当前会话目录并标注「（默认，如无异议按此）」）。
6. 输出「任务确认表」并在**同一轮消息**里请求放行——用户回"确认/开干"即放行；**禁止再单独发起一轮确认提问**。
7. **模板沉淀**：确认后存为 `assets/templates/<任务类型>.yaml`，遵守 `assets/templates/README.md` 的统一 schema；同类型更新而非新建。

## 阶段 2：调研与方案评审（查重优先 + 证据分级）

**未完成查重并给出复用决策前，禁止进入实现环节。**

1. **查重分流**：开发类 → 开源项目/数据集查重（产出对比表 + 直接复用/克隆改造/自研三选一）；调研类 → 既有报告与横评盘点 + 数据实测。
2. **数据一律实测**：GitHub 指标用 `http_fetch.py --github-repo`（含限流退避、多仓库并发、本地缓存）。**禁止二手 star 数**。
3. **证据分级标注**：【RCT】/【基准实测】/【大样本自评】/【官方推荐】/【实践观点】/【反例】。**厂商案例、营销倍数、"N× 效率"不得作为证据**。详见 `references/quality-gates.md`。
4. **并行硬规则**：检索方向 ≥3 个必须并行派子代理（见「子智能体编排」），禁止串行等待。
5. **引入前检查**：许可证（须 MIT/Apache-2.0 等宽松型，CC-BY-4.0 不适合代码）+ 基础安全（混淆脚本/硬编码密钥/异常外联/`pushed_at` 是否停更）。
6. **方案评审**（禁止"只有一条路"就开工）：产出 **≥2 个候选方案**（含"最小改动方案"与"最优方案"），逐项对比 **可行性 / 成本 / 风险 / 可回滚性 / 与验收标准的匹配度**，给出推荐与理由；编码类方案必须引用阶段 1 的影响面分析。**非工程类任务**（数据分析／文档／调研）可换用 口径选择／样本范围／结论稳健性／可复算性 等维度对比。
7. **方案级确认**：若候选方案存在**实质分歧**（交付物形态、成本档位、不可逆操作、需用户拍板的取舍），把选项摆给用户选；无分歧则在确认表已放行的范围内直接采用推荐方案并注明。**不可逆或需外部协作的动作（发布上线、推送远端、花钱）必须单独确认。**

## 阶段 3：AI 处理（计划 → 验证闭环 → 精检索 → 编排 → 质检）

**红线②：未产出 plan.yaml 前禁止调用处理脚本；每步未明确目标前禁止检索。**

1. **任务目录制**：`<工作区根>/tasks/<任务类型>-<YYYY-MM-DD>/` 内存放 plan.yaml、交付物、中间数据（**目录必须建在工作区根下**；`工作区根/tasks/` 才是 `checks.py status/plan` 的基准。技能目录的 `tasks/` 只存技能自身演进类任务，不得存其他项目）。命名规则：任务类型为**不含空格与斜杠**的中文短语，日期取**任务创建日**（非完成日）；同日同类型多次迭代加版本后缀区分，如 `技能增强-ai-workflow-v2.1-2026-09-10/`；归档时整体移入 `tasks/_archive/<YYYY-MM>/`。plan.yaml 严格用 `assets/plan-template.yaml` 的**中文键格式**，**`交付物` 必填且写完整路径**（阶段 5 自动对账依赖它）。状态用 `checks.py mark <plan.yaml> <id> <状态>` 更新，不手工 sed。**本步若新建自研工具/技能文件，须同时写标注块并在 `meta.自研工具` 登记一项**（见「自研工具与技能：标注与公开留痕」）。
2. **验证闭环前置检查**：动手前回答"完成时用什么可判定信号证明它成了？"——有信号就写进「验证方式」；无信号**先补最小验证物**，补不了就显式登记「人审点」。交付前**出示证据而非声称成功**。各类任务的可判定信号参考：代码类＝测试/构建码/lint/断言；Office 类＝写回后用 `office_io` 读回校验；**数据分析类＝交叉校验脚本（总额对账／行数一致／空值率）+ 抽样复算**；**调研类＝资源条目数达标且每条含链接 + 关键链接实测探活（`http_fetch --check-links`）+ 时效性说法经官方页复核 + 反例/过期信息已剔除**；改造类＝`checks.py skill` 全绿 + py_compile 通过；**性能类＝耗时下降「且」输出与旧版逐字节一致**（预热后多次取最小值，≥2 轮独立复现，禁用"冷基线 vs 热优化"的假加速）。**完整信号表见 `references/quality-gates.md`（含未列出的任务类型）**。
3. **逐步执行**：每步前重述目标与判定标准，完成后更新状态；未达成先修正再推进。**执行期六动作、能力路由判定与 R1–R6 重规划触发**见 `references/adaptive-planning.md` 与 `references/capability-routing.md`（按需加载）。
4. **进度汇报节奏**：每完成 2-3 步或切换阶段回报**一行**（`步骤 x/y 完成 → 下一步：…`）；只在受阻、需决策、完成时展开。预计 >5 分钟的任务先报一句预期与中间产出节点。
5. **精确检索（先选对工具再搜）**：网页 → `http_fetch <URL> --grep --max-chars`（缓存自动兜底）；**GitHub 找项目 → `http_fetch --github-search "<query>"`**（限定符 `language:` `stars:` `topic:` `pushed:` `archived:`），**GitHub 找代码实现 → `--github-code-search "<代码片段> repo:owner/name"`**（⭐**强制 `GITHUB_TOKEN`**，返回"哪个仓库的哪个文件"+命中片段），取指标 → `--github-repo a/b,c/d`（凭据按 `--token` → `GITHUB_TOKEN` → `GH_TOKEN` → `gh auth token` 自动解析，已登录 gh CLI 即免配置）；两个搜索都支持 **`--dry-run`**（只打印将请求的 URL，不发请求，无 token 也能验证参数构造）；本地小目录 → 内置 `Grep`/`Glob`；**跨行/结构模式 → `ast-grep`**（"先 A 后 B 且中间无 try"这类；⚠️ 其 pattern **不支持正则**，且**复合语句必须写全**，文本匹配回 `Grep`；符号级用 `ast-grep outline`）；**大数据集/大目录 → `data_query.py files|sql|find`**（DuckDB 零导入直查 Parquet/JSON/CSV；`big` 默认跳过 `.venv` 等依赖目录，旧口径加 `--no-skip`）。**交付物里的外部链接一律先用 `http_fetch.py --check-links <urls.txt>` 并发探活**（只取状态码，不下载正文）。引用标明来源。
6. **Token 纪律**：稳定前缀在前吃 KV 缓存；上下文分热/温/冷三层、每层压缩 40-60%（**禁止 90%+**）；记忆只读索引与摘要；**项目上下文文件保持 30-60 行、只写模型推断不出的内容、禁止提交 `/init` 产物**（ETH Zurich 实测 -3% 成功率 / +20% 成本）；选型黑名单 RouteLLM/GPTCache/Memobase，LiteLLM 须 ≥1.83.7。
### 阶段感知模型路由（降本 70–90%，不损质量）

不同阶段的推理成本弹性差异极大（规划容错低、探索/测试/文档容错高）。按阶段选模型，而非全程烧旗舰——这是 `工作流瓶颈分析与优化方案` 优化3（路由报告行业实测编码场景成本降 70–90%，findings-3 Haiku 比 Opus 便宜约 15×）：

| 阶段 / 任务类型 | 模型档位 | 理由 |
| --- | --- | --- |
| **规划**（六要素澄清、方案评审、≥2 候选对比） | 强模型（旗舰） | 策略错则全盘错，容错低 |
| **关键实现 / 安全相关代码** | 强模型 + 人工双审 | 红线不可破；正确性优先于成本 |
| **实现（常规）** | 中档 | 容错中等，成本敏感 |
| **探索 / 调研 / 测试 / 文档 / 格式转换** | 便宜模型（本地 Ollama + DeepSeek/Qwen，或 Haiku 级） | 只读或可容忍，烧旗舰是浪费 |

硬规则：① 探索段**默认**便宜模型，不得无理由升档；② 红线相关的产出（安全/资金/权限/凭据/不可逆动作）仍强模型 + 必要双审，不因降本放松查收；③ 模型档位在 plan.yaml 的「验证方式」或阶段 1 确认表注明，便于回溯与成本核对。

**落地与实测**：调用侧用 `ai_call.py --tier <strong|mid|cheap>` 自动按 `scripts/model_tiers.json` 解析 base/key/model/price，并把每次调用的档位/token/成本写入 `~/.workbuddy/cache/ai-workflow/usage_ledger.jsonl` 账本（详见 `references/ops.md` 脚本表）。**降本% = (全部强模型成本 − 路由后成本) / 全部强模型成本**，按账本真实 token 用量 × 官方价目表折算（避免"感觉快了"幻觉，必须有可判定信号）。本机若未设 `AI_API_KEY` 或 Ollama 未起，则先用「真实 token 用量 × 官方价目表」做可复现测算（token 只取决于文本，与是否联网无关），配好凭据/本地模型后账本直接出真实账单口径降本%。

7. 批量总结/生成用 `ai_call.py`（`--batch-file` + `--concurrency` 并发、`--model` 覆盖、`--stats` 用量回显）；项目差异化流程见 `references/playbook.md`（模板已就绪，可走 L1）。

### 子智能体编排

细则见 `references/orchestration.md`。硬规则：**Handoff 结构化**（`task`/`background`/`input_paths`/`acceptance`/`deliverable_path`/`evidence_rule`，禁止自由文本）；**独立才并行**（建议 ≤4；有依赖必须串行；只读调研用轻量模型）；子代理**只回传浓缩结论**（1-2k token）；主代理负责拆分与汇总。

### 质量把关（信任但验证，通用原则）

细则见 `references/quality-gates.md`。硬规则：子代理产出**未经核实不得进入下一环节**（优先抽查可复现硬数据）；评审由模型完成时须**防六偏差**（位置交换双跑、异家族裁判、长度/格式不计分、pin 版本、容差带）。

> 本小节是**通用原则**（主代理对子代理产出做核实）；下方「独立审核」是**抽查制的送审规则**——默认不派审核子代理，命中三类情形时必须派。两者不互相替代：**"我已经检查过了"不能替代送审**。

### 独立审核（抽查制：默认自查，三类必送）

> **v2.5.3 变更（用户 2026-09-11 决定）**：本环节由"每个关键产出都必须送审"的**强制门禁降级为抽查制**——理由是稳定发生的双倍人力税（见 `工作流瓶颈分析与优化方案` B3）。**"执行者不得自审"这条禁令未取消**，它只在命中下方三类情形时生效；未命中的产出改由**主代理自查 + 机器门禁**兜底。

**默认路径（不派独立代理）**：机器门禁构成确定性的兜底——`checks.py plan`（交付物对账 + 熔断门禁）、`checks.py skill`、`http_fetch.py --check-links`（链接全量探活）、`office_io` 写回读回、py_compile／测试断言。产出须在 `plan.yaml` 的验证方式注明 `抽查豁免（<理由>）`。

**必送独立审核的三类情形**（命中任一即必须送审，**不得**以"我已检查过／质量把关已做／产出很小"为由自查替代）：

1. **高危产出**：涉及安全 / 资金 / 权限 / 凭据 / 不可逆动作（发布上线、推送远端、删改数据、动生产配置）的代码或配置改动；
2. **用户点名**：用户要求「复核一下／帮我验一下／找个人看看」；
3. **复发或触顶**：同一反模式在本任务内**第 2 次**被指出，或返修已达上限 → 按「返修复查」第 3 条转**对抗式互审**。

**送审时的规则**（沿用 v2.5.0 口径，未放松）：

1. 审核者必须是**未参与该步执行**的子代理；
2. 审核者只拿**产出物 + 验收标准**，不看执行者的自述理由，独立复算／复现／查证，输出「通过 / 具体问题清单」；
3. 审核者须亲手验证至少一项**可复现硬证据**（重跑脚本、抽查数据、重新探活链接、读回文件）；
4. **只报影响正确性的问题**——建议类／风格类／"还可以更好"类不进问题清单（防"被要求找问题的审查者总能报出问题"引发的过度工程，Anthropic 官方口径）；
5. 审核不通过 → 进「返修复查」，**未清零不得进入下一环节**；
6. **批次规则**：同一任务的多个送审产出可合并为一次送审——审核者拿到**全部产出物 + 统一验收标准**，单批建议 ≤5 个文件；**不得**以合并为由减少覆盖面（批次内每个产出物都要被实质看到，不是抽一个代表）；
7. **留痕两处**：`plan.yaml` 的验证方式 + 工作区 `Ledger.md` 的「独立审核」列，写 `通过` 或 `抽查豁免（<理由>）`。

### 返修复查（回归闭环）

1. 返修必须**带具体问题清单**（现象 + 定位 + 期望），不含糊说"再优化一下"。
2. 返修后**强制复查**：**送审过的**产出由原审核者（或新的独立审核者）针对问题清单**逐条回归**；**未送审的**由**主代理逐条回归并出示硬证据**（禁"我已确认改好了"这种口令式结论）。两种情形都要确认修复未引入新问题（跑一遍原有验证信号）。
3. 同一产出**返修上限 2 轮**；2 轮仍不过 → 改派两个子代理**对抗式互审**，主代理汇总裁决；仍不通过则**触发熔断（F1）**，按「熔断机制」五步执行（停/冻/证/拦/报），**不得再自行重试**。
4. 每轮返修与复查结论**回写 plan.yaml 状态**（`checks.py mark`），留痕不靠记忆。

## 阶段 4：Office 产出

> **非 Office 交付（纯 markdown／代码／数据文件）跳过本阶段**；但**报告类交付仍须参照 `assets/report-template.md`**（不要因为跳过阶段 4 就丢掉报告模板）。

1. 用 `scripts/office_io.py` 生成 Excel/Word/PDF（多 sheet、列宽/冻结首行）；报告结构参照 `assets/report-template.md`。
2. **数据来源写法**：`excel-read` 可直接读 xlsx/CSV；但 `excel-write` **只接受 `--rows` 的 JSON**——CSV 必须先 `excel-read <csv> --fmt json > rows.json`，再 `excel-write <out.xlsx> --rows rows.json`（**直接喂 CSV 会抛 `JSONDecodeError`**）。
3. 写文件统一 `utf-8-sig`；Word 中文必须设两层字体（`run.font.name` + `w:eastAsia`）。
4. 产出前先列将写入的文件路径清单，**逐条确认落在工作区根内**（红线③）；库的坑见 `references/office-guide.md`。

## 阶段 5：测试交付与验收

1. **测试证据**（先测后交）：跑一遍该任务的**可判定验证信号**并出示原始输出（测试/构建/lint/断言、`office_io` 读回、`checks.py skill`、链接探活、性能双条件）；**证据是"跑出来的输出"，不是"我认为没问题"**。
2. **Anti-drop 对账 + 熔断门禁**：`checks.py plan <plan.yaml> --base <工作区根>` 自动核对交付物是否真实存在、字段是否齐全、状态是否合法（**非当前目录时必须带 `--base`，否则相对路径解析基准错**；非文件型交付物标 SKIP 需人工确认）。**FAIL 未清零不得交付**。同一命令同时执行**熔断门禁**：`熔断状态: 已熔断` 或任一步骤状态为 `熔断` 时直接 FAIL——**已熔断的任务不得作为可交付物**。
3. **审核状态已闭合 + 未熔断**：命中送审三类的产出须有「通过」结论且返修问题已逐条回归；未命中的须在 `plan.yaml` 验证方式与 `Ledger.md` 留痕 `抽查豁免（<理由>）`。同时 `熔断状态` 为 `正常`（红线：**未闭合或已熔断，均不得交付**）。
4. 输出自检状态表（**交付前自检**）：文件存在、可打开、编码正确、数据完整、符合验收标准、**验证闭环证据已出示**、**审核状态已闭合（通过 / 抽查豁免）**、**熔断状态为正常**、**产出全部落在工作区根内**、**references 按需加载（未预读全量 / 主上下文未超量）**；此表由**主代理**在交付前给出，检验"东西是否做得对"；**报告类交付物的结构规范见 `assets/report-template.md`**（不要因为任务跳过阶段 4 就丢掉该模板）。
5. 全部通过后用 present_files 交付；不通过则回到对应阶段修复后重新自检。
6. **用户验收确认点**：明确列出「请你验收什么」（交付物路径 + 验收口径 + 已知限制），等用户点头才算完成；用户提出修改则回到阶段 3 返修复查。
7. **发布协作**（仅当任务含发布/上线/推送/交付给对方）：**发布属不可逆动作，执行前单独确认**（目标环境、影响范围、回滚方式）；执行后验证线上可达性（探活/冒烟）并把结果写入履历。
8. 交付说明简洁：结论 + 关键文件 + 下一步建议；**本次若产生或更新了自研工具/技能，必须附其仓库链接**（见「自研工具与技能：标注与公开留痕」）。

## 阶段 6：结果核验、履历归档与复盘

任务交付后（L2 默认执行，L1 可跳过）：

1. **结果核验**（**交付后核验**，与阶段 5 的分工：阶段 5 是交付前"做得对不对"的自检 + 请你点头；此处是**收尾核验**）：对照阶段 1 的验收标准逐条给出「达成 / 未达成 + 证据」，**并纳入用户在阶段 5 验收中提出的意见与最终结论**；未达成的写明原因与后续处理。**已熔断的任务不做达成判定**，改写「已熔断 + 熔断报告路径」。
2. **履历归档**：把本次任务追加一行到工作区根的 `Ledger.md`（格式见 `assets/ledger-template.md`）——入口类型 / 日期 / 任务 / 交付物 / 验收结论 / 独立审核结论 / 越界授权记录。**台账是追加而非重写**，历史条目不得删改；**熔断任务的「验收结论」列写「已熔断」+ 报告路径，「独立审核」列写熔断触发前的最后审核结论**。
3. **复盘四问**：哪个阶段返工最多？哪条假设被推翻？踩了什么坑？下次少走哪一步？
4. **沉淀分流**：流程 → 更新本 skill；配置 → 更新 `assets/templates/<类型>.yaml`；坑 → 追加 `references/quality-gates.md` 反模式清单；**自研工具/技能 → 补标注块、`push --apply` 推送 public 仓库、`verify` 验证版本一致、登记 `meta.自研工具`**；仅本次有效的事实 → 工作区 memory 一行带过。
5. 复盘结论压缩 3-5 行，随交付说明给出。
6. **任务目录治理**：`checks.py status` 会提示陈旧/超额任务；超 10 个或超 30 天的移入 `tasks/_archive/<YYYY-MM>/`（保留 plan.yaml 与最终交付物，中间产物可删），**只移动不删除**，并在回复中列出移动清单。**用 `scripts/archive_tasks.py` 执行**（默认 dry-run，先看计划再 `--apply`）：它带一道**前置门禁**——被 `SKILL.md`／`references/*.md` 引用的目录会被自动剔除并给出引用出处，因为归档它们会使「文档引用完整性」FAIL；移动前后还会用逐文件 sha256 快照自证零丢失。

## 自研工具与技能：标注与公开留痕（贯穿阶段 3–6，v3.1.0 新增）

> **起因（用户 2026-09-15 指定）**：任务中常需自写脚本/技能补齐能力缺口（能力路由的「手脚」类）。这类产物**既不是第三方依赖、也不是用户既有资产**——若不声明来源、不公开留痕，事后无人知道它是什么、从哪来、还能不能用；更糟的是它会被当成成熟库而承担不该有的信任。

### 判定：什么算「自研」

同时满足两条即命中：① **本任务（或本技能）自行编写**，非复用/克隆第三方、非用户既有资产；② **以文件形式落地**——脚本（`.py`/`.ps1`/`.sh`）、技能包（`SKILL.md` + 配套文件）、模板、配置生成器等。

**不算自研**（不触发）：未落盘的一次性内联命令、对既有文件的局部修改、第三方项目的 fork 改造（仍走阶段 2 引入前检查，并注明上游来源与许可证）。

### 三条硬要求

| # | 要求 | 落在哪 | 可判定信号 |
| --- | --- | --- | --- |
| 1 | **代码内标注**：自研标识 / **名称** / **用途** / **适用场景** / 仓库链接，五项齐全 | 脚本＝文件头注释块；技能包＝`SKILL.md` frontmatter + 正文首段 | 该文件头部 `grep` 命中 `[自研` |
| 2 | **输出中标注**：交付说明、报告、`Ledger.md` 凡引用该工具处，须出现「自研」+ 名称 + 仓库链接 | 交付说明 / 报告 / 台账 | 人审：产物里能直接看到三者 |
| 3 | **同步推送 public 仓库**：完整源码推送到公开仓库；**首次创建后每次新增/修改都要及时推送，保持版本一致** | `scripts/publish_tools.py` | `publish_tools.py verify` 报 `public=true` 且远端 blob sha 与本地一致 |

**禁止**：把自研产出写得像成熟第三方库（不标来源、只给工具名不给链接）；把尚未推送的本地脚本当既有能力引用。

### 标注模板

脚本**文件最前面**的注释块：

```text
# [自研工具] <名称>
# 用途：<一句话，它做什么>
# 适用场景：<什么情况该用它；什么情况不该用>
# 作者：ai-workflow 自研（<任务名> <YYYY-MM-DD>）
# 仓库：<repo_url>/blob/main/<path>　← 推送后回填；未推送前写「待推送」
```

技能包：`SKILL.md` frontmatter 加 `selfbuilt: true` 与 `repo: <url>`，正文首段给出用途 + 适用场景。

### 仓库与推送工具

- **默认仓库**：`https://github.com/Garvin666/ai-workflow-tools`（**public**，放**自研工具**源码）。覆盖方式：`SELFTOOL_REPO` 环境变量，或 `publish_tools.py --repo OWNER/NAME`。
- **两仓分工（用户 2026-09-16 定）**：**自研工具** → `ai-workflow-tools`；**技能本体** → `Garvin666/ai-workflow-skill`（本技能自身即自研技能，已在 frontmatter 标 `selfbuilt: true` + `repo:`，正文首段给出用途／适用场景／链接）。推送技能本体用 `--repo Garvin666/ai-workflow-skill`。
- **`scripts/publish_tools.py init --name <仓库名> [--apply]`**：创建 **public** 仓库（本规则不允许 private，故无 `--private`）；默认 dry-run。**幂等**：仓库已存在且为 public → 打印 `[OK] …跳过创建（幂等）` 且退出码 0；已存在但为 private → FAIL 并给出改可见性指引。
- **`scripts/publish_tools.py push --file <本地路径> [--dest <仓内路径>] [--message …]`**：默认 **dry-run**（只打印将写入的仓内路径与 blob sha），加 `--apply` 才真写；已存在文件带远端 `sha` 更新，**幂等可重跑**（内容一致则 `[SKIP] … 一致，无需推送`）。
- **`publish_tools.py verify [--file …]`**：查仓库 `private` 字段（**为 true 直接 FAIL——规则要求 public**）+ 比对远端 blob sha 与本地是否一致。**这是"版本一致"的机器判据**，不靠"我记得推过了"。
- 网络口径：一律走 `api.github.com`（本机 `github.com` 主域间歇不通）；凭据优先级 `--token` → `GITHUB_TOKEN` → `GH_TOKEN` → `gh auth token`，**只读不落盘、不打印完整值**。
- ⚠️ **创建仓库与首次推送属不可逆对外动作**，须按阶段 2 第 7 条**单独取得用户确认**后再执行。

### 两类资源的分流推送路由（v3.2.0，用户 2026-09-16 指定）

> **分工**：本体（Ontology）→ `ai-workflow-skill`；自研工具 → `ai-workflow-tools`。
> **一条命令**：`python scripts/push_router.py push --base <rev> --head <rev> [--apply]`（**默认 dry-run**）。
> ⚠️ **最容易误解的一点：分流不是互斥二选一。** 本体 ＝ 技能仓改动面的**全量**（技能本体 + 任务档案）；自研工具 ＝ 其中的**子集**。所以 `scripts/` 下的自研脚本属**双属** —— 既作为本体快照进技能仓，又作为独立工具进工具仓，两条通道各推一份（内容同源；同一文件在两仓的 blob sha 相同，这本身就是「版本一致」的天然证据）。

#### ① 触发条件

| 改动类型 | 本体通道（→ 技能仓） | 自研工具通道（→ 工具仓） |
| --- | --- | --- |
| 新增 A | 推 | 推（仅当判定为自研工具） |
| 更新 M | 推；内容一致则幂等跳过 | 推；同上 |
| 重命名 R | 按「删旧 + 增新」 | 新路径更新；旧路径删除须 `--allow-delete` |
| 删除 D | **默认只列出、不执行**，须显式 `--allow-delete` | **不自动删**（避免误删公开产物，需网页人工删） |
| 无变化 | `[=] … 同内容跳过` | `[SKIP] … 一致，无需推送` |

**推送时点**：阶段 6 沉淀 —— 与「首次创建后每次新增/修改都要及时推送、保持版本一致」同一条要求。

#### ② 判定依据（标注头 + 登记表交叉校验）

| 维度 | 判据 |
| --- | --- |
| **本体** | 路径落在技能仓内（技能本体 + 任务档案）。`git diff` 的文件名即判据 |
| **自研工具** | 三条**同时**满足：① **事实**——文件首部含 `[自研工具]`/`[自研技能]` 标注块（脚本），或 `SKILL.md` frontmatter 含 `selfbuilt: true`（技能包）；② **意图**——任意 `tasks/**/plan.yaml` 的 `meta.自研工具` 有对应登记项；③ **指向**——该登记项的仓库链接指向**工具仓** |

**交叉校验（不一致即 FAIL，且阻塞推送）**：

| 情形 | 判定 |
| --- | --- |
| 有标注头，但登记表无此项 → **漏登记** | FAIL（用 `checks.py selftool` 补登记） |
| 登记项可映射到该文件，但该文件无标注头 → **登记与实现不符** | FAIL |
| 有标注头且已登记，但链接指向**本体仓**（技能本体自身即如此） | OK —— 只走本体通道，不推工具仓 |
| 标注头缺「名称 / 用途 / 适用场景 / 仓库」任一项 | FAIL |
| 登记项未涉及本次改动面 | SKIP（历史登记或本次未改动，非错误） |
| 文件在技能根之外 | WARN —— 不归本分流器管辖，直接用 `publish_tools.py` |

#### ③ 推送格式（两条通道形态不同，由分流器包装）

| | 本体通道 | 自研工具通道 |
| --- | --- | --- |
| 脚本 | `scripts/push_ontology.py` | `scripts/publish_tools.py push` |
| API | Git Data API：blob → tree（带 `base_tree`）→ commit → PATCH ref | contents API：单文件 PUT |
| 粒度 | 一次 commit 覆盖本次全部改动 | **逐文件**，每文件一次提交 |
| 换行符口径 | 取 `git cat-file blob`（git 对象库天然 LF） | CRLF→LF **归一化后**再算 sha |
| 仓内路径 | ＝ 相对技能根路径（与本地一致） | ＝ 相对技能根路径（同一规则） |
| 删除 | 支持（需 `--allow-delete`） | 不支持（人工在网页删） |
| 理由 | 远端是**混合仓**且其历史由 API 创建、与本地不互通 → `git push` 会被拒 | 工具仓是纯净单文件仓，contents API 足够 |

#### ④ 冲突处理策略

| 冲突 | 策略 |
| --- | --- |
| 内容已一致（重复推送） | **幂等跳过**，可反复重跑 |
| 远端被并发推送 | 本体通道的 commit parent 取远端**当前** HEAD，且 `PATCH ref` 用 `force=false` → 非快进即失败；另可用 `--expect-remote <sha>` 显式锁基线 |
| 新建 blob 的 sha 与远端返回不符 | **立即抛错**（换行符/编码口径问题的机器判据，绝不静默改写） |
| 混合仓中远端独有项（工作区档案侧） | **永不因本次推送被删** |
| 删除项 | 默认只列出待删；须显式 `--allow-delete` |
| 交叉校验 FAIL | **阻塞两条通道** —— 不推半截 |
| 本体通道失败 | **中止**，不再继续工具通道（避免半推送状态扩大） |
| **工作区有未提交改动** | **阻塞推送**（须显式 `--allow-dirty` 才放行）—— 本体通道按 **commit 内容**推、工具通道按 **工作区文件**推；两者不同源时，同一文件在两仓 blob sha 会不一致、公开仓可能躺着已修好的 bug 的旧版本。**这条是实测踩出来的**：v3.2.0 首轮推送时一处修复发生在 commit 之后，两仓内容一度不同（见 `Ledger.md`）。 |
| 登记项与技能包的映射 | 用 frontmatter `repo` 与登记链接**同仓比对**，**不**依赖名称里是否含中文关键词（那种判据一改命名就失效） |
| ⚠️ **已知边界**（如实登记，不假装有保护） | 无跨会话基线时，**无法检测「远端被第三方改写」**这类漂移；如需该保护，推送后记录 sha 并用 `--expect-remote` 显式约束 |

### 机器门禁与留痕

- `plan.yaml` 的 `meta.自研工具` 逐项登记（四项必填：**名称 / 用途 / 适用场景 / 仓库链接**），用 `checks.py selftool <plan> --name … --purpose … --scenario … --repo <url>` 追加。
- `checks.py plan` 新增 `_check_selftools()`：**字段可选，一旦填写必须合法**（与 `_check_autonomy` 同口径——不强制填空，但填了就不能糊弄）：四项非空 + 仓库链接须为 `http(s)` 且非占位符。
- 代码里写了自研工具却未登记＝**漏登记**，阶段 5 自检不通过，补登记后再交付。

### 与各阶段的接缝

| 位置 | 行为 |
| --- | --- |
| 阶段 3 第 1 步 | 新建自研文件的**同一时间**写标注块，并在 `plan.yaml` 登记一项 |
| 阶段 5 交付说明 | **附上仓库链接**（便于查阅） |
| 阶段 6 沉淀 | 自研工具每次更新后重跑 `push --apply` + `verify`，保持版本一致；**改动跨两类时用 `push_router.py push` 一次分流推两仓**（先不加 `--apply` 看计划） |
| 阶段 6 交付 | 附**两仓**链接与交叉校验结果（`push_router.py classify` 的输出即证据）｜`Ledger.md` 交付物列注明「自研 + 链接」 |

### 技能库卫生（新增/修改技能时的四条硬约定，v3.1.1 新增）

> **起因（2026-09-16 技能库归并梳理）**：一次 32 个技能的全面盘点发现，问题**不是内容重复**
> （段落级比对零重复），而是**导航缺失 + 元数据不全 + 跨模块口径分叉**。四条约定随之定型。

| # | 约定 | 为什么 | 怎么判定 |
| --- | --- | --- | --- |
| 1 | **先查索引再新建**：`.workbuddy/skills/README.md`（或所在技能根的索引文件）与同簇技能先过一遍；能扩写既有技能就不新建 | 技能库最怕的不是重复而是**边界模糊**——两个技能各写一半，检索时两边都命中却都不全 | 人审：新技能目录的建立理由能否用一句话说清「为什么不并进 X」 |
| 2 | **`description` 必须带触发锚**：显式 `触发词：a / b / c`，**或**用户原话（「…」），**或**「当…时」 | description 是唯一被自动匹配的字段；无锚＝只能靠语义碰运气。且三种锚**任选其一即可**，别把"没写触发词标签"误判成"没有锚"（实测该误判会让缺口统计虚高 12 倍） | 脚本：`description` 命中 `触发词\s*[：:]` 或 `当[^。！？]{0,300}?(时\|立即触发)` 或 ≥2 处「」话术 |
| 3 | **改了内容就 bump `version`**，并在文末「变更记录」写一句；**过时内容加时点注记（`⚠️ 已过时（YYYY-MM-DD）`），不删原文** | 版本能让人一眼看出"这份经验是哪一版的数据"；删原文等于毁掉证据本身 | 人审 + `version` 字段存在性 |
| 4 | **同一物理量的判据必须跨模块同源**，且配一条**跨模块守卫测试**（含**阴性对照**：把旧实现回滚验一次，确认守卫会红） | 阈值不同向＝两套实现，差异可能不抛异常地静默放大 100 倍。**"抽样对拍通过" ≠ "判据正确"**（抽样只覆盖典型样本） | 测试：存在断言"两实现的阈值/判向一致"的用例；并有脚本证明旧实现会被它抓到 |

**⚠️ 排除名单**：**没有 `agent_created: true` 的技能是人工手写的**（如 `quantity-website-restart`），
自动化工具**不得改写**——只能报问题、提建议，改动须由人执行。

**⚠️ 计数纪律**：技能库类统计**必须先校准口径再报数**。本次实测教训——"缺触发锚的技能数"
用窄口径量得 12，放宽到「当…时（不限长度）」后降到 2，最终核对为 **1**；同一口径连错两次，
差 12 倍。**报数前先反问：这个正则/关键词，漏掉长句与同义表达了吗？**

## 熔断机制（流程末梢兜底，机器可判定）

> 设计依据：工业界断路器三态（Closed / Open / Half-Open）＋"**写在提示里的熔断不是熔断，是建议**——必须落在推理够不着的地方"。故本机制不只写在文档里，**由 `checks.py plan` 强制执行**。

### 状态（**两个存储态 + 一次复位迁移**，唯一叫法）

存储态只有两个，`VALID_FUSE` 的值域就是这两项；"复位"是一次**迁移动作**而非第三个存储态（这点必须说准，否则会误以为 `plan.yaml` 里能写"复位"）：

| 状态 | 类型 | 含义 | 谁可进入 |
| --- | --- | --- | --- |
| `正常` | 存储态 | 正常流转（Closed）；`plan.yaml` 留空即视为正常 | — |
| `已熔断` | 存储态 | 自动化已停止，等待用户决策（Open） | 主代理，命中任一触发条件时 |
| 复位 | **迁移动作**（Half-Open） | 由 `已熔断` 迁回 `正常`，仅放行**一次**针对性重试 | **只有用户明示**，主代理不得自行解除 |

### 触发条件（质量 + 环境两类；每条都必须有硬证据，缺证据不构成触发）

| 编号 | 类别 | 触发条件（可判定） | 必须留痕的证据 |
| --- | --- | --- | --- |
| **F1** | 质量 | 同一产出返修 **2 轮仍不过**，且对抗式互审**仍不通过** | 返修轮次记录 + 互审两份报告 + 主代理裁决书 |
| **F2** | 质量 | **同类缺陷 ≥3 次复发**（同一反模式编号在本任务内第 3 次被指出） | 反模式编号 + 三次指出的留痕（独立审核意见 / 主代理自查记录 / 用户指出均可，须可回溯；抽查制下多数复发出现在自查链，故不以"必须有审核记录"为限） |
| **F3** | 环境 | **验证信号不可得**：依赖缺失且装不上 / 权限凭据不可得 / 证据文件被污染失去价值 | 失败命令的**原始输出** + 已试过的替代路径清单 |
| **F4** | 环境 | **越界或授权缺失**：所需输入或产出在工作区根之外且未获当次授权（红线③） | 越界路径 + 已发出的授权请求原文 |
| **F5** | 环境 | 外部配额/限流硬阻断（如 GitHub 配额耗尽且退避无效），致当前步骤取不到证据 | 配额查询原始输出 + 退避尝试记录 |

**不构成触发**（防误熔断）：单次返修失败、单次工具报错（未耗尽重试）、审核意见仅属"建议/轻微"、用户已明示"继续试"。

### 熔断动作（五步，次序固定）

1. **停**——立即停止全部自动化动作：不再重试、不再派子代理、不再改产出物。
2. **冻**——产出物保持**未提交/未发布**；用 `checks.py mark <plan> <id> 熔断` 置步骤状态、`checks.py mark <plan> --fuse 已熔断` 置 meta；**禁止把任何步骤标为「完成」**。
3. **证**——产出 `tasks/<任务>/熔断报告.md`（模板 `assets/熔断报告模板.md`），必含五节：**触发条件（编号 + 硬证据原文）/ 已试路径（逐条含结果）/ 卡点根因（区分"证据支持"与"推测"）/ 待决策选项（≥2 个，各写代价与回滚方式）/ 复位条件**。
4. **拦**——`checks.py plan` 在 `熔断状态: 已熔断` 或存在 `熔断` 状态步骤时输出 `[FAIL] 已熔断：不得作为可交付物`，**机械阻断**"熔断后仍被当成交付"。
5. **报**——升级用户，只给选项与代价；`Ledger.md` 追加一行，验收结论写「已熔断」+ 报告路径。

### 复位条件（**H1 修正**：复位是两步，缺一不可）

> ⚠️ 熔断由**两个**独立条件触发（meta `熔断状态` **或** 任一步骤状态为 `熔断`，任一命中即阻断，属 fail-closed 设计）。因此**只改 meta 复位不了**——实测会继续 `[FAIL] … 步骤 <id> 状态为『熔断』`。

- **只有用户明示才能复位**（无 owner 的开放断路器＝一堆卡死的工作）；主代理不得自行解除。
- **复位动作（两步，缺一不可；两步**都由 `mark` 支持**，不需要手改 YAML）**：
  1. meta：`checks.py mark <plan> --fuse 正常`（把 `熔断状态` 写回 `正常`；该键缺失时自动插入）；
  2. 步骤：`checks.py mark <plan> <id> <非熔断状态>`（`VALID_STATUS` 为 待办/进行中/完成/受阻/熔断）。
  - 合成一句即：`checks.py mark <plan> <id> 完成 --fuse 正常`。
  - ⚠️ **只做第 1 步复位不了**——门禁会继续以 `[FAIL] … 步骤 <id> 状态为『熔断』` 阻断（fail-closed）。
- **复位后必须重跑 `checks.py plan <plan> --base <工作区根>` 确认 FAIL 清零**，并把该次输出留档——"我认为复位好了"不算数。
- 在 `Ledger.md` 记录复位依据（用户原话摘要 + 时间）；**熔断报告不删**，追加「复位记录」一节作为历史证据。
- 复位后**只允许一次**针对性重试；同一条件再次触发 → 再次熔断，并升级为"需用户改方案"。
  - ⚠️ 该项为**纪律约束**，**不是机器判定**：`checks.py` 不记录复位次数，复位后与"从未熔断"不可区分。别把它当成已由工具保证。

### 与各阶段的接缝

| 位置 | 行为 |
| --- | --- |
| 阶段 0 | 读 `plan.yaml` 的 `熔断状态`；为 `已熔断` 则**禁止自动续跑** |
| 阶段 3 返修复查 | 触顶即触发 **F1**（见上「返修复查」第 3 条） |
| 阶段 5 | 交付前 `熔断状态` 必须为 `正常`，否则不得交付 |
| 阶段 6 | 熔断任务改写"已熔断 + 报告路径"，Ledger 如实标注 |

## 手册与脚本索引

| 文件 | 内容 |
| --- | --- |
| `references/ops.md` | **脚本用法速查、环境检测、故障排查、变更日志** |
| `references/playbook.md` | 四类项目差异化流程（量化/选品/小说/学习资料）**+ 缺陷修复类（Bug 入口）** |
| `references/quality-gates.md` | 证据分级、验证闭环、CI 门禁、评审六偏差、反模式清单 |
| `references/orchestration.md` | 子代理编排手册、Handoff 模板、并行规则、失败与熔断升级路径 |
| `references/capability-routing.md` | **能力路由**：四类能力（知识/算力/事实/手脚）判据、决策三步、反合理化红线表、声明制 |
| `references/adaptive-planning.md` | **自适应计划**：拆解四规则、R1–R6 重规划触发、三层留痕、执行期六动作 |
| `references/office-guide.md` | openpyxl / python-docx / pypdf 避坑 |
| `assets/plan-template.yaml` ／ `report-template.md` ／ `templates/README.md` | 计划、报告、确认表规范 |
| `assets/ledger-template.md` | **交付履历台账**（工作区根 `Ledger.md`）格式与字段说明 |
| `assets/熔断报告模板.md` | **熔断报告**（触发后产出 `tasks/<任务>/熔断报告.md`）格式与必填五节 |

核心命令：`checks.py skill`（技能自检）｜`checks.py plan <plan.yaml> --base <工作区根>`（对账 + **熔断门禁**）｜`checks.py status`（任务总览，**已熔断任务带 ⚡ 标记**）｜`checks.py mark <plan> [<id> <状态>] [--fuse 正常\|已熔断]`（状态更新；`--fuse` 用于熔断与复位，**不必手改 YAML**）｜`http_fetch.py --github-search "..."`（GitHub 找项目）｜`http_fetch.py --github-code-search "..."`（GitHub 找代码，需 token）｜`data_query.py files|sql|find`（大数据集检索）｜`checks.py decide <plan> --point … --basis … --capability <类> --choice <手段>`（**能力决策留痕**，四个参数均必填）｜`checks.py revise <plan> --trigger R<n> --change …`（**计划修订留痕**）｜`checks.py selftool <plan> --name … --purpose … --scenario … --repo <url>`（**自研工具登记**，四项必填）｜`publish_tools.py push --file <本地路径> [--apply]`（**推送自研工具到 public 仓**，默认 dry-run）｜`publish_tools.py verify`（**校验仓库为 public 且远端 sha 与本地一致**）｜`push_router.py push --base <rev> --head <rev> [--apply]`（**按「本体／自研工具」两类规则分流推两仓**，默认 dry-run）｜`push_router.py classify`（**离线分类 + 标注头×登记表交叉校验**，不联网）｜`push_ontology.py --base <rev> --head <rev> [--apply]`（**本体专用通道**，Git Data API + base_tree，默认 dry-run）。完整参数见 `references/ops.md`。
