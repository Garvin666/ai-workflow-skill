# Agent 可控性三层架构 · 升级方案

> 版本：v2.0 ｜ 日期：2026-09-16
> 升级对象：面向支付／开户类业务场景的 Agent 三层架构（任务分级 → 执行链路兜底 → 架构层优化）
> 输入依据：用户提供的三层架构说明材料（三张设计说明截图）
> 设计意图（**贯穿全文的第一性原则，不做变更**）：**在业务稳定性面前，Agent 的可控性远比智能化更重要。**
>
> **数据口径声明**：本方案中标注「现状」的部分来自输入材料原文；标注「预期／目标」的均为**设计目标值**（由架构约束推导而来），不是实测数据。落地后须以 L0 平面的实测指标替换，不得把目标值当成绩汇报。

---

## 0. 一页摘要：改了什么

| 层 | 现状 | 升级后 | 关键变化 |
|---|---|---|---|
| **L1 任务分级** | 强／弱二分类；强状态走状态机，弱状态走模型记忆 | **二维分型（状态强度 × 业务风险）+ 动态升降级 + FSM 规格化** | ①二分类 → 4 类策略；②补量化判据与阈值；③补运行期重分类（原设计缺）；④FSM 从"知道要走"变"有规格" |
| **L2 执行链路** | 幂等校验（绑 `task_id`）／断点续办（超时挂起）／指令优先级（核心 > 补充） | **复合幂等键 + 租约心跳 + Saga 补偿 + 分层抢占 + 韧性三件套** | ①幂等键从 1 元组升为 4 元组；②挂起后补"唤醒"闭环；③补补偿回滚；④补重试退避／熔断／死信 |
| **L3 架构层** | 结果预摘要／记忆切面（最近 3 轮 + 全局状态）／硬规则兜底 | **双通道摘要 + 预算驱动三层记忆 + 独立规则引擎 + 人机协同** | ①摘要从"精简"变"可校验"；②固定 3 轮 → Token 预算自适应；③规则从"声明"变"可执行、有版本、可解释" |
| **L0（新增）** | 无 | **观测与治理平面（贯穿 L1–L3）** | 补可观测性：trace／指标／审计／规则与 FSM 版本中心 |

**一句话总结**：原架构的**判断是对的、方向是对的**，问题在于控制点被**声明**了却没有被**执行**——判据没量化、参数没定值、异常路径没闭环、规则没版本。升级的核心动作是**把每条"声明"落成有参数、有证据、可回滚的机制**，并补一条贯穿的观测平面，让"可控"这件事本身可被度量。

---

## 1. 现状复盘：节点、逻辑与参数

### 1.1 全景

```
用户请求
   ↓
L1 任务分级 ── 强/弱二分类 ──┬─→ 强状态：状态机管护 · 每步工具结果持久化 · 每轮推理前读状态节点
                             └─→ 弱状态：大模型原生对话记忆
   ↓
L2 执行链路兜底与校验 ── 幂等校验(task_id) / 断点续办(超时挂起) / 指令优先级拦截(核心>补充)
   ↓
L3 架构层优化 ── 结果预摘要 / 记忆切面(最近3轮+全局任务状态) / 硬规则兜底(支付·开户)
   ↓
业务系统 / 工具
```

### 1.2 逐层节点清单

| 层 | 节点 | 现有逻辑 | 现有参数 | 缺口 |
|---|---|---|---|---|
| L1 | 任务分型器 | 区分强／弱状态任务 | **无**（材料未给任何判据） | 判据未量化 → 分型不可复现；无中间态；无运行期重分类 |
| L1 | 状态机引擎 | 强状态走状态机管护 | **未定义**状态集、迁移边、守卫条件 | 无规格 → 无法实现、无法测试、无法收敛 |
| L1 | 状态读取 | 每轮推理前读"当前状态节点" | **未定义**读取粒度 | 读多少？全量还是增量？→ 直接影响上下文成本 |
| L1 | 持久化 | 每步工具调用结果持久化 | **未定义**保留期、写失败语义 | 写失败是否推进状态？（原子性未定义） |
| L2 | 幂等校验 | 所有工具请求绑唯一任务 ID，重复调用返回缓存结果 | `key = task_id` | **键位不足**：同一任务内不同参数的多次调用会被误判为重复；无 TTL；无失效策略 |
| L2 | 断点续办 | 超时自动挂起 + 保留现场状态 | 超时阈值 **未定义** | 挂起后**谁来唤醒**？无看门狗、无退避、无补偿 |
| L2 | 指令优先级 | 核心业务指令 > 补充指令；附加指令不许打断主流程 | 两级（核心／补充）+ 附加 | 无冲突检测、无抢占规则、无被抢占指令的去处 |
| L3 | 结果预摘要 | 对返回的原始长数据做精简预处理 | **策略未定义** | 截断还是摘要？信息损失如何监控？ |
| L3 | 记忆切面 | 只保留最近 3 轮核心交互 + 全局任务状态 | `rounds = 3`（**静态**） | 静态窗口不随模型／任务变化；无裁剪优先级 |
| L3 | 硬规则兜底 | 支付、开户等高风险业务加规则校验，不符合直接拦截修正 | 场景清单 | 规则住在哪？谁改？改了怎么追溯？拦截是否可解释？ |

### 1.3 三个根因

**根因 A：控制点被声明，未被参数化。**
材料里的每一句都是正确的方向，但**没有一个可执行的具体值**："超时"是多少秒？"最近三轮"为什么是三？判据是什么？—— 结果是每支团队各自发挥，行为不可复现。

**根因 B：异常路径未闭环。**
- 挂起之后没有唤醒 → 任务永久停在 `SUSPENDED`
- 失败之后没有补偿 → 有副作用的步骤无法回滚（支付类场景这是硬伤）
- 拦截之后没有申诉 → 规则误拦即业务中断，无出口

**根因 C：三层之间是串行叠加，没有贯穿的观测面。**
出问题时无法回答："这次是分型错了、幂等误判了，还是规则拦错了？"—— 因为没有统一 `trace_id`，三层日志各自为政。

---

## 2. 升级目标与范围

### 2.1 保持不变（设计意图守护清单）

以下四条**不做任何变更**，作为后续所有取舍的裁决标准：

1. **可控性 > 智能化**：任何使 Agent"更聪明"但降低可控性的改动，一律不做。
2. **强状态任务强制状态机管护**：不因为"大模型上下文变长了"就取消状态机。
3. **不依赖大模型临时记忆**：关键状态的事实源永远是持久层，不是对话历史。
4. **高风险业务加硬规则兜底**：规则先于模型判断，模型不得绕过。

### 2.2 升级目标（可度量）

| 维度 | 目标 | 目标值（设计目标，非实测） |
|---|---|---|
| 可控性 | 高风险动作（R2/R3）经规则校验的比例 | **100%** |
| 可控性 | 规则误拦率 | ≤ **1%** |
| 可靠性 | 同一 `idem_key` 产生重复副作用的次数 | **0** |
| 可靠性 | 挂起任务成功恢复的比例 | ≥ **99%** |
| 可靠性 | 有副作用步骤的补偿覆盖率 | **100%**（每个写步骤均有注册） |
| 效率 | 单次推理上下文占用（对比固定全量） | 降低 ≥ **40%** |
| 效率 | 长工具结果的压缩比 | ≥ **3:1** |
| 可观测 | 任意一次请求可完整回溯到三层决策依据 | **100%** |

### 2.3 范围

**做**
- 三层内部节点重构、参数定值、异常路径补齐
- 新增 L0 观测与治理平面
- 新增三项韧性能力（重试退避／熔断／死信）与人机协同出口

**不做**
- 不追求模型能力提升（换更大模型、加微调）—— 与设计意图无关
- 不改业务语义（风控口径、额度规则由业务方提供，本方案只定义规则的承载形态）
- 不引入额外的 LLM 依赖到控制路径（规则引擎、幂等、状态机全部是确定性代码）

---

## 3. L1 升级：任务分型与状态编排

### 3.1 节点结构（从 2 节点 → 5 节点）

```
任务分型器 ──→ [状态分型 + 风险分级] ──→ 策略矩阵查表 ──→ 状态机引擎 / 原生对话路径
                     ↑                                              │
                     └──── 动态重分类器 ←──── 运行期信号 ────────────┘
```

| 节点 | 职责 | 新增/改造 |
|---|---|---|
| 状态强度评估 | 判定 S0／S1／S2 | 新增（原为隐含） |
| 风险分级 | 判定 R0／R1／R2／R3 | 新增 |
| 策略矩阵查表 | 由 (S, R) 得出执行策略 | 新增（原为二分类） |
| 动态重分类器 | 运行期触发升降级 | **新增能力**（原设计缺） |
| 状态机引擎 | 执行 FSM + 持久化 | 改造：补规格定义 |

### 3.2 处理逻辑

#### （1）二维分型替代二分类

**维度 A · 状态强度（state_intensity）**

| 级别 | 定义 | 判定信号 |
|---|---|---|
| S0 无状态 | 单轮内完成，无跨轮依赖 | 全部工具为只读，或单次写操作 |
| S1 会话级状态 | 多轮内需保持一致，会话结束即失效 | 步骤 ≥ 3 且有数据依赖，但不涉及跨会话续办 |
| S2 持久状态 | 需跨会话／跨进程续办，状态必须落库 | 含"稍后""明天""继续上次"等跨会话语义，或任务预计时长 > 单会话 |

**维度 B · 业务风险（risk_class）**

| 级别 | 定义 | 典型动作 |
|---|---|---|
| R0 只读 | 无副作用 | 查余额、查账单、查产品 |
| R1 低风险写 | 可逆、可重做 | 修改偏好、保存草稿、标记 |
| R2 高风险 | 涉及资金或账户状态变更 | 支付、转账、开户 |
| R3 不可逆 | 生效后无法通过系统回滚 | 资金已划出、合同已签署、账户已开立 |

**打分式分型（把判据变成可复现计算）**

```
state_score = 4 × has_amount_signal
            + 3 × has_write_tool
            + 2 × cross_session_signal
            + 2 × (dependent_steps >= 3)
            + 1 × needs_multi_turn_clarify

强状态（走状态机）  ⟺  state_score >= 4
```

风险分级独立计算：

```
risk_class = R3  若 action ∈ irreversible_actions
           = R2  若 action ∈ {payment, transfer, account_open} 或 has_amount_signal
           = R1  若 tool.is_write
           = R0  否则
```

#### （2）策略矩阵

| | R0 只读 | R1 低风险写 | R2 高风险 | R3 不可逆 |
|---|---|---|---|---|
| **S0** | 原生对话 | 原生对话 + 幂等键 | 轻状态化 + 规则前置 | 状态机 + 规则 + 复核 |
| **S1** | 原生对话 | 状态机（会话级） | 状态机 + 规则 + 审计 | 状态机 + 规则 + 复核 + 审计 |
| **S2** | 状态机（持久） | 状态机（持久） | 状态机 + 规则 + 审计 | 状态机 + 规则 + 复核 + 审计 |

> 与现状的差别：原二分类只有"强/弱"两档。新矩阵把 R2/R3 单独拎出来 —— **风险维度可以独立于状态维度触发强制管控**（例如"单轮支付"在旧设计里可能被判为弱状态而只走模型记忆，但它显然不该如此）。

#### （3）动态重分类（原设计缺失的关键能力）

**升级触发（满足任一即升级到强状态）：**

| 触发条件 | 参数 | 说明 |
|---|---|---|
| 工具连续失败 | `escalate_on_fail_count = 2` | 说明"一次对话能搞定"的假设已破产 |
| 命中高风险动作意图 | `escalate_on_risk_tag = true` | 检测到支付／转账／开户语义 |
| 轮次未收敛 | `escalate_on_turns = 6` | 6 轮后仍未产出最终动作 |
| 会话中断 | `escalate_on_idle = 300s` | 上下文可能已丢 |
| 用户显式要求 | 关键词或按钮 | "记住这个""下次接着办" |

**升级动作（必须原子完成）：**

```
1. 冻结当前对话上下文 → 生成 bootstrap_snapshot
2. 初始化 FSM，当前状态置为 INIT
3. 把已完成步骤回填为 done_steps（标记为"以对话形式完成，未持久化"）
4. 写 escalation_event（原因 / 触发点 / 快照 hash / 时间）
5. 切换执行路径，后续步骤全部走状态机
```

**降级规则（对原设计的修正）**：原设计"只能升级、不能回退"在**任务进行中**是正确的（防止状态丢失），但**任务终态**必须允许受控归档降级：任务进入 `COMPLETED` / `ABORTED` 后，状态从活跃存储迁出到归档，释放状态机槽位。否则长期运行后状态机会被历史任务撑爆。

### 3.3 参数配置

| 参数 | 默认值 | 说明 | 调整建议 |
|---|---|---|---|
| `classify_threshold` | `4` | 强状态判定阈值 | 高风险业务可降到 3（更保守） |
| `state_read_payload_max_tokens` | `800` | 每轮注入 Agent 的状态载荷上限 | 状态多时靠摘要而非全量 |
| `fsm_version` | 随任务冻结 | 任务启动时锁定，任务期间不随规则升级变化 | 不可动态切换 |
| `fsm_max_states` | `24` | 单流程状态节点上限 | 超过说明流程该拆 |
| `snapshot_retention` | `30d` | 状态快照保留期 | 合规要求更长时上调 |
| `snapshot_write_retry` | `3` | 快照写失败重试次数 | 3 次后挂起，不推进状态 |

### 3.4 异常处理

| 异常 | 处理 | 兜底 |
|---|---|---|
| FSM 定义缺失／版本不匹配 | `fail_closed`：拒绝执行 R2/R3 动作 | 转人工 |
| 迁移无匹配边（死锁） | 置 `DEADLOCK` 状态 + 告警 | 人工介入，写出边补齐后回流 |
| 快照写失败 | **不推进状态**（写失败即不提交） | 重试 3 次 → 挂起 |
| 分型器超时／异常 | 保守降级：按 `R2 + S2` 处理（最严策略） | 记录 `classify_degraded=true` |
| 升级过程中会话丢失 | 从 `bootstrap_snapshot` 重建，标记"上下文可能有损" | 关键参数重新向用户确认 |

---

## 4. L2 升级：执行韧性链路

### 4.1 节点结构

```
工具调用请求
   ↓
[幂等键计算] → [在途锁检查] → [规则引擎前置校验] → [执行]
   ↑                                                ↓
   └── [租约/心跳续期] ←──────────────┬── [结果落库 + 副作用标记]
                                      ↓
                              [失败 → 退避重试 → 熔断 → 死信]
```

### 4.2 处理逻辑

#### （1）幂等键升级：从 1 元组到 4 元组

**现状**：`key = task_id`
**问题**：同一任务内连续调用同一工具（这是常态，例如先查余额 A 再查余额 B）会被误判为重复，直接返回 A 的缓存结果 —— **这是数据正确性事故，不只是效率问题**。

**升级**：

```
idem_key = sha256( task_id ‖ step_id ‖ tool_name ‖ canonical_json(args) )
```

`canonical_json` 规范化规则（保证同语义参数必得同 key）：
- 对象键按字典序排序
- 剔除 `null` / 空字符串字段
- 数字统一为定点表示（`1` 与 `1.0` 归一）
- 时间统一为 UTC ISO-8601 秒级精度
- 数组保持顺序（顺序敏感）；如业务上顺序无关，由工具声明 `args_order_insensitive=true`

**三级幂等（按工具性质分流）**

| 级别 | 适用 | 机制 | 参数 |
|---|---|---|---|
| L1 结果缓存 | 只读工具 | 同 key 直接返回缓存结果 | `read_cache_ttl = 300s` |
| L2 在途锁 | 写工具 | 同 key 在途 → 拒绝或等待返回 `IN_PROGRESS` | `lock_ttl = 60s` |
| L3 副作用标记 | 写工具 | 三态 `PENDING / COMMITTED / ROLLED_BACK`，崩溃后据此判定是否需要补偿 | 永久保留（随任务归档） |

**失效策略**（原设计完全缺失）：
- 显式失效：业务代码主动 `invalidate(idem_key)`
- 事件失效：订阅业务事件，如 `payment.success` → 失效该账户的余额类缓存
- 时间失效：仅限 L1 结果缓存
- **L3 副作用标记永不自动失效**（它是判重的最终依据）

#### （2）断点续办：补齐"唤醒"闭环

**现状**：超时挂起 + 保留现场 —— 但**挂起之后没有下文**，任务会永久停住。

**升级为「租约 + 心跳 + 看门狗 + Saga」四件套**：

| 机制 | 参数 | 逻辑 |
|---|---|---|
| 心跳 | `heartbeat_interval = 10s` | 执行中定期续租 |
| 租约 | `lease_ttl = 30s` | 3 倍心跳，容忍 2 次丢包；超期未续 → 判定失联 |
| 挂起 | — | 写 `SUSPENDED` + 现场快照（输入／中间结果／已完成步骤） |
| 看门狗 | `watchdog_interval = 15s` | 周期扫描 `SUSPENDED` 任务，按退避策略唤醒 |
| 退避唤醒 | `resume_backoff = [30s, 2m, 10m, 1h]` | 累计 4 次仍失败 → 死信 + 人工 |
| Saga 补偿 | — | 每个写步骤注册 `compensation_action`，失败时**逆序**执行 |

**补偿的幂等要求**：补偿动作本身也必须幂等（补偿失败重试不能造成二次副作用），因此补偿同样走 `idem_key` 机制。

#### （3）指令优先级：从两级到四层 + 抢占规则

**分层定义**

| 层级 | 内容 | 示例 |
|---|---|---|
| P0 核心业务指令 | 用户明确的目标动作 | "给张三转 500" |
| P1 前置校验指令 | 风控、额度、身份校验 | 系统插入的校验步骤 |
| P2 补充指令 | 细节参数 | "用尾号 1234 的卡" |
| P3 附加指令 | 格式、语气、附加说明 | "顺便帮我总结一下" |

**冲突矩阵（谁可以打断谁）**

| 发起层 | 可打断 | 不可打断 |
|---|---|---|
| P0 | P2、P3 | P1（前置校验必须完成） |
| P1 | P3 | P0、P2 |
| P2 | P3 | P0、P1 |
| P3 | 无 | 全部 |

**抢占规则**
- 被抢占的指令进入 `hold_queue`（`hold_queue_max = 10`），主流程结束后按 FIFO 补执行，或由用户确认丢弃
- P0 抢占须在 `preempt_deadline = 5s` 内完成上下文切换，超时放弃抢占并排队
- **冲突 ≠ 覆盖**：新旧指令抽取 `{action, target, amount, constraints}`，若 `action` + `target` 相同但 `amount` 或 `constraints` 不同 → 判为**冲突**（需用户确认），不静默覆盖

**可撤销性裁决（关键约束）**
- 已提交的不可逆动作（已完成的支付／已开立的账户）**不允许被抢占**，只能追加补偿指令
- 已完成的前置校验若与新指令的约束冲突 → **重新校验**，不复用旧结果

#### （4）新增：韧性三件套

| 能力 | 参数 | 逻辑 |
|---|---|---|
| 重试退避 | `base = 1s`, `factor = 2`, `max_attempts = 3`, `jitter = ±20%` | 抖动防雪崩；仅对**可重试错误**（超时、5xx）重试，对业务拒绝不重试 |
| 熔断 | `window = 60s`, `error_rate_threshold = 50%`, `min_samples = 10`, `breaker_open = 120s` | 熔断期间直接返回降级结果；半开时放 1 个探针 |
| 死信 | — | 重试耗尽 → 落死信表 + 告警 + 人工工单入口 |

> **重要边界**：熔断与重试**只作用于只读与可重试写操作**。R2/R3 动作失败时**不重试**，直接走补偿 + 人工 —— 因为"重试一次支付"的期望损失远大于收益。

### 4.3 异常处理

| 异常 | 处理 |
|---|---|
| 幂等锁超时（`lock_ttl` 到期仍未完成） | 释放锁，标记原执行为 `UNKNOWN`，**不自动重试**，转人工核查副作用状态 |
| 心跳丢失但进程仍活 | 续租成功即恢复；看门狗发现租约被他人接管则本进程立即自杀（防双写） |
| 补偿失败 | 补偿同样重试；补偿耗尽 → 死信 + 高优告警（**这是最高级别告警**，因为状态可能不一致） |
| 规则引擎不可用 | `fail_mode = closed`：R2/R3 拒绝，R0/R1 放行 |
| 指令冲突无法判定 | 保守处理：暂停执行，向用户澄清 |

---

## 5. L3 升级：上下文与规则治理

### 5.1 节点结构

```
工具原始结果
   ↓
[是否有 response_schema?] ── 是 ──→ [通道 A 结构化抽取] ──→ 保真度 100%
        │
        └─ 否 ──→ [通道 B 语义摘要] ──→ [保真校验] ── 通过 ──→ 入上下文
                                              └── 失败 ──→ 回退「首尾截断 + 省略标记」

上下文组装 = LF 长期事实 + TS 任务摘要 + WM 工作集（按 Token 预算分配）

工具调用前 → [独立规则引擎] ── allow / allow_with_audit / require_review / block
```

### 5.2 处理逻辑

#### （1）预摘要：从"精简"到"可校验"

**现状**："对返回的原始长数据做精简预处理" —— 但没有说怎么精简，也没有说怎么防止精简掉关键信息。

**升级为双通道 + 保真校验**：

| 通道 | 适用 | 方法 | 保真度 |
|---|---|---|---|
| **A 结构化抽取**（首选） | 工具有 `response_schema` | 按 schema 抽白名单字段：**金额、状态、ID、时间、错误码**；丢弃日志、HTML 标签、冗余元数据 | **100%**（无损） |
| **B 语义摘要**（兜底） | 无 schema 的长文本 | LLM 摘要，prompt 强制保留：实体、数字、时间、**否定词**（不／未／无）、条件状语 | ≥ 95%（受校验保护） |

**保真校验（这是本条升级的核心）**：

```
1. 从原文正则抽取全部数字、金额、ID、日期
2. 检查每一项是否出现在摘要中
3. 任一缺失 → 判定摘要不可用
   → 丢弃摘要，回退「首尾各 N token 截断 + […中间省略 M tokens…] 标记」
   → 结果标记 digest_lossy = true（供 L0 统计）
```

> 为什么要专门防"否定词丢失"：摘要模型最容易把"**未**扣款成功"压缩成"扣款成功"。在支付场景这是致命错误。

参数：`digest_trigger_tokens = 1200`（超过才摘要）、`digest_max_tokens = 512`、`digest_fidelity_check = true`

#### （2）记忆切面：从固定 3 轮到预算驱动

**现状**："只保留最近三轮核心交互加全局任务状态" —— `3` 是个拍脑袋的静态值。而三轮对话的实际 Token 量在不同任务间可能差 10 倍（一次查余额 3 轮 ≈ 300 tokens；一次开户 3 轮 ≈ 6000 tokens）。

**升级为三层结构 + Token 预算自适应**：

| 层 | 内容 | 预算占比 | 说明 |
|---|---|---|---|
| **LF 长期事实** | 用户偏好、账户别名、稳定身份信息 | 10% | 存 KV，不进对话历史 |
| **TS 任务摘要** | 滚动摘要，只保留**决策与事实**（不保留寒暄与试错过程） | 30% | 每轮更新，超阈值时压缩 |
| **WM 工作集** | 最近 N 轮原始对话 | 60% | N 动态计算 |

```
ctx_budget = min( model_ctx × 0.5 , 8192 )
```
> 只占 50%：给系统提示、工具 schema 描述、当轮工具结果留出空间。

```
wm_rounds = clamp( floor(WM_budget / avg_round_tokens) , wm_min_rounds=2 , wm_max_rounds=8 )
```

**裁剪优先级（超预算时从低到高裁）**：

```
priority = w1 × risk_relevance    高风险动作相关的交互 → 最高，不可裁
         + w2 × recency           越近越高
         + w3 × referenced_by_plan 被当前计划引用的参数 → 不可裁
```

具体规则：
- **R2/R3 相关的交互永不裁剪**（哪怕它是最早的一轮）
- 被 plan 引用的参数值永不裁剪
- 寒暄、已废弃的尝试、重复的确认往返优先裁剪
- 裁剪前先把内容降级为 TS 摘要，再丢弃原文（保证信息不直接蒸发）

参数：`wm_min_rounds = 2`、`wm_max_rounds = 8`、`summary_trigger_rounds = 4`、`ctx_budget_ratio = 0.5`

#### （3）硬规则兜底：从"场景清单"到独立规则引擎

**现状**："支付、开户等高风险业务加规则校验，不符合业务流程直接拦截修正" —— 三个未答问题：**规则住在哪？谁改？改了怎么追溯？**

**升级为与 LLM 完全解耦的规则引擎**：

**约束（不可妥协）**
- 规则判定发生在**工具调用之前**，不是之后
- LLM **不能**修改规则、**不能**绕过规则、**不能**解释掉一次拦截
- 规则是确定性代码，不引入任何模型判断

**规则形态（决策表）**

```yaml
- rule_id: R-PAY-001
  scope: [transfer, payment]
  when: "amount > daily_limit_remaining"
  then: block
  evidence: [amount, daily_limit_remaining, account_id]
  severity: high

- rule_id: R-PAY-002
  scope: [transfer]
  when: "payee_is_first_time and amount > 5000"
  then: require_review
  evidence: [payee, amount, payee_is_first_time]

- rule_id: R-ACC-003
  scope: [account_open]
  when: "identity_verified == false"
  then: block
  evidence: [identity_verified]
```

**四类动作**：`allow` ／ `allow_with_audit`（放行但留痕）／ `require_review`（转人工复核）／ `block`（拦截）

**四项治理能力**（原设计全缺）

| 能力 | 作用 |
|---|---|
| **版本化** | 每条决策日志写入 `ruleset_version`，可追溯到"当时用的是哪版规则" |
| **影子模式** | 新规则先 `shadow`（只记录不拦截），累计 `shadow_hits ≥ 50` 且误判率 < 2% 后转 `active` |
| **可解释** | 拦截时产出 `{rule_id, matched_condition, evidence, action, suggestion}`，直接给用户／客服看 |
| **失效模式** | `fail_mode = closed`：规则引擎自身异常时，R2/R3 默认**拒绝**，R0/R1 放行 |

> `fail_mode = closed` 是"可控性优先"原则在异常路径上的直接体现：宁可业务暂时不出单，也不能在规则失效时放行高风险动作。

#### （4）新增：人机协同出口

原设计有三处会产生"Agent 卡住"：规则要求复核、重试耗尽、规则冲突。原设计没有出口 —— 升级补上。

- **触发**：`require_review` ／ 重试耗尽 ／ 死信 ／ 规则冲突
- **动作**：生成人工工单，附**完整上下文快照**（trace_id、当前 FSM 状态、已完成步骤、规则证据、原始工具结果）
- **回写**：人工决议写回 FSM（`APPROVED` / `REJECTED` + 理由），并进入审计流

### 5.3 异常处理

| 异常 | 处理 |
|---|---|
| 摘要保真校验失败 | 回退截断，标记 `digest_lossy`，计入 L0 指标 |
| 上下文仍超预算（裁剪后） | 最后兜底：硬截断最旧的 WM，并写 `ctx_truncated` 告警 |
| TS 摘要生成失败 | 保留上一条有效摘要（不覆盖） |
| 规则冲突（多条规则给出矛盾动作） | 取**最严**动作（block > require_review > allow_with_audit > allow），并记 `rule_conflict` |
| 规则引用的字段缺失 | 按 `fail_closed` 处理 R2/R3；R0/R1 该条规则判为不命中 |

---

## 6. L0 新增：观测与治理平面

这是原设计完全没有的一层，也是让"可控性"从口号变成可验证事实的前提。

### 6.1 四个组件

| 组件 | 内容 |
|---|---|
| **全链路 Trace** | `trace_id` 在用户请求入口生成，贯穿 L1–L3 全部节点；每节点记 `span{name, start, end, status, meta}` |
| **运行指标** | 见下表，实时采集 |
| **审计日志** | append-only，每条含 `trace_id, task_id, ruleset_version, fsm_version, decision, evidence`；不可修改 |
| **版本中心** | 规则与 FSM 版本集中管理，支持热更新 + 灰度（按任务比例放量） |

### 6.2 指标清单

| 指标 | 定义 | 目标值 |
|---|---|---|
| 分型准确率 | 人工抽检分类正确的比例 | ≥ 95% |
| 误升级率 | 弱状态任务被误升级的比例 | ≤ 5% |
| 幂等命中率 | 重复请求命中缓存的比例 | 建立基线后监控异常波动 |
| **重复副作用次数** | 同一 `idem_key` 产生 >1 次副作用的次数 | **0**（红线） |
| 断点续办成功率 | 挂起后成功恢复的比例 | ≥ 99% |
| 平均挂起时长 | 从 `SUSPENDED` 到恢复的时长 | P95 ≤ 5min |
| 摘要压缩比 | 摘要后 tokens / 原 tokens | ≥ 3:1 |
| 摘要保真度 | 关键字段保留率 | 结构化 100%，语义 ≥ 95% |
| 规则拦截率 | 被拦截请求数 / 总请求数 | 建立基线 |
| 规则误拦率 | 人工判定为误拦 / 总拦截 | ≤ 1% |
| 补偿执行成功率 | 补偿动作成功 / 需补偿总数 | ≥ 99% |

### 6.3 为什么这一层能兜住前两层

- `重复副作用次数 = 0` 是 L2 幂等键升级的**唯一验收标准**（原方案的 `task_id` 键做不到这一点）
- `规则误拦率` 是 L3 规则引擎上线的**准入门槛**（影子模式达标才允许 active）
- Trace 让"这次是哪一层的决策导致的"从猜测变成查询

---

## 7. 改进点汇总与预期效果

| # | 改进点 | 归属 | 解决的原问题 | 预期效果 |
|---|---|---|---|---|
| 1 | 二分类 → 二维分型矩阵 | L1 | 判据模糊、风险维度不参与分型 | 分型行为可复现；单轮高风险动作不再被漏管 |
| 2 | 分型判据量化（打分 + 阈值） | L1 | "强/弱"无客观标准 | 同一请求必得同一分型结果 |
| 3 | 动态升降级 | L1 | 分型一次定终身，无法纠偏 | 运行期可纠偏；误分类可自愈 |
| 4 | FSM 规格化（状态集/守卫/迁移/版本） | L1 | 状态机只有名字没有规格 | 可实现、可测试、可收敛 |
| 5 | 终态归档降级 | L1 | 状态机可能被历史任务撑爆 | 长期运行不膨胀 |
| 6 | 幂等键 1 元组 → 4 元组 | L2 | **同任务内不同参数调用被误判重复**（正确性事故） | 重复副作用归零 |
| 7 | 幂等分级（缓存/锁/副作用标记） | L2 | 只读与写用同一套，风险不对等 | 读写各得其所 |
| 8 | 缓存失效策略 | L2 | 无 TTL、无失效 → 脏数据 | 余额类查询不再返回陈旧值 |
| 9 | 租约 + 心跳 + 看门狗 | L2 | **挂起后无唤醒，任务永久停住** | 断点续办真正闭环 |
| 10 | Saga 补偿 | L2 | 失败后无法回滚副作用 | 资金类场景可就地收敛 |
| 11 | 指令分层 + 冲突矩阵 + 抢占队列 | L2 | 优先级只有两级，被抢占的指令无处可去 | 优先级真正可执行 |
| 12 | 可撤销性裁决 | L2 | 可能抢占已生效的不可逆动作 | 杜绝"抢占导致资金已出" |
| 13 | 重试退避 + 熔断 + 死信 | L2 | 无韧性机制，单点故障放大 | 故障不扩散，失败有归宿 |
| 14 | 双通道摘要 + 保真校验 | L3 | 摘要可能丢掉金额/否定词且无人知晓 | 关键信息零丢失且可监控 |
| 15 | 预算驱动三层记忆 | L3 | 固定 3 轮，与任务实际长度脱节 | 上下文占用降 ≥ 40%，且不丢关键轮 |
| 16 | 裁剪优先级 | L3 | 裁剪是任意的 | 高风险交互永不被裁 |
| 17 | 独立规则引擎（可解释 + 版本化） | L3 | 规则无处安放、改了无从追溯 | 拦截可解释、变更可追溯 |
| 18 | 影子模式 | L3 | 新规则上线即生效 → 误拦即事故 | 上线前先量化误拦率 |
| 19 | `fail_mode = closed` | L3 | 规则失效时行为未定义 | 异常时默认最严 |
| 20 | 人机协同出口 | L3 | 复核/冲突/耗尽三类场景无出口 | 卡住的任务有归宿 |
| 21 | L0 观测与治理平面 | 新增 | 三层日志各自为政，无法定位 | 任意请求可完整回溯 |

---

## 8. 实施路线

> 排期以「每阶段独立可验收」为原则，前阶段未达标不进下一阶段。

| 阶段 | 目标 | 交付内容 | 验收标准 |
|---|---|---|---|
| **P0 止血** | 把"声明"变成"可执行"，优先修正确性问题 | ① 二维分型判据 + 阈值 ② 复合幂等键（4 元组） ③ 规则引擎骨架（决策表 + block/allow 通路） | 重复副作用 = 0；R2/R3 动作 100% 过规则；分型可复现 |
| **P1 韧性** | 补异常闭环 | ① FSM 规格化 ② 租约 + 心跳 + 看门狗 ③ Saga 补偿 ④ 上下文预算 + 保真校验 | 断点续办 ≥ 99%；上下文占用降 ≥ 40%；摘要关键字段保留 100% |
| **P2 治理** | 让可控可度量、变更可控 | ① L0 Trace + 指标 + 审计 ② 影子模式 ③ 人机协同 ④ 版本中心 + 灰度 | 任意请求可完整回溯；新规则经影子模式达标后上线 |

**P0 优先级说明**：复合幂等键排在第一位，因为它修的是**正确性事故**（同任务内不同参数被误判重复），而不是效率问题 —— 在支付场景，这是必须最先堵住的洞。

---

## 9. 风险与回退

| 风险 | 影响 | 缓解措施 | 回退方案 |
|---|---|---|---|
| 规则误拦导致业务不出单 | 高 | 影子模式先行；能用 `require_review` 就不用 `block` | 一键切 `fail_open` 白名单（限时） |
| 状态机膨胀、维护成本高 | 中 | `fsm_max_states = 24` 上限；终态归档 | 回退到会话级状态（S1） |
| 摘要丢关键信息 | 高 | 保真校验 + `digest_lossy` 标记 + 回退截断 | 关闭语义摘要，只走结构化抽取 |
| 幂等键升级的过渡期 | 中 | 双写过渡（新旧键并存一个版本周期），读新写新 | 保留旧 `task_id` 索引做回退 |
| 补偿动作本身出错 | 高 | 补偿也走幂等；耗尽即最高优告警 | 人工介入（不自动兜底） |
| 租约误判导致双写 | 高 | 租约接管需 CAS 校验；被接管进程立即自杀 | 关闭看门狗自动唤醒，改为人工触发 |

---

## 附录 A：参数总表

| 参数 | 默认值 | 归属 |
|---|---|---|
| `classify_threshold` | 4 | L1 |
| `escalate_on_fail_count` | 2 | L1 |
| `escalate_on_turns` | 6 | L1 |
| `escalate_on_idle` | 300s | L1 |
| `fsm_max_states` | 24 | L1 |
| `state_read_payload_max_tokens` | 800 | L1 |
| `snapshot_retention` | 30d | L1 |
| `snapshot_write_retry` | 3 | L1 |
| `read_cache_ttl` | 300s | L2 |
| `lock_ttl` | 60s | L2 |
| `heartbeat_interval` | 10s | L2 |
| `lease_ttl` | 30s | L2 |
| `watchdog_interval` | 15s | L2 |
| `resume_backoff` | [30s, 2m, 10m, 1h] | L2 |
| `preempt_deadline` | 5s | L2 |
| `hold_queue_max` | 10 | L2 |
| `retry_base` / `factor` / `max_attempts` / `jitter` | 1s / 2 / 3 / ±20% | L2 |
| `breaker_window` / `error_rate_threshold` / `min_samples` / `breaker_open` | 60s / 50% / 10 / 120s | L2 |
| `digest_trigger_tokens` | 1200 | L3 |
| `digest_max_tokens` | 512 | L3 |
| `ctx_budget_ratio` | 0.5 | L3 |
| `ctx_budget_max` | 8192 | L3 |
| `wm_min_rounds` / `wm_max_rounds` | 2 / 8 | L3 |
| `summary_trigger_rounds` | 4 | L3 |
| `shadow_hits_threshold` | 50 | L3 |
| `shadow_false_positive_max` | 2% | L3 |
| `fail_mode` | closed | L3 |

## 附录 B：与原设计的对应关系（确保意图未漂移）

| 原设计表述 | 升级后如何承载 | 意图是否保持 |
|---|---|---|
| 强状态任务强制走状态机管护 | 策略矩阵中 S1/S2 全部走状态机；R2/R3 额外强制 | 保持并加强 |
| 每一步工具调用结果持久化存储 | FSM 快照 append-only + `snapshot_write_retry`；写失败不推进状态 | 保持并补齐原子性 |
| Agent 每轮推理前先读取当前状态节点 | 每轮注入 `current_state + last_tool_result_digest`，上限 800 tokens | 保持并明确粒度 |
| 不依赖大模型临时记忆 | 事实源始终是持久层；LF/TS 仅作辅助 | 保持 |
| 弱状态任务走原生大模型对话记忆 | S0×R0 象限保持原生；一旦风险或失败信号出现即升级 | 保持并补纠偏 |
| 所有工具请求绑定唯一任务 ID | 升级为 4 元组复合键（`task_id` 仍是组成部分） | 保持并修正确性 |
| 重复调用直接返回缓存结果 | 分级缓存（只读 TTL 缓存；写走锁 + 副作用标记） | 保持并分级 |
| 超时自动挂起并保留现场状态 | 租约失联即挂起 + 快照 + 看门狗唤醒 + Saga 补偿 | 保持并补齐唤醒 |
| 核心业务指令优先级高于补充指令 | 四层优先级 + 冲突矩阵 + 抢占队列 | 保持并细化 |
| 附加指令不允许打断主任务流程 | P3 不可打断任何层 | 保持 |
| 工具结果预摘要，减少上下文冗余 | 双通道摘要 + 保真校验 + 压缩比指标 | 保持并可监控 |
| 长记忆切面：最近三轮 + 全局任务状态 | 三层记忆（LF/TS/WM）+ Token 预算自适应（N ∈ [2,8]） | 保持并去掉硬编码 |
| 支付、开户等高风险业务加规则校验 | 独立规则引擎 + 版本化 + 影子模式 + 可解释 | 保持并工程化 |
| 不符合业务流程直接拦截修正 | 四类动作（block/require_review/allow_with_audit/allow） | 保持并分级 |
| **业务稳定性面前，可控性远比智能化更重要** | 贯穿全部 21 项改进；`fail_mode = closed`、高风险不重试、不可逆不抢占均为其直接体现 | **完整保持** |
