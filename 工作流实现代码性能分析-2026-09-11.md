# ai-workflow 实现代码性能 / 冗余 / 资源浪费分析

> 分析日期：2026-09-11 ｜ 分析对象：`ai-workflow` 技能 `scripts/` 全部实现代码
> 方法：逐文件通读 7 个脚本（checks.py / data_query.py / office_io.py / http_fetch.py / ai_call.py / run_stage.ps1 / setup_env.ps1），比对既有 `工作流瓶颈分析与优化方案-2026-09-11.md`（下称「旧文档」），并核对磁盘实际状态（缓存目录、是否已有 stage_router.py）。
> 证据强度标注：**[实测]** = 本机有基准/落盘证据；**[代码确认]** = 直接读源码可见；**[行业]** = 引用外部实测。

---

## 〇、先纠正旧文档的两处过时断言（读代码后必须订正）

| 旧文档说法 | 代码/磁盘事实 | 结论 |
|---|---|---|
| 优化2「跨任务持久缓存」当前**完全空白** | `http_fetch.py` 已有 `CACHE_DIR`（`~/.workbuddy/cache/ai-workflow/http`，TTL 3600s，落盘）+ `LINKCHECK_CACHE_DIR`（TTL 7 天，`probe/`）；磁盘上 `http/` 有 2 个 .bin、`probe/` 有 1 个 .json 缓存文件 | **已部分落地**。链接探活跨任务缓存（7 天）已生效；正文缓存（1h）已生效。剩余缺口：GitHub API 缓存仍是 1h TTL（未按 pushed_at 失效）、无跨任务共享限流计数器 |
| 优化3「阶段路由」P0 写入 SKILL.md | `scripts/` 下**无** stage_router.py / cache_probe.py；仅 `references/orchestration.md:39` 有「只读研究用便宜模型（差 15×）」的**原则性陈述**，无自动化路由代码 | **仍为空缺**。仅在文档层倡导，未实现任何「阶段→模型」自动选择逻辑 |

> 这意味着旧文档路线图里「净新增价值最高的是优化3 与优化2」需改写为：**优化3（阶段路由）确仍为空缺、ROI 最高；优化2 已是半完成态，剩余工作量小于旧文档估计。**

---

## 一、已交付项（核对，不重复邀功）

| 项 | 代码证据 | 状态 |
|---|---|---|
| B1 遍历冗余 9.6× | `data_query.py:_walk_stat` 用 `os.scandir`+`DirEntry.stat`，注释明示 12 万文件 21s→个位数秒 | **[实测] 已交付** |
| B2 检索缺口 | `http_fetch.py` 含 `--github-search` / `--github-code-search`；`data_query.py` 含 DuckDB `find/sql`；`--check-links` 探活 | **[代码确认] 已交付** |
| 优化1 工具链去冗余 | `os.scandir` 模式已固化于 `files/big`；网络侧并发取值已用 | **[代码确认] 已交付** |
| 优化4 强化 checks.py | `checks.py` 含 `_check_fuse`（熔断门禁）、`audit_step`（Anti-drop 交付物对账）、`cmd_status` 总览 | **[代码确认] 已交付** |

---

## 二、代码级瓶颈（N1–N9，按严重度排序）

### N1 — `data_query.py find`：全量内容物化，正是它要处理的 1.4GB 数据集会 OOM 【最高危】
- **现象/代码**：`cmd_find`（191–212 行）对非 `files_only` 模式构造的 SQL 是
  `read_text({49 个 glob})` → `string_split(content, chr(10)) AS lines` → `range(1,len(lines)+1)` 交叉连接，**把每个匹配文件的每一行都展开成一行中间结果**，再 `regexp_matches` 过滤。
  默认 `DEFAULT_GLOBS`（41 行）展开为 **49 个 `**/*.{ext}` 绝对 glob**，DuckDB 会把这些 glob 覆盖的**所有文件正文一次性读入内存**再切行。
- **根因**：为「带行号」的展示，付出了「全树全文件全行物化」的代价。而本工具的设计初衷恰恰是「1.4GB 数据集搜不动」这个场景——**它最可能在这里被自己撑爆**。
- **证据强度**：**[代码确认]**；DuckDB `read_text` 默认 in-memory，未设 `maximum_object_size`/`sample_size`。
- **影响**：在 1.4GB / 大仓库上，峰值内存 = 所有匹配文件正文之和 + 行展开中间表，极易 OOM 或被 OS 杀掉；且行展开交叉连接 CPU 开销巨大。

### N2 — `data_query.py find`：为一条告警二次遍历整棵树
- **代码**：182 行 `SELECT count(*) FROM glob({glob_lit})` 先**完整枚举一次**目录树只为数文件数；随后 202–212 行真正读取时**再枚举一次**。
- **根因**：preflight 计数与真实读取是两条独立枚举路径。
- **影响**：`find` 在 12 万文件目录上等于多付一次全树 stat 遍历（约 2–3s，与 B1 量级同构的 N 次系统调用冗余）。

### N3 — `data_query.py files`：无条件累积全部文件的 `(size,path)` 元组
- **代码**：128 行 `biggest.append((size, fp))` **无条件**执行；仅 136–139 行 `if args.top:` 才消费它。
- **根因**：top-N 清单的收集与「是否要 top-N」解耦。
- **影响**：`files` 默认 `--top 15` 时仍把全部文件路径+大小驻留内存（12 万文件 = 12 万元组）。`cmd_big` 已做对（只留 `>=floor` 的命中），`cmd_files` 没对齐。

### N4 — `http_fetch.py fetch`：整响应读入内存，大文件下载不流式
- **代码**：239 行 `data = resp.read()` 把整个响应体读进内存；`--out` 路径（735–738 行）再 `f.write(content)` 落盘。
- **根因**：未区分「小页面」与「大 artifact 下载」。
- **影响**：下载大文件（模型权重、大 PDF）时峰值内存 = 文件体积；流式（`shutil.copyfileobj(resp, f)`）可减半。

### N5 — `http_fetch.py _resolve_token`：无 env 时每次 github URL 抓取都 spawn `gh` 子进程
- **代码**：226 行 `elif "api.github.com" in url: auto = _resolve_token()`；`_resolve_token`（185–209 行）在无 `GITHUB_TOKEN/GH_TOKEN` 时会 `subprocess.run(["gh","auth","token"])`。
- **根因**：进程内未记忆已解析的 token。
- **影响**：低（单 CLI 调用内 `main` 通常先解析一次并下传；但「裸 `fetch` 一个 api.github.com URL」会重复 spawn）。收益小但零风险可修。

### N6 — `checks.py cmd_status`：每次都把全部任务目录的 plan.yaml 全量解析 + 逐交付物 `exists()`
- **代码**：254–286 行，对每个任务目录 `load_plan` 后 `[audit_step(s, workspace) for s in steps]`；`audit_step`（209–228 行）对每个交付物 token 跑 `any(c.exists() ...)`。
- **根因**：总览视图与「逐交付物存在性审计」绑死。
- **影响**：任务目录 50 个 × 每任务 10 步 = 数百次 YAML 解析 + 数百次 `exists()` 系统调用，而多数时候用户只想看「层级/完成度/是否熔断」概览。无 `--light` 模式。

### N7 — `checks.py cmd_mark` / `_set_meta_fuse`：每步状态更新都整文件重写
- **代码**：`cmd_mark`（361–378 行）与 `_set_meta_fuse`（308–330 行）均 `read_text` 全量 + `write_text` 全量。
- **根因**：单步更新粒度 = 整文件 IO。
- **影响**：若编排脚本循环 `mark` N 个步骤，会触发 N 次整文件重写（虽每次原子、正确，但多余 IO）。旧坑「同消息多 Edit 覆盖」正源于此模式的脆弱性。

### N8 — `setup_env.ps1`：依赖齐全时仍每次跑 `pip install`（触发索引解析）
- **代码**：30–37 行无条件 `pip install $deps`，靠后续 marker 检查判定成败。
- **根因**：pip 退出码在托管 shell 不可靠，故用 marker 兜底；但「先跑 pip」这一步未短路。
- **影响**：即便 7 个依赖全在，每次 setup 仍发起一次 PyPI 索引解析（几秒 + 网络）。

### N9 — `office_io.py _write_sheet`：列宽计算逐格重扫
- **代码**：96–103 行 `max(_disp_width(str(c.value)) for c in ws[col_letter] ...)` 对每列扫全部单元格。
- **根因**：写后再回扫算宽。
- **影响**：大表 O(行×列) 重扫；典型工作表可忽略，超大数据表才显形。

---

## 三、具体优化方案 + 预期收益 + 实施权衡

### 方案 A（对应 N1，最高 ROI 的代码修复）
- **改法**：
  1. `read_text` 增加 `maximum_object_size` / `sample_size` 形参（DuckDB 原生支持），对超阈文件截断或抽样，封顶单文件内存；
  2. `find` 默认走「文件级是否命中」轻路径（`regexp_matches(content,?)` 直接作用于 blob，返回 per-file 布尔），仅当显式 `--with-line` 才做行展开；
  3. 增加 `--max-files` 守卫，超出则报错/抽样并提示。
- **预期收益**：**[代码确认]** 把「在 1.4GB 数据集上可能 OOM/被杀」变为「可稳定跑完」；行模式仍较重但不再无界。
- **权衡**：截断/抽样可能漏掉超阈文件尾部的命中 → 必须保留 `--no-cap` 逃生口并在文档写明「超大文件默认截断，完整扫描用 --no-cap」。

### 方案 B（对应 N2）
- **改法**：去掉 182 行 preflight `count(*)`；当真实查询返回 0 行且需告警时，再按需数一次（或干脆用「glob 模式非空」替代精确计数告警）。
- **预期收益**：**[代码确认]** `find` 在 12 万文件目录省一次全树遍历 ≈ 2–3s（与 B1 同构）。
- **权衡**：丢失「精确扫描 N 个文件」提示文案；可接受（改为「命中 0，请检查 glob/目录」）。

### 方案 C（对应 N3）
- **改法**：128 行改为 `if args.top: biggest.append(...)`。
- **预期收益**：`files` 内存从 O(N) 元组降到 O(top)；CPU 忽略不计。
- **权衡**：无。

### 方案 D（对应 N4）
- **改法**：`--out` 分支用 `shutil.copyfileobj(resp, f)` 流式写；仅 `--text`/stdout 才需整读。
- **预期收益**：大文件下载峰值内存约减半，吞吐不变。
- **权衡**：无（流式是标准做法）。

### 方案 E（对应 N5）
- **改法**：模块级 `_TOKEN_CACHE` 记忆 `_resolve_token` 结果（进程内）。
- **预期收益**：无 env 时省一次 `gh` 子进程（~50–150ms + 鉴权）。
- **权衡**：无（进程内 memo 安全）。

### 方案 F（对应 N6）
- **改法**：`cmd_status` 增加 `--light`（只解析 meta + 步数/状态计数，跳过 `audit_step` 的逐交付物 `exists()`）；全量审计仍由 `checks.py plan <任务>` 逐任务提供。
- **预期收益**：50 任务目录的总览从「数百次 YAML 解析 + exists 系统调用」降到 O(目录数)；`status` 延迟由秒级→近瞬时。
- **权衡**：`--light` 不验证交付物存在（概览本就不该做重校验；精确对账用 `plan` 子命令）。

### 方案 G（对应 N7）
- **改法**：新增 `cmd_mark --batch <json>` 或内部累积多步编辑后单次重写；保持单步 `mark` 行为不变。
- **预期收益**：编排脚本标 N 步由 N 次整文件重写 → 1 次。
- **权衡**：需新增批量契约；单步语义必须 100% 兼容（用 `checks.py skill` + 回归夹具守住）。

### 方案 H（对应 N8）
- **改法**：setup 开头先跑 marker 快检，全在则跳过 `pip install` 直接打印 `[ OK ] 跳过`。
- **预期收益**：依赖齐全时省一次 PyPI 解析（~3–10s + 网络）。
- **权衡**：若某依赖 dist-info 在但包坏，marker（site-packages 目录/可执行文件）仍会捕获，安全。

### 方案 I（对应 N9）
- **改法**：列宽计算改为抽样（每列取前 K 行）或写行时顺带更新 `max_width`。
- **预期收益**：大表 O(行×列)→O(采样)。
- **权衡**：极长单元格偶而被低估列宽；可接受（或用 `--widths` 显式覆盖）。

### 方案 J（补完优化2 剩余）
- **改法**：GitHub API 缓存键加入 `pushed_at` 维度（值变才刷新，否则沿用更长 TTL）；进程间共享限流剩余计数（落 `cache/ai-workflow/` 小文件）。
- **预期收益**：调研类重复 fetch 再降一截；匿名 10/min 限流触发率趋零（旧文档已指出 dask/airflow 曾因限流丢数据）。
- **权衡**：缓存失效策略需谨慎（pushed_at 不变≠内容不变，如 release/issue 变动）→ 仍保留 `--no-cache` 逃生。

### 方案 K（实现优化3 阶段路由 —— ROI 最高、仍空缺）
- **改法**：在 `ai_call.py` 或新增 `stage_router.py` 实现 `route_model(stage, tags)`：规划/方案评审→旗舰；实现→中档；测试/文档/只读调研→便宜模型（本地 Ollama+DeepSeek/Qwen 或 Haiku 级）；`SKILL.md` 阶段 3 写「阶段→模型」决策清单。
- **预期收益**：**[行业]** 探索段成本 ~15×（Haiku vs Opus，findings-3）；整体编码场景推理成本降 70–90%（路由报告 §3.1.4）。
- **权衡（关键）**：**安全相关 / 关键实现代码不得路由到便宜模型**——必须设为不可覆盖的硬约束（与红线一致，强模型+双审）。路由错配的风险是「省钱但降质」，需用「按阶段打 token/成本标签」度量（见第五节）验证探索段质量不损。

---

## 四、落地优先级路线图（证据层级标注）

| 优先级 | 动作 | 对应 | 依赖 | 风险 | 证据层级 |
|---|---|---|---|---|---|
| **P0** | 方案 A：`find` 加 `maximum_object_size`/`--with-line`/`--max-files` | N1 | 无 | 中（截断遗漏，需 --no-cap） | [代码确认] 防 OOM |
| **P0** | 方案 K：阶段路由 `route_model` + SKILL.md 决策清单 | 优化3 | 无 | 中（错配降质，需硬约束） | [行业] 70–90% |
| **P1** | 方案 B/C/D/E/I：find 双遍历去除、files 内存、fetch 流式、token memo、列宽抽样 | N2–N5,N9 | 无 | 低 | [代码确认] |
| **P1** | 方案 J：GitHub 缓存 pushed_at 化 + 共享限流计数 | 优化2 剩余 | 无 | 中（失效策略） | [代码确认] |
| **P2** | 方案 F/G/H：status --light、mark 批量、setup 跳过 pip | N6–N8 | 无 | 低 | [代码确认] |
| **P2** | 优化5：SKILL.md 精简 + references 按需加载自检（流程层，非纯代码） | 优化5 | 无 | 低 | [行业] ETH Zurich +20% |

---

## 五、度量：怎么证明优化生效（避免「感觉快了」）

旧文档第五节已立：METR 教训（自我感知可偏差 39pp）。补两条可落地的：
1. **工具层**：对 N1 用 1.4GB 数据集跑「优化前/后」峰值内存与是否完成（OOM=硬失败）；对 N2 用 `time` 对比 `find` 双遍历去除前后的 wall time。
2. **成本层（方案 K）**：若接 Langfuse 自托管，按阶段打 token/成本标签，验证「探索段成本真降、且独立审核发现的正确性缺陷数不升」——**速度提升但正确性缺陷数上升 = 优化失败**。

---

## 六、红线与下一步

- 本分析**只读代码 + 核对磁盘**，**未改动任何技能代码**（遵守「确认前不执行」+ 技能目录属基础设施例外、改写需当次授权）。
- 以上方案 A–K 任何一项落地，都属于对 `ai-workflow` 技能的实质性改写，须走 `ai-workflow` 流程：澄清六要素 → 计划落盘 → 审核（v2.5.3 起为抽查制：默认自查 + 机器门禁，高危/点名/触顶三类必送审） → 验收 → 归档；且涉及 `C:\Users\26717\.workbuddy\skills\ai-workflow\` 的写入需你明示授权。
- **建议下一步**：先取 P0 两项的**方案 A（防 OOM，纯收益无质量风险）**与**方案 K（阶段路由，ROI 最高但需硬约束）**做计划评审；N2–N5/N8/N9 低风险项可并入同一次「工具链去冗余」提交。

---

## 一句话结论

代码级最危险的不是旧文档说的「遍历」（B1 已修 9.6×），而是 **N1：`find` 对自己的目标场景（大数据集）用了最吃内存的全文行展开路径，可能 OOM**——这是旧文档 B1–B5 完全没覆盖的新瓶颈。旧文档称「优化2 空白、优化3 最高 ROI」需订正为：**优化2 已半落地（链接 7 天缓存 + 正文 1h 缓存在线），优化3 仍空缺且确为最高 ROI**。低风险去冗余（N2–N5/N8/N9）可快速收割；P0 应优先 N1（防 OOM）与 K（阶段路由 70–90%）。

## 实施状态（2026-09-11 续）

已落地并验证：**N1（find 防 OOM）+ N2（去二次遍历）+ N3（files 不累积全量元组）**，改动集中于 `scripts/data_query.py`：
- 新增 `_glob_to_regex`：glob→正则**逐字符分词器**（初版用顺序 `replace` 把 `(?:.*/)?` 里的 `?` 误伤成 `[^/]`，已改为分词器修复）。
- `cmd_find`：单遍 `_walk_stat` 预过滤，只对 ≤`--max-object-size`（默认 32MB）的文件交给 DuckDB `read_text`，大文件直接跳过（不读内容）；默认仅返回命中文件名（内存有界），`--with-line` 才展开行号；`--max-files` 上限文件数；`--max-object-size 0` 且 `--max-files 0` 才回到旧版 glob 路径。
- `cmd_files`：`biggest` 列表仅在 `args.top` 时累积。
- 验证：`checks.py skill` **19/19**；功能测试含 **213MB 大文件（模式仅在末尾）→ 0 命中且「跳过 >32MB 大文件 1 个」**（证明未整读，GB 级也不会 OOM）、21.5MB 文件正常命中、旧版路径仍可命中。
- 注意：本 DuckDB 版本的 `read_text` **不接受命名参数**（`maximum_object_size`/`ignore_errors` 均报 "does not accept any named parameters"），故方案 A 原设计（SQL 层加 cap）不可行，改用 Python 端预过滤——版本无关、更稳。

待办（仍空缺）：**K（阶段路由，最高 ROI）**、优化2 剩余（GitHub 缓存 pushed_at 化 + 共享限流计数）。

---

## 实施状态（2026-09-11 再续 · N4–N9 去冗余打包）

按上一轮收尾建议与用户「做」的授权，将低风险去冗余项 **N4–N9** 并入一次「工具链去冗余」改动，覆盖 4 个脚本。改动均属基础设施例外目录内的工具用途，**未触碰用户项目文件**。

### 已落地并验证（N4–N9）

- **N4（http_fetch.py `fetch` 流式落盘）**：`fetch()` 新增 `out_file` 参数；当 `http_fetch.py <url> --out`（且非 `--text`/`--grep`）时走 `shutil.copyfileobj(resp, f)` 流式写，**整响应不读入内存、不写缓存**；文本/grep/stdout/各 `collect_*` 函数仍走原 `bytes` 返回路径，零回归。
- **N5（http_fetch.py `_resolve_token` 进程内记忆）**：新增 `_TOKEN_CACHE`，env/gh 解析结果（含「无凭据」的 `None`）记忆复用，避免无 env 时每次 github 抓取都 spawn `gh auth token` 子进程；`cli_token` 显式传入不进缓存。
- **N6（checks.py `cmd_status --light`）**：轻量模式只解析 `meta` + 步数/状态计数，跳过 `audit_step` 逐交付物 `exists()` 文件系统检查（交付物列显示 `—`）。
- **N7（checks.py `cmd_mark --batch`）**：新增 `--batch <文件>`（每行 `<step_id> <status>`），**一次性读盘、定位全部目标行、单次 `write_text`**，消除 N 步触发 N 次整文件重写；任一目标缺失则整体失败、不落盘（信任但验证）。
- **N8（setup_env.ps1 跳过 pip）**：脚本顶部上移 marker 定义（供快检与末尾 import 校验共用，去重）；依赖齐全时跳过 `pip install`，仅保留末尾 import 校验作权威门禁。
- **N9（office_io.py `_write_sheet` 列宽抽样）**：列宽估算由全列扫描改为只抽样前 200 行（`ws.cell` 直取），大表避免 O(行×列)。

### 验证（证据）

- `checks.py skill` 复跑：**19/19，无回归**。
- 三个 Python 脚本 `py_compile` 全过。
- 功能用例（临时夹具置于 `~/.workbuddy/cache/ai-workflow/`，验证后清理）：
  - **N4**：本地 http server 提供 2MB 文件，`--out` 下载 `FETCH_EXIT=0`、stderr 出现 `已流式写入 …（2097152 字节，未载入内存）`、源/目标字节完全一致。
  - **N5**：`_resolve_token()` 首次解析经 gh CLI 得 40 字符令牌，`_resolve_token()` 二次调用返回同一对象（`N5_cache_reuse True`），证明未再 spawn 子进程。
  - **N6**：`status --workspace E:\ChatGPT\工作流 --light` 正常运行，交付物列 8 个任务均显示 `—`（跳过 exists 检查）。
  - **N7**：临时 `plan.yaml` 两步经 `--batch` 一次落盘，输出 `步骤状态已更新 — 2 个：s1 … → 完成; s2 … → 完成`。
  - **N9**：长列（30 中文）写表后列宽 = 50.0（上限截断），短列 = 8.0，抽样生效。
  - **N8**：7 个 marker（6 个 site-packages 目录 + `Scripts/ast-grep.exe`）实测**全部存在** → `$allPresent` 为真 → 走跳过 pip 分支（逻辑 + marker 实证）。注：本机 PowerShell 工具吞掉 `Write-Host` 输出且返回噪声退出码，无法直接回显脚本自身 `[ OK ] 跳过 pip` 行，但代码与 marker 双证其分支正确、非回归。
- **未提交 git**（用户未要求）；工作树已改 4 文件。

### 仍空缺 / 后续

- **K（阶段路由）**：ROI 最高，但需硬约束「安全/关键实现不得路由便宜模型」，用户本轮明确先不动，等点名。
- **优化2 剩余**：GitHub 缓存 `pushed_at` 化 + 进程间共享限流计数。
- **独立审核（红线）**：以上为作者自验（编译 + 19/19 + 功能用例）。按 ai-workflow「执行者≠审核者」纪律，**N4–N9 仍需未参与者第三方复核**后方可视作交付；建议作为下一步。
