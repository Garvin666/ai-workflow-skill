# ai-workflow 性能瓶颈分析与提速方案

> **对象**：`C:\Users\26717\.workbuddy\skills\ai-workflow`（v3.3.0，2026-09-16 版）
> **方法**：全部结论以本机实测为准；凡估算均显式标注「估算」。
> **本文是分析文档，未改动技能本体。**
> **日期**：2026-09-16

---

## 〇、一句话结论

本流程的耗时**不在算法，在"配比"**：真正干活的代码只占调用耗时的一小部分，绝大部分被三样东西吃掉 ——
**① 每次脚本调用约 0.4s 的解释器启动地板；② 一个被环境劫持的删除操作（0.62s）；③ 一个能批量却按单条走的命令（8 步 3.46s）**。
三者合计占一个 L2 任务本地脚本耗时的 **约 85%**，且**全部与流程逻辑无关**。
上下文侧同理：`ops.md` 的 **73.7%** 与 `SKILL.md` 的 **24.7%** 是"只在特定阶段才需要"的内容，却按常驻成本付费。

**全部实测优化点的合计收益（本机可测部分）：本地脚本耗时 −78%、常用手册加载 token −74%、缓存目录 −96%。**

---

## 一、测量口径（先说清哪些是实测、哪些是估算）

| 类别 | 口径 |
|---|---|
| **实测** | 直接用本机 Python 跑命令计时，`perf_counter`，取 n≥3 最小值（热态）。原始输出见 `.workbuddy/tmp/perf_out*.txt / rm.txt / batch.txt` |
| **估算** | token 数。本机无离线 tokenizer，用 `中文汉字数 + 非汉字字符数/4` 粗估，**与实际 tokenizer 有偏差，仅用于比较量级与占比，不可当作精确账单** |
| **不可测（如实声明）** | **LLM 推理耗时不在本机产生，无法直接测量**。本文对"每一步耗时"的结论**只覆盖可测的确定性部分**（脚本、子进程、I/O、token 占用），不编造每步的推理秒数 |
| 环境 | Windows / Git Bash；Python 3.13.12（`envs\ai-workflow`）；`PYTHONPATH` 被注入 `…\cli\vendor\shim` |

---

## 二、耗时归因总表（实测）

### 2.1 脚本与进程实测

| 操作 | 冷启动 | 热态（min） | 备注 |
|---|---|---|---|
| Python 解释器启动（managed） | 413 ms | **400.6 ms** | 每次脚本调用的**固定地板** |
| `python -c "import yaml,json"` | 540 ms | 411.2 ms | 依赖导入几乎不额外加钱 |
| `gh --version` | — | **843.1 ms** | 外部 CLI 子进程单价 |
| `gh auth token` | — | **788.8 ms** | **每次未命中缓存都付** |
| `checks.py --help` | 570 ms | **182.1 ms** | ≈ 纯启动，无实际工作 |
| `checks.py plan <plan> --base` | — | **208.1 ms** | 交付物对账 |
| `checks.py status`（11 个 plan） | — | **269.6 ms** | |
| `checks.py status --light` | — | **257.9 ms** | 比全量**只快 11.7 ms（4.3%）** |
| **`checks.py skill`** | — | **996.6 ms**（沙箱内 4,204–4,584 ms） | 拆解见 2.3 |
| `archive_tasks.py`（dry-run） | — | 200.4 ms | |
| `http_fetch <url>` 冷取 | — | **6,598 ms** | 含网络往返 |
| **`http_fetch <url>` 缓存命中** | — | **1,392 ms** | 拆解见 2.4 |

### 2.2 上下文体量（估算 token）

| 文件 | 字节 | 行 | 估算 tok | 加载时机 |
|---|---|---|---|---|
| **SKILL.md** | 72,183 | 536 | **20,677** | **每次触发本技能（常驻）** |
| **references/ops.md** | 103,716 | 360 | **28,998** | 查命令 / 排障（按需） |
| references/quality-gates.md | 16,335 | 120 | 4,752 | 按需 |
| references/security-guide.md | 11,900 | 164 | 3,482 | 按需 |
| references/adaptive-planning.md | 7,290 | 130 | 2,122 | 按需 |
| references/capability-routing.md | 6,928 | 88 | 2,011 | 按需 |
| references/playbook.md | 6,195 | 83 | 1,786 | 按需 |
| references/orchestration.md | 5,603 | 72 | 1,625 | 按需 |
| **references/routing-guide.md** | 2,787 | 60 | **781** | **无任何文档引用（孤儿）** |
| references/office-guide.md | 1,835 | 32 | 523 | 按需 |
| `references/` 合计 | — | — | **46,080** | 全读的代价 |
| 技能全部文件（含 10 个脚本） | — | — | **128,046** | —— |

### 2.3 `checks.py skill` 的 1.0 秒花在哪（cProfile + 受控实验）

cProfile 分解：`check_skill()` cumtime **1.051 s**，其中

| 环节 | 耗时 | 说明 |
|---|---|---|
| `shutil.rmtree` → 沙箱「走回收站」劫持 | **0.875 s** | `checks.py:191-193` 删 `scripts/__pycache__` |
| 真正的检查工作 | **≈0.15 s** | 60 条引用正则 + 8 份模板 schema + plan 键 + 10 次 py_compile |

**受控实验（同一个 `shutil.rmtree`，删 8 个 4KB 文件，只换父目录）**：

| 父目录 | min | 相对倍数 |
|---|---|---|
| 系统临时目录 | **19.5 ms** | 1.0× |
| 工作区 `.workbuddy/tmp` | **564.2 ms** | **28.9×** |
| 技能 `scripts/`（即 `__pycache__` 所在地） | **855.8 ms** | **43.9×** |

→ **成本由路径决定，与被删内容无关。** 根因已定位到环境变量：
`PYTHONPATH=D:\26717\WorkBuddy\resources\app.asar.unpacked\cli\vendor\shim`
该 shim 的 `sitecustomize._safe_shutil_rmtree` 对非临时目录的删除改走回收站（`_try_trash_via_binary`，spawn 子进程）。

**复现真实场景（10 个脚本）**：

| 步骤 | 耗时 |
|---|---|
| `py_compile` 10 个脚本（真正的语法检查） | **131.6 ms** |
| 清理由此产生的 `__pycache__`（10 文件） | **619.7 ms** |
| **合计现状** | **751.3 ms** |
| 改用 `py_compile.compile(..., cfile=<临时目录>)` | **53.7 ms**，且**根本不产生 `__pycache__`** |

### 2.4 `http_fetch` 缓存命中仍要 1.4 秒（构成完全对账）

| 构成 | 耗时 |
|---|---|
| Python 解释器启动 | 400.6 ms |
| `gh auth token` 子进程 | 788.8 ms |
| 实际读缓存 + 解析 | ≈200 ms |
| **计算合计** | **≈1,389 ms** |
| **实测** | **1,392 ms** ✅ |

`http_fetch.py:242-275` 的 `_TOKEN_CACHE` 只做**进程内**记忆 —— 跨进程必然重付 788.8 ms。
**注意**：2026-09-11 的 N5 修复范围本就写的是「进程内」，故这不是回归，而是**当时未覆盖的另一半**。

### 2.5 `mark` 单步 vs 批量（能力已存在，但流程没走）

| 方式 | 实测（8 步 plan） | 单次均价 |
|---|---|---|
| 逐条 `checks.py mark <plan> <id> <状态>` | **3,460.0 ms** | 432 ms |
| `checks.py mark <plan> --batch <文件>` | **413.1 ms** | 413 ms |
| **差异** | **省 3.05 s，降 88%** | —— |

`--batch` 早在 v2.5.3（N7）就已实现（`checks.py:383, 835`），但：
- `SKILL.md:535`「核心命令」串与 `SKILL.md:146` 阶段 3 的操作说明**都只写单条形式**；
- `references/ops.md` 仅在「反模式 48」里作为 BOM bug 的背景提过 `--batch`。

→ **优化做了，流量没走上去。** 这属于"文档与实现脱节"在本流程内的性能表现。

### 2.6 缓存目录构成（303.5 MB / 707 文件）

| 子目录 | 大小 | 性质 |
|---|---|---|
| `_dq_verify` | **235.08 MB** | 测试残留 |
| `_dq_probe` | **51.50 MB** | 测试残留 |
| `http` | **12.72 MB** | **真实缓存（355 文件）** |
| `n4test` | 4.00 MB | 测试残留 |
| 其余 13 个 probe/test 子目录 | ≈0.2 MB | 测试残留 |
| **合计** | **303.5 MB** | 测试残留占 **95.7%** |

`_dq_verify/big.bin.md` 单文件 213.62 MB。缓存命中是 sha1 直接寻址（O(1)），**不影响单次命中速度**，但拖慢任何目录遍历/清理/备份，且是纯废数据。
另：`usage_ledger.jsonl` **不存在** —— 说明「阶段路由降本 70–90%」至今**没有任何本地实测账单**。

### 2.7 阶段 0「索引优先」的落空

| 检查项 | 结果 |
|---|---|
| `C:\Users\26717\.workbuddy\skills\README.md` | **MISSING** |
| `E:\ChatGPT\工作流\.workbuddy\skills\` 目录 | **根本不存在** |
| `assets/templates/README.md` | EXIST（但这是模板规范，非技能索引） |

→ `SKILL.md:107` 的"先读技能库索引，命中才不全量扫"**永远无法命中**，每次任务必然退化为全量扫描：

| 量 | 值 |
|---|---|
| 技能目录 / SKILL.md | 26 个目录 / 25 个 SKILL.md |
| 全部 SKILL.md 字节 | 209,406 B |
| 其中 frontmatter 字节 | 11,976 B ≈ **2,994 tok（估算）** |
| **每次任务固定付出** | **≈2,994 tok + 26 次目录遍历** |

`SKILL.md:108` 自己写着"全量 Glob + 逐个读 description 是每任务固定付出的等待成本，而索引正是为消除它而建的"——**索引没建，这笔成本一分没省**。

---

## 三、瓶颈清单（按投入产出比排序）

### P1 ⭐ 消除「先造垃圾再清理」——`checks.py skill` 的 0.62 s

| 项 | 内容 |
|---|---|
| **现象** | 自检热态 997 ms，其中 620 ms 花在删除自己刚生成的 `__pycache__` |
| **实测证据** | 受控实验 19.5 / 564.2 / 855.8 ms（按路径 43.9× 放大）；复现场景 131.6 + 619.7 ms |
| **根因** | `checks.py:186-193` 先用 `py_compile.compile(str(f))` 默认写出 `scripts/__pycache__`，再 `shutil.rmtree` 清掉；在 `PYTHONPATH` 注入 shim 的环境里该删除被劫持为走回收站 |
| **改法** | `py_compile.compile(str(f), cfile=str(tmpdir / (f.stem + ".pyc")), doraise=True)`，**从根上不产生 `__pycache__`**，清理逻辑整段可删（或保留为兜底但仍改 `unlink` 循环） |
| **预期效果** | **751 ms → 53.7 ms（−93%）**；`checks.py skill` 热态 997 → **≈380 ms（−62%）**；沙箱内 4.2–4.6 s → **≈1.2 s（−74%）** |
| **代价** | 极低。改动 3 行，语义不变（py_compile 的 `doraise` 行为完全保留） |
| **可迁移教训** | **不要为了清理而先产生** —— 能"不产生"就不要"产生 + 删除" |

### P2 ⭐ 让 `mark` 走已实现的批量通道——3.05 s

| 项 | 内容 |
|---|---|
| **现象** | 8 步 plan 逐条 mark 3,460 ms |
| **实测证据** | 逐条 3,460.0 ms vs `--batch` 413.1 ms |
| **根因** | 不是代码问题，是**文档没引导**：`--batch` 已实现（checks.py:835），但 SKILL.md 的核心命令串与阶段 3 操作说明只写单条 |
| **改法** | ① `SKILL.md:146` 阶段 3 操作说明补「多步状态更新用 `--batch`」；② `SKILL.md:535` 核心命令串补 `--batch`；③ `ops.md` 脚本速查表补一行；④ 阶段 3 收尾时把"本批完成的状态"合并一次落盘 |
| **预期效果** | **−88%**；8 步 plan 省 3.05 s。若按 L2 任务平均 8 步计，**每任务省约 3 s** |
| **代价** | 纯文档改动，零风险 |
| **注意（重要）** | **不要**为了省这点时间改成手改 YAML —— 那会破坏「留痕不靠记忆」，是负收益。要省的是**调用次数**，不是"少留痕" |

### P3 ⭐ 缓存命中路径不该解析凭据——0.79 s/次

| 项 | 内容 |
|---|---|
| **现象** | 缓存命中仍 1,392 ms |
| **实测证据** | 400.6（启动）+ 788.8（gh）+ 200 ≈ 1,389 ≈ 实测 1,392（完全对账） |
| **根因** | `_resolve_token()` 在 `fetch()` 之前被调用；命中缓存并不需要网络，也就不需要凭据 |
| **改法** | **惰性解析**：把 token 解析推迟到"确实要发请求"的分支之后；缓存命中直接返回。**不得**把凭据落盘缓存（违反 S1），只能靠惰性化 |
| **预期效果** | 1,392 → **≈600 ms（−57%）**；调研类任务典型 5 次命中 → **省约 4 s**；命中频繁时线性放大 |
| **代价** | 低。需保证 `--github-*` 分支仍在发请求前解析（否则退回无凭据 403） |
| **不可省的部分** | 400 ms 解释器地板（见 P6 的说明，不建议为此改架构） |

### P4 ⭐ 把 `ops.md` 的变更日志拆出去——21,378 tok

| 项 | 内容 |
|---|---|
| **现象** | 「按需加载」的手册单文件 28,998 tok，**比常驻的 SKILL.md 还大 40%** |
| **实测证据** | 章节拆解：**变更日志 21,378 tok = 73.7%**；故障排查 12.0%；脚本速查 8.5%；环境检测 2.4%（完整占比见 §二 与本文件 §五） |
| **根因** | 一个文件同时承担「速查 / 环境 / 排障 / 变更日志」四件事，而**变更日志是追加式无上限增长**的。任何"我要查个命令怎么写"都要付 2.9 万 token |
| **改法** | 拆为 `references/ops.md`（速查+环境+排障+变量+SOP，稳定短）+ `references/changelog.md`（只追加，不进检索路径，需要时读尾部）。**内容不删、只搬家**，`SKILL.md` 索引表补一行 |
| **预期效果** | 常用路径 **28,998 → 7,551 tok（−74%）**；且变更日志今后无上限增长也不再拖累常用路径 |
| **代价** | 一次搬移；须同步更新「文档引用完整性」检查（`checks.py skill` 会扫 references，搬家后要确认引用仍解析） |

### P5 ⭐ 把 `SKILL.md` 的推送路由细则下沉——5,069 tok（24.7%）

| 项 | 内容 |
|---|---|
| **现象** | 常驻 SKILL.md 20,677 tok 中，**「自研工具与技能：标注与公开留痕」独占 5,069 tok（24.7%，178 行）** |
| **实测证据** | 章节 token 占比：该节 5,069 tok（24.7%）> 阶段 3（3,620，17.6%）> 熔断机制（1,716） |
| **根因** | 该整节（两仓分流表、5 列冲突处理矩阵、7 条验收判据、`verify_push` 说明）**只在阶段 6 沉淀时才需要**，却按"每次触发都注入"付费 |
| **改法** | 保留 ≤15 行摘要（自研判定三条 + 两仓分工一句 + 触发时点 + 手册指针），细则移入 `references/push-routing.md`，索引表补行 |
| **预期效果** | 常驻 **20,677 → ≈16,000 tok（−22%）**；收益是**窗口余量**，从而减少 Token 纪律要求的压缩轮次 |
| **诚实标注** | **省的是"首次 prefill + 窗口占用"，不是每一轮都省** —— SKILL.md 是稳定前缀，后续轮次吃 KV 缓存会摊薄重复成本。**不得宣称"每轮省 5k"** |
| **代价** | 中。该节内容密度高，摘要边界需要仔细划（漏掉"首次创建后每次修改都要推"这类硬要求就是倒退） |

### P6 建技能索引（或用真实数字决定不建）——2,994 tok/任务

| 项 | 内容 |
|---|---|
| **现象** | 索引文件不存在，`SKILL.md:107` 的索引优先 100% 落空 |
| **实测证据** | 两处索引路径均 MISSING；25 份 frontmatter = 11,976 B ≈ 2,994 tok |
| **改法** | 写一个生成器（读各 `SKILL.md` frontmatter → 按簇分组输出 `~/.workbuddy/skills/README.md`），在阶段 0 复用；索引过期时按现有条文回落全量并补写 |
| **预期效果** | 阶段 0 **≈2,994 → ≈750 tok（−75%）**，且**每次任务都省**（这是本文里唯一"每任务都必然生效"的 token 项） |
| **代价** | 需一个生成脚本 + 一条维护纪律（技能新增/改名后重生成）。**若不愿维护，替代方案是把该条文改掉**（承认"本机无索引，阶段 0 直接全量扫并接受 3k tok"），**不要留一条永远不生效的条文** —— 那是文档失真 |
| **附** | 技能库已有 32 个技能，**索引的价值随技能数增长**，现在建比以后建便宜 |

### P7 清理缓存目录的测试残留——290.6 MB

| 项 | 内容 |
|---|---|
| **现象** | `~/.workbuddy/cache/ai-workflow/` 303.5 MB / 707 文件，其中 **95.7% 是测试残留** |
| **实测证据** | `_dq_verify` 235.08 MB、`_dq_probe` 51.50 MB、`n4test` 4.00 MB；真实 `http/` 仅 12.72 MB |
| **改法** | ① 清理 14 个 probe/test 子目录（**删除动作，须单独确认**）；② 约定：探针/夹具输出一律放 `tasks/<任务>/tmp/` 或 `cache/_probe/`，**不混入生产缓存根** |
| **预期效果** | **303.5 → ≈13 MB（−96%）** |
| **代价/风险** | **属删改用户数据的不可逆动作**，本文只给计划，**执行前须逐条列出待删清单并取得确认** |
| **诚实说明** | 本条**不改善运行速度**（命中是 O(1) 寻址），只改善磁盘、遍历、备份与"缓存里到底有什么"的可解释性 |

### P8 `gh_core` 限流未区分有无凭据——可能白等 60 s

| 项 | 内容 |
|---|---|
| **现象** | `github_repo_metrics()` **无条件**调用 `_rl_throttle("gh_core", 60, 3600)`，即无论有无 token 都自限 60 次/小时 |
| **实测证据** | `http_fetch.py:376` 无分支；`RATE_LIMIT_MAX_WAIT=60`（`_rl_throttle` 最多睡 60 s）；本机 `gh auth token` 可解析（788.8 ms，返回 41 字节） |
| **影响** | 带凭据时 GitHub 实际配额为 5000/h，**自限到 60/h 会白等**；多仓库指标对比（"≥3 个方向必须并行"是流程硬规则）时最可能触发 |
| **改法** | 有凭据时放宽桶上限（留安全边际），或**读响应头 `X-RateLimit-Remaining/Reset` 动态调整**（更正确，但要防误判） |
| **预期效果** | 多仓库对比场景从"可能睡满 60 s/次"降到 **≈0**；**具体额节省依调用量而定，本文不给未实测的秒数** |
| **代价** | 中。放宽后若真撞 403，会退化为等待+重试 —— 需保留退避路径 |

### P9 变更治理（非性能，但同源，顺带修）

| 编号 | 问题 | 证据 | 建议 |
|---|---|---|---|
| P9-a | `references/routing-guide.md`(781 tok) **不在 SKILL.md 手册索引表中** | 全文检索：`routing-guide` 在 SKILL.md **0 命中**；其价格表与 `model_tiers.json` 一致（4/16、1/2、0/0），未发现分叉 | 补进索引表，或确认已被 SKILL.md 路由段取代后删除 |
| P9-b | **`模型档位` 字段口径两处** | `SKILL.md:164` 说写「验证方式」或确认表；`routing-guide.md:59` 说写「模型档位」字段；`plan-template.yaml:47` **确实有**该字段 | 统一为一处（建议：写专用字段），消除"按 A 文档做、按 B 文档查不到"的返工 |
| P9-c | **`模型档位` 取值无机器校验，真实数据已越界** | 文档 enum 为 `strong/mid/cheap`；`tasks/技能增强-ai-workflow-v3.2.0-2026-09-16/plan.yaml:29,61` 实写 **`default`**；全库检索 `checks.py` 对 `模型档位` **0 命中** | 给 `checks.py` 加取值校验（与 `_check_selftools` 同口径：可选，填了就要合法） |
| P9-d | `usage_ledger.jsonl` **从未生成** | 文件不存在 | 「阶段路由降本 70–90%」目前**只有行业引用、无本地账单**；承诺为"可判定信号"却无数据，建议补一次真实测算或改口径表述 |
| P9-e | `scripts/pip_debug.log` 0 B，无任何文档引用 | 全文检索 `referenced=False` | 删除 |

---

## 四、预期收益汇总

### 4.1 本地脚本耗时（一个 8 步 L2 任务的典型调用组合）

| 调用 | 现状 | 优化后 | 依据 |
|---|---|---|---|
| `status` ×1 | 270 ms | 270 ms | 无优化空间（已测 `--light` 只省 11.7 ms） |
| `mark` ×8 | **3,460 ms** | **413 ms** | P2 实测 |
| `plan` ×1 | 208 ms | 208 ms | —— |
| `skill` ×1 | **997 ms** | **≈380 ms** | P1 实测推算 |
| `archive` ×1 | 200 ms | 200 ms | —— |
| **小计** | **5,135 ms** | **1,471 ms** | **−71%** |
| 若含 5 次 `http_fetch`（缓存命中） | +6,960 ms | +3,000 ms | P3 实测推算 |
| **合计** | **12,095 ms** | **4,471 ms** | **−63%** |

> 该表**只含本机可测的确定性部分**，不含 LLM 推理时间（不可测）。

### 4.2 上下文成本

| 场景 | 现状 | 优化后 | 降幅 |
|---|---|---|---|
| 触发时常驻（SKILL.md） | 20,677 tok | ≈16,000 tok | **−22%** |
| 阶段 0 技能检索 | ≈2,994 tok | ≈750 tok | **−75%**（**每任务必然生效**） |
| 查命令/排障（读 ops.md） | 28,998 tok | 7,551 tok | **−74%** |

### 4.3 磁盘与外部等待

| 项 | 现状 | 优化后 |
|---|---|---|
| 缓存目录 | 303.5 MB / 707 文件 | ≈13 MB / ≈360 文件（**−96%**） |
| `gh_core` 限流等待 | 有凭据时仍按 60/h 自限，单次最多睡 60 s | 按实际配额，目标 ≈0 |

---

## 五、明确不做（防过度优化）

这一节与优化项同等重要 —— **本流程已有"为省时间反而变慢"的先例风险**：

| 不做的事 | 理由 |
|---|---|
| **为省 0.4 s 解释器启动去改成"常驻服务/单进程复用"** | 收益 0.4 s/次，代价是引入常驻进程生命周期、状态污染、僵尸进程等一整类新失败模式。**用短寿命幂等进程 + 批量调用（P2）解决同一问题，成本低一个数量级** |
| **为省 0.2 s 去手改 `plan.yaml` 而不调 `mark`** | 直接破坏「留痕不靠记忆」。**省调用次数可以，省留痕不行** |
| **继续在 `status --light` 这类微优化上投入** | 实测只省 11.7 ms（4.3%）。**当前规模下不可测量** —— 该项的收益随任务数增长，等 `tasks/` 涨到 50+ 再回头看 |
| **把 `references/` 全量预读进主上下文** | 46,080 tok，直接违反 Token 纪律。P4/P5 的目标正是**减少"不得不预读"的压力** |
| **给 P1 的修复加"清空整个 `__pycache__` 目录树"的更彻底实现** | 更彻底 = 更危险（递归删），而目标只是"别产生"。**扩大删除面与优化目标相反** |

---

## 六、落地顺序建议

依赖关系决定了顺序**不该按收益大小排**：

| 批次 | 内容 | 为什么在这一批 |
|---|---|---|
| **0** | 先给 `checks.py skill` 与 `mark` 建立**耗时基线脚本**（把本文的测量固化成可重跑脚本） | **没有基线就无法证明"变快了"**。本流程自己在阶段 3 写明性能类验收须"耗时下降且输出逐字节一致" —— 复用这条自己的标准 |
| **1** | **P1 + P2** | 都是零风险、可独立验证、且都改 `checks.py` 相关路径；P1 有"输出逐字节一致"的现成判据 |
| **2** | **P3 + P8** | 都改 `http_fetch.py`（同文件同批）；P3 必须配「缓存命中不发网络」的断言 |
| **3** | **P4 + P5 + P6** | 都改"文档与索引结构"，会动 `SKILL.md` 与 `references/`，需**一起改一次 `checks.py skill` 全绿**（引用完整性会同时被影响）；P6 需先决定"建索引还是改条文" |
| **4** | **P9-a…e** | 变更治理与卫生，纯文档 + 一处校验；改完须跑全量自检 |
| **5** | **P7 缓存清理** | **必须在最后**，且**必须单独取得用户确认**（不可逆删除） |

**每批的收尾判据（统一）**：
1. 本文 §六 批次 0 的基线脚本重跑，给出 before/after 原始输出；
2. `checks.py skill` 全绿（FAIL=0）；
3. 拿 **2–3 个真实历史 plan.yaml** 重跑 `checks.py plan`，确认**没有误伤历史任务**（真实数据的写法多样性远高于任何夹具）;
4. 性能类改动额外满足：**输出与旧版逐字节一致**（不是"看起来一样"）。

---

## 七、诚实边界

1. **本文未改动技能本体。** 上述改动均落在技能自身目录与运行环境内（属"技能生成所需的越界"），但**改动技能本体是独立任务**，按红线②须先落 `plan.yaml` 走阶段 0–6，本文只能作为**阶段 2 的候选方案之一**。
2. **token 数字全部是估算**，用 `汉字数 + 非汉字/4` 粗估，与实际 tokenizer 有偏差。用于比较量级与占比是可靠的，**不可当作计费依据**。
3. **LLM 推理耗时不可测**，本文对"每步耗时"的结论只覆盖可测的确定性部分。不给出每步的推理秒数，是因为给了就是编的。
4. **P1 的 43.9× 放大是环境产物**：本机 managed Python 被注入 `PYTHONPATH=…\vendor\shim`，该 shim 把非临时目录的删除改走回收站。**在用户自己的终端里，同样的 `rmtree` 只要 ≈19 ms**。所以 P1 的实际收益依"谁在跑"而不同 —— 在 agent 环境里收益大，在裸终端里收益小。**但"不产生就不用删"这个改法在任何环境下都只有好处、没有坏处。**
5. **P8 的秒数未实测**（未构造触发限流的真实场景），故只给方向不给数字。
6. **P7 是唯一涉及删除用户数据的项**，本文只出计划，不越权执行。

---

## 附录 A：测量脚本与原始输出

| 文件 | 内容 |
|---|---|
| `.workbuddy/tmp/perf_probe.py` → `perf_out.txt` | 体量与 token 粗估、references 索引核对、门禁脚本耗时、缓存构成 |
| `.workbuddy/tmp/perf_probe2.py` → `perf_out2.txt` | 各子命令耗时、cProfile 分解、阶段 0 扫描成本、缓存/备份体量 |
| `.workbuddy/tmp/perf_probe3.py` → `perf_out3.txt` | SKILL.md / ops.md 章节 token 占比、rmtree 与 gh 子进程微基准 |
| `.workbuddy/tmp/rm.txt` | **受控实验**：同一 `rmtree` 换父目录 + 复现 `checks.py` 真实场景 + 建议方案对比 |
| `.workbuddy/tmp/batch.txt` | `mark` 单条 vs `--batch` 实测 |
| `.workbuddy/tmp/idx.txt` / `idx2.txt` | 索引文件存在性、脚本引用关系、缓存子目录体量 |
| `.workbuddy/tmp/misc.txt` | 限流常量、`模型档位` 全部出现位置、抓取耗时 |

## 附录 B：与 2026-09-11 报告的关系（避免重复劳动）

| 上轮编号 | 上轮状态 | 本轮实测复核 |
|---|---|---|
| N1–N3（`data_query.py` 防 OOM / 去冗余） | 已落地 | 未复核（不在本轮范围，且已由当时的功能用例验证） |
| N4（流式落盘） | 已落地 | 未复核 |
| **N5（`_resolve_token` 进程内记忆）** | 已落地，**范围明确为"进程内"** | **本轮补上跨进程那一半**：实测每次调用仍付 788.8 ms → P3 |
| **N6（`status --light`）** | 已落地 | **本轮量化其收益：只省 11.7 ms（4.3%）** → 列入"明确不做" |
| **N7（`mark --batch`）** | **已落地但未被文档引导使用** | **本轮实测 8 步省 3.05 s（−88%）** → P2 |
| N8（`setup_env` 跳过 pip） | 已落地 | 未复核 |
| N9（列宽抽样） | 已落地 | 未复核 |
| K（阶段路由） | 已落地（v3.3.0） | **但其账本 `usage_ledger.jsonl` 从未生成** → P9-d |

> **一句话关系**：上一轮解决的是"**代码里有没有冗余**"，本轮解决的是"**调用与内容有没有按最省的方式走**"。
> 两者不重叠 —— 上一轮的修复让每个操作更便宜，本轮的修复让**更少的操作与更少的内容**发生。
