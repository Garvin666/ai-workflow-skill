# 工作流执行流程瓶颈分析与优化方案

> 分析日期：2026-09-11 ｜ 分析对象：本工作区 `ai-workflow` 执行流程本身
> **变更注（2026-09-11 晚，v2.5.3）**：本文原把「独立审核」列为不可破红线。经用户决定，它已按本文**优化4** 落地为**抽查制**（默认自查 + 机器门禁；仅高危产出／用户点名／返修触顶三类必送审）。下文 B3／优化4／§六 已同步改口径，特此标注，避免与技能正文冲突。
> **变更注（2026-09-11 晚，v2.6.0）**：B3 抽查制已于 v2.5.3 落地。本轮（用户明示"按此前确定的计划完成实现"）落地 **优化3 阶段感知模型路由**（SKILL.md 阶段3 新增路由决策清单：规划→强模型、关键实现/安全→强模型+双审、常规实现→中档、探索/调研/测试/文档→便宜模型；依据路由报告行业实测降本 70–90%）、**优化2 措施3 跨任务 GitHub 限流计数器**（http_fetch.py `_rl_throttle`，状态落 `~/.workbuddy/cache/ai-workflow/ratelimit.json`）、**优化5 措施2 references 按需加载自检项**（阶段5 交付前自检表新增）。经 grep 核实：优化1（scandir 9.6×，v2.4.2）与优化2 措施1/2（HTTP 正文缓存、链接探活缓存）**代码已落在跨任务持久层**，不再重写（复用优先）。优化4（抽查制）已落地。**至此原 B1–B5 对应优化1–5 中，仅 优化5 措施1（SKILL.md 持续精简）为常态化维护项、本轮未做大改；最大未释放瓶颈 B4 现已消除。**
> 数据来源（均为本工作区真实记录，非推测）：
> - `tasks/技能增强-ai-workflow-v2.4.2.../基准-公平对比.txt` + `改动对照表-v2.4.2.md`（工具级实测 9.6×）
> - `tasks/技术调研-工作流检索能力-2026-09-10/改造方案-检索能力增强.md`（检索缺口）
> - `tasks/_archive/2026-09/调研-AI效率-2026-09-10/findings-1~4`（方法论/工具/编排/质量，含 RCT 级证据）
> - `调研-模型路由与工作流编排-2026-09-10.md`（阶段感知路由成本数据）
> - 10 个 `tasks/*/plan.yaml` 阶段结构与交付物清单

---

## 一、当前执行流程全景

```
触发 ──► 阶段0 分型(L0/L1/L2)·查已有skill/模板
   │         ├─ L0 轻操作：直接做（豁免全流程）
   │         └─ L1 模板命中：走快速通道
   ▼
阶段1 六要素澄清 · 输出任务确认表  ──[红线①确认前不执行：此处人机往返等待]──► 用户确认
   ▼
阶段2 调研查重(GitHub/网络) · ≥2 候选方案评审
   ▼
阶段3 落盘 plan.yaml · 精检索(Grep/ast-grep/DuckDB) · 子代理并行 · 验证闭环前置
   ▼
阶段4-5 执行 → [审核：默认自查 + 机器门禁；高危/点名/返修触顶三类必送审（v2.5.3 抽查制）] → 交付自检 → 用户验收
   ▼
阶段6 归档 Ledger.md · 复盘沉淀
```

**关键事实**：流程本身有 3 条红线（确认前不执行 / 计划先行 / 工作区边界），其中前两条是**质量门禁**，会主动引入「等待」与「重复劳动」，这是分析瓶颈时必须区分的——它们不是浪费，但是「税」，要衡量是否花在刀刃上。

---

## 二、瓶颈定位（按严重度 + 证据强度排序）

| # | 瓶颈环节 | 现象 | 根因 | 证据强度 |
|---|---|---|---|---|
| **B1** | 支撑工具的目录遍历 | 对大目录全量扫描时每次白等 ~20s | `os.walk` 只给文件名，每个文件再发一次 `stat` 系统调用；12 万文件=12 万次多余调用 | **已实测 9.6×**（v2.4.2 基准） |
| **B2** | 检索能力缺口 → 验证税 | 调研类任务反复网络往返；子代理产出需逐条探活兜底 | 工具不全（GitHub 只取不搜、1.4GB 数据集搜不动、链接需手写探活）；子代理会编假链接 | **已识别+部分已补**（改造方案.md：140 条探活查出 3 条 404，含 1 条编造假链） |
| **B3** | 流程级验证税与串行化 | 每个 L2 任务有人机确认等待 + 关键交付物需另起独立审核（双倍人力） | 红线①②强制（确认等待），且审查者易「为找问题而找问题」导致过度工程。**独立审核的双倍人力已于 2026-09-11 按本文件「优化4」落地为抽查制，此税大部分已消除** | **RCT 级**（METR -19%、DORA 验证税、Anthropic 官方警告） |
| **B4** | 阶段未做成本分级路由 | 所有阶段用同一（强）模型，探索/测试/文档也烧旗舰成本 | 缺阶段感知路由：规划→旗舰、实现→中档、测试/文档→最便宜 | **行业实测**（阶段路由降本 70–90%） |
| **B5** | 大上下文 / 全量加载 | SKILL.md + references 体积大，若每次全量塞入主上下文 | 违反「按需加载勿预读」时，按 ETH Zurich 实测会 +20% 成本且成功率不升 | **基准实测**（ETH Zurich：LLM 生成上下文文件 -3% 成功率 +20% 成本） |

### 逐条说明

**B1 — 遍历开销（已修一处，但模式仍普遍）**
v2.4.2 把 `data_query.py files/big` 从 `os.walk+getsize` 换成 `os.scandir+DirEntry.stat`，在 121,146 文件 / 11.16GB 目录上从 **22.3s → 2.3s（9.6×）**，且输出逐字节一致。根因是「列目录时系统已带回文件大小，却对每个文件再发一次 stat」。这一模式（N 次冗余系统调用 / N 次冗余网络往返）在流程其他环节同样存在。

**B2 — 检索缺口与假链接兜底（验证税的来源之一）**
改造方案.md 显示：GitHub 端点原本「只能 fetch 单个仓库，不能 search」；1.4GB 结构化数据集无法在本机直查；链接需手写 `grep+http_fetch --check-links` 探活。子代理在本次实测中产出 **140 条链接，探活查出 3 条 404，其中 1 条是编造的日期型假链**（`magicliang.github.io/2026/04/17/...` 站点根 200、路径 404）。这迫使每次调研都必须全量探活兜底——本身就是税。P0（`--github-search`）、P1a（DuckDB）、P3（探活 SOP）已补，但**跨任务缓存**仍未做（见 B5 关联）。

**B3 — 验证税与并行串行化（质量门禁的代价）**
- METR 2025 RCT（16 名资深开发者 / 246 真实 issue）：用 AI 反而**慢 19%**，且事前预测快 24%、事后仍觉得快 20%——感知与实测落差 39pp。
- DORA 2025：**「写代码省下的时间，常被重新分配到审计与验证上」**；AI 把「待审代码速率」提高，审查端认知负荷同步上升。
- Amdahl 视角（findings-3 引用）：若写码只占交付总时长 30%，写码快 50% 只带来约 15% 整体提速——**瓶颈已从「写」迁移到「审 + 集成 + 验证」**。
- 本流程原「独立审核」强制门禁正确但昂贵：执行者≠审核者，关键交付物要另起干净上下文审查；Anthropic 官方警告「被要求找问题的审查者总能报出问题」→ 易过度工程。**已于 v2.5.3 落地为抽查制**（默认自查 + 机器门禁，仅高危/点名/触顶三类送审），保留了该环节在高压情形下的价值。

**B4 — 阶段未路由（最高 ROI 的未做项）**
调研-模型路由报告 §3.1.4：阶段感知路由（规划→旗舰、实现→中档、测试/文档→最便宜）在编码场景**成本降 70–90%**；「Plan-and-Execute」模式（强模型定策略、便宜模型执行）可降 90%。findings-3 §3.2：探索用便宜模型（Haiku 比 Opus 便宜约 15×）跑只读调研。**当前流程对所有阶段用同一模型，这块收益完全没拿到。**

**B5 — 大上下文 / 全量加载**
ETH Zurich（arXiv:2602.11988，AGENTbench 138 issue / 4 agent）实测：LLM 自动生成的上下文文件**平均 -3% 成功率、+20%+ 成本**；仓库概览类内容「无用」。本机虽有「按需加载勿预读」规则，但 SKILL.md 自身 + references 若被全量注入，就实付这笔税。

---

## 三、优化方案（目标 / 措施 / 预期提升）

### 优化 1：把 B1 的「去冗余调用」原则推广到全工具链
- **目标**：消除一切 O(N) 冗余 stat / 网络往返。
- **措施**：
  1. 审计 `http_fetch.py`/`data_query.py` 所有「先列后取」路径，确认无逐条二次调用；
  2. 新增大目录扫描统一走 `os.scandir`（v2.4.2 已验证可复用）；
  3. 网络侧：批量取值已用并发（实测 10 仓库 2.5s），保持其「一次往返取多字段」模式，禁逐条 fetch。
- **预期提升**：同类扫描/取值操作 **5–10×**（v2.4.2 实测 9.6× 为下界参考）。

### 优化 2：跨任务持久缓存，消灭重复检索
- **目标**：不同任务间复用已取仓库指标 / 已探活链接 / 已搜结果。
- **措施**：
  1. `http_fetch.py` 本地缓存 TTL 从「单次任务 3600s」升级为**跨任务持久层**（按 `owner/repo` 或 URL 哈希落盘，pushed_at 变化才刷新）；
  2. 链接探活结果落 `.workbuddy/cache/ai-workflow/`，同链接跨任务不重复探；
  3. 复用 GitHub 限流计数器跨任务，避免多任务互相打爆 10/min 匿名限流（findings-1 中 dask/airflow 曾因限流未取到数据）。
- **预期提升**：调研类任务重复 fetch 次数 **降 30–50%**（估计，取决于任务重叠度）；GitHub 限流触发率趋零。

### 优化 3：阶段感知模型路由（最高 ROI）
- **目标**：在不损质量前提下把推理成本压到最低。
- **措施**：
  1. 探索 / 调研 / 测试 / 文档阶段 → 便宜模型（本地 Ollama + DeepSeek/Qwen，或 Haiku 级）；
  2. 规划（六要素澄清、方案评审）→ 强模型；
  3. 关键实现 / 安全相关代码 → 强模型 + 人工双审（红线不可破）；
  4. 在 `SKILL.md` 阶段 3 写入「阶段→模型」决策清单（参考路由报告 §3.1.4）。
- **预期提升**：探索段成本 **~15× 降**（findings-3 Haiku 对比）；整体编码场景推理成本 **降 70–90%**（路由报告行业实测）；质量不损（探索本就可容忍便宜模型）。

### 优化 4：把「验证税」推给机器，压缩人工审查
- **目标**：保留质量门禁，但把审查成本从人挪到机器 + 缩小审查范围。
- **措施**：
  1. 强化 `checks.py`（已存在）做交付前自动对账：plan 状态 / 交付物存在 / 链接探活，fail 才阻断；
  2. **独立审核改为抽查制**（v2.5.3 已落地）：默认**不派**审核子代理，改由主代理自查 + 机器门禁（`checks.py plan` 对账/熔断门禁、`--check-links` 探活、`office_io` 读回、py_compile/断言）兜底；仅三类必送审——① 高危产出（安全/资金/权限/不可逆动作）② 用户点名 ③ 返修触顶。**送审时**仍要求审查者在全新上下文只看 diff + 验收标准，且**只报影响正确性的问题**（Anthropic 官方要求，避免为找问题而过度工程）；
  3. L0 轻操作继续豁免确认；L1 模板命中走快速通道——把确认门只留给真正的 L2 大任务。
- **预期提升**：人工审查耗时 **降 30–50%**（范围收窄 + 机器前置校验）；过度工程返工 **降**（审查者只报正确性）。

### 优化 5：上下文精简化，严守按需加载
- **目标**：主上下文只装「最小高信号 token」，按 ETH Zurich 反证砍掉无效内容。
- **措施**：
  1. SKILL.md 维持精简（删「能从代码/运行推导」的内容，只留不可推断的约束与禁区）；
  2. references/ 严格**按需加载**（本机已有规则，列入每次自检项）；
  3. 领域知识（如各项目约定）移入 Skills / 独立文档，用一行链接指向，不进每次对话；
  4. 仓库概览类 AGENTS.md 内容不写（ETH Zurich：无用且 +20% 成本）。
- **预期提升**：主上下文 token **降 30–50%** → 推理成本对应降；成功率不因精简下降（高信号密度反而可能升）。

---

## 四、落地优先级路线图

| 优先级 | 动作 | 依赖 | 风险 | 是否本工作区可直接做 |
|---|---|---|---|---|
| **P0** | 优化3 阶段路由决策清单写入 SKILL.md | 无 | 低 | ✅ 直接改 `ai-workflow/SKILL.md` |
| **P0** | 优化1 工具链去冗余审计 + scandir 固化 | 无 | 低 | ✅ 改 `data_query.py`/`http_fetch.py` |
| **P1** | 优化2 跨任务持久缓存 | 改 `http_fetch.py` | 中（缓存失效策略） | ✅ |
| **P1** | 优化4 强化 checks.py + 双会话审查 SOP | 无 | 低 | ✅ 改 `references/ops.md` |
| **P2** | 优化5 SKILL.md 精简 + references 按需加载自检 | 无 | 低 | ✅ |

> 说明：B1 的 9.6× 已交付；B2 的 P0/P1a/P3 已补。本轮**净新增价值最高的是优化3（阶段路由）与优化2（跨任务缓存）**——两者当前均为空白，且分别有行业实测 70–90% 与工程常识支撑。

---

## 五、度量：怎么证明优化生效（避免「感觉快了」幻觉）

METR 教训：自我感知可偏差 39pp。建议在本工作区加最小度量：

1. **工具层**：保留 `_bench_walk.py` 式基准脚本，每次改遍历逻辑后跑「优化前/后」对比（已验证有效）。
2. **流程层**：对重复出现的几类任务（修 bug / 写调研 / 改 skill）记录「触发到交付」实际分钟数，连续 2–4 周；优化 3/4/5 上线前后各留一段对照。
3. **成本层**：若接 Langfuse 自托管（findings-4 阶段三方案），按阶段打 token/成本标签，验证「阶段路由」是否真的把探索段成本压下来。
4. **质量层**：独立审核发现的「正确性缺陷数」不升反降，才算优化成功（不是只看速度）。

---

## 六、一句话结论

当前流程最大的**已量化**瓶颈是支撑工具冗余调用（9.6×，已修一处）与检索缺口导致的验证税；最大的**未做**瓶颈是**阶段未做模型路由**（行业实测可降本 70–90%）与**跨任务无缓存**。红线①（确认前不执行）不可破；「独立审核」已于 **2026-09-11 按优化4 落地为抽查制**（默认自查 + 机器门禁，仅高危／点名／返修触顶三类必送审）——**减税但不撤防**，因为该环节是唯一抓到过「自查抓不到」那类缺陷的机制（假 PASS 通道、虚假自述、描述与实现不符均系其发现）。建议按 P0→P1→P2 落地，**优化3 与优化2 是投入产出比最高的两步**。
