# ai-workflow 改造对照表（v1 → v2.0）

> 改造日期：2026-09-10 ｜ 依据：《调研-AI工作效率与质量提升-2026-09-10》+ 本次实测踩坑
> 备份位置：`~/.workbuddy/skills/ai-workflow/_backup-v1/`（SKILL.v1.md + scripts + assets + references 全量原版）
> 验证状态：py_compile 通过 ✓ / 脚本功能实测通过 ✓ / 模板 schema 一致性通过 ✓ / 内部引用完整性通过 ✓

## 一、修复的缺陷（4 项，均有实测证据）

| # | 缺陷 | 证据 | 处理 |
| --- | --- | --- | --- |
| 1 | `references/playbook.md` 是 **0 字节空文件**，但 SKILL.md 阶段 3 要求"查 playbook.md" → 引用断路 | `du` 显示 0KB | 补全 4.4KB 内容（量化/选品/小说/学习资料四类差异化流程） |
| 2 | 模板 schema **三套并存互不兼容**（README 用 `deliverables`、调研报告.yaml 用 `deliverable` 且缺 inputs/research、_示例 又是另一种） | 4 个文件字段对比 | 统一为 v2 schema（8 字段），README 写成强制规范；3 个模板全部重写并通过字段检查（8/8 OK） |
| 3 | `plan-template.yaml` 用英文键（action/expected），实际执行时主代理用中文键 → 模板与实际不符；缺 `交付物` 强制字段，Anti-drop 只能手工对账 | 今日 plan.yaml 已偏离 | 改为中文键 + `交付物` 标为必填 + 新增 `meta.验证信号` 字段 |
| 4 | `http_fetch.py` 把 **403 一律当致命错误不重试**，而 GitHub 匿名限流返回的正是 403 | 今日实测 403 导致主代理复核中断 | 识别 403+限流特征 → 按 Retry-After/X-RateLimit-Reset 等待重试；上限 2 次、单次 ≤60s（可用 `AIWF_GITHUB_MAX_RETRY` / `AIWF_GITHUB_MAX_WAIT` 调）；耗尽时给出"设置 GITHUB_TOKEN"的可执行建议（实测通过） |

## 二、注入的调研硬证据（4 项）

| 变更 | 依据 | 效果预期 |
| --- | --- | --- |
| 阶段 3 新增「**验证闭环前置检查**」硬门禁：无 pass/fail 信号先补最小验证物，否则登记人审点；交付须**出示证据而非声称成功** | Anthropic 官方第一实践 + DORA 2025"验证税" + METR 机制一致性 | 把"看起来完成"变成可判定，避免返工与静默降级 |
| Token 纪律新增「**上下文文件极简**」：30-60 行、只写推断不出的内容、**禁止提交 `/init` 产物** | ETH Zurich arXiv:2602.11988【基准实测】：LLM 生成文件 -3% 成功率 / +20% 成本 | 直接砍掉一个被证明是负收益的常见动作 |
| 阶段 2 新增「**证据分级标注**」（RCT/基准实测/大样本自评/官方推荐/实践观点/反例）+ 禁令（厂商案例、营销倍数、二手 star 数不得作证据） | 今日调研发现二手转述 star 数与实测差达数倍（superpowers 被传"近 10 万"、实测 28.4 万） | 杜绝以营销数字为决策依据 |
| 质量把关新增「**评审防偏差**」：位置交换双跑、异家族裁判、长度/格式不计分、pin 版本、容差带 | arXiv 2604.23178【基准实测】：风格偏差 0.10-0.76 远超位置偏差 | 防止评审分数被排版而非质量左右 |

## 三、结构调整（分层 + 闭环）

| 变更 | 说明 |
| --- | --- |
| **新增任务分层 L0/L1/L2**（置于全文最前） | L0 轻操作豁免（纯问答/单文件读取/改一行）直接做，不走流程；L1 模板命中走快速通道；L2 全流程。解决"改一行字也走六阶段"的摩擦 |
| 红线显式化 | ①用户确认前不执行 ②计划先行——明确标注为"不可豁免"，并说明 L0/L1 的裁剪边界 |
| **新增阶段 6 复盘与沉淀** | 复盘四问 → 分流沉淀（流程→skill／配置→模板／坑→反模式清单／事实→memory），形成知识飞轮 |
| 细节下沉 references | 新建 `quality-gates.md`（证据分级/验证闭环/CI 门禁/评审六偏差/17 条反模式）、`orchestration.md`（Handoff 模板/并行串行判定/上限≤4/回收纪律/失败升级路径）；SKILL.md 只留硬规则，标注"按需加载，勿全文预读" |
| 新增「详细手册索引」表 + 变更日志 | 便于按需加载与版本追溯 |
| §子代理编排补入 | ①Handoff 增加 `evidence_rule` 字段 ②**有依赖必须串行** ③并行上限 ≤4（理由：审查端先饱和 + 易触发 API 限流）④只读研究用轻量模型 |

## 四、脚本增强（向后兼容，未改既有参数语义）

| 脚本 | 新增 | 实测结果 |
| --- | --- | --- |
| `http_fetch.py` | `--github-repo owner/repo[,owner2/repo2]` 结构化取值（TSV/`--json`）；`--text` HTML→纯文本（零依赖正则实现）；403 限流识别与退避；失败时错误透出到 stderr + `error` 列；可选 `GITHUB_TOKEN` | HTML→文本 ✓；slug 格式守卫 ✓；限流快速放弃路径 ✓；持续限流给出可执行建议 ✓ |
| `ai_call.py` | `--model` 覆盖、`--system-file`、`--max-tokens`、`--temperature`、`--stats`（token 用量 + 缓存命中 + 按 `AI_PRICE_IN/OUT` 估算成本）；docstring 写入缓存前缀纪律 | 语法/参数解析 ✓（未实调 API，避免消耗额度） |
| `office_io.py` / `run_stage.ps1` / `setup_env.ps1` | 未改动（检查后确认无缺陷） | py_compile ✓ |

## 五、验收对账（对照任务确认表）

| 验收项 | 结果 |
| --- | --- |
| 每个改动可追溯到调研证据或实测缺陷 | ✓ 见本表一、二节 |
| 改后 skill 内部引用全部有效（无断路） | ✓ 引用完整性检查仅 2 处误报（业务相对路径 `plan.yaml`、工作区路径 `memory/YYYY-MM-DD.md`） |
| 模板 schema 统一 | ✓ 3 个模板 × 8 字段全 OK |
| py_compile / 脚本功能检查通过 | ✓ 见第四节 |
| 对照表逐条可审 | ✓ 本文件 |
| 保留六阶段主干与两条红线 | ✓ |
| 脚本参数向后兼容 | ✓ 仅新增可选参数 |
| 未删除任何现有能力 | ✓ 原版完整备份于 `_backup-v1/` |

## 六、遗留事项

1. `ai_call.py` 的 `--stats` 未做真实 API 调用验证（避免消耗额度），首次实际使用后建议核对 usage 字段名（不同厂商字段可能为 `prompt_tokens`/`input_tokens`）。
2. 反模式清单（quality-gates.md 第五节）需在后续任务中持续追加——阶段 6 复盘时执行。
3. 若 GitHub 需要高频实测，建议配置 `GITHUB_TOKEN` 环境变量（匿名 60 次/小时，今日已被限流两次）。
