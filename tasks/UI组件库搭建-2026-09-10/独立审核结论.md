# 独立审核结论 — @garvin/ui（2026-09-11）

工作目录 `E:\ChatGPT\ui库`。以下每条均为亲手执行所得，未采信任何自述。

## 1 命令级验证
- `npx tsc --noEmit` → 退出码 **0**，零报错。
- `rm -rf dist && npm run build` → 退出码 **0**。dist 顶层：core.js/core.cjs、three.js/three.cjs、ui.css、usePrefersReducedMotion-*.js/.cjs（+map）、types/；**dist/types 已生成（17 个 .d.ts）**。
- 依赖隔离：`grep -o -i three dist/core.js | wc -l` = **0**（对照 `dist/three.js` = 9）。core.js 仅 3 条 import：自身分块 + react + react/jsx-runtime，无 three / R3F / drei。**双入口隔离成立**。
- `npm run build:demo` → 退出码 **0**，600 modules transformed。

## 2 组件核对（源码证据）
- Button 满足：4 variant × 3 size（index.tsx:6-7）、disabled/loading 合并禁用（:41）。
- Input 满足：受控/非受控（:57-61）、prefix/suffix（:103,138）、clearable（:90,118）、disabled（:113）、invalid（:97,115）。
- Card 满足：三区插槽（:63-72）、bordered（:56）、hoverable（:57）。
- CSS 3D 降级 满足：FlipCard 在 reduce 下**不覆盖 transform**（FlipCard.css:91-96），靠 base.css:61-70 把过渡压到 0.001ms → 瞬时翻转，**不会把背面永久压在正面**；TiltCard 关倾斜与高光（TiltCard.css:57-65）；Cube 停动画留静态姿态（Cube.css:96）；Carousel3D 去过渡。四者内容均可见、无错位。
- ModelViewer 满足：加载进度（:147,149,212）、失败兜底（ErrorBoundary :38-58 + 错误层 :221）、自动旋转开关（:144,178,231）三者真实存在。
- Chart3D 满足：hover 高亮 emissiveIntensity（:89）+ 数值 tooltip（:108-121）。

## 3 资源与令牌
- **sample-cube.gltf 是合法 glTF 2.0**：buffer 声明 648 = 实测 648；bufferView 偏移 0/288/576 均 4 字节对齐且在界内；accessor 步长/边界全匹配；POSITION 实算包围盒 = 声明 min/max = ±0.72；索引 0-23 无越界；法线长度恒 1.0，12/12 面法向与顶点法向同向。
- 令牌链路：Button/Input/Card/css3d 的 CSS 无 `#hex`/`rgb(` 字面量；但 **Button.css:132-139 直接消费原始色板 `--ui-gray-950`**（layer-1 非语义），而暗色下 `--ui-color-text-inverse` 正是该值 → 多余硬编码。

## 4 反例（按严重度）
1. **组件总数不符**：实测组件目录 **12 个**（3+4+5），README.md:10 与交付说明却称"共 13 个组件"。→ 期望改 12 或补齐第 13 个。
2. **样式未随入口拆分**：dist/ui.css 实测含 `.ui-flipcard`/`.ui-cube`/`.ui-canvas3d` 等 three 层类名，消费方 `import '@garvin/ui/styles.css'` 会连带 3D 层样式，与 README:3"3D 入口按需引入"不一致。定位 vite.config.ts:26、package.json exports。
3. **组件消费 layer-1 令牌**：Button.css:133,138 使用 `--ui-gray-950`。→ 改用 `--ui-color-text-inverse`。
4. Carousel3D 非激活项标了 `aria-hidden`（index.tsx:110）但仍可聚焦其内部交互元素。→ 补 `inert`。

## 5 exports 逐一对账（无问题）
7 条引用路径实测全部存在：dist/types/index.d.ts、core.js、core.cjs、dist/types/three/index.d.ts、three.js、three.cjs、ui.css —— 全 OK，无 MISS。

## 判定：**通过**
核心功能与双入口隔离实测成立；4 条问题均为文档/一致性/规范级，不阻断交付，建议优先修第 1、3 条。

## 返修复查（第二轮）
复核者：独立第三方（未参与开发与上轮审核）；结论仅基于本轮实测。

**问题1（13 个组件）→ 已清零。** 实测组件数=12（`ls -d src/{components,css3d,three}/*/ | wc -l`=12）。README.md:10/16、examples/App.tsx:75/452、vite.demo.config.ts:6 均为「12」。grep 全仓 `13 个组件` 仅命中 `.workbuddy/memory/2026-09-10.md:10,29` 两处——属工作记忆日志，非交付文档。

**问题2（CSS 按需）→ 已清零（如实说明，非含糊）。** README.md:128 明言「按需拆分的只有 JS 运行时依赖，不含 CSS……多入口样式合并成单个 dist/ui.css」，并给出 `cssCodeSplit` 可选方案。vite.config.ts 确未配 `cssCodeSplit`（默认 false=合并）；实测 `dist/ui.css` 31.95 kB 含 carousel/cube/flip/tilt 选择器。描述与配置、产物三者一致，未夸大。

**问题3（硬编码色板）→ 已清零。** `Button.css` 与 `src/css3d/*/*.css` 中 `#hex`/`rgb()`/`hsl()`/`--ui-gray-*`/`--ui-blue-*` 等原始色板令牌命中数 **0**（遍历 grep 无输出）。Button.css:129-132 danger 仅用 `--ui-color-danger*`+`--ui-color-text-inverse`。可读性推理：亮色 danger=`--ui-red-600`（深红）+ text-inverse=白 → 可读；暗色 danger=`--ui-red-400`（浅红）+ text-inverse=`--ui-gray-950`（近黑）→ 可读。两种主题均 OK。

**问题4（Carousel3D 焦点）→ 已清零。** index.tsx:112 改用 `inert={!isActive}`。`inert` 语义：使元素及其子树退出可聚焦序列、不可点击、并从可访问性树中移除，恰好覆盖「aria-hidden 管不住内部按钮被 Tab」的缺陷，比 aria-hidden 更适合。激活项 `isActive=true` → `inert=false`，未被 inert，仍可交互。（注：isActive 唯一真值，不会误伤。）

**回归验证：** `npx tsc --noEmit` 退出码 **0**；`npm run build` 退出码 **0**（33 模块）；`grep -o -i three dist/core.js | wc -l` = **0**。未引入新问题。

**判定：全部清零。**
