# 改动对照表 — ai-workflow v2.5.3（独立审核 → 抽查制）

> 任务：把「独立审核」由**强制门禁**降级为**抽查制**（用户 2026-09-11 选定）
> 工作区根：`C:\Users\26717\.workbuddy\skills\ai-workflow`（基础设施例外第 1 条；改技能本体属独立任务）
> 附加落点（本会话工作区，显式声明）：`E:\ChatGPT\工作流` —— 仅同步口径 3 处，不改业务文件
> 新口径一句话：**默认主代理自查 + 机器门禁；仅「高危产出 / 用户点名 / 返修触顶」三类必送独立审核。**
> 「执行者不得自审」**未取消**，它只在命中三类情形时生效。

---

## 一、新口径（改后应逐条可读到）

| # | 规则 | 落点 |
| --- | --- | --- |
| 1 | 默认路径 = 主代理自查 + 机器门禁（`checks.py plan` 对账与熔断门禁、`checks.py skill`、`--check-links` 探活、`office_io` 读回、py_compile/断言） | `SKILL.md` 阶段 3「独立审核（抽查制：默认自查，三类必送）」 |
| 2 | **必送三类**：① 高危产出（安全/资金/权限/凭据/不可逆动作）② 用户点名复核 ③ 同一反模式第 2 次复发或返修触顶（转对抗式互审） | 同上 |
| 3 | 送审时未放松：执行者≠审核者；只拿产出物 + 验收标准；须亲手复现硬证据；**只报影响正确性的问题**；批次 ≤5 文件不得减覆盖面 | 同上 |
| 4 | 留痕两处：`plan.yaml` 验证方式 + `Ledger.md`「独立审核」列（`通过` / `抽查豁免（<理由>）`） | 同上 |
| 5 | 交付门禁改为「审核状态已闭合（通过 / 抽查豁免）」+ 熔断状态正常 | `SKILL.md` 阶段 5 第 3、4 条 |
| 6 | 返修复查分两路：送审过的由原审核者回归；未送审的由主代理逐条回归**并出示硬证据** | `SKILL.md` 返修复查第 2 条 |
| 7 | 熔断 F2 取证放宽为「三次可回溯留痕（审核 / 自查 / 用户指出）」 | `SKILL.md` 熔断机制 F2 行 |

---

## 二、逐处改动（改前原文 → 改后）

| # | 文件 | 改前原文（关键片段） | 改后 | 理由 |
| --- | --- | --- | --- | --- |
| 1 | `SKILL.md` frontmatter | `→ 独立审核（执行者不自审）与返修复查 →` | `→ 独立审核**抽查制**（默认自查 + 机器门禁；高危产出／用户点名／返修触顶三类必送审，送审时执行者不自审）与返修复查 →` | 技能描述是触发路由依据，必须与正文一致 |
| 2 | `SKILL.md` 头 | `version: 2.5.2` / `# AI 工作流 v2.5.2` | `version: 2.5.3` / `# AI 工作流 v2.5.3` | 语义级口径变更，须可追溯 |
| 3 | `SKILL.md` 划界注 | `下方「独立审核」是**不可豁免的门禁**` | `下方「独立审核」是**抽查制的送审规则**——默认不派，命中三类必须派` | 消歧 |
| 4 | `SKILL.md` 阶段 3 整节 | 标题「独立审核（执行者≠审核者，强制门禁）」+ 第 1 条四类关键产出判定 + 第 6 条豁免 + 第 7 条优先级（共 8 条） | 标题「独立审核（抽查制：默认自查，三类必送）」+ 默认路径 + 必送三类 + 送审规则 7 条 | **本次核心改动**；原「关键产出判定/豁免/优先级」三件套被「必送三类」取代（更简单、不再需要豁免判定） |
| 5 | `SKILL.md` 返修复查第 2 条 | `由原审核者（或新的独立审核者）针对问题清单**逐条回归**` | 送审过的由原审核者回归；未送审的由主代理逐条回归**并出示硬证据**（禁口令式结论） | 未送审就没审核者，须补上等价强度的复查主体，不留空档 |
| 6 | `SKILL.md` 阶段 5 第 3 条 | `**独立审核已通过 + 未熔断**……（红线：**未通过或已熔断，均不得交付**）` | `**审核状态已闭合 + 未熔断**……未命中的须留痕 `抽查豁免（<理由>）`（红线：**未闭合或已熔断，均不得交付**）` | 门禁语义随口径变；红线强度不变 |
| 7 | `SKILL.md` 阶段 5 第 4 条自检表 | `**独立审核通过**` | `**审核状态已闭合（通过 / 抽查豁免）**` | 同上 |
| 8 | `SKILL.md` 熔断 F2 行 | `第 3 次被**独立审核**指出` / 证据 `三次指出的审核记录` | `第 3 次被指出` / 证据 `三次可回溯留痕（独立审核意见 / 主代理自查记录 / 用户指出均可）` | 抽查制下多数复发出现在自查链，原证据要求会使 F2 **永远无法触发**（本次改动引入的连带缺陷，已修） |
| 9 | `references/quality-gates.md` 反模式 37 | 「执行者自审充当"独立审核"」（旧口径：一律不得自审） | 「命中送审情形却用自审顶替」（明确三类必送，未命中须留痕抽查豁免） | 反模式须与主干一致，否则执行者按旧文照做 |
| 10 | `references/quality-gates.md` 反模式 38 | `必须由独立审核者**针对原问题清单逐条回归**` | 加「送审过的由原审核者做；未送审的由主代理做并出示硬证据」 | 同上 |
| 11 | `references/ops.md` 变更日志 | （无 v2.5.3 条目） | 新增 v2.5.3 条目：口径变更 / 未放松部分 / 连带调整 / 决策依据 / **反证（原样保留）** / 验证 | 变更日志是技能演进的唯一台账 |
| 12 | `assets/ledger-template.md` 字段说明 | `独立审核 \| 通过 / 豁免（理由） \| 阶段 3 独立审核结论` | `通过 / 抽查豁免（理由）` + 说明改为「命中必送三类时为送审结论；未命中写抽查豁免」 | 台账字段语义更新 |
| 13 | `assets/ledger-template.md` 备注区 | 仅 v2.5.0 一条 | 追加 v2.5.3 一条（说明为何列名仍叫「独立审核」） | 避免后续读者误判历史行 |
| 14 | `assets/templates/UI组件库搭建.yaml` flow | `→ 独立审核 → 交付台账` | `→ 单独送审或抽查豁免 → 交付台账` | 模板 flow 串与主干一致 |
| 15 | `E:\ChatGPT\工作流\工作流瓶颈分析与优化方案-2026-09-11.md` | 顶部「分析日期」块 | 加**变更注**：原「独立审核＝不可破红线」已作废，按本文优化4 落地抽查制 | 该文件与技能正文会互相打脸，必须显式标注 |
| 16 | 同上 §一 流程图 | `[红线②独立审核：执行者≠审核者，另起审查]` | `[审核：默认自查 + 机器门禁；高危/点名/返修触顶三类必送审（v2.5.3 抽查制）]` | 顺带修正此处把独立审核称作「红线②」的**误标**（红线②是「计划先行」） |
| 17 | 同上 §B3 表 + 详解 | `关键交付物需另起独立审核（双倍人力）` / `本流程的「独立审核」红线正确但昂贵` | 补「已按优化4 落地为抽查制，此税大部分已消除」/「已于 v2.5.3 落地为抽查制」 | 瓶颈结论需反映现状 |
| 18 | 同上 优化4 措施 2 | `独立审核改用「双会话 writer/reviewer」` | `独立审核改为抽查制（v2.5.3 已落地）` + 保留「送审时」的收窄要求 | 该措施已实施，不该还写作待办 |
| 19 | 同上 §六 结论 | `质量红线（确认前不执行 / 独立审核）不能破` | `红线①不可破；独立审核已落地为抽查制——减税但不撤防` | 结论必须与事实一致 |
| 20 | `E:\ChatGPT\工作流\工作流实现代码性能分析-2026-09-11.md` | `须走 ai-workflow 流程：… → 独立审核 → 验收 → 归档` | `… → 审核（v2.5.3 起为抽查制…） → 验收 → 归档` | 同 19 |
| 21 | `E:\ChatGPT\工作流\tools\stage_router.py` 注释 | `对应工作流「独立审核」红线` | `对应工作流「高危产出必送独立审核」这一类情形（v2.5.3 起为抽查制）` | 代码注释与技能口径一致；CRITICAL 档位策略本身**不变** |
| 22a | `scripts/checks.py:272` | `if args.light:` | `if light:` | **验证阶段撞出的真缺陷①**：`args` 是 `main()` 的局部变量（`:550`），`cmd_status` 内访问必抛 `NameError`，被 `:283` 宽 except 吞成「解析失败(NameError)」 |
| 22b | `scripts/checks.py:279` | `if any(a["缺失"] for a in done):` | `if any(a["缺失"] for a, s in zip(audited, steps) if str(s.get("状态","")).strip() == "完成"):` | **真缺陷②**：`done` 是步骤 dict 列表，却取审计结果字典的键 → 恒 `KeyError`。两处均为 N6「轻量模式」改动引入，合起来使 `status` 的**完成度与交付物两列长期全废** |

> 附带修正（同一处顺手改口径，非独立条目）：`E:\ChatGPT\工作流\工作流瓶颈分析与优化方案-2026-09-11.md` §五 度量第 4 条仍以「独立审核发现的正确性缺陷数」作质量指标——抽查制下该指标的分母会变小，**读该指标时须同时看「自查/机器门禁发现数」**，否则会误读为质量上升。

---

## 三、刻意保留（不删，防止把教训一起删掉）

| 保留项 | 内容 | 理由 |
| --- | --- | --- |
| 反证记录 | `ops.md` v2.5.3 条目里的「反证（刻意保留备查，不删）」 | 该环节是**唯一**抓到过「自查抓不到」类缺陷的机制：`checks.py` `base.parent` 假 PASS 通道（**两轮常规审核未发现**，由对抗互审挖出）、`体验报告` 4 处实质错误、`Ledger.md:15` 虚假自述、「描述与实现不符」3 次复发。删了口径，不删证据 |
| 历史变更日志 | `ops.md` v2.5.0/v2.5.1/v2.5.2 条目中「独立审核…强制」的表述 | 变更日志**只追加不删改** |
| `Ledger.md` 历史行 | 表头列名仍为「独立审核」 | 台账列名变更会破坏历史可读性；改的是**值域** |
| `_backup-v1/_backup-v2/_backup-v2.3` | 旧版快照 | 版本对比依据 |

---

## 四、验证（原始输出见同目录 `验证输出.txt`，按时间顺序 20 段）

| # | 验证项 | 结果 |
| --- | --- | --- |
| 1 | `checks.py skill` | **19/19 通过 FAIL=0**（首跑曾 FAIL=1：ops.md 以反引号写了**工作区**文件 `工作流瓶颈分析与优化方案-2026-09-11.md`，被「文档引用完整性」判为技能内缺失 → 改为反引号内绝对路径。**这是机器门禁抓到的真实问题**） |
| 2 | 全部 `scripts/*.py` py_compile | 通过 |
| 3 | 旧口径关键词扫描（主干 + references + assets，排除 tasks/_backup 历史） | 无**非预期**残留；命中项均为 ① 描述本次变更的正文 ② 变更日志的历史条目（历史**只追加不删改**） |
| 4 | 抽查制新口径存在性 | 19 行命中，覆盖 SKILL.md（:3/:101/:103-:113/:123/:145-:146/:182）、ops.md、quality-gates.md:107、ledger-template.md:22,30、UI组件库搭建.yaml:45 |
| 5 | `checks.py plan` 本任务对账 | **7/7 FAIL=0**（含计划外的 step7） |
| 6 | `status` 缺陷修复回归 | 修复前：12/12 任务全报「解析失败(NameError)」；修①后：仍全报「解析失败(KeyError)」；修②后：**「解析失败」残留 = 0**，两个工作区均显示真实计数 |
| 7 | `status` ⚠ 分支覆盖 | 技能工作区 0 ⚠（已完成步骤交付物齐全）；`E:\ChatGPT\工作流` 侧 5 个任务正确带 ⚠（历史「相对技能目录」写法的已知 20 条，见 ops.md v2.5.2 条目）——**说明被修的判缺失逻辑真的被执行到了** |
| 8 | `E:\ChatGPT\工作流\tools\stage_router.py` py_compile | 通过 |

> ⚠ **诚实标注两处过程失误**：① 首轮 `checks.py skill` FAIL=1 是我自己写坏的引用，属真问题、非误报；② 我在排查 `status` 时先修了 `NameError`，误以为已闭合，**实际同一段里还有 `KeyError`** —— 是回归输出里继续显示「解析失败」才暴露的。教训：**「修好一处就以为收工」在同一个函数里同样会漏**，必须以「修复后的原始输出」判定，而非以「我改了那行」判定。
