# P1/P2 建模落地 · 完成报告

> 任务：`tasks/P1P2建模落地-2026-09-21/plan.yaml`（L2）
> 承接：上一轮「工作流建模分析-2026-09-21」的 P1（决策模型+策略模型）/ P2（流程模型+路由模型）四项
> 用户授权：原话「请基于现有的工作进度，继续完成 P1 与 P2 的剩余内容，保持与已有部分的逻辑衔接和风格一致，并输出 P1 和 P2 完整、可直接使用的最终版本。」（上一轮已授权「直接开始执行，无需再确认」）
> 状态：**已完成**，门禁全绿，已归档 Ledger.md

## 一、交付物（5 文件，全部落技能根）

| 文件 | 角色 | 大小 |
| --- | --- | --- |
| `references/decision-tables.md` | P1-1 决策模型：五张决策表集中映射 | 12,588 B |
| `references/policy.md` | P1-2 策略模型：gate.py 范围×意图 + S1–S4 显式策略集（default-deny + fail-closed） | 8,299 B |
| `references/process-model.md` | P2-1 流程模型：六阶段活动图 + R/F/T 决策节点表 | 6,642 B |
| `references/routing-table.md` | P2-2 路由模型：阶段×档位映射 + category 联动 | 5,250 B |
| `SKILL.md`（编辑） | 索引 +4 行 + 三处散文转引用指针 | — |

配套留痕：`plan.yaml`、`trace.jsonl`（6 条，每条 `note` 声明「收尾补记」）、`_metrics.jsonl`、`tmp/` 门禁输出与 `verify_p1p2.py` 交叉核对脚本。

## 二、验证证据（门禁终态，全部 rc=0）

| 门禁 | 命令 | 结果 |
| --- | --- | --- |
| ① 取值交叉核对 | `verify_p1p2.py` | **全 PASS**：R1–R6 / T1–T3 / 四类能力 / 终止策略 / M1–M5 与源手册逐项相等；新文档零本机绝对路径 |
| ② 技能自检 | `checks.py skill` | **43/43 FAIL=0 SKIP=0 WARN=0** |
| ③ 计划对账 | `checks.py plan` | **FAIL=0**（修复前曾 FAIL=5，因交付物误用相对路径，已改绝对技能根路径） |
| ④ 口径守卫 | `guard_constants.py` | **7 组、漂移 0 组** |
| ⑤ 守卫阴性对照 | `guard_constants.py --selftest` | **rc=0** |
| ⑥ 出站扫描 | `outbound_scan.py`（5 文件） | **FAIL=0，WARN=1**（WARN 为 `SKILL.md:99` 既有内部路径，非本任务新增） |

证据落盘：`tasks/P1P2建模落地-2026-09-21/tmp/g_verify.txt`、`g_skill.txt`、`g_plan.txt`、`g_guard.txt`、`g_guard_self.txt`、`g_outbound.txt`。

## 三、与计划偏差（如实登记）

1. **plan.yaml 交付物路径修复（本会话续作发现）**：上一轮把步骤 5（`SKILL.md`）的交付物改为绝对路径，但步骤 1–4 仍误留相对路径 `references/xxx.md`，导致 `checks.py plan` 判 `FAIL=5`。本会话已将四行统一改为绝对技能根路径（`C:\Users\<用户名>\.workbuddy\skills\ai-workflow\references\xxx.md`），复跑后 **FAIL=0**。此为对上一轮收尾遗漏的订正，不影响交付物本身。
2. **门禁子命令名纠正**：`guard_constants` / `outbound_scan` 是**独立自研脚本**，不是 `checks.py` 子命令（其合法子命令仅 skill/plan/status/mark/decide/revise/selftool/trace/metrics）。上一轮 summary 误记为 `checks.py` 子命令，本会话已按正确调用方式复跑。
3. **trace.jsonl / _metrics.jsonl 补记**：上一轮 5 步已完成但未留 trace，触发 `checks.py plan` 的「步骤闭环不变量」WARN。本会话按 checks 提示补录 `trace.jsonl`（6 条，显式声明「收尾补记」）并运行 `checks.py metrics --finalize` 生成 `_metrics.jsonl`，WARN 消除。

## 四、诚实边界（两项不得读成绿）

1. **决策表只做集中映射，不重述枚举取值、不新增守卫解析器**：P0 已实测「枚举散落」假设部分不准确——取值早已 `checks.py` 常量单源 + `data-model.md` 受守卫镜像。故 P1 沿用 P0-2 D2「零新正则」原则，决策表引用取值而非重述；取值单一源仍由 `checks.py skill` 口径守卫保证。
2. **流程模型为纯文档/可视化，未新增门禁**：六阶段活动图与 R/F/T 节点表描述「可机判定的流转」；人审节点标为人工网关（见 process-model.md §四），不假装能自动执行、不新增门禁。

## 五、变更清单（SKILL.md 4 处）

1. 手册与脚本索引：`state-model.md` 行后新增 `decision-tables` / `policy` / `process-model` / `routing-table` 四行（与 P0 的 data/state 行同格式）。
2. 自主决策层（L52 附近）：句末加「四类决策的集中决策表见 `references/decision-tables.md`（P1-1）」。
3. 阶段感知模型路由（L183 前）：加「档位映射与 self-judge category 联动表见 `references/routing-table.md`（P2-2）」。
4. 信息安全（L373 前）：加「访问控制（gate.py）与 S1–S4 的显式策略集（default-deny + fail-closed 方向）见 `references/policy.md`（P1-2）」。

## 六、出站扫描结果

- 推送面（4 份新文档 + `SKILL.md`）扫描：**FAIL=0**。
- WARN=1 为 `SKILL.md:99` 既有内部路径描述（工作区边界纪律的示例文字），非 P1/P2 新增内容，且属出站清单第 3 项（内部目录结构）仅提示不阻断。
- 全部文档相对路径引用，无本机绝对路径泄露（`verify_p1p2.py` 第②项实测全 OK）。

## 七、越界与不可逆动作

- **越界授权**：写技能根属「技能生成例外」（USER.md 第 2 条例外），已登记 `plan.yaml` 的 `meta.越界授权`；改动落 `references/`（新增 4）+ `SKILL.md`（索引与引用），未触既有文件删除。
- **推送两仓：未执行**（不可逆对外动作，须用户单独确认并附内容清单，与 P0 同口径）。

## 八、下一步（待用户确认）

1. 是否推送 `ai-workflow-skill` 与 `ai-workflow-tools` 两仓（含本次 P1/P2 四项新增 + SKILL.md 改动）？推送前将摆出内容清单并等授权。
2. 如需，可同步更新 `changelog.md`（v4.4.0 → 标注 P1/P2 四项已落地）。
