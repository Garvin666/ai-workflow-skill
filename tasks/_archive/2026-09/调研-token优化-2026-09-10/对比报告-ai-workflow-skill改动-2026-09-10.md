# 对比报告：ai-workflow skill 改动前 vs 改动后

> 报告日期：2026-09-10 ｜ 改动对象：`C:\Users\26717\.workbuddy\skills\ai-workflow\SKILL.md` ｜ 改动依据：《调研-token优化与对话精简-2026-09-10》

---

## 一、改动摘要

| 维度 | 改动前 | 改动后 |
| --- | --- | --- |
| 文件总行数 | 105 行 | 110 行（+5 行净增） |
| 阶段 3 条目数 | 4 条 | 5 条 |
| Token 相关规则 | 无 | 新增「Token 纪律」1 条（含 4 个子项） |
| ai_call.py 条目 | 第 4 条，无附加约束 | 顺延为第 5 条，新增「调用侧同步遵守 Token 纪律」 |
| 其余区块 | — | **零改动**（前置检查、阶段 0/1/2、子智能体编排、质量把关、阶段 4/5、脚本速查、故障排查均未动） |
| frontmatter | name / description / agent_created | 未触碰 |
| Markdown 标题数 | 12 处 | 12 处（结构完整） |

## 二、逐区块对比

| 区块 | 改前 | 改后 | 状态 |
| --- | --- | --- | --- |
| frontmatter（1-5 行） | 3 个元字段 | 相同 | ✅ 无变化 |
| 前置检查 | venv/依赖/AI Key 三项检查表 | 相同 | ✅ 无变化 |
| 阶段 0：查 Skill（4 条） | 含快速通道 | 相同 | ✅ 无变化 |
| 阶段 1：任务理解（4 条） | 含模板沉淀 | 相同 | ✅ 无变化 |
| 阶段 2：调研（4 条） | 含并行检索硬规则、安全许可检查 | 相同 | ✅ 无变化 |
| 阶段 3：AI 处理 | **4 条**（目录制/逐步执行/精确检索/ai_call） | **5 条**（+Token 纪律；ai_call 顺延并附加约束） | 🔄 **唯一改动区** |
| 子智能体编排 | Handoff 5 字段 | 相同 | ✅ 无变化 |
| 质量把关 | 信任但验证 + 对抗式互审 | 相同 | ✅ 无变化 |
| 阶段 4：Office 产出（3 条） | utf-8-sig / 中文字体 | 相同 | ✅ 无变化 |
| 阶段 5：自检交付（4 条） | Anti-drop guard | 相同 | ✅ 无变化 |
| 脚本速查 / 故障排查 | 5 脚本表 + 4 条排查 | 相同 | ✅ 无变化 |

## 三、新增内容明细（阶段 3 第 4 条「Token 纪律」）

| 子项 | 规则内容 | 调研依据 |
| --- | --- | --- |
| **缓存优先** | 稳定前缀在前、动态在后；DeepSeek 保持前缀一致吃磁盘 KV 缓存（命中 98% 折扣）；Anthropic 稳定块加 `cache_control`；多轮任务复用同一系统提示词，禁止每次改写前缀 | 供应商原生 prompt caching：agent 场景实测省 78.5%（Claude） |
| **上下文分层** | 热（近 10 轮原文）/温（滚动摘要）/冷（目标级摘要）三层；每层压缩 40-60%，**禁止压 90%+** | Zylos 基准省 26-54%；Factory.ai 生产教训：压过头重取反升成本 |
| **按需加载** | 只取索引/摘要，命中再读片段；断点续做先读 plan.yaml，不重跑已完成步骤 | awesome-llm-token-optimization 检索纪律 |
| **选型黑名单** | RouteLLM（2024-08 停更）、GPTCache（2025-07 停更）、Memobase（2026-01 后停更）不引入为依赖；LiteLLM 必须 ≥1.83.7 | GitHub API 实测 + bestaiweb CVE 记录（在野 SQL 注入/供应链投毒） |

## 四、验证记录

| 验证项 | 方法 | 结果 |
| --- | --- | --- |
| 改动范围最小化 | `diff -u` 改前备份 vs 改后文件（16 行 patch） | ✅ 仅阶段 3 一处 hunk，其余零差异 |
| 结构完整性 | 对比标题数量（`grep -c '^#'`） | ✅ 改前 12 = 改后 12 |
| frontmatter 完好 | diff 范围不含 1-5 行 | ✅ 未触碰 |
| 端到端加载 | Skill 系统实际重载 ai-workflow | ✅ 加载成功，Token 纪律正常注入 |

## 五、影响分析

- **正向**：后续所有走 ai-workflow 的任务自动带上省 token 约束——调用 DeepSeek/Anthropic 时按缓存友好结构组织 prompt；长任务上下文按三层管理；停更依赖被硬性排除。
- **代价/风险**：规则为硬性指令（"必须/禁止"措辞），极端场景（如必须改动前缀的任务）可能多一轮确认；40-60% 压缩上限是保守值，纯摘要类任务可手动放宽。
- **回滚方案**：改前完整备份在 `tasks/调研-token优化-2026-09-10/ai-workflow-SKILL-改前备份.md`，整文件覆盖即回滚。

## 六、文件清单

| 文件 | 用途 |
| --- | --- |
| `C:\Users\26717\.workbuddy\skills\ai-workflow\SKILL.md` | 改后 skill（生效中） |
| `tasks/调研-token优化-2026-09-10/ai-workflow-SKILL-改前备份.md` | 改前完整备份 |
| `tasks/调研-token优化-2026-09-10/对比-diff.patch` | 统一 diff（16 行） |
| `tasks/调研-token优化-2026-09-10/调研-token优化与对话精简-2026-09-10.md` | 规则来源调研报告 |
