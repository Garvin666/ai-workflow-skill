# 独立审核意见

> 审核者：未参与评估的独立第三方 ｜ 审核日期：2026-09-10 ｜ 方法：有罪推定 + 亲手复现
> 审核对象：`体验报告.md`、`可优化方向.md` ｜ 只读，未修改任何待审文件

## 1. 审核结论

**不通过。** 引证真实性与"无编造"两项合格（抽查 15 处全部相符），但**验收标准 4（降级有据）与标准 6（无明显遗漏）双双不满足**：① 对 `excel-write` 的降级用了**与原始指控不同的一条命令**做复现，原指控实际可逐字复现；② 至少 4 条降级未在第四节披露；③ 漏掉 `checks.py` 跨技能目录兜底造成**假 PASS** 的真实缺陷，导致报告 §三 亮点 3 与 `可优化方向.md` 的"唯一真实代码缺陷"结论失真。

---

## 2. 引证抽查（15 处，全部亲手打开核对）

| # | 报告引证 | 实际内容 | 判定 |
| --- | --- | --- | --- |
| 1 | `SKILL.md:58` 红线① | "未确认前禁止执行任何脚本或产出文件（L0/L1 除外）" | 相符 |
| 2 | `SKILL.md:53` 阶段 0.5 | "工作区状态一瞥：`checks.py status` 看已有任务与交付物完成度" | 相符 |
| 3 | `SKILL.md:85` 五类信号 | 代码/Office/调研/改造/性能五类，**确无"数据分析"** | 相符 |
| 4 | `SKILL.md:64` 六要素 | ①目标②输入③交付物④验收标准⑤边界约束⑥**工作区根** | 相符 |
| 5 | `SKILL.md:77` 方案评审 | "≥2 个候选方案…可行性/成本/风险/可回滚性/与验收标准的匹配度" | 相符 |
| 6 | `SKILL.md:106④` | 关键产出判定④"任何对外交付物（给对方看的文件、发布内容、上线动作）" | 相符 |
| 7 | `references/quality-gates.md:27` | `| 数据分析 | 交叉校验脚本（总额对账、行数一致、空值率）、抽样复算 |` | 相符 |
| 8 | `checks.py:369 / :228 / :372` | `--base`=Path.cwd() / `tasks_root = workspace / "tasks"` / `--workspace`=Path.cwd() | 相符 |
| 9 | `setup_env.ps1:8 / :41 / :39` | 7 个 deps / `$pkgDir` 仅 5 项映射 / 注释"host-proof" | 相符 |
| 10 | `run_stage.ps1:24-25` | `& $venvPython $target @ScriptArgs` + `exit $LASTEXITCODE` | 相符 |
| 11 | `ops.md:22` plan 示例带 `--base` | `checks.py plan tasks/x/plan.yaml --base "E:/ChatGPT/工作流"` | 相符 |
| 12 | `report-template.md:9-13` | 目标/输入/交付物/验收标准/边界约束（**无工作区根**） | 相符 |
| 13 | `templates/README.md:32` | "示例文件 `_示例-数据分析.yaml` 保留作字段说明" | 相符 |
| 14 | `probe_pkgdir.txt:2/3/5/6`、`:28` | `joined=[…site-packages\]` / `Test-Path=True` / `native child output=[]` | 相符 |
| 15 | `SKILL.md:100`、`:74`、`:98` | 质量把关≠独立审核的划界 / 证据分级指向 / 质量把关指向 | 相符 |

**结论：无一处引证被编造，行号与原文全部对得上。** §2 的"无编造"与"可回溯"两项成立。

---

## 3. 硬证据链独立复现（setup_env 假绿）

`setup_env.ps1:41` 的 `$pkgDir` 只映射 5 项，而 `:8` 的 `$deps` 有 7 项。我按脚本 :43-51 的逻辑**用 PowerShell 工具重写一遍**（Bash 调 PowerShell 被安全策略拒绝，这正是报告 N4 所述现象），输出落盘后读取：

```
dep=requests     mapped=[requests] joined=[…site-packages\requests]  Test-Path=True
dep=openpyxl     mapped=[openpyxl] joined=[…site-packages\openpyxl]  Test-Path=True
dep=python-docx  mapped=[docx]     joined=[…site-packages\docx]      Test-Path=True
dep=pypdf        mapped=[pypdf]    joined=[…site-packages\pypdf]     Test-Path=True
dep=pyyaml       mapped=[yaml]     joined=[…site-packages\yaml]      Test-Path=True
dep=duckdb       mapped=[]         joined=[…site-packages\]          Test-Path=True   ← 假绿
dep=ast-grep-cli mapped=[]         joined=[…site-packages\]          Test-Path=True   ← 假绿
PSVersion=5.1.26100.9444
```
（原始输出文件：`cache/ai-workflow/audit_probe_pkgdir.txt`）

**判定：报告 R5 成立，且引证精确。** `Join-Path $sitePkgs $null` 返回父目录本身 → `Test-Path` 恒为 True → `duckdb`/`ast-grep-cli` 从未被校验。唯一瑕疵：报告建议"补 `duckdb`→`duckdb`、`ast-grep-cli`→`ast_grep_cli*`"里，`ast-grep-cli` 在 site-packages 下**只有 `ast_grep_cli-0.45.3.dist-info`，没有 `ast_grep_cli` 包目录**（该包主要装 exe），脚本已自认"需按 dist-info 匹配"——建议方向正确但不可直接照抄。

**观察（环境事实，与报告 N4 一致）**：本次 PowerShell 工具调用的 stdout 被吞（只回 `Command completed with exit code 0`，无回显），必须落盘再读；Bash 工具调用 `powershell.exe` 被安全策略整条拒绝。报告 N4 描述属实。

---

## 4. 技能自检实跑结果

命令（指定 venv 解释器）：
```
"C:/Users/26717/.workbuddy/binaries/python/envs/ai-workflow/Scripts/python.exe" \
  "C:/Users/26717/.workbuddy/skills/ai-workflow/scripts/checks.py" skill
```
原始输出（尾部）：
```
[ OK ] 文档引用完整性 — 检查 23 条引用
[ OK ] 模板 schema：_示例-数据分析.yaml — 9/9
[ OK ] 模板 schema：学习资料生成.yaml — 9/9
[ OK ] 模板 schema：小说写作.yaml — 9/9
[ OK ] 模板 schema：技术调研.yaml — 9/9
[ OK ] 模板 schema：调研报告.yaml — 9/9
[ OK ] 模板 schema：选品分析.yaml — 9/9
[ OK ] 模板 schema：量化分析.yaml — 9/9
[ OK ] plan-template 必填键 — 齐全
[ OK ] py_compile：ai_call.py / checks.py / data_query.py / http_fetch.py / office_io.py
=== 结果：18/18 通过，FAIL=0 ===
EXIT=0
```
**与报告一致**（`ops.md:137` 记录的 18/18 FAIL=0 可复现）。报告未在正文引用该数字，但可优化方向 §六 的"回归口径：`checks.py skill` 全绿"可达成。

---

## 5. 问题清单（按严重度）

| # | 严重度 | 报告中的问题 | 证据（我的复现） | 应如何修正 |
| --- | --- | --- | --- | --- |
| H1 | **高** | **`excel-write` 的降级依据错误**：报告 §4 称"体验官 B 所称裸异常不成立，实测是干净的 argparse 错误（无 traceback）" | 复核：体验官 B 原命令是 `office_io.py excel-write out.xlsx --rows sales.csv`（CSV 当 `--rows` 值，见 `体验官B-可执行.md:63`）。我**逐字复现该命令 → `json.decoder.JSONDecodeError: Expecting value: line 1 column 1 (char 0)`，EXIT=1**（与 B 所述完全一致）。报告用的是另一条命令 `excel-write sales.csv`（CSV 当**输出文件**位置参数）→ argparse 错。**用另一条命令反驳原指控，复现不对口** | 撤回该降级；`excel-write` 至少维持"绕路"（要用两步 `excel-read→excel-write` 才能走通，且首次必撞 traceback） |
| H2 | **高** | **漏掉"跨技能目录兜底 = 假 PASS"的真实缺陷**，并连带使两处结论失真 | 我构造 `/tmp/audit2/plan.yaml`（`交付物: references/quality-gates.md`，该目录下**并无此文件**），从该目录跑 `checks.py plan`（不带 `--base`）→ **`[ OK ] Anti-drop 对账 — 交付物确认 1 个`、`7/7 通过 FAIL=0`、EXIT=0**。根因：`checks.py:164-165` 把 `~/.workbuddy/skills/*/` 全部加入候选路径 | ① 把该问题补入清单（体验官 B#5 已上报）；② 收窄兜底：仅在路径以 `references/`/`scripts/`/`assets/` 开头且落在本任务 `--base`/工作区根时才启用；③ 相应地**下调报告 §三 亮点 3** 与 `可优化方向.md` "R5 是唯一真实代码缺陷" 的措辞 |
| M1 | 中 | **综合评分 3.4/5 不可复算** | 表中 6 维（3.0/3.5/3.0/4.0/5.0/2.5）简单平均 = **3.5**，报告写 3.4 且未披露权重 | 给出权重表，或改正为 3.5；`可优化方向.md` 的"3.4→4.3"基线随之修正 |
| M2 | 中 | **降级披露不全**：§4 声称"把 3 条指控降级" | 逐条比对体验官原始级别：N1（A#6 绕路→不适）、N5（A#8 绕路→不适）、N6（A#7 绕路→不适）、N7（A#9 绕路→不适）**共 4 条额外降级未在 §4 披露** | 在 §4 补齐这 4 条的降级理由，或恢复其级别 |
| M3 | 中 | `可优化方向.md:58` 交叉引用错误：称三条被降级指控是"**R3-R5 的对应项**" | R3=数据分析缺信号、R4=审核粒度、R5=setup_env 假绿，**这三条都没被降级**；被降级的实为 N2/N3/N8 | 改为"（N2/N3/N8 的对应项）" |
| M4 | 中 | `可优化方向.md` 称新增的"反模式 40/41"属"**没被现有 39 条反模式覆盖**" | 现有清单确为 39 条（`quality-gates.md:65-109`，无编号冲突，此点正确）；但**第 19 条**（"自检/对账工具在依赖或输入缺失时仍输出'全绿'…退出码 0"）与拟增的"反模式 40（取不到就成真）"实质同源，且 `体验报告.md` R5 自己已写该 bug"撞了本技能自己的反模式清单" | 把"反模式 40"表述为**对 #19 的机理补强 + 强制负向测试**，而非"未覆盖的新类型" |
| L1 | 低 | "`SKILL.md` 161 行""`scripts/` 5 个共 1,887 行" | `SKILL.md` 内容 161 行但末尾无换行，`wc -l` 记 160；`scripts/` 实际 7 个文件（5 py + `run_stage.ps1` + `setup_env.ps1`），1,887 行只是 5 个 py 之和——而报告的两条核心问题（R5/N2）恰是 ps1 | 补一句口径（"5 个 .py 脚本"），ps1 单列 |
| L2 | 低 | N3 建议只改 `ops.md`，未触及真正误导源 | `SKILL.md:122` 明写 office_io"支持 CSV 读入"，正是 B 踩坑的依据（B#3 建议本就指向它） | N3 建议改为同时修正 `SKILL.md:122` 措辞 |
| L3 | 低 | `可优化方向.md` 的"3.4→约 4.3/5" | 标注"⚠️ 本预估为推断"确实存在（:82），但：① 一位小数的"4.3"是伪精度；② 警示是**脚注**，位于粗体结论之后；③ 基线 3.4 本身不可复算（见 M1） | 改区间/定性表述，并把警示与数字同处呈现 |

---

## 6. 定级审查

| 条目 | 报告级别 | 我的判断 |
| --- | --- | --- |
| R1/R2/R4/R5 | 绕路 | **无异议**（R5 虽为最危险缺陷，但依赖实际存在、不致中断，绕路恰当） |
| R3 | 绕路 | **无异议**（"要翻手册才不踩坑"，符合绕路定义） |
| N1 report-template 缺工作区根 | 不适 | **偏低**。该字段是红线③依据、`SKILL.md:64` 必填、`checks.py` 对账会 FAIL；体验官 A 原判即为**绕路**，报告降为不适且未在 §4 说明——应维持**绕路** |
| N2 run_stage | 不适 | **无异议**：降级理由（:18-19 是路径检查、`ops.md:77` 已预警）我可独立验证。保留 B 自陈的"真实终端待复核"不确定性即可 |
| N3 excel-write | 不适 | **偏低（且依据错误，见 H1）**。B 原命令可逐字复现 traceback + 需两步绕行 → 应维持**绕路** |
| N4 沙箱原生子进程 | 不适 | **可议**：属环境事实，不入技能缺陷更妥；报告已注明"非技能缺陷"，可接受 |
| N5 / N6 / N7 | 不适 | 级别本身**可接受**，但均由体验官 A 原判的**绕路**下调而来且**未披露**（见 M2），程序上不合规 |
| N8 `--base` | 不适 | **无异议**（降级理由 = A 自陈未实跑，可验证） |

---

## 7. 遗漏项（报告未覆盖但真实存在）

1. **`checks.py:164-165` 跨技能兜底 → 假 PASS**（已实测坐实，见 H2）。这是与 R5 同类的"验证工具自身不可信"缺陷，且它直接反驳报告 §三 亮点 3 的"程序区分说做了/真做了"。
2. **`checks.py status` 缺 `--workspace` 时只报裸 FAIL**（B#4）：`cmd_status` 默认 `Path.cwd()`，非工作区根执行仅输出 `[FAIL] tasks/ 目录存在 — <cwd>/tasks`，无"请加 --workspace"提示。与 N8 同类却未采纳。
3. **`checks.py:24` docstring 与 `:55` 代码不一致**（B#11）：docstring 写"meta 必填（任务 / 验证信号）"，实际 `PLAN_META_REQUIRED` 含"工作区根"。报告 N1 盯住了 report-template 的同类问题，却漏掉脚本自身。
4. **豁免条件与关键产出判定互相打架**（B#7）：`SKILL.md:106`（对外交付物必审）vs `:111`（纯格式转换可豁免），缺失适用优先级。
5. **`report-template.md` 的指针只挂在阶段 4**（A#5）：非 Office 的 md 报告会跳过阶段 4，从而看不到这份模板；N1 只补了字段、没补指针位置。
6. **元问题——报告静默丢弃了约 16 条子代理发现**：A(16)+B(13) 合并为 13 条，正文既未声明"以下未纳入"，也未在 §五 盲区交代；其中 B#5、B#12（office_io/ai_call 缺依赖抛裸 traceback）、A#15 等属实质问题。

---

## 8. 我不确定的地方（诚实声明）

1. **未读 `plan.yaml`**（按审核约定），因此 R4 的自证"本任务用 1 次送审覆盖 2 个交付物"、以及 `可优化方向.md` 自称"合并送审"，我**未独立核实**。
2. **`run_stage.ps1` 在真实交互终端的行为未测**：我只在本工具链下验证了"原生子进程不执行 + stdout 丢失"（与 N2/N4 一致）；交互终端是否正常，我无法区分——体验官 B 自己也留了同一不确定性。
3. **报告对体验官 B"把自己探针里的 `WRAPPER_LASTEXITCODE=0` 读成脚本断言"这一具体指责，我无法证实/证伪**（B 的探针文件未在我审阅范围内）；但 N2 的降级不依赖此点（`ops.md:77` 已足够）。
4. **体验官 A/B 的 5 维评分我只抽查了可对应项**（B 的脚本可用性 3/5、流程 4/5 对得上），未逐维复算其"加权总分 3.6/5"的算法。
5. PowerShell 工具吞 stdout 的稳健范围（是否所有调用都如此）我只观察到本次数例，未系统性穷举。
