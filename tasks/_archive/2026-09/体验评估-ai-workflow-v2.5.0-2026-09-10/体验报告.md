# 体验报告：ai-workflow v2.5.0（返修版 v3）

> 评估时间：2026-09-10 23:20-23:55 ｜ 被评对象：ai-workflow v2.5.0（技能目录快照）
> 入口类型：正式需求 ｜ 任务层级：L2 ｜ 工作区根：`C:\Users\26717\.workbuddy\skills\ai-workflow`
> 评估方法：**两遍法 + 双体验官并行 + 主代理核实 + 独立审核 + 返修复查**（四名子代理，执行者与审核者分离）
> 原始记录：`体验官A-冷启动.md`（16 条）／`体验官B-可执行.md`（13 条）／`独立审核意见.md`

> **v2 修订说明（返修复查产出）**：初稿经独立审核判**不通过**（9 条问题，2 高）。本版修正了 4 处实质错误——① **撤回**对 `excel-write` 的错误降级（我用不对口的命令反驳了原指控）；② **补入**被静默丢弃的 16 条子代理发现（初稿只合并 13/29 条）；③ 修正被打脸的第 3 条亮点；④ 修正综合分算法。逐条对照见 **第六节**。
> **v3 修订说明（第二轮回归产出）**：第二轮逐条回归发现 2 条未解决（综合分仍写错、N1 提升只改了说明未改表体），已修正；并把原 N1 正式提升为 §二 **R7**。第三轮回归判定**通过（有条件）**，剩余 3 条纯标注问题（版本号、计数、交叉引用）已在本版一并修正。完整两轮记录见 **第六节**。

---

## 〇、方法与可信度声明

| 角色 | 视角 | 方法 | 产出 |
| --- | --- | --- | --- |
| 体验官 A | 冷启动可读性 | 第一遍**只读 SKILL.md** 并模拟执行；第二遍才开手册，逐条判定「已解决／真实缺口」 | 16 条（0 阻断/9 绕路/7 不适） |
| 体验官 B | 端到端可执行性 | 读全量手册 + **实跑 29 个命令/探查步**（环境探测／--help 对账／CSV→xlsx 读回／plan 正反向测试／dry-run／探活／data_query） | 13 条（1 阻断/8 绕路/4 不适） |
| 主代理 | 汇总与核实 | 合并去重、**逐条抽查原始证据**、重定级别、补做 4 组独立复现实验 | 30 条（0/7/23） |
| 独立审核 | 报告本身 | 只拿两份交付物 + 验收标准，**抽查 15 处引证 + 独立复现硬证据 + 实跑自检** | 判不通过，9 条问题 → 本版已逐条返修 |

**被评对象规模**（评估起始快照）：`SKILL.md` 161 行；`references/` 5 份共 466 行；`scripts/` 5 个共 1,887 行；模板 **7** 类 + 3 份资产文件。

> ⚠️ **被评对象在评估期间被并发修改**（重要前提）：评估进行中（23:28-23:33），另一会话向技能目录写入了 `assets/templates/UI组件库搭建.yaml`（模板 7 → **8** 类）与 `tasks/UI组件库搭建-2026-09-10/`，使 `checks.py skill` 的自检项从 18 → **19** 项。
> **含义**：本报告是**某一时刻的快照评价，不是稳定基线**；数字（模板数、自检项数）会随并发写入变化。同时这也**提供了 R2 的活证据**（见 §二 R2）。所有引证在回归复核时抽查 10 处仍全部相符。

**可信度声明（必须先说）**：
1. **体验官 A 全程未跑脚本**（分工所致），其结论为"读文字"而非"看行为"，凡涉及脚本行为的描述均需 B 或主代理佐证。
2. 两名体验官**互不可见**，结论重合处可信度更高，冲突处（见第五节）已由主代理实测裁决。
3. **主代理本身也会错**：初稿中 1 条降级被独立审核证伪并撤回（见第五节）。**本版数字均为"经一轮对抗后的结果"，非一次成型**。
4. 本报告只评估 `toy` 规模任务，**未测多任务并发、长链路、真实业务体量**。

---

## 一、总体结论与评分

| 维度 | 体验官原评 | 主代理 v1 | **v2 定稿** | 依据 |
| --- | --- | --- | --- | --- |
| 冷启动可读性 | 2.6/5 | 3.0 | **3.0** | 16 条中 0 阻断；但"验证信号缺数据分析类"确属主干指引漏洞 |
| 文档准确度 | 4.0/5 | 3.5 | **3.5** | `report-template` 确认表少一个必填要素；`check-links` 均值差一个数量级；`checks.py` docstring 与代码不符 |
| 脚本可用性 | 3.0/5 | 3.0 | **3.0** | `setup_env.ps1` 假绿 + `checks.py` 跨技能兜底假 PASS，**两处真实代码弱点** |
| 流程可跑通度 | 4.0/5 | 4.0 | **4.0** | 核心链路 12/15 成功，失败项均有绕法 |
| 验证闭环可操作性 | 5.0/5 | 5.0 | **4.5** | 「先测后交」仍是最强项；但 R6 显示对账存在假 PASS 通道，**不能全信** |
| 独立审核可操作性 | 2.0/5 | 2.5 | **2.5** | 缺粒度/批次规则属实；且豁免条款与关键产出判定互相打架（N19） |
| **综合** | — | 3.4 | **3.4** | 见下注 |

> **综合分算法**：6 个维度**等权算术平均** (3.0+3.5+3.0+4.0+4.5+2.5)/6 = 20.5/6 = **3.42 ≈ 3.4**。这是**判断而非测量**——维度权重因人而异，请按你关心的维度看单列分，不要只看这个数。
>
> ⚠️ **这条数字改过两次，如实记录**：v1 表内均值 21/6 = 3.5 却写 3.4（偏低 0.1）；v2 把「验证闭环」从 5.0 调到 4.5 后均值变为 3.42，却写成 3.5（偏高 0.1）。**两次都是"数字与自己的表不符"，方向相反。** 这说明：**手算平均值也会错，且自查抓不到**——两次都是被独立审核逐条回归才发现的。最终值 **3.4**。

**一句话结论**：**"主干正确、细节欠收口"**——0 条阻断意味着照它干活不会卡死；但 7 条绕路集中在"两处主干自相矛盾 + 两处脚本假绿/假 PASS + 一处审核粒度缺失 + 一处文档-代码不符 + 一处模板缺字段"，都是能一次性修掉的收口问题。

---

## 二、绕路级问题（7 条，需要动手修）

| # | 问题 | 证据 | 主代理核对 | 建议 |
| --- | --- | --- | --- | --- |
| R1 | **红线①与阶段 0 自相矛盾**：红线①写"未确认前禁止执行**任何脚本**"，阶段 0 第 5 步却要求跑 `checks.py status`，而阶段 0 必然早于确认 | `SKILL.md:58` vs `:53`（另见 `:10`） | **成立**（已比对原文）。两条主干对撞，读者必须自行裁决优先级 | 红线①限定为"**处理类**脚本与产出文件"；显式豁免只读检查（`status`/`skill`/`Read`） |
| R2 | **`tasks/` 与工作区根关系未定义**：一处暗示 tasks/ 在工作区根内，例外表又允许技能目录写 tasks/，脚本基准是 `workspace/tasks` | `SKILL.md:27`、`:33`、`checks.py:228`、`checks.py:372` | **成立，且已被现场复现（活证据）**：评估进行中的 **2026-09-10 23:28**，另一会话执行 `tasks/UI组件库搭建-2026-09-10/` 任务——其 plan.yaml 明写 `工作区根: "E:\ChatGPT\ui库"`，但**任务目录、plan.yaml、方案评审.md 全部落在了技能目录 `tasks/` 下**，工作区根内反而什么都没有。三套口径并存，以致一个称职的执行者当场选错。**若按"影响交付物落点／是否触犯红线③"计，此条接近阻断级** | 明写"L2 任务目录 = **工作区根**/tasks/；技能目录 `tasks/` 仅记录技能自身演进（改技能类任务）"；例外表把"写 `tasks/` 记录"改为"**仅限技能自身演进任务**" |
| R3 | **数据分析类缺内联验证信号**：主干只列 代码/Office/调研/改造/性能 五类 | `SKILL.md:85`（五类）vs `references/quality-gates.md:27`（"数据分析｜交叉校验脚本、抽样复算"） | **成立**（已 grep 确认 :27 内容）。且 SKILL.md 指向 quality-gates 的两处（`:74` 证据分级 / `:98` 质量把关）**理由都不同**，冷启动不知道去那份手册找信号 | `:85` 补「数据分析」一行，注明"完整清单见 quality-gates.md" |
| R4 | **独立审核缺粒度/批次规则**：明确"任何对外交付物"必审，但无"能否合并送审"的规则 → N 个产出＝N 次派发 | `SKILL.md:106④`；自证：本任务 1 次送审覆盖 2 个交付物，而两份体验官记录（命中①）未设 | **成立**。最坏情形（1 产出返修 2 轮）可派发 5 次审核 | 补规则："同一任务的多个交付物可**合并为一次送审**（建议单批 ≤5 文件），前提是审核者拿到全部产出物 + 统一验收标准" |
| R5 | **`setup_env.ps1` 依赖自检假绿（真实代码弱点）**：`deps` 列 7 个包，`$pkgDir` 只映射 5 个 → `duckdb`、`ast-grep-cli` 取到空值 | `scripts/setup_env.ps1:8` vs `:41`；实验 `cache/ai-workflow/probe_pkgdir.txt` | **成立，硬证据**：`Join-Path` 遇空子路径**返回父目录本身**（`probe_pkgdir.txt:2/5`），"查子目录"退化成"查 site-packages"，`Test-Path` **恒为 True**（:3/:6）→ 两依赖**从未被校验**，缺失也报 `[ OK ]` 且退出 0。违背脚本自身注释（`:39` "host-proof"），也撞了本技能自己的反模式 **#19**（`quality-gates.md:89`） | 补全映射（`duckdb`→`duckdb`；`ast-grep-cli` 需按 `ast_grep_cli*.dist-info` 匹配）；**未映射项显式 FAIL** 而非静默通过 |
| R6 | **`checks.py` 跨技能目录兜底导致假 PASS（真实代码弱点）**：Anti-drop 对账的相对路径会在**任意技能目录**下再试一遍 | `checks.py:164-165`（`cands.extend(sd / p for sd in SKILLS_ROOT.glob("*/"))`） | **成立**：体验官 B 从 `/tmp` 不带 `--base` 仍得 `7/7 通过`，独立审核独立复现同一结果。**但需公道说明**：该兜底是**有意设计**（注释写明"skill 内相对路径逐个技能目录再试"），用于解析 `references/…`、`scripts/…` 这类技能内部交付物；问题在于**范围过宽**——它让"工作区里缺失的文件"也能判为存在 | 兜底**仅当路径显式以 `references/`／`scripts/`／`assets/` 开头时启用**，其余一律只在本任务工作区根与 `--base` 内解析 |

| R7 | **`report-template.md` 确认表缺「工作区根」**（原编号 N1，**由不适提升至此**）。该表是阶段 1 六要素的唯一成文载体，缺一个必填要素 → 使用者按模板填写就会**漏掉红线③的依据**，而 R2 已证明"漏掉工作区根"会导致交付物落错位置 | `assets/report-template.md:9-13`（仅目标/输入/交付物/验收标准/边界约束）；对照 `SKILL.md:64`（六要素含⑥工作区根）、`assets/templates/README.md:13`（workspace 必填） | **提升成立**：这不是观感问题，而是**结构性缺字段**，且与 R2 构成因果链 | 确认表补第 6 行「工作区根」 |

> ⚠️ **R5 与 R6 的定性**：初稿称"R5 是唯一真实代码缺陷"，**不成立**——R6 同样是代码级弱点（假 PASS 通道），且独立审核已复现。两处都应视为真缺陷。

---

## 三、不适级问题（23 条，文档收口为主）

| # | 问题 | 证据 | 建议 |
| --- | --- | --- | --- |
| N2 | **沙箱/宿主编排下原生子进程不执行**：`run_stage.ps1` 静默 no-op 却退出 0 | `run_stage.ps1:24-25`；实测 `& python -c "print(42)"` 输出为空（PS 5.1.26100.9444，`probe_pkgdir.txt:28`） | 排障表补：**首选 Bash 直调 venv 解释器**；ps1 包装器仅作后备；`run_stage.ps1` 加执行后校验 |
| N3 | `SKILL.md:122` 称 office_io "支持 CSV 读入"，实际 `excel-write` **只吃 JSON**，直接喂 CSV 抛裸异常 | `SKILL.md:122` + `office_io.py:109` + 实测 `excel-write out.xlsx --rows sales.csv` → `json.decoder.JSONDecodeError: Expecting value: line 1 column 1 (char 0)`，EXIT=1 | `SKILL.md:122` 改为"CSV 经 `excel-read --fmt json` 转 JSON 后再 `excel-write`"；或 `excel-write` 增 `--from-csv` |
| N4 | 文档推荐的 `powershell -File scripts\setup_env.ps1` **无法从 Bash 工具发出**，而本工具链 PowerShell 通道又不回显 stdout | `ops.md:22/:51`；实测 `Command blocked for security: Invoking PowerShell from Bash...` | `ops.md:76` 补：用 **PowerShell 工具**执行 ps1；其 stdout 需**自行落盘再读** |
| N5 | 阶段 2 强制 ≥2 候选方案，但对比维度（可行性/成本/风险/可回滚性/验收匹配度）**偏工程**，数据分析与文档类套不上 | `SKILL.md:46` + `:77` | 补"非工程类任务可换用 口径/样本/结论稳健性 等维度" |
| N6 | L1 快通道判定"命中 `assets/templates/` 同类模板"与示例文件命名打架：`_示例-数据分析.yaml` 是**字段说明文件**却能被当成命中项 | `SKILL.md:17` vs `assets/templates/README.md:32` | L1 判定加"排除 `_` 开头文件" |
| N7 | 阶段 0「列本地 skill」**无方法/命令** | `SKILL.md:50`；`checks.py` 子命令仅 `skill/plan/status/mark`（`checks.py:362-379`），`ops.md` 亦无 | 补 `Glob ~/.workbuddy/skills/*/SKILL.md` 读 frontmatter |
| N8 | `checks.py plan` 在 SKILL.md 中的示例**未带 `--base`**，从非工作区目录调用时相对路径可能解析错 | `SKILL.md:129` vs `ops.md:22`（带 `--base`） | SKILL.md 命令补 `--base <工作区根>` |
| N9 | **报告模板指针只在阶段 4 出现**，而非 Office 的 md 报告会跳过阶段 4，从而看不到模板 | `SKILL.md:122`（位于 `:120` 标题「Office 产出」下），而 `report-template.md` 本身是 `.md` | 阶段 3 交付物 / 阶段 5 处也指向 report-template |
| N10 | **L1 无法在"第一件事"处判定**：分层说是开工第一件事，但查模板在阶段 0 | `SKILL.md:12` vs `:52` | 分层表注明"L1 需待阶段 0 查模板后确认" |
| N11 | `find-skills`／`推荐市场` 在 SKILL.md / ops.md 内**无定义** | `SKILL.md:50` | 补一句调用方式或指向 |
| N12 | Token 纪律段术语密集且无定义（KV 缓存、ETH Zurich 实测、RouteLLM/GPTCache/Memobase、LiteLLM ≥1.83.7） | `SKILL.md:89` | 移到手册，主干留一句可执行规则 |
| N13 | 阶段 3.5 是**超长混排段落**（单条含约 10 个工具 + ⭐/⚠️ + 限定符） | `SKILL.md:88` | 拆成决策表 |
| N14 | 脚本调用在主干**无统一前缀**（`scripts\office_io.py` 反斜杠、`checks.py` 无前缀、`http_fetch` 无 `.py`） | `SKILL.md:122`／`:53,:129`／`:88`；完整调用法仅在 `ops.md:11-14` | 主干统一写 `<技能根>/scripts/xxx.py`，或统一指 ops.md |
| N15 | 阶段 4 标题「Office 产出」但要求"完整走阶段 0-6"，md 交付是否跳过阶段 4 **未明说** | `SKILL.md:46` vs `:120` | 阶段 4 加"非 Office 交付跳过本阶段" |
| N16 | 独立审核对"新建 md 报告"是强制门禁，但主干**未说明如何派子代理**（用哪个工具） | `SKILL.md:106`／`:111`／`:94` | 点明派发机制 |
| N17 | 阶段 0 标注"约 1 分钟"与实际动作量（列 skill/查模板/status/确认根/环境预检）不符 | `SKILL.md:39` | 改"快速检查" |
| N18 | `checks.py status` 默认工作区=cwd，在非工作区根跑**只报 FAIL 无参数提示** | `checks.py:229-230`；实测 `[FAIL] tasks/ 目录存在 — <cwd>/tasks` EXIT=1 | FAIL 文案补"若工作区非当前目录，请加 `--workspace <工作区根>`" |
| N19 | **豁免条款与关键产出判定互相打架**：纯格式转换产生的对外文件同时命中"必审④"与"可豁免" | `SKILL.md:106`（④）vs `:111`（"纯格式转换可豁免"） | `:111` 补优先级："豁免仅在**产出未命中 :106 四类**时适用" |
| N20 | SKILL.md 把"环境预检命令"指向 ops.md，但 ops.md 只有本机不可用的 `setup_env.ps1`，**无一条可照抄的 import 探测命令** | `SKILL.md:22` + `ops.md §四` 全节 | ops.md §四 增：`"$PY" -c "import requests,openpyxl,docx,pypdf,yaml,duckdb"` |
| N21 | `--check-links` 平均耗时被写成**定值**，实测差一个数量级 | `ops.md:107`（227ms/条）、`:180`（156ms/条）vs 实测 `[STATS] 平均 2573 ms/条` | 改区间并注明"取决于超时条数"，或只标并发数 |
| N22 | `checks.py` docstring 与代码不一致（meta 必填字段数） | `checks.py:24`（"任务 / 验证信号"）vs `:55`（含"工作区根"） | 同步 docstring |
| N23 | `office_io.py` / `ai_call.py` 缺依赖时**抛裸 traceback**，与 `checks.py` / `data_query.py` 的 `[ERROR]+[HINT]` 守卫风格不一致 | 实测裸解释器跑 office_io → `ModuleNotFoundError` + traceback；对照 `checks.py:173-183`、`data_query.py:55-62` | 加同样的 `_require()` 守卫（退出码 2 + venv 路径提示） |
| N24 | `data_query.py files` 的 `[提示]` 走 stderr，`2>&1` 下插到正文之前，观感错位 | 实测首行为 `[提示] 结构化(JSON/CSV/Parquet)优先用 sql…` | 提示移到最后或改走 stdout 尾部 |

> **编号说明**：原 **N1** 已提升为 §二 **R7**，故本表实为 **23 条**（N2–N24）。**N 系列编号不回填**——保持与两名体验官原始记录的编号一一对应，便于追溯。全报告总计：**绕路 7 条（R1–R7）+ 不适 23 条（N2–N24）= 30 条**。

---

## 四、亮点（经复核与修正）

1. **「验证闭环」仍是全套设计最强项**（体验官 B 给 5/5，主代理 v1 认同，**v2 下调至 4.5/5**）。`SKILL.md:85` 把"完成时用什么可判定信号证明它成了"设为动手前置门禁，且细到"性能类＝耗时下降**且**输出逐字节一致"这种可证伪程度。本次评估中它逼出了 `setup_env` 假绿的**确凿证据**，而非停留在"我觉得没问题"。
   > **修正**：v1 未指出其边界——R6 证明**对账本身存在假 PASS 通道**，"先测后交"能防"没测就交"，但**不能防"测了个假的"**。故降为 4.5。
2. **「质量把关 ≠ 独立审核」的显式划界**（`SKILL.md:100`）**被本次实践直接验证有效**。它拦住了"质量把关已做＝可以免审"，独立审核随后逐条推翻了初稿的 4 处问题（含主代理自己的一条错误降级）。**该划界有实际约束力，不是文字游戏。**
3. ~~`plan.yaml` 必填字段 ↔ 对账脚本构成硬约束，把"说做了"和"真的做了"用程序区分开~~ → **该亮点被证伪，撤回**。R6 显示对账存在跨技能兜底的假 PASS 通道，它区分的是"**文件能不能被找到**"，不等于"**交付物真的在工作区里**"。**降级为：对账是有效的初筛，但不能作为唯一依据（这正是本人 v1 犯的错）。**

---

## 五、被推翻或降级的指控（含主代理自身错误）

主代理对重指控逐条独立复现。以下记载**全部裁决**，包括**主代理自己搞错的一条**：

| 指控 | 原级别 | 裁决 | 最终级别 | 关键差异 |
| --- | --- | --- | --- | --- |
| `run_stage.ps1` 静默 no-op 却报成功 | B 报**阻断** | **降级成立**：第 18-19 行的 `[ OK ] venv/script` 是**前置路径检查**，不是执行成功声明；体验官把自己探针文件里追加的 `WRAPPER_LASTEXITCODE=0` 读成了脚本断言。且 `ops.md` 排障表已预警该坑 | 不适（N2） | 根因是**宿主通道差异**而非脚本；实质建议（加执行后校验）保留 |
| `excel-write` 直喂 CSV → **裸异常** | B 报绕路 | **⚠️ 主代理 v1 的错误降级已被撤回**：v1 用另一条命令（`excel-write sales.csv`，缺 `--rows`）得到 argparse 错误，据此宣称"裸异常不成立"。经独立审核指出并逐字复现 B 的原命令（`excel-write out.xlsx --rows sales.csv`），**确实抛 `JSONDecodeError` traceback，EXIT=1**。原指控正确 | **绕路（N3）** | v1 犯了"**用不对口的实验反驳结论**"——复现必须与被反驳命题同口径同参数 |
| `checks.py plan` 漏 `--base` 会**误报 FAIL** | A 报绕路 | **部分成立**：`--base` 参数确实存在（`checks.py:369`），但"误报 FAIL"是**未经实跑的推断** | 不适（N8） | 保留文档完整性建议，去掉故障定性 |

> **方法论结论（本报告最重要的一条）**：本次 4 处裁决错误（3 条由主代理发现，1 条由独立审核证伪主代理）**全部发生在"描述了推断的行为、而非观察到的行为"处**。这反向证明技能对证据的要求是必要的——而"独立审核"这一强制门禁，**在本次评估中直接改变了最终结论的质量**（若无它，本报告会带着一条错误的降级和 16 条被丢弃的发现交付）。

---

## 六、v1 → v2 逐条返修对账（独立审核 9 条问题）

| 审核问题 | 严重度 | 本版处置 | 复核方式 |
| --- | --- | --- | --- |
| H1 `excel-write` 降级依据错误（用不对口命令反驳） | 高 | 撤回降级，恢复 N3 为**绕路**；写入第五节并标注为自身错误 | 逐字复现 B 原命令，取得 `JSONDecodeError` EXIT=1 |
| H2 漏掉 `checks.py:164-165` 假 PASS | 高 | 补为 **R6**；撤回亮点 3；下调验证闭环至 4.5；修正"R5 是唯一真实代码缺陷"的表述 | 读 `checks.py:155-167` 确认兜底分支存在 |
| 综合分 3.4 不可复算（表内均值 3.5） | 中 | 改为 **3.5** 并写明算法与"判断非测量" | 重算 6 维均值 |
| 4 条降级未在第五节披露 | 中 | 第五节改为**全部裁决记载**（含主代理自身错误） | 与两名体验官原始条目逐条比对 |
| "R3-R5 对应项"交叉引用错 | 中 | 已删除该引用，改为按事实描述 | 核对降级项实为 N2/N3/N8 |
| 反模式 40 与现有 #19 同源 | 中 | 改为"**补充 #19 的第 2 种形态**"，不再作为独立新条目 | 读 `quality-gates.md:89`（#19 原文）比对 |
| 定级偏低 2 条（N1/N3） | 中 | N3 恢复绕路；N1 因"缺必填字段可致红线③失守"**提升为绕路** | 见第二节 R 系列与第三节 N1 |
| 遗漏 B#4/#11/#12、A#5 等 | 低 | 全部补入（N18/N22/N23/N9） | 见第三节 |
| 静默丢弃约 16 条子代理发现 | 低 | 本版共 **30 条**（29 条原始 + 1 条拆分），**全量收录**（A7 拆为 N6+N10） | 第六节对账 |

### 第二轮：回归复核发现 2 条未解决（本文档 = v3）

第一轮返修提交后，由新的独立审核者**逐条回归**该 9 条问题，判定「已解决 6 / 部分解决 1 / 未解决 2」，并另报 4 条新问题（1 实质 + 2 计数 + 1 残留）。本轮处置：

| 回归发现 | 严重度 | 本版（v3）处置 | 复核方式 |
| --- | --- | --- | --- |
| **综合分仍不可复算**：v2 把「验证闭环」5.0→4.5 后，均值应为 20.5/6 = **3.4**，报告却写 3.5（**与 v1 同型错误、方向相反**） | 中（实质） | 综合分改 **3.4**，并在注中如实记载两次方向相反的算术错误 | 用解释器实算 `sum([3.0,3.5,3.0,4.0,4.5,2.5])/6` |
| **N1 提升未落实**：§六称 N1"提升为绕路"，但 §三 仍有 N1 行、§二无 R7 → 计数与表体自相矛盾 | 中（计数） | N1 正式移入 §二成为 **R7** 并从 §三移除；补编号说明，**N 系列不回填** | 全文行数与标题计数一致：绕路 7 行、不适 23 行 |
| §二标题称「7 条绕路」实列 6 行；§三称「23 条不适」实列 24 行 | 低（计数） | 同上，已一致 | 逐行计数 |
| 被评对象在评估期间被并发修改（模板 7→8、自检 18→19 项）未披露 | 低（残留） | 已在 §〇 顶部加显著前提声明；并据此补强 **R2 的活证据** | 查 `assets/templates/` 实际清单与 git 未跟踪状态 |

> **第二轮教训**：2 条"未解决"里，1 条是我**把一个本来正确的数字改成错的**（3.4→3.5），1 条是**我声称"已提升"但只改了说明、没改表体**。两者都不是理解问题，而是**改一处忘一处**——印证技能的「返修复查」必须**由审核者逐条回归**，作者自查抓不到这类错误（本次两次都靠回归才发现）。

---

## 七、盲区与未覆盖（诚实声明）

1. **规模维度未测**：只跑了 toy 级任务（1 个 CSV / 1 个交付物）。多任务并发、10+ 交付物、跨天长链路下的表现未知——**而 R4（审核粒度）、R2（tasks 口径）、R6（假 PASS）恰恰在规模化时才真正恶化**。
2. **脚本分支覆盖不全**：`office_io.py` 的 word/pdf 路径、`ai_call.py` 除"无 Key 优雅退出"外的分支、`http_fetch.py` 的 `--github-code-search` 带 token 端到端（本机无 PAT），均未实测。
3. **成本指标缺失**：未建立 token/耗时基线，因此"这套流程比随手做贵多少"无法回答。体验官 B 记录的"29 个命令步、重复劳动感"是主观判断，不构成量化结论。
4. **真实业务任务未跑**：评估只用了人造数据，未在量化/小说/学习资料等真实场景中检验。
5. **子代理同源偏差**：四名子代理均属同一模型家族，同源审美与同源盲区无法排除，**异家族裁判未引入**（技能 `quality-gates.md` 的"评审六偏差"要求异家族裁判，本次未满足——如实登记为人审点）。
6. **本次评估自身未做规模验证**：唯一"经对抗验证"的环节是报告文本，不是技能在真实任务中的表现。

---

## 八、原始记录索引

| 文件 | 内容 |
| --- | --- |
| `体验官A-冷启动.md` | 冷启动两遍法全过程、16 条问题、5 维评分（2.6/5） |
| `体验官B-可执行.md` | 29 个命令步实测记录、13 条问题、5 维评分（3.6/5）、toy 产物清单 |
| `独立审核意见.md` | 本报告（v1）的独立审核结论：**不通过**，9 条问题（含 2 高） |
| `cache/ai-workflow/probe_pkgdir.txt` | 主代理复现实验原始输出（R5 与 N2 的硬证据） |
