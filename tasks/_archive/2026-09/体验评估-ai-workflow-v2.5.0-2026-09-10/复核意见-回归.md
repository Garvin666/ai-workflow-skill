# 复核意见（返修复查）

> 复核者：第二轮独立审核者（未参与评估与返修） ｜ 日期：2026-09-10 ｜ 方法：有罪推定 + 亲手复现 + 逐条回归
> 复核对象：`体验报告.md`（返修版 v2）、`可优化方向.md`（返修版 v2） ｜ **只读，未修改任何待审文件**
> 回归基准：`独立审核意见.md`（上一轮 9 条问题） ｜ 新增写入：仅本文件

## 1. 复核结论

**不通过。** 两条高优先问题（H1、H2）已实质修好并有独立复现支撑，M3/M4 及 L1/L2 也已修复；但 **M1（综合分仍不可复算）与 M2（4 条降级仍未披露）两条中优先问题未解决**，且返修**新引入 1 处分类计数自相矛盾**（声称 7 绕路/23 不适，实列 6/24，N1 的"提升为绕路"未落到正文）与 **1 处算式错误**（3.42 写成 3.5）。

## 2. 9 条问题逐条回归

| 编号 | 判定 | 证据（返修版位置 + 我核对的技能文件位置） |
| --- | --- | --- |
| **H1** `excel-write` 降级依据错误 | **已解决** | 返修版 §五 第 2 行（`体验报告.md:111`）标注"主代理 v1 的错误降级已被撤回…原指控正确"，最终级别恢复**绕路（N3）**；§三 `:70`、§六 `:122` 一致。我独立复现两条命令失败形态**确实不同**（见 §4）→ 原指控成立、"恢复绕路"正确 |
| **H2** 漏掉 `checks.py:164-165` 假 PASS | **已解决** | 已补为 **R6**（`体验报告.md:58`），并撤回亮点 3（`:100`）、下调验证闭环 5.0→4.5（`:39`）、删除"R5 是唯一真实代码缺陷"（`:60`、`可优化方向.md:5`）。我读 `checks.py:157-166`：`:163` 注释"skill 内相对路径…逐个技能目录再试一遍"（**有意设计**成立），`:164-165` 兜底分支存在。返修版**已不再声称 R5 唯一** |
| **M1** 综合分 3.4 不可复算 | **未解决** | `体验报告.md:43` 写"（3.0+3.5+3.0+4.0+4.5+2.5)/6 = **3.5**"；我实算 = **3.4167≈3.4**（见 §4）。v2 表内 6 维均值 = 3.42，与所写 3.5 不符。**同一"不可复算"缺陷换了方向仍在**（v1 表均值 3.5 却写 3.4；v2 表均值 3.4 却写 3.5） |
| **M2** 4 条降级未披露 | **未解决** | §五（`:104-112`）**仅 3 行**（run_stage→N2、excel-write→N3、checks.py plan→N8），未含体验官 A 原判为**绕路**、返修版仍列**不适**的 N1(A#6)、N5(A#8)、N6(A#7)、N7(A#9)。§六 `:125` 却称"第五节改为**全部裁决记载**"——与实际不符 |
| **M3** "R3-R5 对应项"交叉引用错 | **已解决** | 全文 grep 无"R3-R5/对应项"；§五改为按事实列出被裁项（`:110-112`），§六 `:126` 自认"已删除该引用，实为 N2/N3/N8" |
| **M4** 反模式 40 与 #19 同源 | **已解决** | `可优化方向.md:61` 改为"**补充 #19 的第 2 种形态（不新增编号）**"；新 `#40` 已换为"用不对口的实验反驳结论"（`:63`），与 #19 不再同源。核对 `quality-gates.md:89` 确为 #19（依赖/输入缺失仍报全绿） |
| **M5** 定级偏低 2 条（N1/N3） | **部分解决** | N3 已恢复**绕路**（`:70`/`:111`）✅；N1 在 §六 `:128` 被声称"提升为绕路"，但 §三 `:68` **仍列 N1 为不适**、§二**无 R7 行** → 提升只写在返修对账表里，正文未落实 |
| **L1** 遗漏 B#4/#11/#12、A#5 | **已解决** | 逐条落位：B#4→N18（`:85`）、B#11→N22（`:89`）、B#12→N23（`:90`）、A#5→N9（`:76`）；另 B#7→N19（`:86`）亦已在（上一轮 §7 遗漏项 4） |
| **L2** 静默丢弃约 16 条发现 | **已解决** | 30 条 = 29 原始 + A7 拆为 N6/N10，29 条原始**逐条可对上**（见 §7），无凭空多出、无遗漏。但分类计数有误，见 §6 |

> 另：上一轮审核 §5 自带的 L1（`SKILL.md` 161 行 / `scripts/` "5 个"口径未澄清）与 L3（`可优化方向.md` 的预估采用一位小数"4.4"、⚠️ 警示仍与数字分处两段）**本次未处理**——这两条不在任务给定的 9 条内，附此备查。

## 3. 新引证抽查（新增/改动处，抽查 10 处）

| # | 返修版引证 | 我打开文件看到的 | 判定 |
| --- | --- | --- | --- |
| 1 | R6：`checks.py:164-165`（`cands.extend(sd / p for sd in SKILLS_ROOT.glob("*/"))`） | `:164` `if SKILLS_ROOT.exists():`；`:165` `cands.extend(...)`；`:163` 注释确认"逐个技能目录再试一遍" | 相符 |
| 2 | N3：`SKILL.md:122` 称 office_io "支持 CSV 读入" | `SKILL.md:122`「…生成 Excel/Word/PDF（**支持 CSV 读入**、多 sheet、列宽/冻结首行）」 | 相符 |
| 3 | R5：`setup_env.ps1:8`（7 deps）vs `:41`（5 映射） | `:8` 7 项；`:41` 仅 requests/openpyxl/docx/pypdf/yaml 五项；`:39` 注释"host-proof" | 相符 |
| 4 | N2：`run_stage.ps1:24-25` | `:24` `& $venvPython $target @ScriptArgs`；`:25` `exit $LASTEXITCODE` | 相符 |
| 5 | P2：`quality-gates.md:89` = #19 | `:89` 恰为 #19「自检/对账工具在依赖或输入缺失时仍输出"全绿"」 | 相符 |
| 6 | N1：`assets/report-template.md:9-13` 仅 5 要素 | `:9-13` = 目标/输入/交付物/验收标准/边界约束（**无工作区根**）；`templates/README.md:13` workspace 必填 | 相符 |
| 7 | N22：`checks.py:24` vs `:55` | `:24` "meta 必填（任务 / 验证信号）"；`:55` 含"工作区根"（3 字段） | 相符 |
| 8 | N21：`ops.md:107`（227ms）/`:180`（156ms） | `:107` "实测 140 条平均 227ms/条"；`:180` "平均 156ms/条" | 相符 |
| 9 | N19：`SKILL.md:106`（④）vs `:111`（可豁免） | `:106④`"任何对外交付物"；`:111`"豁免…单文件微改/纯格式转换/仅追加文字" | 相符 |
| 10 | N7：`checks.py:362-379` 仅 skill/plan/status/mark | `:362-379` 正是四个 sub.add_parser | 相符 |

**结论：抽查 10 处全部相符，未发现新引证编造。**

## 4. H1 / H2 独立复现（原始输出）

### 4.1 H1 — 两条命令的失败形态（临时文件建于 `C:\Users\26717\.workbuddy\cache\ai-workflow\复核回归\`）

命令 A（= 体验官 B 原命令，CSV 当 `--rows` 值）：
```
python office_io.py excel-write .../a.xlsx --rows .../sales.csv
  File "...\office_io.py", line 109, in cmd_excel_write
    payload = json.loads(_read_source(args.rows))
json.decoder.JSONDecodeError: Expecting value: line 1 column 1 (char 0)
EXIT=1
```
命令 B（CSV 当输出位置参数，缺 `--rows`）：
```
python office_io.py excel-write .../sales.csv
usage: office_io.py excel-write [-h] --rows ROWS [--sheet SHEET] [--no-format] [--widths WIDTHS] file
office_io.py excel-write: error: the following arguments are required: --rows
EXIT=2
```
**判定**：两者失败形态**确实不同**（A=裸 `JSONDecodeError` traceback/EXIT=1；B=argparse 用法错/EXIT=2）。原指控成立 → 返修版"撤回降级、恢复 N3 为绕路"**正确**。

### 4.2 H2 — `_candidate_paths` 机制

`checks.py:157-166` 的候选路径 = `base/p`、`~/.workbuddy/p`、`base.parent/p`，外加 `:164-165` 的"逐个技能目录再试一遍"。即把 `~/.workbuddy/skills/<每个技能>/<p>` 全部并入候选 → 凡以 `references/…`、`scripts/…` 等技能内相对路径写的交付物，即使**本任务工作区里并不存在**，也会被别的技能目录"救活"→ 假 PASS。返修版所述"**有意设计（注释写明）但范围过宽**"**准确**；返修版亦未再声称"R5 唯一"。

## 5. 原有验证信号回归

命令（venv 解释器）：
```
python .../ai-workflow/scripts/checks.py skill
```
原始输出（尾部）：
```
[ OK ] 文档引用完整性 — 检查 23 条引用
[ OK ] 模板 schema：_示例-数据分析.yaml — 9/9
[ OK ] 模板 schema：UI组件库搭建.yaml — 9/9
…（共 8 个模板 yaml 全 9/9）
[ OK ] plan-template 必填键 — 齐全
[ OK ] py_compile：ai_call.py / checks.py / data_query.py / http_fetch.py / office_io.py
=== 结果：19/19 通过，FAIL=0 ===
EXIT=0
```
**未回退，仍全绿**（`可优化方向.md` §六"回归口径：`checks.py skill` 全绿"可达成）。
**观察**：上一轮审核记录为 **18/18**、模板 7 类；本轮为 **19/19**、模板 8 类——`assets/templates/` 多了一份 `UI组件库搭建.yaml`（mtime 2026-09-10 23:33，落在本次评估时间窗内）。技能本体在两次审核之间被改动过，属环境事实，非返修文本所致；但它使 `体验报告.md:21` 的"模板 **7 类**"与现状不再吻合。

## 6. 返修引入的新问题

**未"未发现"，共 4 条（1 实质 + 2 计数 + 1 残留）：**

1. **【实质·中高】综合分算式仍不成立（M1 的翻版，方向相反）**。`体验报告.md:43` 白纸黑字"（3.0+3.5+3.0+4.0+4.5+2.5)/6 = 3.5"，实算 20.5/6 = **3.4167**。v2 表内均值 = 3.42 ≈ **3.4**，报告却写 **3.5**。返修声明"初稿写 3.4 而表内均值实为 3.5，属算术错误，已修正"——但作者同时把"验证闭环"由 5.0 降到 4.5，均值随之降为 3.42，**修正后的 3.5 是新的错值**。
2. **【实质·中】分类计数与正文自相矛盾**。§二标题/§一均称 **7 条绕路**，实列 **R1–R6（6 行）**；§三标题称 **23 条不适**，实列 **N1–N24（24 行）**；§六 `:128` 称 N1"提升为绕路"，但 §三 `:68` N1 仍在、§二无 R7。三处指向同一漏改：**N1 的搬迁只改了计数、没落到表格**。
3. **【低】`可优化方向.md:21`**"其余 **26** 条（含 23 条不适）"——30 − 杠杆项 5（R1/R2/R4/R5/R6）= 25，写 26，差 1。
4. **【低·残留】**上一轮 §5 的 L1（`SKILL.md 161 行`/`scripts/ 5 个` 口径未澄清，仍混计 5 个 .py 与 ps1）与 L3（预估用一位小数"4.4"、⚠️ 与数字仍分处两段）本次未改。

## 7. 29 条原始发现的对账结果

**条目层面成立，分类计数不成立。**

- 29 条原始发现 = 体验官 A 16 条（A#1–16）+ 体验官 B 13 条（B#1–13），**逐条落到返修版 30 条**（29 + A#7 拆为 N6/N10），**无凭空多出、无遗漏**：

| 原始 | 落点 | 原始 | 落点 | 原始 | 落点 | 原始 | 落点 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A#1 | R1 | A#9 | N7 | B#2 | R5 | B#8 | N20 |
| A#2 | R3 | A#10 | N11 | B#3 | N3 | B#9 | N4 |
| A#3 | R2 | A#11 | N12 | B#4 | N18 | B#10 | N21 |
| A#4 | N8 | A#12 | N13 | B#5 | R6 | B#11 | N22 |
| A#5 | N9 | A#13 | N14 | B#6 | R4 | B#12 | N23 |
| A#6 | N1 | A#14 | N15 | B#7 | N19 | B#13 | N24 |
| A#7 | N6+N10 | A#15 | N16 | B#1 | N2 | | |
| A#8 | N5 | A#16 | N17 | | | | |

- **但"7 绕路 + 23 不适"的分类对不上表体**：§二实为 6 条、§三实为 24 条（见 §6-2）。若按作者意图（N1 提升为绕路）成立，则应为 R1–R7 + N2–N24 = 7 + 23，总数同为 30；**缺的正是"把 N1 从 §三 搬进 §二 并补 R7 行"这一步**。故返修版"**共 30 条（29 条原始 + 1 条拆分）**"这句话本身**成立**，"7/23"这一分类则是**未落实的意图**。

## 8. 我不确定的地方

1. **v1 报告原文未在我审阅范围内**：M1/M2 的"初稿"状态（如 v1 §4 原文、v1 是否已有 N1/N5/N6/N7）我依 `独立审核意见.md` 的转述判定，未读到 v1 原件。
2. **`体验报告.md:45` 的 7 条枚举**（"两处主干自相矛盾＋两处脚本假绿/假 PASS＋一处审核粒度缺失＋一处文档-代码不符＋一处模板缺字段"）我未能与 6 行 R 表**逐一对齐**（R3"数据分析缺信号"无处安放），故只能确定"数字 7 ≠ 行数 6"，其枚举内部是否另有错位未深究。
3. **技能本体在两次审核之间被改动**（模板 7→8），我无法判定这是并行任务还是评估过程所致；故 `体验报告.md:21` 的"模板 7 类"是否"当时正确"我不下定论。
4. **`plan.yaml` 仍未读**（沿用只读约定），R4 的"合并送审"自证未独立核实。
5. 新引证我抽查了 10 处（全部相符），**未穷举**全部 30 条证据的行号。
