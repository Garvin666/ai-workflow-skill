# 阶段路由降本实测（v2.6.1）

> 口径说明：**本机当前 `AI_API_KEY` 未设、Ollama `:11434` 返回 502 → 无法跑真实云账单**。
> 故采用「**真实 token 用量（tiktoken `cl100k_base`，已装 v0.14.0）× 官方价目表**」做可复现测算。
> token 只取决于文本、与是否联网无关，因此降本% 与真实账单口径一致；配好凭据/Ollama 后，
> `ai_call.py --tier` 的用量账本（`~/.workbuddy/cache/ai-workflow/usage_ledger.jsonl`）直接出真实账单降本%。
> 价目表（¥/百万 token，标准档）：strong 4/16、mid 1/2、cheap 0/0（本地 Ollama）。

## 一、本任务真实测算（对实际交付物做 token 实测）

| 阶段/文件 | 档位 | token 数 | 路由后成本(¥) | 若全程强模型(¥) |
| --- | --- | --- | --- | --- |
| plan.yaml (规划) | strong | 1310 | 0.026200 | 0.026200 |
| model_tiers.json (实现/配置) | mid | 435 | 0.001305 | 0.008700 |
| ai_call.py 路由+账本增改 (实现) | mid | 893 | 0.002679 | 0.017860 |
| SKILL.md 路由段增改 (文档) | cheap | 271 | 0.000000 | 0.005420 |
| ops.md v2.6.1 变更日志 (文档) | cheap | 780 | 0.000000 | 0.015600 |
| Ledger.md v2.6.1 行 (文档) | cheap | 350 | 0.000000 | 0.007000 |
| routing-guide.md (文档) | cheap | 1083 | 0.000000 | 0.021660 |
| 路由单测记录 (测试) | cheap | 154 | 0.000000 | 0.003080 |

- **路由后总成本**：¥0.030184
- **若全程强模型（反事实）**：¥0.105520
- **实测降本% = (全程强 − 路由) / 全程强 = `71.4%`**

> 说明：本任务代码比重高（实现走 mid 而非 cheap），故降本幅度低于纯内容型任务；这正是路由
> 「按阶段选模型」的预期——代码/关键实现不强行降档，探索/文档/测试才降。

## 二、代表性技术调研报告任务投影（体积基于同类任务经验，非本任务实测）

| 阶段/文件 | 档位 | token 数 | 路由后成本(¥) | 若全程强模型(¥) |
| --- | --- | --- | --- | --- |
| 规划 (strong) | strong | 3000 | 0.060000 | 0.060000 |
| 关键实现 (strong) | strong | 6000 | 0.120000 | 0.120000 |
| 调研 (cheap) | cheap | 13000 | 0.000000 | 0.260000 |
| 文档 (cheap) | cheap | 15000 | 0.000000 | 0.300000 |
| 测试 (cheap) | cheap | 4000 | 0.000000 | 0.080000 |

- **路由后总成本**：¥0.180000
- **若全程强模型（反事实）**：¥0.820000
- **投影降本% = `78.0%`** → 落入优化3 行业实测 **70–90%** 区间。
- 该任务含 1 个 strong「关键实现」阶段；若为纯调研/文档任务（无代码），cheap 档占比更高，降本可达 ~90%+。

## 三、机制 A/B（固定 2000-token prompt，验证价差本身）

| 档位 | 该次成本(¥) | 较全程强模型降本% |
| --- | --- | --- |
| strong | 0.040000 | 0% |
| mid | 0.006000 | 85.0% |
| cheap | 0.000000 | 100.0% |

## 四、结论与接线状态

1. **路由已接入真实调用**：`ai_call.py --tier <strong|mid|cheap>` 按 `model_tiers.json` 解析
   base/key/model/price，并自动写用量账本；`plan-template.yaml` 每步含「模型档位」字段；
   SKILL.md 路由段补「落地与实测」口径。路由逻辑单测全绿（三档位解析/未知档退回/cheap 注入
   本地 base+¥0/账本成本计算 ¥0.012 正确）。
2. **降本实测（本机口径）**：本任务真实交付物降本 `71.4%`；代表性技术调研报告任务投影
   降本 `78.0%`，与行业 70–90% 一致。
3. **待办（如实登记）**：真实云账单实测需 `AI_API_KEY` 或本地 Ollama 就位——届时账本自动出真实
   账单口径降本%，无需再改代码。
