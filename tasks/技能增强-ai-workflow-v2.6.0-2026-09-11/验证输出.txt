# ai-workflow v2.6.0 交付前验证输出（机器门禁证据）

> 生成时间：2026-09-11 ｜ 验证命令均用 ai-workflow venv 解释器运行

## 1. checks.py skill（技能自检）
结果：19/19 通过，FAIL=0
- SKILL.md 存在 / frontmatter 含 name/description/agent_created
- 文档引用完整性：检查 29 条引用，全部 OK
- 8 个任务模板 schema 9/9
- plan-template 必填键齐全
- py_compile：ai_call.py / checks.py / data_query.py / http_fetch.py / office_io.py 全部通过

## 2. http_fetch.py 编译
`python -m py_compile scripts/http_fetch.py` → OK（最终复核通过）

## 3. 跨任务限流节流 smoke 测试
- 方法：_rl_throttle("test_smoke", 2, 1) 连调 3 次（limit=2/window=1）
- 结果：elapsed=1.21s（>=0.9s，证明触发了 ~1s 等待）；状态文件已生成
- state 内容：{"test_smoke": [<timestamp>]}
- 结论：SMOKE PASS（节流逻辑生效；状态落盘生效）

## 4. 节流调用点 grep 确认（共 3 处 + 1 定义）
- 206: def _rl_throttle(bucket, limit, window)
- 376: _rl_throttle("gh_core", 60, 3600)        ← github_repo_metrics
- 441: _rl_throttle("gh_search", 10, 60)        ← github_search
- 522: _rl_throttle("gh_search", 10, 60)        ← github_code_search

## 5. SKILL.md 改动 grep 确认
- 第 5 行：version: 2.6.0
- 第 91 行：### 阶段感知模型路由（降本 70–90%，不损质量）  ← 优化3
- 第 159 行（Token 纪律）：references 严格按需加载  ← 优化5 措施2
- 阶段5 交付前自检表：含 "references 按需加载（未预读全量 / 主上下文未超量）"  ← 优化5 措施2

## 6. 计划对账（checks.py plan）
- 步骤 1–4 交付物（SKILL.md / http_fetch.py / ops.md）均真实存在
- 本文件补齐后：预期 9/9 FAIL=0

## 7. 如实登记：编辑工具假成功事件
- 批量编辑 http_fetch.py 时（文件锁 EBUSY 期间），"gh_core" 与 "github_code_search" 两处 _rl_throttle 调用被报告 "Successfully edited" 却未落盘；经读取确切行发现缺失，改用 Python 确定性读改写补齐并复核。
- 教训：对大文件批量编辑后必须读取确切行复核，不能仅凭工具回显判定"已改"（即「描述与实现不符」在本任务内的投影）。
