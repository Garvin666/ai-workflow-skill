# 独立审核意见（v2.5.1 修复）

> 审核者：独立审核子代理（未参与本次修复）｜日期：2026-09-10/11｜待审对象：ai-workflow v2.5.1 修复
> 方法：以代码/二进制为准，不采信 `ops.md` 变更日志自述；R5/R6 亲手复现；对 v2.5.0 旧版（git HEAD）做对照。
> 环境观察（供后续复现参考）：① Bash 里 `Invoke-Expression` 被安全策略拦截；② 本工具链 PowerShell 通道**吞掉 stdout**（须 `*>` 落盘、且落盘为 UTF-16LE，需转码再读）；③ 该通道下 ps1 内的原生子进程**确实不执行**（真跑 setup_env.ps1 时 pip 一步无任何输出，与 ops.md:18 描述一致）。
> 原始证据落盘：`tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/tmp/`（`r5_scen_b.txt`、`r5_scen_c.txt`、`setup_env_run.txt`、`checks_old_v250.py`、`unrelated-r6/`）。

## 1. 审核结论（通过 / 不通过 —— 一句话）

**不通过** —— 两处真缺陷 R5/R6 **确实已改且我亲手复现关闭**（R6 旧版 7/7 假 PASS → 新版 7/8 FAIL=1；R5 未映射依赖与 duckdb 缺失均真报 FAIL），但 **`ops.md` 对 R6 修法的描述（"收窄为仅 `references/`／`scripts/`／`assets/` 开头 + 新增常量 `SKILL_INTERNAL_PREFIXES`"）与代码实际行为（彻底删除兜底、全库无该常量）直接矛盾**，且回归证据 1 的括注数字**不可复现**，故必须返修文档与自述。

## 2. 逐条修复真伪核对

| 编号 | 声称的修法 | 实际是否改了 | 证据（文件:行 + 原文） | 判定 |
| --- | --- | --- | --- | --- |
| **R5** | 补全 `$pkgDir`（duckdb、ast-grep-cli）+ 未映射显式 FAIL | 是 | `scripts/setup_env.ps1:8` `$deps = @("requests","openpyxl","python-docx","pypdf","pyyaml","duckdb","ast-grep-cli")`（7 项）；`:41` `$pkgDir = @{ ... "duckdb"="duckdb"; "ast-grep-cli"="ast_grep_cli*.dist-info" }`（7 项，与 `$deps` 等长）；`:49-53` `if ([string]::IsNullOrEmpty($dirName)) { Write-Host "[FAIL] $d has no entry in $pkgDir ..."; $failedList += $d; continue }` | **真改** |
| **R6** | 彻底移除跨技能兜底 | 是（但描述与代码不符，见 §8-N1） | `scripts/checks.py:167` `return [base / p, Path.home() / ".workbuddy" / p, base.parent / p]`（旧版 `:163-165` 的 `if SKILLS_ROOT.exists(): cands.extend(sd / p for sd in SKILLS_ROOT.glob("*/"))` 已删除）；`:162-166` 新增说明注释 | **真改**（代码层） |
| **R1** | 红线①限定"处理类脚本"，豁免只读检查 | 是 | `SKILL.md:59` `**红线①：未确认前禁止执行处理类脚本或产出文件**（\`checks.py status\`／\`skill\`、\`Read\`／\`Grep\`／\`Glob\` 等**只读检查不受限**——阶段 0 的工作区状态一瞥即属此类；L0/L1 除外）。` | **真改** |
| **R2** | 任务目录一律建在工作区根下 `tasks/`；技能目录 `tasks/` 仅限自身演进 | 是 | `SKILL.md:27` `- **任务目录一律建在工作区根下的 \`tasks/\`**（含 plan.yaml、交付物、\`tmp/\`）……**技能目录的 \`tasks/\` 只记录技能自身演进类的任务**`；`:34` 例外表行改为 `……**仅"技能自身演进"类任务**才可写 \`tasks/\` 记录`；`:85` 阶段 3 第 1 条同步 | **真改** |
| **R3** | 主干信号清单补"数据分析类" | 是 | `SKILL.md:86` `……**数据分析类＝交叉校验脚本（总额对账／行数一致／空值率）+ 抽样复算**；……**完整信号表见 \`references/quality-gates.md\`（含未列出的任务类型）**。` | **真改** |
| **R4** | 独立审核补优先级条款 + 批次规则 | 是 | `SKILL.md:113` `7. **优先级（消解冲突）**：**豁免仅在产出未命中第 1 条四类时适用。**……`；`:114` `8. **批次规则（控制派发量）**：**同一任务的多个交付物可合并为一次送审**……单批建议 ≤5 个文件……**不得**以"合并批次"为由减少送审覆盖面` | **真改** |
| **P1-1** report-template 补「工作区根」 | 补第 6 行 | 是 | `assets/report-template.md:14` `\| **工作区根** \| {本任务允许读写的目录绝对路径；红线③依据——任务目录与产出均落在此目录内} \|` | **真改** |
| **P1-2** run_stage 加执行后自检 + ops §一首选声明 | 加自检；§一 声明首选直调 | 是 | `scripts/run_stage.ps1:27-34` `$global:LASTEXITCODE = $null` → `& $venvPython $target @ScriptArgs` → `if ($null -eq $code) { … [FAIL] … ; exit 1 }`；`references/ops.md:18` `> **优先级（重要）**：**首选**……直接调用；\`run_stage.ps1\` 只作为**交互式终端**里的便捷入口。` | **真改** |
| **P1-3** SKILL 改"CSV 经 excel-read 转 JSON 再 excel-write" | 是 | `SKILL.md:128` `……但 \`excel-write\` **只接受 \`--rows\` 的 JSON**——CSV 必须先 \`excel-read <csv> --fmt json > rows.json\`，再 \`excel-write <out.xlsx> --rows rows.json\`（**直接喂 CSV 会抛 \`JSONDecodeError\`**）。` | **真改** |
| **P1-4** ops 补排障 + import 探测命令 | 是 | `references/ops.md:58` `**不依赖 ps1 的最小探测命令**……`；排障表新增 6 行 `:103`（ps1 静默 no-op）、`:104`（Bash 拒调 ps1）、`:105`（excel-write CSV）、`:106`（status 无 `--workspace`）、`:107`（依赖自检假 OK）、`:108`（对账假 PASS） | **真改** |
| **P1-5** L1 排除 `_` 开头 + 注明需阶段 0 确认 | 是 | `SKILL.md:17` `……（**排除 \`_\` 开头的示例／字段说明文件**，如 \`assets/templates/_示例-数据分析.yaml\`）；**L1 需待阶段 0 查过模板后才能确认**……` | **真改** |
| **P1-6** 阶段 0「列本地 skill」给做法 | 是 | `SKILL.md:51` `2. **列本地 skill**：\`Glob ~/.workbuddy/skills/*/SKILL.md\` 与 \`.workbuddy/skills/*/SKILL.md\`，读各自 frontmatter 的 \`name\`／\`description\` 判断是否匹配；无匹配则调用 **\`find-skills\` skill**……` | **真改** |
| **P1-7** plan 示例补 `--base`；status FAIL 文案补提示 | 是 | `SKILL.md:135` `……\`checks.py plan <plan.yaml> --base <工作区根>\`……（**非当前目录时必须带 \`--base\`……**）`；`scripts/checks.py:232-233` `print("       提示：工作区不是当前目录时请加 --workspace <工作区根>；任务目录应建在工作区根下的 tasks/（见 SKILL.md 红线③）")` | **真改** |
| **P1-8** 阶段 2 非工程类维度 + playbook 数据分析 | 是 | `SKILL.md:78` `……**非工程类任务**（数据分析／文档／调研）可换用 口径选择／样本范围／结论稳健性／可复算性 等维度对比。`；`references/playbook.md:9-10` `> **通用数据分析类任务也参照本节**——数据完整性三查、对账差额、空值率报告是同族验证信号……` | **真改** |
| **P1-9** 阶段 4 非 Office 跳过；报告模板指针在阶段 3 也出现 | **前半是；后半未做** | `SKILL.md:125` `> **非 Office 交付（纯 markdown／代码／数据文件）跳过本阶段**；但**报告类交付仍须参照 \`assets/report-template.md\`**……`；但 `report-template` 在 SKILL.md 仅出现于 `:125`、`:127`（阶段 4）与 `:163`（索引），**阶段 3 内无该指针**（`:81-91` 未见） | **部分** |
| **P1-10** 小口径（docstring／均值改区间／office_io 守卫／ai_call／data_query） | 是 | `scripts/checks.py:24` `2. meta 必填（任务 / 验证信号 / 工作区根）；……`；`references/ops.md:125-126` 均值改区间并注"不可当固定基线"、`:210` 同步；`scripts/office_io.py:206-221` `_require(...)` + `:266-276` `_NEED` 覆盖 6 子命令；`scripts/ai_call.py:26-34` 仅 `argparse/concurrent.futures/json/os/sys/time/urllib/pathlib`（**纯标准库，核实为真**，无需守卫）；`scripts/data_query.py:140-142` `[提示]` 改走 stdout | **真改** |

## 3. R6 独立复现（原始输出）

测试夹具 `tmp/unrelated-r6/`：**目录内只有 `plan.yaml`**（无 `references/`）；其步骤 `状态: 完成`、`交付物: "references/quality-gates.md"`。

对照组 = `git show HEAD:scripts/checks.py`（v2.5.0 旧版）导出为 `tmp/checks_old_v250.py`。

**新版（v2.5.1，工作区代码）** —— 命令 `checks.py plan tmp/unrelated-r6/plan.yaml --base tmp/unrelated-r6`：
```
=== ai-workflow 计划对账 ===
[ OK ] plan.yaml 存在 — tmp\unrelated-r6\plan.yaml
[ OK ] plan.yaml 可解析
[ OK ] meta.任务
[ OK ] meta.验证信号
[ OK ] meta.工作区根
[ OK ] steps 非空 — 1 步
[FAIL] 步骤 1 交付物缺失 — references/quality-gates.md
[ OK ] Anti-drop 对账 — 已完成步骤 1 个，交付物确认 0 个
=== 结果：7/8 通过，FAIL=1 ===      EXIT=1
```
**旧版对照（v2.5.0）** —— 同命令、同夹具：
```
[ OK ] Anti-drop 对账 — 已完成步骤 1 个，交付物确认 1 个
=== 结果：7/7 通过，FAIL=0 ===      EXIT=0
```
→ **R6 确证关闭**：旧版把别的技能目录里的 `references/quality-gates.md` 当作命中（假 PASS），新版正确报 `[FAIL]`。（与作者回归证据 2 的 7/8、FAIL=1 完全一致。）

## 4. R5 独立复现（含 duckdb 改名实验与复原确认）

**静态核对**：`$deps` = 7 项（`setup_env.ps1:8`），`$pkgDir` = 7 项（`:41`）——**条目数相等**；未映射分支 `:49-53` 存在且 `continue` 前 `$failedList += $d`，故最终 `:61` `if ($failedList.Count -gt 0) { exit 1 }` 会真退出 1。

**验证方式说明**：避免 pip 副作用 + 该通道吞 stdout，我用「**从真实 `setup_env.ps1` 里正则提取 `$deps`/`$pkgDir` 定义与第 4 步循环体，再用 `[scriptblock]::Create()` 点源执行**」的等价方式跑（因 `Invoke-Expression` 被安全策略拦截）；场景 (a) 则**真跑整个 ps1**。

**(a) 正常清单** —— 真跑 `powershell -File setup_env.ps1`：
```
[ OK ] requests / openpyxl / python-docx / pypdf / pyyaml / duckdb / ast-grep-cli
=== Setup complete ===        （失败项 0，与作者回归 4(a) 一致）
```
（观察：pip 一步无任何输出——本通道宿主确实不执行原生子进程，与 ops.md 记载吻合。）

**(b) 注入未映射依赖** `definitely-not-real-xyz`（真实 7 依赖 + 1 假依赖）：
```
deps 数 = 8 ; pkgDir 条目数 = 7
[ OK ] requests ... [ OK ] ast-grep-cli
[FAIL] definitely-not-real-xyz has no entry in $pkgDir (add one; silent pass is forbidden)
```
→ 未映射项**确实显式 FAIL**，不再静默通过（与作者回归 4(b) 一致）。

**(c) duckdb 目录改名实验**（`try/finally` 保证复原）：
```
duckdb 目录改名前存在 = True
== 已临时改名 ==  duckdb 存在 = False ; bak 存在 = True
[ OK ] requests ... [ OK ] pyyaml
[FAIL] duckdb not found under C:\...\Lib\site-packages
[ OK ] ast-grep-cli
失败项数 = 1 ; 失败项 = duckdb
== finally 复原后 ==  duckdb 存在 = True ; bak 残留 = False
### 复原确认（第二遍）：duckdb 存在 = True
```
独立二次确认：`ls` 显示 `site-packages/duckdb` 存在、`duckdb__review_bak` 不存在；`python -c "import duckdb"` → `duckdb import OK, version= 1.5.5`。**已完整复原**（与作者回归 4(c) 一致）。

## 5. 原有信号回归（skill / plan 实测输出）

| 命令 | 实测 | 与作者自述 |
| --- | --- | --- |
| `checks.py skill` | `=== 结果：19/19 通过，FAIL=0 ===`，EXIT=0 | 一致 |
| `plan tasks/技能增强-ai-workflow-v2.5.1.../plan.yaml --base <技能目录>` | `7/7 通过，FAIL=0`（已完成 6 步、交付物确认 6 个） | 一致 |
| `plan tasks/体验评估-ai-workflow-v2.5.0.../plan.yaml --base <技能目录>` | `7/7 通过，FAIL=0` | 一致 |
| 追加抽查 `v2.4.4` / `v2.5.0` / `UI组件库搭建` 计划 | 均 `7/7 通过，FAIL=0` | 作者未测，**未被误伤** |
| 追加抽查 `v2.4.3` 计划 | `6/7 通过，FAIL=1`（`meta.工作区根 — 为空`）；**旧版 v2.5.0 同命令同样 6/7 FAIL=1** | 属**历史遗留**（v2.4.3 早于工作区根字段），**非本次修复引入** |

## 6. 硬约束检查（.ps1 非 ASCII 字节数）

逐字节统计（`python` 读 `rb`，计 `>127` 的字节）：
- `scripts/setup_env.ps1`：总 3058 字节，**非 ASCII 字节数 = 0** ✅
- `scripts/run_stage.ps1`：总 2008 字节，**非 ASCII 字节数 = 0** ✅

两者均为纯 ASCII，满足"含中文会因 GBK 误读静默失败"的硬约束。

## 7. 作者回归证据 vs 实测 对照表

| 作者自述 | 我的实测 | 判定 |
| --- | --- | --- |
| 回归 1：`checks.py skill` 19/19 通过 FAIL=0；**（修复前 18/19 FAIL=1，FAIL 项=文档引用完整性：`_示例-数据分析.yaml` 被当成引用）** | 19/19 FAIL=0 ✅；但**旧树（git HEAD）用旧版 checks.py 实测 `18/18 通过，FAIL=0`**，无任何 FAIL；18→19 的唯一来源是评估期新增的模板 `assets/templates/UI组件库搭建.yaml`（模板 7→8 类），**与"引用完整性被修"无关**。旧 SKILL.md/references **根本不含** `_示例-数据分析.yaml` 引用（grep 为空） | 结论一致；**括注不符（不可复现）** |
| 回归 2：R6 负向测试 `7/8 通过，FAIL=1` | 复现 `7/8，FAIL=1`，`[FAIL] 步骤 1 交付物缺失` | 一致 |
| 回归 3：两存量计划均 `7/7 FAIL=0` | v2.5.1、v2.5.0 两计划均 `7/7 FAIL=0` | 一致 |
| 回归 4：(a) 失败 0；(b) 失败 1；(c) 失败含 duckdb 且复原 True | (a) 7/7 OK；(b) `[FAIL] definitely-not-real-xyz`；(c) `[FAIL] duckdb`，失败项=duckdb，复原 `True`+import OK | 一致 |

## 8. 新问题

- **N1（严重，文档 vs 代码矛盾）**：`references/ops.md:147` 变更日志称 R6"修复：兜底**收窄为仅 `references/`／`scripts/`／`assets/` 开头**的相对路径才启用（**新增常量 `SKILL_INTERNAL_PREFIXES`**）"，`references/ops.md:108` 排障表亦称"v2.5.1 已收窄为**仅 `references/`／`scripts/`／`assets/` 开头**才启用兜底"。但代码 `scripts/checks.py:157-167` 是**彻底删除**兜底（只留 base/主目录/base 上级三条），且**全库无 `SKILL_INTERNAL_PREFIXES`**（`grep` 仅命中 ops.md 自身）。→ 变更日志描述了**另一个从未存在的实现**；这既是"自述≠事实"，也与"禁止只写不实"的纪律自相矛盾。
- **N2（中，自述数字不可复现）**：回归证据 1 的括注"修复前 18/19 FAIL=1"无法复现（见 §7）；真相是 18/18 FAIL=0。属"把推断/记忆当观察上报"（正是 P2 反模式 #41 所禁止的形态）。
- **N3（轻，死代码）**：`scripts/checks.py:43` `SKILLS_ROOT = Path.home() / ".workbuddy" / "skills"` 在移除兜底后**已无任何使用点**（`:163` 仅注释提及），应删；否则后续维护者会误以为仍存在跨技能解析。
- **N4（轻，未收口却宣称收口）**：P1-9 第二半（阶段 3 交付物处也给出报告模板指针）**未实现**，但 `ops.md:153` 的 P1 段落笼统称"文档收口"已完成（未按项点清单核对）。
- **N5（观察，非缺陷）**：`setup_env.ps1:41` 用 `ast_grep_cli*.dist-info`（**dist-info 目录**）作为 `ast-grep-cli` 的存在标记，而非可导入模块；若仅 dist-info 残留、包体被删，会误判 OK。当前 venv 只有该 dist-info（`ls` 已核），属合理近似，但语义上是"安装标记"而非"可导入"。
- **未引入的新断裂**：`office_io.py` 的 `_NEED`（`:266-273`）覆盖了**全部 6 个子命令**（excel-read/write、word-read/write、pdf-extract/merge），无遗漏；移除兜底后，技能内 `references/…`/`scripts/…` 型交付物（如 v2.4.4/v2.5.0/v2.5.1 计划）在 `--base <技能目录>` 下仍由 `base/p` 解析通过，未见误伤。

## 9. 我不确定的地方

1. **作者"修复前 18/19 FAIL=1"的来源**：可能取自某个瞬时工作区（例如 `UI组件库搭建.yaml` 已生成而某引用尚未落地的那一刻），但我无法重建该瞬间；我以 `git archive HEAD` 重建的 v2.5.0 旧树为准，得 18/18 FAIL=0。若作者能给出当时的完整命令与工作区快照，我愿意复核。
2. **R5 的执行方式**：场景 (b)/(c) 我用"提取真实脚本片段后点源执行"的等价实现（因 `Invoke-Expression` 被拦、且为免 pip 副作用），而非整脚本真跑；场景 (a) 是整脚本真跑。等价实现提取的是 `setup_env.ps1` 原文而非重写，但严格意义上仍非"同一个进程跑同一个文件"。
3. **`meta.工作区根` 为空导致 v2.4.3 计划 FAIL=1**：我判定为历史遗留（旧版同样 FAIL），但未核实 v2.4.3 当时是否本就无此要求、以及是否应回填。
4. **ops.md 中 v2.5.1 之前的历史条目**：未逐条复算（本次审核范围为 v2.5.1 改动）。
5. **`run_stage.ps1:30-34` 的判据**：逻辑上"调用后 `$LASTEXITCODE` 仍为 `$null` ⇒ 判失败"依赖"调用原生程序必然写 `$LASTEXITCODE`"。若某宿主既执行了子进程又因别处调用污染了 `$LASTEXITCODE`，可能误判方向相反（我只是提出，未构造出可复现样例）。

---

### 附：审核者抽查的引证/改动（≥6 处，逐条）

| # | 引证（文件:行 + 原文） | 相符/不符 |
| --- | --- | --- |
| 1 | `scripts/checks.py:167` `return [base / p, Path.home() / ".workbuddy" / p, base.parent / p]`（兜底已删） | 相符（与"彻底移除"一致） |
| 2 | `references/ops.md:147` "……收窄为仅 `references/`／`scripts/`／`assets/` 开头……（新增常量 `SKILL_INTERNAL_PREFIXES`）" | **不符**（代码无此实现/常量） |
| 3 | `scripts/setup_env.ps1:41` `$pkgDir = @{ … "duckdb"="duckdb"; "ast-grep-cli"="ast_grep_cli*.dist-info" }` | 相符 |
| 4 | `SKILL.md:59` 红线①"禁止执行**处理类**脚本……只读检查不受限" | 相符 |
| 5 | `SKILL.md:114` 独立审核第 8 条批次规则（≤5 文件、不得以合并减少覆盖面） | 相符 |
| 6 | `assets/report-template.md:14` `\| **工作区根** \| {……红线③依据……} \|` | 相符 |
| 7 | `scripts/data_query.py:140-142` `[提示]` 改走 stdout | 相符 |
| 8 | 回归证据 1 括注"修复前 18/19 FAIL=1" | **不符**（实测旧树 18/18 FAIL=0） |
