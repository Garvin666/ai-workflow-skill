# 复核意见（v2.5.1 返修复查）

> 复核者：独立审核子代理（第二轮／返修复查，未参与本次修复）
> 方法：以代码/二进制为准，不采信 `ops.md` 变更日志与 `回归证据.txt` 自述；R5/R6 亲手复现；对 v2.5.0 旧版（`git HEAD` = `e89a005`）做对照。
> 复核环境：Windows + Git Bash；脚本一律用 `C:\Users\26717\.workbuddy\binaries\python\envs\ai-workflow\Scripts\python.exe`（实测 3.13.14）。
> 实验落盘：`%TEMP%\r6rev\`（`plan.yaml`、`new.txt`、`old.txt`、`checks_old.py`）、`%TEMP%\r5rev\`（`r5_review.ps1`、`report.txt`）。**被审文件一律未改动**（R5 的改名实验只在 venv 内临时改名并已复原）。

## 1. 复核结论（通过 / 不通过 —— 一句话）

**不通过** —— 两处真缺陷 R5/R6 经我亲手复现确认关闭（R6 旧版 7/7 假 PASS → 新版 7/8 FAIL=1；R5 未映射项、duckdb 缺失、ast-grep.exe 缺失三项均真报 FAIL 且已复原），5 条回归中 N2/N3/N5 已解决、N1/N4 仅部分解决；**核心未收口点是 N1 的同类残留仍在：`references/ops.md:81` 仍写"skill 内相对路径会逐技能目录尝试"，与同文件 `:110`／`:149` 及 `checks.py:156-166` 的"彻底移除跨技能兜底"直接矛盾**——这正是上一轮判不通过的那一类"文档描述了不存在的实现"。另 `ops.md:155` 称"排障表 +6 行"实测为 +8 行。

## 2. 5 条逐条回归

| 编号 | 判定 | 证据（文件:行 + 原文） |
| --- | --- | --- |
| **N1** 严重 | **部分解决** | **已修（引证两处）**：`references/ops.md:149` 现写「修复：**彻底移除跨技能兜底**——`_candidate_paths()` 现只在「本任务基准／`~/.workbuddy`／基准的上级」三处解析」，并如实补记「⚠️ **实现过程如实记录**：先试过"收窄为仅 `references/`／`scripts/`／`assets/` 开头才兜底"，但负向测试**仍报 PASS**……该中间方案作废、对应常量已删除」；`references/ops.md:110` 亦改为「v2.5.1 已**彻底移除跨技能兜底**」。`grep -rn SKILL_INTERNAL_PREFIXES` 全库仅命中 `tasks/.../独立审核意见.md`（历史审核文档），**技能文档内已无该常量**。<br>**未收口**：`references/ops.md:81`（该行本次未改动）仍写「\| `checks.py plan` 报交付物缺失 \| 检查路径是否写全……；**skill 内相对路径会逐技能目录尝试** \|」——这句描述的正是已被删除的跨技能兜底。同一文件相隔 29 行自相矛盾。详见 §8-NP1。 |
| **N2** 中 | **已解决** | `tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/回归证据.txt:5-9` 已改写为「说明（如实更正）：自检项数由 18 → 19 是因为**评估期间新增了模板 UI组件库搭建.yaml**……v2.5.0 发布态本身是 18/18 FAIL=0。修复过程中曾出现 18/19 FAIL=1，那是**本次给 SKILL.md 写入示例文件名引用后**触发的告警（`_示例-数据分析.yaml` 被引用完整性校验当成引用且解析不到），属本次自身引入、已修。—— 该结论经独立审核者以旧树对照实测后更正，原稿"修复前 18/19 FAIL=1"的表述不准确。」<br>**独立核验**：`git ls-tree -r HEAD --name-only \| grep assets/templates/` 在 `v2.5.0`（HEAD）下为 **7 个模板**（不含 README）→ 1+3+1+7+1+5 = **18 项**，与"发布态 18/18"吻合；工作区现有 8 个模板（新增 `UI组件库搭建.yaml`）→ **19 项**。机制亦成立：导入 `checks.py` 实测 `_resolve('_示例-数据分析.yaml')` → `None`（`REF_PLACEHOLDER` 不跳过它），而 `_resolve('assets/templates/_示例-数据分析.yaml')` 解析成功 → 裸文件名确会触发引用完整性 FAIL。 |
| **N3** 轻 | **已解决** | `scripts/checks.py` 现第 42 行为 `DEFAULT_SKILL_DIR = Path.home() / ".workbuddy" / "skills" / "ai-workflow"`，**`SKILLS_ROOT` 已删除**；`grep -rn SKILLS_ROOT` 在 `scripts/*.py` 命中 **0 处**（命中仅：`_backup-v2.3/scripts/checks.py:43`、`tmp/checks_old_v250.py`、以及历史审核/证据文档）。`checks.py:161-165` 仅以自然语言注释说明历史，未再引用该常量。**无残留引用、无断链**。 |
| **N4** 轻 | **部分解决** | **不实宣称已消除**：`references/ops.md:155` 现如实写「阶段 4 明确「非 Office 交付跳过本阶段」+ **阶段 5 交付前自检处也给出** `assets/report-template.md` 指针」。**指针确已落地**：`SKILL.md:137`「……**报告类交付物的结构规范见 `assets/report-template.md`**（不要因为任务跳过阶段 4 就丢掉该模板）」。<br>**但**：`可优化方向.md:53` 的 P1-9 原文要求是「报告模板指针在**阶段 3 交付物处**也出现」；`git diff HEAD -- SKILL.md` 显示新增指针只落在阶段 5 第 4 条，`SKILL.md:81-91`（阶段 3）内**仍无**该指针。位置与原要求不符（意图已覆盖，见 §9-2）。 |
| **N5** 观察 | **已解决** | `scripts/setup_env.ps1:46` 现为 `$cliDir = @{ "ast-grep-cli" = "Scripts\ast-grep.exe" }`——已按**可执行文件**判定，不再用 `ast_grep_cli*.dist-info`；`:44-45` 注释说明理由（"the executable is a stronger marker than its dist-info"）。**实证**：我在 R5 场景 (d) 临时改名 `Scripts\ast-grep.exe` 后，该项真报 `[FAIL] ast-grep-cli not found: ...\Scripts\ast-grep.exe`（见 §4-(d)），证明检查的是可执行文件本体而非安装标记。 |

### 2.1 抽查引证（≥6 处，逐条）

| # | 引证（文件:行 + 原文） | 相符/不符 |
| --- | --- | --- |
| 1 | `scripts/checks.py:166` `return [base / p, Path.home() / ".workbuddy" / p, base.parent / p]`（兜底已删，仅三处候选） | **相符** |
| 2 | `scripts/checks.py:161-165` 注释「交付物只允许在「本任务基准 / 用户主目录 / 基准的上级」内解析。」+ 记明历史假 PASS | **相符** |
| 3 | `scripts/setup_env.ps1:50-51` `if (-not [string]::IsNullOrEmpty($pkgDir[$d]))     { $marker = Join-Path $sitePkgs $pkgDir[$d] }` / `elseif (-not [string]::IsNullOrEmpty($cliDir[$d])) { $marker = Join-Path $venvDir  $cliDir[$d] }` | **相符** |
| 4 | `references/ops.md:81` 「……skill 内相对路径会**逐技能目录尝试**」 | **不符**（与 `:110`/`:149` 及代码矛盾，见 §8-NP1） |
| 5 | `references/ops.md:149` 「修复：**彻底移除跨技能兜底**……该中间方案作废、对应常量已删除」 | **相符** |
| 6 | `SKILL.md:137` 「**报告类交付物的结构规范见 `assets/report-template.md`**（不要因为任务跳过阶段 4 就丢掉该模板）」 | **相符**（但位于阶段 5，非阶段 3） |
| 7 | `scripts/run_stage.ps1:27-34` `$global:LASTEXITCODE = $null` → `& $venvPython $target @ScriptArgs` → `if ($null -eq $code) { Write-Host "[FAIL] child produced no exit code …"; exit 1 }` | **相符** |
| 8 | `scripts/office_io.py:266-273` `_NEED = { "excel-read"…, "excel-write"…, "word-read"…, "word-write"…, "pdf-extract"…, "pdf-merge"… }`（6 子命令全覆盖） | **相符** |
| 9 | `scripts/data_query.py:141` `print("\n[提示] 结构化(JSON/CSV/Parquet)优先用 `sql` 子命令直查；…"`（无 `file=sys.stderr` → 走 stdout） | **相符** |

## 3. R6 独立复现（含旧版对照）

**夹具**：`%TEMP%\r6rev\plan.yaml`——**目录内只有 `plan.yaml`，无 `references/`**；步骤 `状态: 完成`、`交付物: "references/quality-gates.md"`。
**命令**：`checks.py plan <夹具>/plan.yaml --base "C:\Users\26717\AppData\Local\Temp\r6rev"`（**字面 Windows 路径**，未用 `$(pwd)`）。

**新版（工作区 v2.5.1）** —— `EXIT=1`：
```
=== ai-workflow 计划对账 ===
[ OK ] plan.yaml 存在 — C:\Users\26717\AppData\Local\Temp\r6rev\plan.yaml
[ OK ] plan.yaml 可解析
[ OK ] meta.任务 / meta.验证信号 / meta.工作区根
[ OK ] steps 非空 — 1 步
[FAIL] 步骤 1 交付物缺失 — references/quality-gates.md
[ OK ] Anti-drop 对账 — 已完成步骤 1 个，交付物确认 0 个
=== 结果：7/8 通过，FAIL=1 ===
```

**旧版对照（`git show HEAD:scripts/checks.py`，v2.5.0）** —— 同夹具、同命令、`EXIT=0`：
```
[ OK ] Anti-drop 对账 — 已完成步骤 1 个，交付物确认 1 个
=== 结果：7/7 通过，FAIL=0 ===
```
旧实现的相关行：`tmp/checks_old.py:43` `SKILLS_ROOT = Path.home() / ".workbuddy" / "skills"`；`:164-165` `if (SKILLS_ROOT.exists()): cands.extend(sd / p for sd in SKILLS_ROOT.glob("*/"))`——把工作区里不存在的交付物用**别的技能目录的同名文件**"救活"，故为**假 PASS**。

→ **R6 确证关闭**：新版正确报 `[FAIL]`，旧版假 PASS。（与作者回归 2 的 7/8、FAIL=1 一致。）

## 4. R5 独立复现（i/ii/iii 三项 + 复原确认）

**静态核对（`$deps`/`$pkgDir`/`$cliDir` 覆盖与歧义）**：`$deps`（`setup_env.ps1:8`）7 项；`$pkgDir`（`:42`）6 项；`$cliDir`（`:46`）1 项。实测：**并集 == `$deps`（完全覆盖，True），`pkgDir ∩ cliDir == ∅`（无重复）**；`:50-51` 用 `if / elseif` 分支，同一依赖**不可能被两表同时判定**，无歧义。未映射项走 `:56-59` → `$failedList += $d` → `:68 if ($failedList.Count -gt 0) { exit 1 }`。

**执行方式说明**：为避免 pip 网络副作用、且本通道可能吞 stdout，我用「**从真实 `setup_env.ps1` 原文提取 `$deps`/`$pkgDir`/`$cliDir` 定义 + 第 4 步循环体（`setup_env.ps1:47` 起 20 行，去掉 `exit 1`），用 `[scriptblock]::Create()` 执行**」的等价实现（提取的是脚本原文，非重写）。**结果（`%TEMP%\r5rev\report.txt`）**：

- **(a) 正常清单** → `R5FAILED=0`，输出 `[ OK ] requests | openpyxl | python-docx | pypdf | pyyaml | duckdb | ast-grep-cli`。
- **(i) 注入未映射假依赖** `definitely-not-real-xyz`（`$deps` 改为 8 项）→ `R5FAILED=1::definitely-not-real-xyz`，输出含 `[FAIL] definitely-not-real-xyz has no marker in pkgDir/cliDir (add one; silent pass is forbidden)`。**不再静默通过。**
- **(ii) 把 venv `Lib\site-packages\duckdb` 临时改名**（`-> duckdb__r5bak`）→ `R5FAILED=1::duckdb`，输出 `[FAIL] duckdb not found: C:\Users\26717\.workbuddy\binaries\python\envs\ai-workflow\Lib\site-packages\duckdb`；**finally 复原后** `duckdb exists = True ; bak = False`。
- **(iii) 把 venv `Scripts\ast-grep.exe` 临时改名**（`-> ast-grep.exe.r5bak`）→ `R5FAILED=1::ast-grep-cli`，输出 `[FAIL] ast-grep-cli not found: ...\Scripts\ast-grep.exe`；**finally 复原后** `ast-grep.exe exists = True ; bak = False`。

**复原二次独立确认**（Bash，另起进程）：`site-packages/duckdb` 存在、`duckdb__r5bak` 不存在；`Scripts/ast-grep.exe` 存在（51400704 字节）、`.r5bak` 不存在；`python -c "import duckdb,requests,openpyxl,docx,pypdf,yaml"` → `import OK, duckdb 1.5.5`；`ast-grep.exe --version` → `ast-grep 0.45.3`。**venv 已完整复原。**

→ **R5 确证关闭**（三项负向均真报 FAIL，未见误判/歧义）。

## 5. 原有信号回归（实测输出）

| 命令 | 实测 |
| --- | --- |
| `checks.py skill --skill-dir <技能目录>` | `=== 结果：19/19 通过，FAIL=0 ===`，`EXIT=0`；含「文档引用完整性 — 检查 25 条引用」「8 个模板 schema 各 9/9」「5 个 py_compile OK」 |
| `plan tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/plan.yaml --base "C:\Users\26717\.workbuddy\skills\ai-workflow"` | `7/7 通过，FAIL=0`，`EXIT=0`（已完成步骤 6、交付物确认 6） |
| `plan tasks/体验评估-ai-workflow-v2.5.0-2026-09-10/plan.yaml --base <同上>` | `7/7 通过，FAIL=0`，`EXIT=0` |
| `plan tasks/技能增强-ai-workflow-v2.4.4-2026-09-10/plan.yaml --base <同上>` | `7/7 通过，FAIL=0`，`EXIT=0` |

→ **无回归**；移除跨技能兜底后，技能内部相对路径交付物（三份存量计划）在 `--base <技能目录>` 下仍由 `base/p` 正常解析，未见误伤。

## 6. 硬约束检查（.ps1 非 ASCII 字节数）

逐字节统计（Python 以 `rb` 读入，计 `>127` 的字节）：

| 文件 | 总字节 | 非 ASCII 字节数 |
| --- | --- | --- |
| `scripts/setup_env.ps1` | 3565 | **0** ✅ |
| `scripts/run_stage.ps1` | 2008 | **0** ✅ |

两者均为**纯 ASCII**，满足"含中文会因 GBK 误读静默失败"的硬约束。

## 7. 作者回归证据 vs 实测 对照表

| 作者自述（`回归证据.txt`） | 我的实测 | 判定 |
| --- | --- | --- |
| 回归 1：`checks.py skill` 19/19 通过 FAIL=0；18→19 源于评估期新增 `UI组件库搭建.yaml`；v2.5.0 发布态为 18/18 FAIL=0 | 19/19 FAIL=0 ✅；HEAD 下模板 7 个 → 计数 18 ✅（新增 1 个 → 19） | **一致** |
| 回归 2：R6 负向 `7/8 通过，FAIL=1`（修复前 7/7 FAIL=0） | 复现 `7/8 FAIL=1`；旧版真跑 `7/7 FAIL=0` | **一致** |
| 回归 3：两存量计划均 `7/7 FAIL=0` | v2.5.1、v2.5.0 均 `7/7 FAIL=0` | **一致** |
| 回归 4：(a) 失败 0；(b) 失败 1；(c) 含 duckdb 且复原 True | (a) 0；(b) `definitely-not-real-xyz` FAIL；(c) `duckdb` FAIL，复原 True | **一致** |
| 回归 5：`$pkgDir` 6 项 + `$cliDir` 1 项 + 7 依赖全覆盖；(a)0 (b)1 (c)duckdb (d)ast-grep-cli，复原均 True | 静态：6+1 完全覆盖、无交集 ✅；四项结果与复原均一致 ✅ | **结论一致**；但转录格式与真实脚本输出不符（见 §8-NP4）；`:59` 算式"7 + 6 + 1"表述不当 |
| 回归 6：`grep SKILLS_ROOT in scripts/*.py` → 0 处（常量已删，注释保留说明历史） | `scripts/*.py` 内 0 处 ✅；注释在 `checks.py:161-165` 仅述历史、不引用常量 | **一致** |
| 回归 7：v2.5.1 / v2.5.0 / v2.4.4 三计划均 `7/7 FAIL=0` | 三者均 `7/7 FAIL=0` | **一致** |

→ 7 组结论主体与实测一致；差异仅在**转录保真度**与**算式表述**（不影响结论）。

## 8. 返修引入的新问题

- **NP1（中，N1 同类：文档 vs 代码矛盾，仍未清）**：`references/ops.md:81` 故障排查行仍写「……**skill 内相对路径会逐技能目录尝试**」。本次修复已把跨技能兜底**彻底移除**（`checks.py:156-166`），同文件 `:110`／`:149` 也明说"彻底移除、只在三处解析"。该行是本次行为的直接反面描述，会误导用户在"交付物缺失"时去找并不存在的兜底。**根因**：返修只改了被点名的 `:147`／`:108` 两处，未全库检索同类表述；`git diff HEAD -- references/ops.md` 显示该行未被触碰。
- **NP2（轻，计数不实）**：`references/ops.md:155` 称「排障表 **+6 行**（ps1 静默 no-op、Bash 拒调 ps1、excel-write CSV、status 无 `--workspace`、依赖自检假 OK、对账假 PASS）」。`git diff HEAD -- references/ops.md` 实测新增 **8 行**——除上述 6 项外，另有 `:108`（`--base` 传 POSIX 路径 → 假 FAIL）与 `:109`（`meta.工作区根` 为空 → 历史遗留）。清单与数字均漏列 2 项。
- **NP3（轻，任务计划措辞未同步）**：`tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/plan.yaml:25` 步骤 3 仍写「checks.py 跨技能兜底**收窄**」——"收窄"正是已被作废的中间方案（最终为"彻底移除"）。属本任务留存的计划文本，影响小，但与 `ops.md:149` 的最终口径不一致。
- **NP4（观察，转录保真度）**：`回归证据.txt:26-53`（回归 4）、`:60-98`（回归 5）的输出形如 `[ OK ] requests  (requests)` / `[ OK ] duckdb  -> C:\...\duckdb`，而**真实 `setup_env.ps1` 只打印 `[ OK ] $d`**（无括号别名、无 `-> 路径`）。可见这些"原始输出"来自等价 harness 的转录，而非脚本原样 stdout；结论正确，但"原始输出"的标注不够精确。另 `:59` 的"7 + 6 + 1 = 全覆盖"算式把"依赖数"与"标记数"相加，表述不当（正确说法应为"两表键的并集 = `$deps` 全集"）。
- **NP5（观察，历史遗留，非本次引入）**：`SKILL.md:22` 环境预检仍只列 `requests/openpyxl/python-docx/pypdf/pyyaml` 5 个依赖，缺 `duckdb` / `ast-grep-cli`，与 `ops.md:68` 的 7 项全量不一致。

**未发现的其他新断裂**：删 `SKILLS_ROOT` 后无残留引用/断链（§2-N3）；`$deps`/`$pkgDir`/`$cliDir` 三者完全覆盖且无重复判定歧义（§4 静态核对）；`SKILL.md` 与 `references/*.md` 的 25 条文档引用经 `checks.py skill` 校验**全部可解析**，无引用不存在的文件；抽查 9 处引证仅 1 处不符（§2.1-#4，即 NP1）。

## 9. 我不确定的地方

1. **N2 的中间态"18/19 FAIL=1"无法重建**：工作区未提交该瞬间，我只能验证"裸文件名 `_示例-数据分析.yaml` 会被引用完整性判 FAIL"这一机制成立，无法观察到作者当时的现场。作者更正后的结论与我的实测一致，故按已解决处理。
2. **N4 的位置取舍**：`可优化方向.md:53` 字面要求"阶段 **3**"，作者落在阶段 **5** 且如实注明。若认定"只要跳过阶段 4 的任务能看到模板"即为达标，则 N4 可算已解决；我按**字面要求**从严判为部分解决，请以你的口径为准。
3. **R5 的执行方式**：我未整脚本真跑（避免 pip 副作用与网络噪声），而是从 `setup_env.ps1` **原文**提取定义与循环体后以 `[scriptblock]::Create()` 执行（等价实现）。改名实验 (ii)/(iii) 是对 venv 的**真实改动**，已 `finally` 复原并二次确认。
4. **NP3 的定性**：`plan.yaml` 是被审对象之一，但其步骤 3 描述属"计划先行"的历史文本；我未把它计入 N1 正文，仅列为轻问题，是否要求同步取决于你是否把任务计划也纳入"自述一致性"范围。
5. **`ops.md` 中 v2.5.1 之前的历史条目**：未逐条复算（本次复核范围为 v2.5.1 改动与上一轮 5 条）。
