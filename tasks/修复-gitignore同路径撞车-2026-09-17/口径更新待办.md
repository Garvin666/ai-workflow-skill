# 口径更新待办 · `.gitignore` 撞车已修，但 `push-routing.md` 尚未同步

> 日期：2026-09-17 ｜ 任务：`tasks/修复-gitignore同路径撞车-2026-09-17/`
> **性质：未闭环项登记（不是遗漏）。** 本次**未执行**该更新，理由是纪律而非疏忽——见下方「为什么本次不做」。

## 一、待更新的位置（精确到行）

**文件**：`references/push-routing.md`
**区段**：第 **169–174 行**，小标题为「**限制一：`.gitignore` 在同一远端路径上撞车（未解决，待拍板）**」

**现状原文（169–174 行）**：

```
**限制一：`.gitignore` 在同一远端路径上撞车（未解决，待拍板）**。两条本地来源各有自己的 `.gitignore`，
且**策略相反** —— 技能仓要忽略 `tasks/*/plan.yaml`、`tasks/*/tmp/`，工作区仓要**跟踪**它的 `tasks/*/plan.yaml`。
远端只存一份 → **后推者覆盖先推者**，而另一侧的判据 2 恒 FAIL（本轮实测：本地 `5138ffeea1` ≠ 远端 `3d53038d4d`；
远端当前为技能侧版本，未被覆盖）。
**现行约束：工作区通道的改动面不得包含 `.gitignore`** —— 否则会静默覆盖技能侧的忽略策略。
候选修法（**属高风险，须先确认，勿自行选**）：① 两侧共用同一份合并内容；② 工作区侧改用 `.git/info/exclude`（本地专用、不入库）放弃该路径主张 —— 但删远端路径不可逆；③ 验收器引入「共享路径」概念，对该集合断言「远端 = 两侧之一的版本」而非「= 本侧版本」（把不可满足的断言换成仍能抓第三方篡改的断言）。
```

**问题**：标题的「未解决，待拍板」与「候选修法…勿自行选」**已与事实不符**——2026-09-17 已选定并落地修法 2，且经独立验收机器验证 FAIL 归零。

## 二、建议替换文本（保留原表述痕迹，不静默改写）

```markdown
**限制一：`.gitignore` 在同一远端路径上撞车（2026-09-17 已按修法 2 解决；原文写「未解决，待拍板」）**。
两条本地来源各有自己的 `.gitignore`，且**策略相反** —— 技能仓要忽略 `tasks/*/plan.yaml`、`tasks/*/tmp/`，
工作区仓要**跟踪**它的 `tasks/*/plan.yaml`。远端只存一份 → **后推者覆盖先推者**，而另一侧的判据 2 恒 FAIL
（实测：本地 `5138ffeea1` ≠ 远端 `3d53038d4d`）。

**已落地修法（2026-09-17，修法 2：工作区侧弃权）**：工作区仓 `git rm --cached .gitignore` 并本地提交
（`7011bf0` → `8fb80fc`），原有忽略规则**逐条迁入 `.git/info/exclude`**（含 `.gitignore` 自身）；
`.gitignore` 文件保留在磁盘上（非破坏性），仅退出跟踪。
**效果（独立验收）**：通道 B 判据 2 由 `FAIL 1 → FAIL 0`；阴性对照——同一验收器对修复前 rev `7011bf0`
仍判 `[FAIL] 不一致 1 项`、对修复后 `8fb80fc` 判 `[OK] 不一致 0 项`。
**残留约束**：① 不要恢复工作区侧 `.gitignore` 的跟踪（一旦恢复，撞车与 FAIL 复发）；
② 不要在工作区仓的 `.git/info/exclude` 加入 `tasks/*/plan.yaml`（该仓对该路径是**跟踪**语义）；
③ 忽略语义自此为工作区**本地私有**、不再随仓分发 —— 这是本修法的已知代价。

候选修法（原文三条，**本次未采用，保留供回溯**）：① 两侧共用同一份合并内容（抗漂移弱，且会使工作区仓
新增 `plan.yaml` 不再被跟踪）；② ~~工作区侧改用 `.git/info/exclude` 放弃该路径主张~~ ← **本次采用**；
③ 验收器引入「共享路径」概念（只让门禁接受现状，不除根，与 Ledger 2026-09-16「不得靠放松判据掩盖」相悖）。

证据：`tasks/修复-gitignore同路径撞车-2026-09-17/`（`通道B-验收.txt`、`tmp/修复前快照.txt`）。
```

## 三、为什么本次不做（纪律，不是遗漏）

1. `references/push-routing.md` 属**技能本体**，改动它会使技能仓工作树变脏；
2. 技能仓刚在 v3.4.0 完成「远端与本地一致」的收尾，**变脏后必须再推一次才能维持该一致性**；
3. 本任务受**「不推送远端」**约束（用户 2026-09-17 明确）；
4. 且 Ledger 已立**「自证链条到此终止」**规则：最后一次「证据+履历」提交的验收不再回写，不递归。

→ 因此本次只**登记**、不执行。执行时机由用户决定（下一次允许推送时，随该次推送一并完成）。
**在更新之前，`push-routing.md` 该节描述的是过时状态——引用它时须注意。**

## 四、连带发现（非本任务范围，如实登记）

工作区仓 `E:/ChatGPT/工作流` 有 **3 项未提交产出**（来自 2026-09-17 的「ai-workflow 工作流完整评估」任务）：
`Ledger.md`、`评估-ai-workflow工作流-2026-09-17.md`、`tasks/评估-ai-workflow工作流-2026-09-17/`。
本次验收因此出现 `[WARN] 工作区无未提交改动 —— 3 项未提交`（已用 `--allow-dirty` 放行）。
**影响**：`push_router.py` 有「工作区干净」门禁，该 3 项会使下一次工作区通道推送被阻塞。
**处置建议**：由用户决定是否将这 3 项纳入工作区档案提交——本次**未动**（超出授权范围）。

## 五、其他未闭环项（同类：都因「不推送」约束而留待）

### 5.1 技能根任务目录超阈值，归档未执行

`checks.py status` 报 **13 个任务目录 > 阈值 10**，建议归档最旧的若干个。
**未执行的理由**：`tasks/*/plan.yaml` 虽被忽略，但**证据文件（`.txt`/`.py`）是跟踪状态** —— 归档会移动它们，
使技能仓工作树变脏，进而需要一次提交+推送才能维持「远端与本地一致」。与本文件 §三 同一约束。
**执行方式**（下次允许推送时）：`python scripts/archive_tasks.py --dirs <目录名…>`（默认 dry-run，看计划后加 `--apply`）。
该脚本带前置门禁：被 `SKILL.md`／`references/*.md` 引用的目录会被自动剔除并给出引用出处。

### 5.2 「Anti-drop 只核对已完成步骤」这一门禁盲区尚未写进反模式清单

**本次实测发现的盲区**：`checks.py plan` 的 Anti-drop 对账只遍历**状态为「完成」的步骤**。
v3.4.0 任务当时报 `FAIL=0`，原文是「已完成步骤 **6** 个，交付物确认 **6** 个」——两个坏路径
（`远端推送验收.txt`／`远端推送独立验收.txt`，**均不存在**）**完全没被核对**；回写状态后才覆盖到 8 步。
**阴性对照**：只回写状态、不订正路径 → `12/14 通过，FAIL=2`、`RC=1`。

**建议动作**：把这条写进 `references/quality-gates.md` 的反模式清单（SKILL.md 阶段 6 第 4 条的「坑 → 反模式清单」分流）。
**建议措辞**：

> **反模式：把 `FAIL=0` 当成全覆盖结论。** `checks.py plan` 的 Anti-drop 只核对**已完成**的步骤——
> 未完成的步骤连门禁一起逃逸。**读 `FAIL=0` 时必须连带读「已完成 N 个」这一行**，并与 `steps` 总数比对；
> 两者不等时，`FAIL=0` 只证明"已做的那部分没问题"，不证明"全部没问题"。

**未执行的理由**：同 §三（改 `references/` 会使技能仓变脏，需一次推送才能闭环）。

---

## 六、✅ 已执行（2026-09-17，v3.5.0 任务）

**执行者**：`tasks/技能增强-ai-workflow-v3.5.0-2026-09-17/`（步骤 7 / P2-3）。
**动作**：`references/push-routing.md` 的「限制一」整节已按本文件 §二 的建议重写。

**与 §二 建议的一处偏离（据实说明，非静默）**：§二 建议在标题里**内联保留**旧表述
（`（…已按修法 2 解决；原文写「未解决，待拍板」）`），但本步的验收口径要求
「该节不再出现该表述（**Grep 命中 0**）」。二者不可同时满足。
**采用的折中**：标题只写现状；追溯改为一句「**本节此前的状态标注已作废**，原文见 git 历史与本文件
§二 的逐字引用」——即**明示改动 + 指向原文出处**，既满足 `Grep=0`，也不构成静默改写。
旧表述的逐字原文仍完整保留在本文件 §一/§二，未丢失。

**补做的取证（§二 原文缺证据的一处）**：§二 声称「阴性对照：同一验收器对修复前 rev `7011bf0`
仍判 `[FAIL] 不一致 1 项`」，但 `通道B-验收.txt` 与 `tmp/修复前快照.txt` 里**只有修复后的全绿结果**，
没有这条阴性对照的原始输出。已用**本地确定性复核**补齐（不依赖网络）：

```
git rev-parse 7011bf0:.gitignore   → 5138ffeea1acf62af256239c643f4b586e2baab4   （≠ 远端 3d53038d4d）
git ls-tree -r 7011bf0 --name-only | grep -c "^\.gitignore$"   → 1
git ls-tree -r 8fb80fc --name-only | grep -c "^\.gitignore$"   → 0
git ls-tree -r 7011bf0 / 8fb80fc --name-only | wc -l           → 73 / 72
```

即：修复前树中确有 1 项 `.gitignore` 且其 blob sha 与远端不等 → 判据 2 必报 1 项；
修复后该项消失 → 判据 2 报 0 项。**结论与 §二 一致，但现在是可复现的**。
