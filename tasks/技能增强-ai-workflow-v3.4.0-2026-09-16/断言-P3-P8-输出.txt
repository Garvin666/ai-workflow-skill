=== P3 / P8 功能断言（新版 vs v3.3.0 旧版，同输入对照）===
[INFO] 缓存命中（6101 字节，TTL 3600s）：https://api.github.com/repos/psf/requests
[INFO] 缓存命中（6101 字节，TTL 3600s）：https://api.github.com/repos/psf/requests
  新版：C:\Users\26717\.workbuddy\skills\ai-workflow\scripts\http_fetch.py
  旧版：C:\Users\26717\.workbuddy\skills\ai-workflow\_backup-v3.3.0\scripts\http_fetch.v3.3.0.py

  前置：https://api.github.com/repos/psf/requests 已有有效缓存（TTL 3600s）

-- 断言 1／P3：缓存命中不得解析凭据（新版 main 真实入口）--
  [PASS] 新版 --github-repo 走缓存命中且退出码 0  —— rc=0，输出含『缓存命中』=True
  [PASS] 新版 _resolve_token 调用次数 == 0（P3 的直接判据）  —— 实际 0 次

-- 断言 2／P3 阴性对照：v3.3.0 旧版在同一输入下必然解析凭据 --
  [PASS] 旧版同样走缓存命中且退出码 0  —— rc=0，输出含『缓存命中』=True
  [PASS] 旧版 _resolve_token 调用次数 >= 1（证明改前确实白付这笔）  —— 实际 1 次  ← 这就是 P3 要消除的成本

-- 断言 3／P8：桶限额按「有无凭据」分档（新版 github_repo_metrics）--
  [PASS] 无凭据 → 匿名桶上限  —— throttle=('gh_core', 60, 3600)（期望 gh_core/60/3600）
  [PASS] 有凭据 → 认证桶上限（不再自限在匿名档）  —— throttle=('gh_core', 4500, 3600)（期望 gh_core/4500/3600）
  [PASS] 两档确有差异（不是把常量改了个名）  —— 4500 vs 60

-- 断言 4／P8 阴性对照：v3.3.0 旧版与凭据无关，恒按 60/h 自限 --
  [PASS] 旧版无条件按 gh_core/60/h 计数（含缓存命中）  —— 调用 1 次，首次实参=('gh_core', 60, 3600)

-- 断言 5／P8：新版 fetch 的节流只在缓存未命中之后（缓存命中 = 0 次）--
  [PASS] 新版缓存命中的 fetch 不进入限流桶  —— 返回 6101 字节，_rl_throttle 调用 0 次

-- 断言 6／P8：`--github-code-search --dry-run` 不发请求也不解析凭据 --
  [PASS] dry-run 只打印 URL 不经凭据  —— rc=0，_resolve_token 0 次

=== 结果：10/10 通过，FAIL=0 ===
