# 独立审核意见（批 D · 返修复查）

- 审核对象：`scripts/checks.py`、`SKILL.md`、`assets/熔断报告模板.md`、`references/ops.md`
- 审核方式：有罪推定；只认亲手跑出的输出；解释器固定 `C:/Users/26717/.workbuddy/binaries/python/envs/ai-workflow/Scripts/python.exe`
- 自建夹具：`C:/Users/26717/.workbuddy/cache/ai-workflow/v2.5.2-fuse/review-d/review/`（未复用执行者夹具）
- 未修改任何被审文件（git 中 `M` 均为执行者 v2.5.2 既有改动；本会话仅读）

## 1. 结论

**不通过**：H1（复位链路）已真正闭合、L1/L2/L4 与全部不回归项均通过；但 **M1 标题级校验仍可被「代码块封装」绕过**（g2_b3 实测 `熔断报告必含标题节 — 5/5`），与原始「任意子串命中」属同类缺陷——机器门禁未真正 fail-closed。判据「任一条实质缺陷未闭合 → 不通过」触发。

---

## 2. 逐条判定（组 1–5）

### 组 1（H1 复位链路闭合）
- **1.1** `plan g1_fused` → `[FAIL] 已熔断：不得作为可交付物 — meta.熔断状态=已熔断；步骤 1 状态为『熔断』`；`结果 9/10 FAIL=1`；`EXIT=1` → **符合**（已熔断确实 FAIL）
- **1.2** 仅 `mark g1_s2/plan.yaml --fuse 正常` → 重跑仍 `[FAIL] 已熔断…步骤 1 状态为『熔断』`；`EXIT=1` → **符合**（只改 meta 不解锁，fail-closed）
- **1.3** 仅 `mark g1_s3/plan.yaml 1 完成` → 重跑仍 `[FAIL] 已熔断…meta.熔断状态=已熔断`；`EXIT=1` → **符合**
- **1.4** `mark g1_s4/plan.yaml 1 完成 --fuse 正常` → 重跑 `熔断状态 — 正常`；`结果 9/9 通过 FAIL=0`；`EXIT=0` → **符合**（两步都做才 PASS）
- **1.5** `grep -n fuse scripts/checks.py` → cmd_mark 支持（`334/338-339/353-357`），main 支持（`498-499/512`）；`yaml.safe_load` 解析 g1_s4 → 合法、`熔断状态='正常'`、出现 1 次、原始行 `'  熔断状态: 正常'`（2 空格缩进）→ **符合**（全程无需手改 YAML）
- **1.6** 对无 `熔断状态` 键的 g1_nokey 执行 `mark --fuse 已熔断` → `meta 键: ['任务','验证信号','工作区根','熔断状态']`、值 `已熔断`、出现 1 次、缩进 2 空格 → **符合**（正确插入 meta 块内、无重复键）
- **1.7** 三处文档复位命令一致性：`SKILL.md:194-196`、`熔断报告模板.md:58-59`、`ops.md:121` 均指向 `mark <plan> --fuse 正常` + `mark <plan> <id> <非熔断状态>`（模板第 3 条为「重跑 plan 确认清零」，措辞一致）→ **符合**，无矛盾

### 组 2（M1 标题级校验）
- **2.8** 报告五节名仅出现于正文行（g2_body）→ `[FAIL] 熔断报告必含标题节 — 缺标题 触发条件、已试路径、卡点根因、待决策选项、复位条件` → **符合**（原 body-only 绕过已封）
- **2.9** 五节均为标题行（g2_titles）→ `[ OK ] 熔断报告必含标题节 — 5/5` → **符合**
- **2.10** 缺「待决策选项」标题（g2_missing）→ `[FAIL] …缺标题 待决策选项` → **符合**（明指缺哪节）
- **2.11** 绕过尝试：
  - `## 复位条件xx`（g2_b1）→ `[ OK ] …5/5`：前缀匹配 `^#+\s*复位条件` 命中，**标题后跟多余字符不被拦截**（低危残留）
  - `### 触发条件`（g2_b2）→ `[ OK ] …5/5`：`#+` 含三级标题，属合理（仍为真标题）
  - **节名置于代码块内（g2_b3）→ `[ OK ] …5/5`：原始文本里的 `## 触发条件…` 被正则命中，门禁被绕过** → **不符合**（实质漏洞）

### 组 3（L1 status 可见性 + 回归）
- **3.12** `status --workspace ws_fused`（含 1 个已熔断任务）→ 表内 `fused_task …0 天 ⚡已熔断`；`[WARN] 1 个任务处于「已熔断」：fused_task`；`[ OK ] 任务总览 — 1 个任务目录，其中 1 个已熔断` → **符合**
- **3.13** `status --workspace ws_notasks`（无 tasks/）→ `EXIT=1`；`[FAIL] tasks/ 目录存在` 出现次数 = **1**（无重复打印）→ **符合**（回归无重复）
- **3.14** `status --workspace ws_normal`（无熔断）→ 输出含「已熔断」行数 = **0** → **符合**（不误报）

### 组 4（不回归）
- **4.15** `checks.py skill` → `=== 结果：19/19 通过，FAIL=0 ===`；`EXIT=0` → **符合**
- **4.16** 三份存量 plan（`--base`=技能目录）→ `10/10 FAIL=0` / `8/8 FAIL=0` / `8/8 FAIL=0` → **符合**
- **4.17** `python -m py_compile scripts/checks.py` → `py_compile OK`（无错；已清理 __pycache__）→ **符合**
- **4.18** 旧用法 `mark <plan> <id> <状态>`（无 `--fuse`）→ `[ OK ] 步骤状态已更新 … 待办 → 进行中`；`EXIT=0` → **符合**
- **4.19** mark 边界：①不给参数→`[FAIL] 参数…` EXIT=1；②只给 step_id→`[FAIL] 给出 <step_id> 时必须同时给出 <状态>` EXIT=1；③非法 `--fuse 暂停`→EXIT=2（argparse 拒绝）；④非法 status `暂停`→EXIT=2 → **符合**（均清晰失败，无崩溃/静默成功）

### 组 5（L2/L4 与对抗式）
- **5.20** `grep -rn "三态" .`（限定技能正文 SKILL.md/references/*.md/assets/*.md/scripts/checks.py/Ledger.md）：命中共 2 处且均在允许语境——`ops.md:163`（「工业界断路器三态…『复位』是一次迁移动作、不是第三个存储态」，同行即声明）；`SKILL.md:156`（「工业界断路器三态（Closed / Open / Half-Open）」设计依据，且 `SKILL.md:158-160` 显式「复位是一次迁移动作而非第三个存储态」）。**`scripts/checks.py` 现已零命中**（批 E 残留已修）。tasks/ 下历史评审文档的「三态」不在本次范围 → **符合**
- **5.21** `SKILL.md:200-201` → 「复位后只允许一次…⚠️ 该项为**纪律约束**，**不是机器判定**：`checks.py` 不记录复位次数…别把它当成已由工具保证」 → **符合**（明确标注非机器判定）
- **5.22** 对抗式边界（均 fail-closed）：
  - meta 块无其它键 + 单步熔断（g5a）→ 仍 `[FAIL] 已熔断…meta.熔断状态=已熔断；步骤 1 状态为『熔断』` → 符合
  - 两步骤都熔断（g5b）→ `[FAIL] …步骤 1、2 状态为『熔断』`（明指两者）→ 符合
  - 熔断报告空文件（g5c）→ `[FAIL] …缺标题 触发条件、已试路径、卡点根因、待决策选项、复位条件` → 符合
  - `熔断状态: 正常 `（尾随空格，g5d）→ `[ OK ] 熔断状态 — 正常`（strip 容错，方向安全：不会误放行熔断）→ 符合
  - 节名用英文（g5e）→ `[FAIL] …缺标题 触发条件、…、复位条件`（5 节全缺）→ 符合
  - 节名顺序错乱但齐全（g5f）→ `[ OK ] …5/5`（顺序无关，属合理）→ 符合
  - 空 meta 块 `mark --fuse 已熔断`（g5m）→ `[FAIL] meta.熔断状态 写入 — 未找到 meta: 块`；`EXIT=1`（优雅失败，非崩溃）→ 符合

---

## 3. 问题清单（按严重度）

**中（M1 残留 · 本次阻断项）**
- 🔴 现象：`checks.py plan` 的「熔断报告必含标题节」仍用 `re.search(rf"^#+\s*{re.escape(s)}", text, re.M)` 对**原始文本**做行匹配；将五节标题整体放入 fenced code block 内（g2_b3 实测），正则照常命中 → `[ OK ] 5/5`，门禁被绕过。与原始 M1「任意子串命中」是**同类缺陷**（都未做 markdown 结构解析，只扫原始文本）。
- 📍 实测行号：`scripts/checks.py:416`（`miss = [s … if not re.search(rf"^#+\s*{re.escape(s)}", text, re.M)]`）；输出 `g2_b3` → `熔断报告必含标题节 — 5/5`。
- 🎯 期望：校验应排除代码块（先剥离 ```` ``` ```` 围栏段再匹配），或至少要求节标题不在 code fence 内；否则「标题级校验」名不副实，仍可一行/一块绕过。

**低（M1 残留 · 次要）**
- 🔴 现象：`^#+\s*{节名}` 为**前缀**匹配，`## 复位条件xx`（节名后跟别字）也判存在（g2_b1 → `5/5`）。模板要求「五节标题必须**原样**出现」，实际只要以节名开头即过。
- 📍 实测行号：`scripts/checks.py:416`；输出 `g2_b1` → `5/5`。
- 🎯 期望：锚定行尾或整行（如 `^#+\s*{节名}\s*$` / 排除后续非标点字符），或在文档注明允许「节名 + 说明」的宽松口径。

---

## 4. 我无法确认的项

1. **熔断五步中「冻（未提交/未发布）」「报（升级用户）」** 无法由产出物独立核验——`checks.py` 只管机器门禁，是否真的停了自动化、是否真升级用户，需会话记录，不在本轮可跑证据内。
2. **F1/F2 触发链**（返修 2 轮 + 对抗式互审仍不通过）依赖 `tasks/对抗互审-ai-workflow-v2.5.1-2026-09-11/裁决.md` 等历史材料，未逐条回溯真伪。
3. **`ops.md:165` 声称的「156 个交付物 token 中 20 个转 FAIL」**：该统计基于 `E:\ChatGPT\工作流` 历史任务，超出本技能目录，未在我能力内复算（仅记录为待核）。
4. **回归证据的数字与「技能正确」的关系**：本论所有数字均为我亲手重跑所得，但「19/19」属自检项自洽、「8/8/10/10」为执行者自身计划，不做超出证据的推断。

---

## 5. 一句话汇报（≤200 字）

H1 复位链路**已真正闭合**（两步 `mark` 缺一不可、无需手改 YAML、键插入正确、三处文档一致）；L1/L2/L4 与不回归（skill 19/19、三存量 plan 全 0、py_compile、旧 mark、边界）**全部通过**。但 M1 标题级校验**未真正闭合**：用 fenced code block 包裹五节标题即可骗过 `^#+\s*{节名}`（g2_b3 实测 5/5），与原始「任意子串命中」同类——属**说修好但没修干净**。结论：**不通过**。剩余问题：中 1（代码块绕过）、低 1（前缀匹配过宽）。
