# 独立审核意见（批 E — 返修复查）

审核者：独立第三方（未参与 v2.5.2 执行，代号「批 E」）
日期：2026-09-11
被审对象：v2.5.2 对批 C（记录类 7 条）+ 批 A `L3` 的返修
- `tasks/技能增强-ai-workflow-v2.5.2-2026-09-11/基线-复现与回归风险.md`
- `tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/回归证据.txt`
- `tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/plan.yaml`
- `references/ops.md`、`scripts/checks.py`、`SKILL.md`、`Ledger.md`、`assets/plan-template.yaml`
解释器（全程）：`C:/Users/26717/.workbuddy/binaries/python/envs/ai-workflow/Scripts/python.exe`
独立复算脚本（我自建，只读运行）：`C:/Users/26717/.workbuddy/cache/ai-workflow/v2.5.2-recount/e_recount.py`

## 1. 结论：**不通过**

一句话理由：**7 条批 C 中有 5 条（#1/#2/#3/#5/#6/#7）实质闭合、数字可复现，但 #4 仍未闭合——`plan.yaml` 第 2 步的重复交付物修正依旧无【更正】注释（静默改动），且该步因此脱离 Anti-drop 覆盖的代价仍未写明；批 A `L3` 亦未落实（v2.5.2 `回归证据.txt` 中一条命令行都没有）。** 按「改了但不留痕」「要求未落实」判不通过。

复现的硬数字（全部由我亲手重跑）：token 总数 = **156**（对账 156 = 20 + 20 + 116 ✔）、三份存量 plan = **10/10 / 8/8 / 8/8**、`skill` 自检 = **19/19**、`收窄为` 命中 **1**（`ops.md:171`）、禁用串 **0/0/1/1**。**没有一处「数字对不上」。** 不通过只因「留痕」与「落实」两类缺陷。

---

## 2. 逐条判定

### #1 基线 token 总数 —— **通过（含 1 处低危遗留）**

命令与原始输出（我自写脚本，独立实现口径，未 import `checks`）：

```
$ python .../v2.5.2-recount/e_recount.py
执行时点：2026-09-11 14:35:59
- 工作区 ai-workflow: 91
- 工作区 工作流: 65
- 合计：156
- 明细行数：156
```

| 核对项 | 实测 | 判定 |
| --- | --- | --- |
| 基线文件写的总数 | `基线-复现与回归风险.md:28` = **156** | ✅ 与我算的 156 一致 |
| 口径措辞 vs 脚本实现 | 文件 `:21` 写"全部 plan.yaml（**递归**，含 `_archive/`），排除 `/tmp/`、`/toy/`"；`step1b_rescan.py:28-35` 实现 = `tasks/**/plan.yaml` 且跳过含 `/tmp/`、`/toy/` | ✅ 一致 |
| 时点标注 | `:22` "扫描时点：2026-09-11 14:23（执行时点；token 总数是**时点快照**…）" | ✅ 已标 |
| 加总自洽 | `:33` `156 = 20 + 20 + 116` | ✅ |

**低危遗留（[轻]）**：文件头 `:4-5` 仍把「原始输出（逐条可复算）」「复现脚本」指向**旧的** `tmp/step1-原始输出.txt` / `tmp/step1_baseline.py`；而该旧脚本 `:101-106` 仍是**单层 glob**，其原始输出 `:201` 仍写 `合计…144`，且该文件对 144 无任何"已被取代"标注。B 节虽已另指 `step1b-复算原始输出.txt`，但文件头指向未同步，读者顺头读会取到 144。**这与批 C #1 的"措辞与实现不符"同源，属未清尾。**

### #2 `7/7` 行 —— **通过**

含 `7/7` 的全部行（`grep -n "7/7"`）：

```
38:        结果：7/8 通过，FAIL=1     （修复前 7/7 通过 FAIL=0 —— 假 PASS 已关闭）
45:  技能增强-ai-workflow-v2.5.1-2026-09-10/plan.yaml -> 7/7 通过 FAIL=0
46:  体验评估-ai-workflow-v2.5.0-2026-09-10/plan.yaml -> 7/7 通过 FAIL=0
148:  技能增强-v2.5.1 / 体验评估-v2.5.0 / 技能增强-v2.4.4 三者均 7/7 通过 FAIL=0
173:    plan 技能增强-ai-workflow-v2.5.1-2026-09-10  : === 结果：7/7 通过，FAIL=0 ===
174:    plan 体验评估-ai-workflow-v2.5.0-2026-09-10  : === 结果：7/7 通过，FAIL=0 ===
175:    plan 技能增强-ai-workflow-v2.4.4-2026-09-10  : === 结果：7/7 通过，FAIL=0 ===
193:     `19/19` 两个时点一致…；三份 plan 由 7/7 变为 8~10/10，
```

逐行判定（来源/时点标注是否**恰好**解释了"为何现在跑不出"）：

| 行 | 邻近标注 | 是否恰好解释 |
| --- | --- | --- |
| 38 | `:38` 自带"**修复前** 7/7"，`:39-41` 补"现树复跑 `8/9 FAIL=1`，差额来自 v2.5.2 新增「熔断状态」检查项" | ✅ |
| 45/46 | `:47-50` "以上为 **2026-09-10、v2.5.1 时代 `checks.py`（当时 7 项检查）** 取值…现树重跑为 10/10、8/8…**数字变化源于检查项增加**" | ✅ |
| 148 | `:149-150` "同为 **2026-09-10 v2.5.1 时代 `checks.py`（7 项）** 取值…现树三份依次为 **10/10、8/8、8/8**" | ✅ |
| 173/174/175 | `:171` 段首"── **原时点取值（2026-09-10，v2.5.1 时代 checks.py = 7 项检查）** ──"，`:179-183` 另列现树复跑 | ✅ |
| 193 | 汇总句，引用同一更正常 | ✅ |

我自己重跑三份 plan（`cd` 至技能目录，`--base` 用 Windows 字面路径）：

```
=== 技能增强-ai-workflow-v2.5.1-2026-09-10 ===  →  === 结果：10/10 通过，FAIL=0 ===
=== 体验评估-ai-workflow-v2.5.0-2026-09-10 ===  →  === 结果：8/8 通过，FAIL=0 ===
=== 技能增强-ai-workflow-v2.4.4-2026-09-10 ===  →  === 结果：8/8 通过，FAIL=0 ===
```

与文件所写**一致**。**通过。**

### #3 `ops.md` 命中定位 —— **通过**

```
$ grep -rn "收窄为" SKILL.md references/*.md assets/ scripts/
references/ops.md:171:  - **R6 真缺陷｜`checks.py` 路径候选过宽致假 PASS**：…当时只把候选"收窄为固定三处"…
$ grep -rn "兜底收窄" …            → （无输出，0 处）
$ sed -n '149p' references/ops.md  → **分类标准（务必区分）**：
```

命中数 **1**、在 `references/ops.md:171`；`ops.md:149` 确为「分类标准」。`回归证据.txt:159-163` 的更正与此**完全一致**（"原稿写 `:149` 定位错误…真正命中在 `:171`…`grep -rn "收窄为"` 命中 1 处…"）。**通过。**

### #4 `plan.yaml` 第 2 步更正注释 —— **不通过（未闭合）**

```
$ grep -n "更正" tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/plan.yaml
29:    # 【2026-09-11 更正】原文此处写"跨技能兜底**彻底移除**"…
72:    # 【2026-09-11 更正】原文交付物写 `回归证据.txt`，与步骤 6 重复…
```

全文**只有 2 处**更正注释（步骤 3、步骤 8）。第 2 步（`plan.yaml:17-23`）的交付物 `:23` 现为：

```
交付物: "根因结论（写入 复现基线.txt 的「根因」节，非文件型交付物）"
```

其**上方无任何【更正】注释**。核实其确为静默改动：

- 原值证据：`tmp/step1-原始输出.txt:124`（14:04 快照）`| … | 技能增强-ai-workflow-v2.5.1-2026-09-10 | 2 | …复现基线.txt |`；`互审-甲-质证.md:155` `23: … 复现基线.txt ← 与步骤1重复`。
- 批 C #4 的期望是"在 `:23` 上方补一条与 `:72` **同格式**的更正注释，写明'原文写 `复现基线.txt`、与步骤 1 重复'"。**未落实。**

即：**同一必改项的两对重复（step1/2、step6/8），步骤 8 已留痕、步骤 2 仍静默**——正是批 C 判不通过的那一处，原样保留。**不通过。**

### #5 字面禁用串计数 —— **通过**

```
$ grep -o 'SAFE_DELETE'        回归证据.txt | wc -l  → 0
$ grep -o '(no stdout, rc=1)'  回归证据.txt | wc -l  → 0
$ grep -o '## 回归 8'          回归证据.txt | wc -l  → 1
$ grep -o '## 回归 9'          回归证据.txt | wc -l  → 1
定位：152:## 回归 8：全库同类检索（…）   /   165:## 回归 9：全部信号复跑
```

四类子串现为 **0 / 0 / 1 / 1**；仅剩的两处 `## 回归 8`/`## 回归 9` 是**章节标题本身**（属正文结构，非被剔除的证据内容），且 `:5` 的重写说明已改写为"回归 8、回归 9 两个**章节标题**各重复 6 次"（不带 `##` 前缀）。我逐条读过 `:5-7`、`:149-152` 的出现位置：均属**说明性描述**，不是原拦截回执/空输出证据本身。**通过。**

### #6 第 3 步注释补 `base.parent/<p>` 且与实现一致 —— **通过**

`plan.yaml:29-36` 现完整列出两条残留通道：

```
30:    # 候选**，即 `base/<p>`、`~/.workbuddy/<p>`、`base.parent/<p>`，同型假 PASS 通道**两条仍在**
33:    #   · `base.parent/<p>` —— 使落在**工作区根之外**的交付物也判 PASS，**撞红线③（工作区边界）**，危害更重。
```

读 `scripts/checks.py:165-176` 确认真实现状：

```
165:def _candidate_paths(raw: str, base: Path) -> list[Path]:
168:    if p.is_absolute():
169:        return [p]
170:    # 【v2.5.2】交付物**只**在「本次任务基准目录（--base，应为工作区根）」下解析，候选唯一。
176:    return [base / p]
```

候选**只有** `base/<p>`（绝对路径直通），与注释"两条通道已移除、候选唯一"**一致**。**通过。**

### #7 `ops.md` 总数 —— **通过**

```
$ grep -n "144\|156\|交付物 token\|时点\|快照" references/ops.md
165:  - **`checks.py` 交付物路径候选收敛为仅 `base/<p>`**：…（口径：两个工作区 tasks/ 下**全部** plan.yaml，递归含 `_archive/`、排除 `tmp/` 夹具；时点 2026-09-11 14:23）：文件型交付物 token 合计 **156**，其中 **20 个**…
$ grep -rn "144" SKILL.md references/ Ledger.md assets/  → （无输出，0 处）
```

`ops.md:165` 已为 **156** 且**带口径与时点**；技能本体再无 `144` 残留（`ops.md:234` 的 `156ms` 是链接探活均值，与本话题无关）。**通过。**

### 批 A `L3`（v2.5.2 `回归证据.txt` 每组附命令行）—— **不通过（未落实）**

命令：

```
$ grep -n "命令" tasks/技能增强-ai-workflow-v2.5.2-2026-09-11/回归证据.txt   → （无输出，0 处）
$ grep -n "命令\|--base" .../回归证据.txt  → 仅 7 处，且全部落在 [FAIL] 报错正文里（--base=<工作区根> 字样）
```

`L3` 原文（`独立审核意见-批A.md:100-101`）："**`回归证据.txt` 只贴输出、不贴命令**；R10 的 `--base` 必须是**技能目录**才得 `9/10`… 期望：**每组附一行命令（含 `--base`）**"。

实测：v2.5.2 `回归证据.txt` 全 375 行**没有任何一行命令行**（R1–R14、R13、R14 各节只有"期望/实际"与输出代码块）。命令只存在于生成脚本 `tmp/step7_regression.py:73-104`（内含 `--base`），但**未打印进证据文件**。故读者拿到证据文件仍**无法逐组复现、仍看不到 R10 的 `--base` 是技能目录**——批 A 的要求**原样未满足**。**不通过（低）。**

（注：与之同名、位于 v2.5.1 任务目录下的另一份 `回归证据.txt` **确已每组带命令**，但批 A `L3` 的审核对象是 **v2.5.2** 那份，不能相互顶替。）

### 对抗式（搜寻"为过关而毁证"）—— **有实质发现**

| 检查 | 结论 |
| --- | --- |
| 不可复现数字被**删除**？ | 否。`7/7` 全部保留并补时点（见 #2）；`ops.md:149` 错行未被删，而是更正为 `:171` 并注明"原稿定位错误"。✅ |
| 问题段落被**整段移除**？ | 无证据。`回归证据.txt` 是对原版整份重写（`裁决.md` §5 授权），`:3-15` 逐条列出原污染四点，未隐藏。✅ |
| "无法确认"被说成"已确认"？ | 未见。反而 `SKILL.md:201`、`回归证据.txt:190-192` 主动标注"纪律约束、非机器判定""拦截回执不是命令输出"。✅ |
| **`plan.yaml` 第 2 步改"非文件型"后是否脱离 Anti-drop 覆盖、代价是否写明？** | **脱离已坐实、代价未写明**。`checks.py plan` 现输出 `[SKIP] 步骤 2 交付物 — 『根因结论』非文件型，需人工确认`，Anti-drop 汇总为"已完成步骤 8 个，**交付物确认 7 个**"——步骤 2 已不计入自动对账覆盖。而该步**无任何注释说明**"这会使步骤 2 失去 Anti-drop 覆盖"。**静默降级了可核验性**（并入问题 #1）。 |

### 一致性（熔断机制措辞）—— **基本一致，1 处残留**

```
$ grep -rn "三态" SKILL.md references/ Ledger.md assets/
SKILL.md:156:> 设计依据：工业界断路器三态（Closed / Open / Half-Open）＋…
references/ops.md:163:  …设计依据：工业界断路器三态，以及…
$ grep -rn "熔断三态\|三态熔断" .  → 0 处
$ grep -rn "三态的唯一叫法\|唯一叫法\|第三个存储态" …
SKILL.md:158:### 状态（**两个存储态 + 一次复位迁移**，唯一叫法）
SKILL.md:160:存储态只有两个…"复位"是一次**迁移动作**而非第三个存储态…
references/ops.md:163:…**「复位」是一次迁移动作、不是第三个存储态**（`VALID_FUSE` 只有前两项）…
scripts/checks.py:62:# 熔断机制（v2.5.2）：三态的唯一叫法 —— 正常（Closed）/ 已熔断（Open）；复位=改回「正常」并留痕
```

四份正文文件（`SKILL.md`/`ops.md`/`Ledger.md`/`assets/plan-template.yaml`）措辞**互相一致**："两个存储态 + 一次复位迁移""不是第三个存储态"；`SKILL.md:156`、`ops.md:163` 出现的"三态"均指**工业界断路器的设计依据**（并显式说明复位不是第三态），属合法引用。
**但 `scripts/checks.py:62` 仍有自相矛盾残留**：注释写"**三态**的唯一叫法 —— 正常/已熔断"，只列**两项**，与 `SKILL.md:158-160` 的"两个存储态、唯一叫法"**直接冲突**。"三态"这个叫法**未在技能本体全部改掉**。

---

## 3. 问题清单（按严重度）

1. **[中] `plan.yaml` 第 2 步重复修正仍是静默改动，且脱离 Anti-drop 覆盖的代价未写明（批 C #4 未闭合）。**
   🔴 现象：`交付物: "根因结论（写入 复现基线.txt 的「根因」节，非文件型交付物）"` 上方无【更正】注释；全文件仅 `:29`、`:72` 两处更正注释。原值 `复现基线.txt`（`tmp/step1-原始输出.txt:124`、`互审-甲-质证.md:155`）被改掉而无痕迹。后果：`checks.py plan` 现对该步 `[SKIP]`，Anti-drop 从"确认 8 个"降为"确认 7 个"，此代价无处说明。
   📍 精确定位：`tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/plan.yaml:23`（应在其上补注释；参照 `:72-74` 的格式）。
   🎯 期望：在 `:23` 上方补一条与 `:72` 同格式的 `# 【2026-09-11 更正】原文交付物写 \`复现基线.txt\`，与步骤 1 重复；本步真实产物是写入既有文件的「根因」节，故改述为非文件型交付物——代价是该步自此不再计入 Anti-drop 自动对账，需人工确认。`

2. **[低] v2.5.2 `回归证据.txt` 未落实批 A `L3`：全篇无一条命令行（含 `--base`）。**
   🔴 现象：`grep -n "命令"` 命中 **0**；`R10` 的 `--base`（应为技能目录）在文件中不可见，读者无法逐组复现。命令只存在于 `tmp/step7_regression.py:73-104`，未打印。
   📍 精确定位：`tasks/技能增强-ai-workflow-v2.5.2-2026-09-11/回归证据.txt`（R1–R14 每节）；生成源 `tmp/step7_regression.py:38-59`（`group()` 未输出命令）。
   🎯 期望：在 `group()` 里把 `cmd` 以一行 `命令：<python> checks.py plan <plan> --base <base>` 形式打印进每组（尤其 R10 的 `--base` 须显示为技能目录），重生成证据文件。

3. **[轻] `scripts/checks.py:62` 残留"三态"叫法且自相矛盾。**
   🔴 现象：注释写"**三态**的唯一叫法 —— 正常（Closed）/ 已熔断（Open）"，只列两项，与 `SKILL.md:158-160`/`ops.md:163` 的"两个存储态、唯一叫法"冲突。
   📍 精确定位：`scripts/checks.py:62`（对照 `SKILL.md:158,160`、`references/ops.md:163`）。
   🎯 期望：改为"**两个存储态**的唯一叫法 —— 正常（Closed）/ 已熔断（Open）；复位是一次迁移动作（Half-Open），不是第三个存储态"。

4. **[轻] 基线文件头仍指向旧脚本/旧原始输出，与已修正口径不一致。**
   🔴 现象：`基线-复现与回归风险.md:4-5` 把"原始输出（逐条可复算）/复现脚本"指向 `tmp/step1-原始输出.txt`、`tmp/step1_baseline.py`；后者 `:101-106` 仍是**单层 glob**，其输出 `:201` 仍写 `合计…144`（无"已被取代"标注）。B 节虽另指 `step1b-*`，但文件头未同步。
   📍 精确定位：`基线-复现与回归风险.md:4-5`；`tmp/step1_baseline.py:101-106`；`tmp/step1-原始输出.txt:201`。
   🎯 期望：文件头区分 A/B 两节各自的脚本与原始输出，或给 `step1-原始输出.txt:201` 的 144 加"（旧单层口径、已由 156 取代）"标注。

---

## 4. 我无法确认的项（诚实列出）

1. **原版 v2.5.1 `回归证据.txt` 的"3 种互斥内容 2/4/1 处"原始分布无法独立验证**：原版已被整份覆盖，全库无备份（`find -iname "*回归证据*"` 仅命中现版两份；`git` 中该任务目录为未跟踪 `??`，HEAD 停在 v2.5.0）。我只能确认**现版**不含禁用串（#5），无法比对"到底删了什么"。
2. **`7/7` 是否确为 v2.5.1 时代 `checks.py` 的真实输出**：`tmp/checks_old_v250.py` 是 v2.5.0 版、非 v2.5.1 版；我能证明其"现在跑不出"，不能证明其"当时为真"。
3. **回归 4 / 回归 5 的 (a)–(d) 逐条输出**：依赖"临时改名 venv 的 `duckdb`/`ast-grep.exe`"，受"不得修改技能既有文件"约束，我只静态核到 `$deps` 7 项、`$pkgDir` 6 项、`$cliDir` 1 项、`6+1=7`，未复现 (b)(c)(d) 的 `[FAIL]` 行与"复原 True"。
4. **熔断五步中"冻（未提交/未发布）""报（升级用户）"**：无本次会话对话记录可核，仅能做到"证据文件未反证"。
5. **基线 A 节夹具 A/B/C 的"修复前 7/7 假 PASS"取值**：属 v2.5.1 旧代码行为，现树已无法回放（旧通道已移除），仅能凭 `step1-原始输出.txt` 的当时记录。

---

**复核用命令留档**：`e_recount.py`（token 复算）、`checks.py plan … --base …` ×4、`checks.py skill`、`grep -o|wc -l` 系列、`grep -rn "收窄为|兜底收窄|三态|更正|命令"`。所有原始输出见本文件引用行。
