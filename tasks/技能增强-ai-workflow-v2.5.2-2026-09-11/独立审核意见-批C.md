# 独立审核意见（批 C）

审核者：独立第三方（未参与 v2.5.2 执行）
日期：2026-09-11
被审对象（批 C，记录类修正 3 个文件）：
1. `tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/回归证据.txt`
2. `tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/plan.yaml`
3. `tasks/技能增强-ai-workflow-v2.5.2-2026-09-11/基线-复现与回归风险.md`
对照物：`tasks/对抗互审-ai-workflow-v2.5.1-2026-09-11/裁决.md`、`互审-甲-质证.md`、`scripts/checks.py`
解释器：`C:\Users\26717\.workbuddy\binaries\python\envs\ai-workflow\Scripts\python.exe`（全程使用）

## 结论：不通过

理由摘要：必改项 2、3、4 的**实质缺陷**均已闭合（重复段落消失、不再声称"彻底移除"、交付物 token 无重复且对账 FAIL=0），但验收标准 1 的**字面判据**（出现 1 次 / 全文不含）+ **可复算性**不成立，验收标准 4 的**总数 144 不可复现**，验收标准 5 存在**未留痕的静默改动**。按题面"数字对不上就是不通过"，判不通过。

---

## 逐条判定（验收标准 1–5）

### 标准 1 —— 必改项 2 已闭合：**不通过**

**（1a）`## 回归 8` / `## 回归 9` 的出现次数（命令计数，非目测）**

```
$ grep -c "## 回归 8" 回归证据.txt          → 2
$ grep -c "## 回归 9" 回归证据.txt          → 2
$ grep -o "## 回归 8" 回归证据.txt | wc -l  → 2
$ grep -o "## 回归 9" 回归证据.txt | wc -l  → 2
$ grep -c "^## 回归 8" 回归证据.txt         → 1   （仅行首标题）
$ grep -c "^## 回归 9" 回归证据.txt         → 1
$ grep -n "## 回归 8" 回归证据.txt
5:> 原版已失去证据价值，具体污染：① `## 回归 8` 与 `## 回归 9` **各重复 6 次**；② 2 处
133:## 回归 8：全库同类检索（修正上一轮「只改被点名处、未做同类清扫」的教训）
$ grep -n "## 回归 9" 回归证据.txt
5:> （同上，line 5 引用）
140:## 回归 9：全部信号复跑
```

判定：**"整段重复 6 次"的实质缺陷已闭合**——作为**章节标题**的 `## 回归 8`/`## 回归 9` 各出现 1 次（行首计数 = 1）。但验收判据的字面要求（"出现 1 次"）**不成立**：line 5 的"重写说明"引用了 `## 回归 8`/`## 回归 9` 字面，使子串计数 = 2。此属说明性引用而非重复段落。

**（1b）禁用字样**

```
$ grep -c "SAFE_DELETE" 回归证据.txt           → 2
$ grep -n "SAFE_DELETE" 回归证据.txt
6:> **安全策略拦截回执**（`[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {...}` JSON）
149:     **3 种互斥内容** —— `[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {...}` 拦截回执（2 处）、
$ grep -o "(no stdout, rc=1)" 回归证据.txt | wc -l → 3
$ grep -n "no stdout" 回归证据.txt
7:> 被当成命令输出写入；③ 1 处记录为 `(no stdout, rc=1)`；④ `回归 5` 的算式 `7 + 6 + 1`
150:     `19/19 通过，FAIL=0`（4 处）、`(no stdout, rc=1)`（1 处）。
152:     `(no stdout, rc=1)` 是**根本没跑出结果**，二者均不构成证据，已剔除。
```

判定：**"拦截回执被当成命令输出写进证据"的实质缺陷已闭合**——全文再无 `[safe-delete]…JSON` 载荷块（对比原版 line 120/151 的载荷，见 `裁决.md` §5 必改项 2）。但**"全文不含 `SAFE_DELETE`"不成立**（2 处）、**"不含 `(no stdout, rc=1)`"不成立**（3 处）。这些出现均在"重写说明/取值来源说明"里，属"声明已剔除"的元文本，不是证据本身。**判据字面不通过，实质通过。**

**（1c）逐条复算该文件的结论性数字（我自己重跑）**

| 文件处的声明 | 我的复跑命令与输出 | 一致？ |
| --- | --- | --- |
| 回归 1：`checks.py skill` = 19/19 通过，FAIL=0（line 22/141） | `checks.py skill` → `=== 结果：19/19 通过，FAIL=0 ===` | ✅ |
| 回归 3/7/9：三份存量 plan = **7/7 通过 FAIL=0**（line 37-38/131/142-144） | v2.5.1 → `10/10 通过，FAIL=0`；体验评估-v2.5.0 → `8/8`；v2.4.4 → `8/8` | ❌ 三处全不一致 |
| 回归 5：`覆盖算式：pkgDir + cliDir = 6 + 1 = 7 = 依赖数`（line 84） | 读 `scripts/setup_env.ps1:8`（`$deps` 7 项）、`:42`（`$pkgDir` 6 项）、`:46`（`$cliDir` 1 项） | ✅ 算术正确 |
| 回归 6：`grep SKILLS_ROOT in scripts/*.py` = 0（line 128） | `grep -rn SKILLS_ROOT scripts/*.py \| wc -l` → `0` | ✅ |
| 回归 8：命中数 = 1，`references/ops.md:149`（line 136-137） | `sed -n '149p' references/ops.md` → `**分类标准（务必区分）**：`；实际 `收窄为` 命中在 `references/ops.md:171`（另有 `:91`、`:165`） | ❌ 定位错误、计数错误 |
| 回归 9：`run_stage.ps1`/`setup_env.ps1` 非 ASCII 字节 = 0（line 145-146） | 实测 0 / 0 | ✅ |
| 回归 9：`checks.py` 内 SKILLS_ROOT 引用数 = 0（line 147） | 实测 0 | ✅ |

判定：**"每处声明取值可复算"不成立**。失败项有三类：① 回归 3/7/9 的 `7/7` 在现树重跑为 `10/10`、`8/8`、`8/8`（因 v2.5.2 给 `checks.py` 增加了检查项）；② 回归 8 的 `ops.md:149` 定位与"命中数=1"均不可复现；③ 该文件**自定的规则**（line 12："取值不可复现的须标注来源与时间"）在上述行**未被遵守**——`7/7` 与 `ops.md:149` 均无来源与时间标注。注：`7/7` 对 v2.5.1 时代的 `checks.py` 曾为真，属"版本漂移"，非凭空捏造，但作为"证据文件"已不可复算。

**（1d）"取值来源说明"是否诚实（line 148-153）**：**诚实**。
- 它主动承认原版该节"重复 6 次"且 `checks.py skill` 一行存在 **3 种互斥内容**，并逐一点明是哪三种（拦截回执 2 处 / 19/19 通过 4 处 / (no stdout, rc=1) 1 处）与**剔除哪两类**（拦截回执、无输出记录），保留可复现的多数值。
- 我实测 `19/19` 在该说明所称的"当前树"确实复现（见上表），与"与 4 处一致值相符"吻合。
- 未发现把不可复现值伪装成可复现的措辞。

---

### 标准 2 —— 必改项 3 已闭合：**通过**

**（2a）`plan.yaml` 中不再有"彻底移除"式的失实表述**

```
$ grep -n "彻底移除" tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/plan.yaml
29:    # 【2026-09-11 更正】原文此处写"跨技能兜底**彻底移除**"，与当时实现不符：实为**收窄为固定
```

判定：唯一命中在 **【更正】注释**内，且该注释**明确否证**该表述（"与当时实现不符"）。不存在任何"声称已彻底移除"的陈述。**通过。**

**（2b）注释描述是否与真实实现一致（读 `scripts/checks.py` 核实）**

```
$ sed -n '165,176p' scripts/checks.py
165:def _candidate_paths(raw: str, base: Path) -> list[Path]:
168:    if p.is_absolute():
169:        return [p]
170:    # 【v2.5.2】交付物**只**在「本次任务基准目录（--base，应为工作区根）」下解析，候选唯一。
176:    return [base / p]
```

判定：与题面给的真实现状"候选**只有** `base/<p>`，绝对路径直通，没有其他候选位置"**完全一致**。`plan.yaml:31-32` 的注释也正确区分了"v2.5.1 只是收窄 / v2.5.2 才收敛"，与 `裁决.md` §4 的根因代码 `return [base / p, Path.home()/".workbuddy"/p, base.parent / p]` 相符。**通过。**
（小瑕，见问题清单 #6：注释只点名 `~/.workbuddy/<p>` 一条残留通道，未提 `base.parent/<p>`；`裁决.md` §4 实为两条。）

**（2c）`回归证据.txt` 算术是否改正且真的等于正确值**

```
$ sed -n '83,86p' 回归证据.txt
83:  依赖数 = 7，pkgDir 条目 = 6，cliDir 条目 = 1
84:  覆盖算式：pkgDir + cliDir = 6 + 1 = 7 = 依赖数   （全覆盖，无未映射项）
85:  ⚠️ 原版此处写作 `7 + 6 + 1 = 全覆盖`，**算式本身错误**（7 是被覆盖的对象，不是加数），
86:     已按必改项 3 更正为 `6 + 1 = 7`。
```
`6 + 1 = 7` 算术正确（我实测 `$pkgDir` 6 项 + `$cliDir` 1 项 = 7 = `$deps` 7 项）。且原文错误 `7 + 6 + 1` 被**保留展示并标注为错误**。**通过。**

---

### 标准 3 —— 必改项 4 已闭合：**通过**

**（3a）交付物两两比对（枚举自算，不目测）**

```
[任务3] v2.5.1 plan.yaml 交付物 token 枚举
  step 1: tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/复现基线.txt
  step 3: scripts/setup_env.ps1
  step 3: scripts/checks.py
  step 4: SKILL.md
  step 5: assets/report-template.md
  step 5: references/ops.md
  step 5: scripts/run_stage.ps1
  step 5: references/playbook.md
  step 6: tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/回归证据.txt
  step 7: tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/独立审核意见.md
  step 8: Ledger.md
  --- 重复交付物（不同步骤写同一文件）---  重复: 无
  文件型 token 总数: 11 去重后: 11
```

判定：**不存在两个不同步骤写同一交付物文件**。原 2 对重复（step1/2 → `复现基线.txt`；step6/8 → `回归证据.txt`，见 `互审-甲-质证.md:154-155,161,170`）均已消除。**通过。**

**（3b）对账仍能通过**

```
$ python checks.py plan ".../技能增强-ai-workflow-v2.5.1-2026-09-10/plan.yaml" --base "...\skills\ai-workflow"
[ OK ] steps 非空 — 8 步
[SKIP] 步骤 2 交付物 — 『根因结论』非文件型，需人工确认
[SKIP] 步骤 8 交付物 — 『验收确认点』非文件型，需人工确认
[ OK ] Anti-drop 对账 — 已完成步骤 8 个，交付物确认 7 个
[ OK ] 熔断状态 — 正常（未声明）
=== 结果：10/10 通过，FAIL=0 ===
RC=0
```

判定：**FAIL=0** ✓。**SKIP 项合理**：两处 SKIP 的 token（步骤 2 的『根因结论』、步骤 8 的『验收确认点』）确为**非文件型产物**（根因结论是写入既有文件的章节、验收确认点是对话记录），`checks.py:396` 对非文件型一律 SKIP 并要求人工确认，语义正确。
（附带说明，见问题清单 #4：这两个 SKIP 正是"去重"的实现方式——把重复的手写产物**改述为非文件型**，从而不再计入文件 token。对账口径上正确，但代价是这两步从此**不被 Anti-drop 覆盖**；且步骤 2 的这次改述**没有任何更正注释**。）

---

### 标准 4 —— 基线报告的计数可信：**不通过**

**我独立重算（`~/.workbuddy/cache/ai-workflow/recount.py`、`recount2.py`）**

| 数字 | 报告值 | 我算出的值 | 一致？ |
| --- | --- | --- | --- |
| 扫描到的文件型交付物 token 总数 | **144** | **156**（复刻报告脚本口径：`tasks/*/plan.yaml` + `tasks/_archive/*/*/plan.yaml`）<br>**173**（按报告**措辞**"tasks/ 含 _archive/ 下**所有** plan.yaml"字面递归） | ❌ |
| 收敛后 PASS → FAIL | **20** | **20** | ✅ 且**逐条一致** |
| 当前已 FAIL（收敛前后均 FAIL） | **20** | **20** | ✅ |

我的 20 条 `PASS→FAIL` 明细与报告《20 个风险 token 明细》表**逐行一致**（全部在 `E:\ChatGPT\工作流`，写法为 `skills/ai-workflow/…`；含 v2.1 的 11 条、v2.2 的 7 条、技能改造的 2 条）。

**"144 从何而来"——我已定位**：它是 **14:04 的快照值**，不是当前值。
- 报告自己的原始输出 `tmp/step1-原始输出.txt` 里确有 **144 个 token 数据行**、合计行写 144，且其表格**不含** `技能增强-ai-workflow-v2.5.2-2026-09-11` 任务；
- 该表格中 v2.5.1 任务为 **12** 个 token（含 step2 的 `复现基线.txt`、step8 的 `回归证据.txt` 两条重复），现为 **11**；
- 故 144 = 156 − 13（v2.5.2 任务当时尚不存在）+ 1（v2.5.1 当时 12、现 11）。
即：**批 C 自己对 v2.5.1 plan 的去重（12→11）与 v2.5.2 任务自身的成长（0→13），共同把总数从 144 推到了 156。**

**"21 还是 20"：20 才对，21 是错算**——与报告 line 30 的自我订正一致。

判定：**"数字对不上就是不通过"**。20 / 20 两个数字我完整复现（含逐条身份），但**总数 144 在现树不可复现**（156），且报告措辞"全部任务目录（含 _archive/）…所有 plan.yaml"与其脚本实际口径（单层 glob，**漏掉** `tasks/*/tmp/*/plan.yaml`、`tasks/*/toy/plan.yaml` 等嵌套，共 15+2 个 token）**不符**。**不通过。**

---

### 标准 5 —— 没有掩盖痕迹：**不通过（部分）**

| 改动 | 是否留痕（"原为错、现为对"） |
| --- | --- |
| `回归证据.txt` 整份重写 | ✅ 有 line 3-15「重写说明」逐条列出原污染四点 + line 148-153「取值来源说明」 |
| `plan.yaml` step 3 的"彻底移除" | ✅ line 29-32【2026-09-11 更正】保留原表述并否证 |
| `plan.yaml` step 8 的重复交付物 | ✅ line 68-70【2026-09-11 更正】写明"原文交付物写 `回归证据.txt`，与步骤 6 重复" |
| `基线-复现与回归风险.md` 的 21→20 订正 | ✅ line 30 明写"本人转述错误，已修正" |
| **`plan.yaml` step 2 的重复交付物** | ❌ **无任何注释** |

**step 2 的具体证据**：`裁决.md` §5 必改项 4 明列 **2 对**重复（step1/2、step6/8），`互审-甲-质证.md:154-155` 给出原值：
```
16: … 复现基线.txt
23: … 复现基线.txt      ← 与步骤1重复
```
`tmp/step1-原始输出.txt`（14:04 快照）证实当时 step 2 仍为 `tasks/…/复现基线.txt`。而现 `plan.yaml:23` 已变为
`交付物: "根因结论（写入 复现基线.txt 的「根因」节，非文件型交付物）"`
——**该行上方无任何【更正】注释**（全文仅 line 29、line 68 两处更正注释）。即：**同一必改项的两对重复，一对留痕、一对静默**。

判定：**存在"把原表述改掉而不留说明"的情况**（step 2）。虽新值自带"非文件型交付物"的自我说明，但缺少"原为 `复现基线.txt`、与步骤 1 重复"的痕迹，与 step 8 的处理不对称。**不通过（部分）。**

---

## 独立重算结果（144 / 20 / 20 三个数字）

| 指标 | 报告声称 | 我独立重算 | 结论 |
| --- | --- | --- | --- |
| 文件型交付物 token 总数 | 144 | **156**（复刻其脚本口径）/ **173**（其措辞"所有 plan.yaml"字面） | 对不上 → 判不通过 |
| 收敛后 PASS→FAIL | 20 | **20**（明细逐条一致） | 一致 |
| 当前已 FAIL | 20 | **20** | 一致 |

> 复算脚本：`C:\Users\26717\.workbuddy\cache\ai-workflow\recount.py`、`recount2.py`（只读，已落盘）。
> 差异归因：`144 = 156 − 13（v2.5.2 任务原本不存在）+ 1（v2.5.1 plan 原本 12 token、批 C 去重后 11）`。

---

## 问题清单（现象 + 精确定位 + 期望），按严重度排序

1. **[中] 基线总数 144 在现树不可复现（现为 156），且"扫描范围"措辞与脚本口径不符。**
   现象：`基线-复现与回归风险.md:21` 写"扫描范围：**两个工作区的全部任务目录**（含 `_archive/`）里**所有**「状态=完成」步骤的文件型交付物 token"，`:25` 报 **144**；但 `tmp/step1_baseline.py:102-105` 只 glob `tasks/*/plan.yaml` 与 `tasks/_archive/*/*/plan.yaml`（单层），**漏掉** `tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/tmp/unrelated/plan.yaml`、`tmp/unrelated-r6/plan.yaml`、`技能增强-ai-workflow-v2.5.2-2026-09-11/tmp/fuse-e2e/plan.yaml`、`体验评估-ai-workflow-v2.5.0-2026-09-10/toy/plan.yaml`（共 17 个 token）。
   定位：`基线-复现与回归风险.md:21,25,30`；`tasks/技能增强-ai-workflow-v2.5.2-2026-09-11/tmp/step1_baseline.py:102-105`。
   期望：把 144 改为可复算的 **156** 并标注"快照时间 2026-09-11 14:04（v2.5.2 独立扫描时点）"，或把口径明确写成"任务目录一级 + `_archive/` 二级"；不要把"快照值"表述为"当前扫描值"。

2. **[中] `回归证据.txt` 中 plan 类取值不可复算，违反该文件自定规则。**
   现象：`回归证据.txt:37-38,131,142-144` 称三份存量 plan 均 `7/7 通过 FAIL=0`；我现树重跑为 `10/10`、`8/8`、`8/8`。该文件 `:12` 自定"**取值不可复现的须标注来源与时间**"，但这些行无任何来源/时间标注，也未标注"基于 v2.5.1 时代 checks.py（7 项）"。
   定位：`tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/回归证据.txt:37,38,131,142,143,144`。
   期望：这些行补注"（基于 2026-09-10 v2.5.1 时代 `checks.py`，当时 7 项检查；v2.5.2 后为 10/8/8 项）"或直接重跑为现值。

3. **[中] `回归证据.txt` 回归 8 的定位与计数错误。**
   现象：`:137` 称 `references/ops.md:149 — 描述的是【旧实现，已废弃】`；实测 `references/ops.md:149` = `**分类标准（务必区分）**：`（与旧实现无关），真正的"收窄为"命中在 `references/ops.md:171`（另有 `:91`、`:165` 描述旧通道），"命中数 = 1"（`:136`）不成立。
   定位：`tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/回归证据.txt:136,137`。
   期望：更正为实际行号与数量，或标注"基于 2026-09-10 的 `ops.md`（其后被 v2.5.2 改写，行号已漂移）"。

4. **[中] `plan.yaml` step 2 的重复修正无痕迹（静默改动）。**
   现象：`裁决.md` §5 必改项 4 明列 2 对重复（step1/2、step6/8）；现 `plan.yaml`:23 已由 `tasks/…/复现基线.txt` 改为 `根因结论（写入 复现基线.txt 的「根因」节，非文件型交付物）`，但该行**无【更正】注释**，而 step 8（`plan.yaml`:68-70）有。14:04 快照（`tmp/step1-原始输出.txt`）证明当时 step 2 仍写 `复现基线.txt`。
   定位：`tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/plan.yaml:23`。
   期望：在 line 23 上方补一条与 line 68 同格式的【更正】注释，写明"原文写 `复现基线.txt`，与步骤 1 重复"。

5. **[轻] `回归证据.txt` 字面仍含禁用串，不满足验收判据的字面要求。**
   现象：`SAFE_DELETE` 出现 2 次（`:6`、`:149`）；`(no stdout, rc=1)` 出现 3 次（`:7`、`:150`、`:152`）；`## 回归 8`/`## 回归 9` 子串各出现 2 次（`:5` 引用 + `:133`/`:140` 标题）。
   定位：`tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/回归证据.txt:5,6,7,133,140,149,150,152`。
   期望：若验收判据按字面执行，应把说明文字改为不解引原文串（如"安全策略拦截回执 JSON""无输出记录"），把原文串只留在不纳入统计的另附注释中；否则须显式声明"判据字面不适用"。

6. **[轻] `plan.yaml` 更正注释对 v2.5.1 残留通道的描述不完整。**
   现象：`:29-31` 只点名 `~/.workbuddy/<p>` 一条，未提 `base.parent/<p>`；而 `裁决.md` §4 实测两条（`return [base / p, Path.home()/".workbuddy"/p, base.parent / p]`），且 `base.parent/<p>` 恰是"撞红线③"那条更严重者。
   定位：`tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/plan.yaml:29-32`。
   期望：补提 `base.parent/<p>`。

7. **[轻] 错误的 144 已扩散到技能本体文档（非批 C 文件，但同源）。**
   现象：`references/ops.md:165` 写"两个工作区共 **144** 个交付物 token 中 **20 个**…"，与批 C 基线同源、同样不可复现。
   定位：`skills/ai-workflow/references/ops.md:165`。
   期望：随 #1 一并更正为 156（并注明快照时点）。

---

## 我无法确认的项（诚实列出）

1. **回归 4 / 回归 5 的 (a)–(d) 逐条输出无法逐字复核**：它们依赖"临时改名 venv 的 `duckdb` 目录 / `ast-grep.exe`"，而题面要求"不要修改任何被审文件"、且 `裁决.md` §7.2 亦禁止真跑 `setup_env.ps1`。我只核到了**可静态验证的部分**（`$deps` 7 项、`$pkgDir` 6 项、`$cliDir` 1 项、`6+1=7`），未复现 (b)(c)(d) 的 `[FAIL]` 行与"复原 True"。
2. **原版 `回归证据.txt` 的"3 种互斥内容 2/4/1 处"原始分布无法独立验证**：原版已被整份覆盖，全库无备份（`find -iname "*回归证据*"` 仅命中现版两份）。该分布只能依赖 `裁决.md:40-41` 与 `互审-甲-证伪.md:150`（甲：`[safe-delete]`×2、`19/19 通过`×3、`(no stdout, rc=1)`×1）与说明中的"×4"**互有出入**——故"4 处"这一具体数字我**无法确认真伪**。
3. **`7/7` 是否确为 v2.5.1 时代 `checks.py` 的真实输出**：我未取得 v2.5.1 时代的 `checks.py` 副本（`tmp/checks_old_v250.py` 是 v2.5.0 版，非 v2.5.1 版），只能证明其"现在不可复现"，不能证明其"当时为真"。
4. **`回归 8` 所称"命中数 = 1"在其原定范围（含 `tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/`）下的原值**：该范围已随本次改动变化，无法回放。
