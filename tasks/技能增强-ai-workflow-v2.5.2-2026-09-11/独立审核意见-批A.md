# 独立审核意见（批 A）

- 审核对象：`scripts/checks.py`、`assets/plan-template.yaml`、`SKILL.md`、`assets/熔断报告模板.md`、`tasks/技能增强-ai-workflow-v2.5.2-2026-09-11/回归证据.txt`
- 审核方式：有罪推定；不采信执行者自述；只认亲手跑出的输出。解释器统一 `C:/Users/26717/.workbuddy/binaries/python/envs/ai-workflow/Scripts/python.exe`
- 我的夹具（自建，未复用执行者的）：`C:/Users/26717/.workbuddy/cache/ai-workflow/auditA-2026-09-11/`（原始输出见 `auditA-原始输出.txt`、`auditA2-原始输出.txt`）
- 未修改任何被审文件

## 结论：不通过

理由：验收标准 3、4 不通过——「复位」在原子里**不闭合**：只按文档做复位，`checks.py plan` 仍永久 rc=1，任务无法交付。标准 1、2、5 通过。

## 逐条判定

### 标准 1 路径候选已收敛 —— **通过**

自建夹具（独立于执行者的 `v2.5.2-verdict`）：

```
### 夹具A 工作区外同名文件（交付物 skills/ai-workflow/scripts/setup_env.ps1，真实存在于 ~/.workbuddy/ 下）rc=1
[FAIL] 步骤 1 交付物缺失 — skills/ai-workflow/scripts/setup_env.ps1（未在 --base=<工作区根> 下找到；…）
[ OK ] Anti-drop 对账 — 已完成步骤 1 个，交付物确认 0 个
### 夹具B 上级目录文件（target.md 落在 base/.. 且真实存在）rc=1
[FAIL] 步骤 1 交付物缺失 — target.md（未在 --base=<工作区根> 下找到；…）
### 夹具C 绝对路径（指向技能内真实文件）rc=0
[ OK ] Anti-drop 对账 — 已完成步骤 1 个，交付物确认 1 个
### 夹具D base 内相对路径 rc=0
[ OK ] Anti-drop 对账 — 已完成步骤 1 个，交付物确认 1 个
```

三种情形 (i)(ii)(iii) 分别 FAIL/FAIL/PASS，与代码 `_candidate_paths()` 的唯一候选 `base/p`（`scripts/checks.py:176`）一致；`grep SKILLS_ROOT` 已无残留引用（该常量随 diff 删除）。执行者的 fixA/fixB/fixC 我用同一命令重跑，结论与数字逐字相同（8/9 rc=1、8/9 rc=1、8/8 rc=0）。

### 标准 2 熔断门禁真的生效 —— **通过**

五情形全覆盖（rc 即进程退出码）：
```
n5-complete 已熔断+报告齐全   rc=1  [FAIL] 已熔断：不得作为可交付物 … [ OK ] 熔断报告必含字段 — 5/5   结果 9/10
n6-noreport 已熔断但缺报告     rc=1  [FAIL] 熔断报告存在 — 缺失 …\n6-noreport\熔断报告.md  结果 7/9
n7-partial  报告缺字段         rc=1  [FAIL] 熔断报告必含字段 — 缺 卡点根因、待决策选项、复位条件  结果 8/10
n8-badvalue meta 取值非法      rc=1  [FAIL] meta.熔断状态 合法 — 实际『暂停』，应为 正常 / 已熔断 之一…  结果 7/8
p4-normal   显式 正常          rc=0  [ OK ] 熔断状态 — 正常   结果 8/8（未误报）
p5-blank    留空               rc=0  [ OK ] 熔断状态 — 正常（未声明）   结果 8/8
n9-steponly meta 留空但步骤=熔断 rc=1  [FAIL] 已熔断：不得作为可交付物 — 步骤 1 状态为『熔断』
```
已熔断的 FAIL 均 rc≠0（`main()` 的 `return 1 if n_fail else code`，`checks.py:453`）。执行者 `v2.5.2-fuse/*` 七个夹具我全部重跑，数字与其证据完全一致。

### 标准 3 文档与代码口径一致 —— **不通过**

- 通过的部分：三态值域（`正常/已熔断`）、F1–F5 编号、五步动作（停/冻/证/拦/报）、五节字段名（`FUSE_SECTIONS` == `熔断报告模板.md` 五节 == `plan-template.yaml:6` 注释）、`plan-template.yaml:3` 状态枚举 == `VALID_STATUS`（`checks.py:61`）——逐条对得上。
- **不通过的部分：复位条件两侧矛盾。** `SKILL.md:189` 与 `assets/熔断报告模板.md:57` 定义的复位动作**只有**「meta `熔断状态` 改回 `正常` + Ledger 留痕」；而 `checks.py:330-338` 的触发条件是 **meta=已熔断 **或** 任一步骤状态=熔断**，且 `SKILL.md:181`（冻）要求用 `checks.py mark <id> 熔断` 把步骤置为「熔断」。按文档执行复位后实测：

```
### 1a 熔断态（已完成「冻」：meta=已熔断、步骤1=熔断）    rc=1
[FAIL] 已熔断：不得作为可交付物 — meta.熔断状态=已熔断；步骤 1 状态为『熔断』 …
### 1b 严格按 SKILL.md:189 复位（仅把 meta 改回 正常）    rc=1
[FAIL] 已熔断：不得作为可交付物 — 步骤 1 状态为『熔断』 —— 自动化已停止…
=== 结果：9/10 通过，FAIL=1 ===
### 1c 额外执行 checks.py mark <plan> 1 进行中 后才解除   rc=0
[ OK ] 熔断状态 — 正常    结果 8/8
```

即：**文档描述的复位动作无法解除门禁**；`SKILL.md:136`（阶段 5）只要求 `熔断状态` 为 `正常`，满足后 `plan` 仍 FAIL。这是文档与实现互相矛盾（且是"假 FAIL"——按规则复位的任务被永久拦死）。

### 标准 4 没有"描述与实现不符" —— **不通过**

- 上述复位问题是同一根因的第二种表现（文档描述的复位行为 ≠ 实现行为），直接命中本条。
- 其余抽查到的描述与实现一致：`--base` 口径（`SKILL.md:135` 称"非当前目录必须带 --base，否则基准错"，实测 cwd=`C:/` 不带 `--base` → `[FAIL] 步骤 1 交付物缺失 — out.md` rc=1）；`checks.py mark … 熔断` 可用（实测成功把步骤置 `熔断`，随后 `plan` 立即 FAIL）；`回归证据.txt` 的"防污染守卫"在 `tmp/step7_regression.py:20,37-45` 真实存在（`BANNED` 标记 + 命中即不写入正文并整体判失败）；`assets/熔断报告模板.md` 五节可被 `checks.py` 校验（5/5）。
- 另有两条口径偏弱/不符（见问题清单 M1、L1）。

### 标准 5 回归证据可信 —— **通过**

- 污染项：`grep -c "SAFE_DELETE"` = 0、`grep -c "(no stdout"` = 0。
- 整段重复：按 `## ` 切小节共 20 块，**重复小节数 = 0**；重复的 5 行窗口 14 处，全部是各组共用的框架行（`[ OK ] plan.yaml 可解析 / meta.任务 / …`），非"整段重复"。
- 抽查重跑（≥2 组，实际做了 9 组），数字逐字一致：
  - R1 `checks.py skill` → `=== 结果：19/19 通过，FAIL=0 ===`、`[ OK ] 文档引用完整性 — 检查 28 条引用`（与 R1 完全一致）
  - R2 本任务 plan → `[ OK ] steps 非空 — 8 步` / `Anti-drop 对账 — 已完成步骤 6 个，交付物确认 6 个` / `熔断状态 — 正常` / `8/8 FAIL=0`
  - R6/R7/R8 → `8/9 FAIL=1`、`8/9 FAIL=1`、`8/8 FAIL=0`，与证据逐字一致
  - R9-N5/N6/N7/N8/N9、P4、P5 → `9/10`、`7/9`、`8/10`、`7/8`、`9/10`、`8/8`、`8/8`，全部一致
  - R10（真实计划置熔断）→ `[ OK ] Anti-drop 对账 — 已完成步骤 6 个…` `[FAIL] 已熔断…` `9/10 FAIL=1`，一致（注意其 `--base` 是**技能目录**，见问题清单 L3）
  - R11 `setup_env.ps1` / `run_stage.ps1` 非 ASCII 字节数 = 0（复核一致）

## 问题清单（按严重度排序）

**H1（高）｜复位链路不闭合：按文档复位后门禁仍 FAIL，任务永久不可交付**
- 现象：熔断后执行 `checks.py mark <plan> <id> 熔断` + meta 置 `已熔断`；用户授权后按文档复位（meta 改回 `正常`），`checks.py plan` 仍 `[FAIL] 已熔断：不得作为可交付物 — 步骤 1 状态为『熔断』`、rc=1（实测 1b）。只有额外把步骤状态 `mark` 成 `进行中/待办` 才解除（1c）。
- 定位：`SKILL.md:189`（复位动作只写 meta）、`assets/熔断报告模板.md:57`（`复位后动作` 同）vs `scripts/checks.py:330`（`tripped_steps`）+ `scripts/checks.py:331,337-338`；对照 `SKILL.md:181`（冻时要求步骤置「熔断」）、`SKILL.md:136`（阶段 5 只要求 meta 为 `正常`）。
- 期望：复位动作补齐「并将熔断步骤状态改回（`进行中`/`待办`）」并写成可执行命令（如 `checks.py mark <plan> <id> 进行中`）；或让 `_check_fuse` 仅在 meta=已熔断 时阻断、步骤状态只作提示。二者取一，文档与代码同步。

**M1（中）｜「熔断报告必含五节」实为任意子串命中，可被一行话绕过**
- 现象：构造一份**没有任何小节标题**的报告（正文仅一句"触发条件为 F3；已试路径…；卡点根因待定；待决策选项未定；复位条件未定"），`checks.py plan` 仍输出 `[ OK ] 熔断报告必含字段 — 5/5`。
- 定位：`scripts/checks.py:346`（`miss = [s for s in FUSE_SECTIONS if s not in text]`）vs `assets/熔断报告模板.md:4`（"五节标题**必须原样出现**（`checks.py plan` 会校验）"）。
- 期望：按标题级校验（如 `re.search(rf"^#+\s*{s}", text, re.M)`），否则文档应把口径改述为"文件中须出现这五个词"。

**L1（低）｜`SKILL.md:54` 的"状态一瞥"承诺不成立：`status` 不暴露熔断状态**
- 现象：夹具工作区里存在一个 `熔断状态: 已熔断` 的任务，`checks.py status --workspace` 输出表中无任何熔断信息（实测"输出中含『熔断』字样？否"），且 `0/2`、`0/1` 与正常任务无异 → 无法据 `status` "留意各任务 plan.yaml 的熔断状态"而不打开文件。
- 定位：`SKILL.md:54` vs `scripts/checks.py:237-286`（`cmd_status` 未读取/打印 `熔断状态`）。期望：或在 status 表格加一列/标记，或改述该句（接缝表 `SKILL.md:197` 已写"读 plan.yaml 的 熔断状态"，两处应统一）。

**L2（低）｜"三态"叫法在两侧不完全对齐**
- `SKILL.md:158-164` 标题为「三态（唯一叫法）」并列 3 行（`正常`/`已熔断`/`复位`）；`scripts/checks.py:62` 注释只列"正常（Closed）/ 已熔断（Open）；复位=改回正常并留痕"。值域 `VALID_FUSE` 只有两项本身正确（复位是迁移而非存储态），但"第三态"的措辞在两侧不一致，建议统一为"两态 + 复位迁移"。

**L3（低）｜回归证据未记录命令行，个别组的可复现性依赖外部脚本**
- `回归证据.txt` 只贴输出、不贴命令；R10 的 `--base` 必须是**技能目录**才得 `9/10`（若按同目录 `--base` 复跑得 `9/23 FAIL=14`，因为 R10 用的是真实 plan，其交付物相对工作区根）。实际命令见 `tmp/step7_regression.py:103`（`--base SK`）。期望：每组附一行命令（含 `--base`），避免读者复现歧义。

**L4（低）｜"复位后只允许一次"无机器约束**
- `SKILL.md:191` 规定复位后只允许一次针对性重试，但 `checks.py` 无任何复位计数/状态残留（复位后与从未熔断完全不可区分）。若坚持"机器可判定"的定位（`SKILL.md:156`），建议在熔断报告或 plan 中留可校验的复位次数；否则明确标注该项为纯纪律。

## 我无法确认的项

1. **熔断五步中"冻（未提交/未发布）"与"报（升级用户）"**无法由产出物独立核验：`回归证据.txt` 不构成"已停止自动化/已升级用户"的证据，我也没有本次会话的对话记录可比对（不排除执行者已按流程执行，但按有罪推定，这两步只属**未验证**）。
2. **F1/F2 触发链**（返修 2 轮 + 对抗式互审仍不通过）依赖 `tasks/对抗互审-ai-workflow-v2.5.1-2026-09-11/裁决.md` 等历史材料，不在批 A 清单内，我未逐条回溯其真伪。
3. **`references/ops.md:165` 声称的"144 个交付物 token 中 20 个会转为 FAIL"**（E:\ChatGPT\工作流 历史任务）未在我能力范围内核验：批 A 不含该工作区，且我不主动改动他处文件，仅记录为待核。
4. **`回归证据.txt` 的"数字"与"真相"的关系**：数字我全部复现一致，但"该数字证明的是技能正确"这一层（例如 19/19 是自检项自洽、R2 的 `8/8` 是执行者自己的 plan）我不做超出证据的推断。
