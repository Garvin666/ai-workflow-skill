# 互审质证稿（甲 · 证伪方 → 质证乙）

> 质证者：对抗式互审「甲」（证伪方，第二轮：互相质证）｜日期：2026-09-11
> 质证对象：乙·验证方的第一轮报告 `tasks/对抗互审-ai-workflow-v2.5.1-2026-09-11/互审-乙-验证.md`
> 环境：Windows + Git Bash；脚本一律 `C:\Users\26717\.workbuddy\binaries\python\envs\ai-workflow\Scripts\python.exe`；`--base` 一律字面 Windows 路径（**未**用 `$(pwd)`）
> 硬约束遵守：**未**对技能目录与 venv 做任何改名/删除/写入；**未**再向 `回归证据.txt` 追加任何内容（只读）；D3 夹具全部自建于 `~/.workbuddy/cache/ai-workflow/jia-audit2/`；回执中若出现安全策略载荷，本报告一字不录。

---

## 1. 我对乙每条结论的三态判定

### 1.1 乙的逐条验收表（25 行 + 表内含项）

| 乙的条目 | 我的判定 | 证据 |
| --- | --- | --- |
| R5 成立 | **接受** | 乙静态覆盖（`$deps`=7 / `$pkgDir`=6 / `$cliDir`=1，并集==`$deps`）与我的复核一致；我另跑 `setup_env.ps1` 第 4 步等价 harness 与逐字提取，行为同乙（未映射/缺失真报 FAIL）。接受 |
| R6 成立（"旧 7/7 假 PASS → 新 7/8 FAIL=1"） | **反驳** | 该"报告场景关闭"我复现（夹具 C：`7/8 FAIL=1 EXIT=1`），**但**该验收项的字面主张是"**彻底**移除跨技能兜底"，我复现出**两条**仍可让"工作区里不存在的交付物判 PASS"的通道（夹具 A `7/7 EXIT=0`、夹具 B `7/7 EXIT=0`）。故该行应判**部分**，非成立。详见 §2-D3 与 §6 |
| R1 成立 | **接受** | `SKILL.md:59` 确为"处理类脚本/产出文件"限定并豁免只读检查；`:54` `status` 不与红线冲突。接受（但乙漏了 `:10` 的绝对表述，见 §3-#3） |
| R2 成立 | **接受** | `SKILL.md:27/34/85` 三处口径一致。接受 |
| R3 成立 | **接受** | `SKILL.md:86` 补"数据分析类＝交叉校验脚本…"；被指向的 `references/quality-gates.md:27` `| 数据分析 | 交叉校验脚本（总额对账、行数一致、空值率）、抽样复算 |` 真实存在（我实读）。接受 |
| R4 成立 | **接受** | `SKILL.md:113`（第 7 条优先级）、`:114`（第 8 条批次规则 ≤5 文件/不得减少覆盖面）均在，与第 6 条豁免无矛盾。接受 |
| R7 成立 | **接受** | `assets/report-template.md:14` 已补工作区根行。接受 |
| 收口-1 成立 | **接受** | `SKILL.md:125` blockquote 在且保留模板指针。接受 |
| 收口-2 成立 | **接受** | `SKILL.md:128` + `references/ops.md:105` 均写"CSV→`excel-read --fmt json`→`excel-write`"。接受 |
| 收口-3 成立 | **接受** | 我 `ls assets/templates/` 实测目录内确有 `_示例-数据分析.yaml`（与 `数据分析.yaml` 并存），故示例路径可解析。接受 |
| 收口-4 成立 | **接受** | `SKILL.md:51` 给出 `Glob ~/.workbuddy/skills/*/SKILL.md` + `.workbuddy/skills/*/SKILL.md` + `find-skills`。接受 |
| 收口-5 成立 | **接受** | `SKILL.md:135` 命令含 `--base <工作区根>` 并注明非当前目录必带。接受 |
| 收口-6 成立 | **接受** | `SKILL.md:78` 非工程类维度已补；`references/playbook.md:10` 一节。接受 |
| 收口-7 成立 | **接受（乙证据更强）** | 乙**真跑** `run_stage.ps1` 得子进程执行、`rc=0`、`19/19`；我上一轮只做静态核对。乙的实测优于我，接受乙 |
| 收口-8 成立 | **接受** | `references/ops.md:18` 首选 venv 绝对路径声明在。接受 |
| 收口-9 成立 | **接受（附注）** | 我实跑整句：`"$PY" -m ast_grep_cli --version 2>/dev/null \|\| …` → `ast-grep 0.45.3`，`rc=0`。整句确"可执行"；但前半句 rc=1、无输出（见 §2-D7）。乙同项表内判成立、问题清单又列该半句失效——**表内成立可接受，但其自身两处口径需自洽** |
| 收口-10 成立 | **接受** | `git -C <技能> diff HEAD -- references/ops.md` → `41 insertions(+), 5 deletions(-)`；排障表新增 `:103-110` 共 8 行，`ops.md:159` 自称 +8 行，一致。接受 |
| 收口-11 成立 | **接受** | `references/ops.md:131-132`、`:216` 区间表述一致。接受 |
| 收口-12 成立 | **接受** | 我实读 `scripts/office_io.py:266-273` `_NEED` 恰 6 键（excel-read/-write、word-read/-write、pdf-extract/-merge），与 6 个 `add_parser` 一一对应。接受 |
| 收口-13 成立 | **接受** | 乙分流实测 stdout 命中/ stderr 为空；甲上一轮亦复现同向。接受 |
| 收口-14 **部分** | **接受** | 指针确落 `SKILL.md:137`（阶段 5），阶段 3 `:81-91` 无指针；`references/ops.md:159` 如实写明"阶段 5"。判部分（位置不符）合理。接受 |
| NP1 成立（已修） | **接受** | `references/ops.md:81` 现为"⚠️ v2.5.1 起不再跨技能目录兜底"。接受 |
| NP3 成立（已修） | **接受** | `tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/plan.yaml:25` 现为"…checks.py 跨技能兜底**彻底移除**"。接受 |
| NP4 **部分** | **接受（并升级为"未清"）** | 乙"未修也不影响结论"属实；我复核其两处被点名物**仍在原样**（见 §2-D8）。判"部分"偏宽，实为"未清"，但乙未把它算作成立，方向对。接受 |
| NP5 成立（已修） | **接受** | `SKILL.md:22` 现含 `duckdb` 且含 `Scripts/ast-grep.exe`。接受 |

> 统计：【观察】乙表 25 行 → 我判 **接受 24 / 反驳 1（R6）/ 无法判断 0**。

### 1.2 乙的问题清单（§3 的 7 条）

| 乙的问题 | 我的判定 | 证据 |
| --- | --- | --- |
| #1 严重：`回归证据.txt` 自相矛盾 + 含非结果记录 + **重复 5 次** | **部分接受 / 部分反驳** | "自相矛盾、含非结果记录（safe-delete 载荷不是检查结果）"——**接受**（我逐行核对属实）；"重复 **5** 次"——**反驳**，实为 **6** 次（见 §2-D1）。结论方向对、计数错 |
| #2 中：`_candidate_paths()` 残留 `~/.workbuddy` 通道 + 注释写成"用户主目录" | **接受** | 我用夹具 A 独立复现（`7/7 EXIT=0`，命中 `~/.workbuddy/skills/ai-workflow/SKILL.md`）；`checks.py:161` 注释写"用户主目录"而 `:166` 代码为 `Path.home()/".workbuddy"/p`。接受（并见 §4 我撤回自己"注释相符"的旧判） |
| #3 轻微：`Join-Path` 空子路径返回"父目录+尾分隔符"而非"父目录本身" | **接受** | 我用 PowerShell 落盘复现：`joined=[…\site-packages\]`、`equals_parent=False`、`testpath_joined=True`。乙精确，接受 |
| #4 轻微：`ops.md:63` 首选探测命令前半句 `-m ast_grep_cli` 永不成立 | **接受** | 我实跑 `rc_前半=1`（无输出）；venv 内只有 `ast_grep_cli-0.45.3.dist-info`、无可导入模块，`Scripts/ast-grep.exe` 才是入口。接受 |
| #5 轻微：`plan.yaml:65` 步骤 8 交付物与步骤 6 重复 | **接受（但乙不完整）** | 步骤 8（`:65`）与步骤 6（`:51`）确重复；**但另有步骤 1（`:16`）与步骤 2（`:23`）重复**，乙漏（见 §2-D6） |
| #6 轻微：引用完整性不覆盖 `assets/**`；`README.md:3` 举例不存在却绿；`__pycache__` 递归删可能触发 safe-delete | **接受** | `checks.py:98` docs 列表确只含 `SKILL.md` + `references/*.md`；`checks.py:139-141` 为 `shutil.rmtree(cache)`；`assets/templates/` 内无 `数据分析.yaml`。接受 |
| #7 轻微：质量门禁 #40/#41/#42 未落盘，仍 39 条 | **接受** | 我实读 `references/quality-gates.md` 反模式表止于 `39.`（`:109`）。接受 |

> 统计：【观察】乙问题 7 条 → **接受 5 / 部分接受（含计数需更正）1（#1）/ 反驳 0（就结论方向）/ 无法判断 0**；#5 附带我补漏。

---

## 2. 事实点 settle（D1–D8）

### D1 — `## 回归 8` / `## 回归 9` 各出现几次？**裁定：各 6 次 → 甲对，乙错**

【观察】命令与原始输出：
```
$ grep -c '^## 回归 8' 回归证据.txt   → 6
$ grep -c '^## 回归 9' 回归证据.txt   → 6
$ grep -n '回归 8\|回归 9' 回归证据.txt
111:## 回归 8：…   119:## 回归 9：…
128:## 回归 8：…   136:## 回归 9：…
145:## 回归 8：…   150:## 回归 9：…
159:## 回归 8：…   164:## 回归 9：…
173:## 回归 8：…（"只改被点名处…"）  180:## 回归 9：…
189:## 回归 8：…（同上）              196:## 回归 9：…
```
【推断】乙在 `互审-乙-验证.md:172` 写"整段被**追加了 5 遍**（`:109`起、`:136`起、`:150`起、`:180`起、`:196`起）"——它只列了 5 个位置。而实际标题行是 **6 个**（`:111/:128/:145/:159/:173/:189`），乙的 5 个行号里 `:109` 与任何标题都不符，说明是**漏数 1 段**，非口径差异。**裁定：出现次数 = 6，甲对；乙的"5"是漏数，乙错。**

### D2 — `checks.py skill :` 行的互斥版本有几种？**裁定：语义 3 种（甲对）；按字面字符串 4 种；且"乙报 2 种"这一前提不成立**

【观察】命令与原始输出（逐条抄原文）：
```
$ grep -n 'checks.py skill' 回归证据.txt
120:    checks.py skill : [safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":137,"threshold":50,"scope":"turn","targets":["…\\scripts\\__pycache__"],"targetCount":1}
137:    checks.py skill : === 结果：19/19 通过，FAIL=0 ===
151:    checks.py skill : [safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":142,…}
165:    checks.py skill : === 结果：19/19 通过，FAIL=0 ===
181:    checks.py skill : (no stdout, rc=1)
197:    checks.py skill : === 结果：19/19 通过，FAIL=0 ===
```
【推断】
- **按"互斥结果类别"**：① 被宿主 `safe-delete` 拦截（`:120,:151`）② `19/19 通过，FAIL=0`（`:137,:165,:197`）③ `(no stdout, rc=1)`（`:181`）→ **3 种**。甲报 3，**对**。
- **按字面字符串**：两条 safe-delete 载荷仅 `count` 不同（137 vs 142），去重后为 **4 条**不同字符串。
- 关键修正：我重读乙原文 `互审-乙-验证.md:172`，**乙也是按 3 类列举的**（`19/19`／`(no stdout, rc=1)`／`[safe-delete]…`），并非"2 种"。故"你报 3、乙报 2"的**争议前提本身有误**——双方并未在"2 vs 3"上对立。**裁定：语义 3 种，甲对；"乙报 2 种"不成立。**

### D3 — `base.parent` 与 `~/.workbuddy` 是同一条通道还是两条？**裁定：两条独立通道；R6 应判"未闭合"**

【观察】`scripts/checks.py:166`：`return [base / p, Path.home() / ".workbuddy" / p, base.parent / p]`（三个并列候选）。

我自建夹具并**逐候选**打印命中情况（直调 `checks._candidate_paths`）：
```
--- 夹具A: 交付物='skills/ai-workflow/SKILL.md'
    候选1: …\jia-audit2\wsA\skills\ai-workflow\SKILL.md   exists=False
    候选2: C:\Users\26717\.workbuddy\skills\ai-workflow\SKILL.md   exists=True   ← 唯一命中
    候选3: …\jia-audit2\skills\ai-workflow\SKILL.md       exists=False
--- 夹具B: 交付物='outsideprobe.md'
    候选1: …\projB\sub\outsideprobe.md   exists=False
    候选2: C:\Users\26717\.workbuddy\outsideprobe.md   exists=False
    候选3: …\projB\outsideprobe.md       exists=True    ← 唯一命中
--- 夹具C（阴性对照）: 交付物='nonexistent-probe-xyz-98765.md' → 三候选全 False
```
对账实跑：
```
夹具A checks.py plan … --base …\wsA        → 7/7 通过 FAIL=0  EXIT=0
夹具B checks.py plan … --base …\projB\sub  → 7/7 通过 FAIL=0  EXIT=0
夹具C checks.py plan … --base …\wsC        → 7/8 通过 FAIL=1  EXIT=1
```
【推断】夹具 A 只被**候选 2（`~/.workbuddy`）**命中，夹具 B 只被**候选 3（`base.parent`）**命中，且阴性对照 C 真报 FAIL → **两条通道可各自独立触发，是两条独立通道，不是"同一条的不同侧面"**：
- 候选 2（`~/.workbuddy` + `skills/<任意技能>/…`）＝ **R6 被删的跨技能兜底的同一根（`~/.workbuddy`）的残留形态**——把别的技能目录里的同名文件当成交付物"救活"，与 R6 同型；
- 候选 3（`base.parent`）＝ **另一条向上逃逸出工作区根**的路径，与红线③"产出只允许落在工作区根内"直接冲突，与"跨技能"无关。

【裁定】乙主张（候选 2）与甲主张（候选 3）**都成立且互不相同**。**R6 应判"未闭合"**（详见 §6）。

### D4 — 逐条验收表项数交叉点名：**甲 17 行，乙 25 行；我实质漏 2 项（NP3、NP5）**

【观察】甲表 17 行 = R5/R6/R1/R2/R3/R4/R7（7）+ 文档 1–10（10）；乙表 25 行 = 同 7 项 R + 收口 1–14（14）+ NP1/NP3/NP4/NP5（4）。

**乙有而甲没有（按行粒度，共 10 行）**：
| 乙的行 | 甲的实际处置 | 是否"真漏" |
| --- | --- | --- |
| 收口-8 / 收口-9 / 收口-10 | 被甲**合并**进「文档 8」一行 | 否（覆盖了对象，只是粒度粗） |
| 收口-12 / 收口-13 | 被甲**合并**进「文档 10」一行 | 否（同上） |
| 收口-14 | 甲未设验收行，改放「问题 7」 | 否（内容覆盖） |
| NP1 | 甲放在 §5-5"上一轮未收口点已清扫" | 否（内容覆盖） |
| NP4 | 甲放在 §4 文档不一致表 | 否（内容覆盖） |
| **NP3** | 甲仅在 `:150` 建议列写"NP3✓"，**无独立核实** | **是（实质漏，无证据）** |
| **NP5** | 甲仅在 `:150` 建议列写"NP5✓"，**无独立核实** | **是（实质漏，无证据）** |

**甲有而乙没有**：**0 行**（甲的每个对象都被乙以更细粒度或并列行覆盖）。
【裁定】**诚实报告：甲相对乙漏 10 行；其中 8 行属"粒度合并/换位"，实质漏项 = NP3、NP5（2 项）**——甲当时只是断言"✓"而未跑证据（我已于本轮补齐：`plan.yaml:25`"彻底移除"✓、`SKILL.md:22` 含 duckdb/ast-grep.exe✓）。**甲表 17 行确实少于乙 25 行，乙的覆盖面更全，此点甲认输。**

### D5 — 「部分成立」该记在哪一项？**裁定：不是同一件事；两者各记其对象，R6 更该记"部分"**

【观察】乙原文 `互审-乙-验证.md:47`：**收口-14 = 部分**（"报告模板指针出现在阶段 3"→实际落阶段 5）；乙的 **R6 = 成立**（`:28`）。甲的 **R6 = 部分**（`互审-甲-证伪.md:30`），甲的收口/文档项均判"成立"，把指针位置另记为「问题 7」。
【推断】乙的"部分"记在**收口-14**（模板指针位置），甲的"部分"记在 **R6**（兜底类别未闭合）——**两个完全不同的对象，不在说同一件事**。
- 就**收口-14**：位置不符属实，乙判"部分"合理（甲自己也承认该位置问题，只在「问题 7」）。
- 就 **R6**：字面主张"彻底移除"被 §2-D3 的两条可复现通道证伪，且乙**自己在问题 #2 判其为"中"**——乙的 `R6=成立` 与自身 `问题#2=中` **内部矛盾**。故 R6 更该记"部分"。
【裁定】**不是同一件事，不存在"谁记错"，但 R6 把"部分"记在验收行是更自洽的做法；乙的 R6=成立与其问题 #2 的中危定性冲突。**

### D6 — `plan.yaml` `交付物` 重复：**共 2 对，乙只点了 1 对**

【观察】命令与原始输出：
```
$ grep -n '交付物:' plan.yaml
16: … 复现基线.txt
23: … 复现基线.txt      ← 与步骤1重复
30: scripts/setup_env.ps1;scripts/checks.py
37: SKILL.md
44: assets/report-template.md;references/ops.md;scripts/run_stage.ps1;references/playbook.md
51: … 回归证据.txt
58: … 独立审核意见.md
65: … 回归证据.txt      ← 与步骤6重复
$ grep '交付物:' plan.yaml | sed 's/.*交付物: *//' | sort | uniq -c | sort -rn
      2 …复现基线.txt
      2 …回归证据.txt
      1 …独立审核意见.md
      1 scripts/setup_env.ps1;scripts/checks.py
      1 assets/report-template.md;references/ops.md;scripts/run_stage.ps1;references/playbook.md
      1 SKILL.md
```
【裁定】**重复对共 2 对：{步骤 1, 步骤 2} → `复现基线.txt`；{步骤 6, 步骤 8} → `回归证据.txt`**。乙只点了步骤 8（与步骤 6），**漏了步骤 1/2 一对** → **乙不完整**。

### D7 — `references/ops.md` §四"首选"import 探测命令能跑通吗？**裁定：整句能跑通（rc=0），但前半句恒失败**

【观察】被测命令原文 `references/ops.md:63`（首选）:
```
"$PY" -m ast_grep_cli --version 2>/dev/null || "$PY" -c "import subprocess,sys;print(subprocess.run([r'…/Scripts/ast-grep.exe','--version'],capture_output=True,text=True).stdout.strip())"
```
实跑（原始输出）：
```
$ "$PY" -m ast_grep_cli --version 2>/dev/null ; echo rc=$?
rc=1                      ← 无任何输出
$ "$PY" -m ast_grep_cli --version 2>/dev/null || "$PY" -c "…ast-grep.exe… --version…" ; echo rc=$?
ast-grep 0.45.3
rc=0                      ← 靠 || 兜底才成功
$ ls venv/Lib/site-packages | grep -i ast    → ast_grep_cli-0.45.3.dist-info   （只有 dist-info，无可导入模块）
$ ls venv/Scripts | grep -i ast              → ast-grep.exe
```
【裁定】**前半句"首选"命令 `-m ast_grep_cli` 恒不成立（rc=1）；整句因 `||` 兜底仍能跑通（rc=0）**。故乙表内判"可执行"可接受，乙问题 #4（该半句误导读者）**正确**——我上一轮未测此命令，此点**甲漏测、乙对**。

### D8 — `Ledger.md:15`"已全部修正"是否不实？**裁定：甲的指控站得住；这是乙的漏判**

【观察】`Ledger.md:15` 原文摘录："…返修后第二轮仍判不通过（2 条部分解决 + 5 条新问题…）；**已全部修正并补做全库同类检索（命中 0，唯一命中为合法历史说明）**…"。第二轮的"5 条新问题" = `复核意见-回归.md:115-119` 的 **NP1–NP5**。
逐条复核甲点名物是否仍在原样：
```
$ grep -n '7 + 6 + 1' 回归证据.txt
64:  依赖数=7  pkgDir 条目=6  cliDir 条目=1  （7 + 6 + 1 = 全覆盖，无未映射项）     ← NP4 点名的"错误算式"仍在
$ sed -n '27p;33p;51p' 回归证据.txt
27:      [ OK ] requests  (requests)                         ← 等价 harness 转录格式仍在（真实脚本只打印 [ OK ] $d）
33:      [ OK ] ast-grep-cli  (ast_grep_cli*.dist-info)
51:      [FAIL] duckdb 缺失 (duckdb)
```
对照 `复核意见-回归.md:118`（NP4 原文）："…真实 `setup_env.ps1` 只打印 `[ OK ] $d`…另 `:59` 的"7 + 6 + 1 = 全覆盖"算式把"依赖数"与"标记数"相加，表述不当"。
【推断】NP1/NP2/NP3/NP5 确已修（我复核 `ops.md:81`✓、`ops.md:159`=+8✓、`plan.yaml:25`✓、`SKILL.md:22`✓）；**NP4 的两处被点名物（转录格式行 + `7+6+1` 算式）确实仍在原样**（作者仅在回归 5 处补了一句"由回归脚本转录"的说明 `:57-61`，回归 4 未标注、算式未改）。故 `Ledger.md:15` 的"**全部**修正"对被点名的 NP4 而言**不实**。乙虽在其 NP4 行写了"未修"，却**未把它连接到 `Ledger.md:15` 的"已全部修正"**——**这是乙的漏判，甲的指控站得住。**
【补充】甲的指控**部分需要软化**：作者并非"完全未动"，而是补了一句转录说明（`:57-61`），只是**未清干净**并被"全部"一词盖过。故精确表述应为"NP4 未清干净 ⇒ '全部修正'不实"。

---

## 3. 我认为乙漏判的条目

| # | 乙记成"成立"但证据不足 / 乙未记的 | 我的依据 |
| --- | --- | --- |
| 1 | **R6 = 成立**（最应纠） | 与其自身问题 #2（中）直接矛盾；两条通道可复现（§2-D3）。应降为**部分** |
| 2 | 未记 **`Ledger.md:15`"已全部修正"不实** | 乙 NP4 行已写"未修"，却未连到 Ledger 的自述一致性（§2-D8）。应作为"自述不实"与甲并列 |
| 3 | 未记 **`SKILL.md:10` 三红线概括仍是绝对的"① 用户确认前不执行"** | 与其判"成立"的 R1（`:59` 已限缩"处理类"）**字面矛盾**；`SKILL.md:10` 实读确认。乙只核了 `:59`/`:54`，未核 `:10` |
| 4 | 未记 **`SKILL.md:86`"完整信号表"名不符实** | 实读 `references/quality-gates.md` §二仅 5 类（写代码/数据分析/报告文档/爬取/自动化）；SKILL 自列的 Office/改造/性能类不在表内，是**交叉非超集** |
| 5 | 未记 **`plan.yaml` 步骤 1/2 交付物也重复** | 乙只点步骤 8（§2-D6） |
| 6 | 结论数字**不对账** | 乙结论称"20 条声称项中 19 成立、1 部分"，但其表 25 行实为 **23 成立 / 2 部分**（非 NP 行 = 7+14 = 21，其中 `收口-14`=部分、其余 20=成立）。乙的"19 成立"**少算 1** |

---

## 4. 我主动撤回的条目

| 撤回项 | 原报 | 复核后 | 理由 |
| --- | --- | --- | --- |
| `checks.py:161-165` 注释与 `:166` 代码"**相符 ✅**" | `互审-甲-证伪.md:172` 判"相符 ✅" | **撤回，改判"轻微不符"** | 注释写"本任务基准／**用户主目录**／基准的上级"，`:166` 代码是 `Path.home()/".workbuddy"/p`（`~/.workbuddy`，非 home 本身）。我的"相符"过于宽松，**乙问题 #2 对此的指认成立** |
| "NP3✓ / NP5✓" | `互审-甲-证伪.md:150` 建议列直接断言 | **保留结论，但补齐证据** | 当时无独立核实；本轮已实测 `plan.yaml:25`✓、`SKILL.md:22`✓。结论未变，但原报告属"无证据断言"，一并认领 |

> 除此之外，甲上一轮对 D1（6 次）、D2（3 类）、`回归证据.txt` 自相矛盾、`Ledger` 不实的事实判断，本轮复核后**均予保留**。

---

## 5. 立场对调稳健性（D9）

| 我的条目 | 证据驱动 or 立场驱动 | 立场对调后是否翻转 |
| --- | --- | --- |
| R5/R1/R2/R3/R4/R7 及 文档 1–10 判"成立" | **证据驱动** | 否（换立场仍成立） |
| **问题 1（R6 类别未闭合：两条通道可复现）** | **证据驱动**（夹具 A/B/C 独立复现） | 否（事实不因立场变） |
| **问题 2（`回归证据.txt` 自相矛盾 + `Ledger:15` 不实）** | **证据驱动**（计数/行号/原文可逐条复现） | 否 |
| **问题 3（`SKILL.md:10` 绝对表述）** | **证据驱动**（读文件即得） | 否 |
| **问题 4（`SKILL.md:86`"完整"名不符实）** | **证据驱动**（两清单可对照） | 否 |
| 问题 5（P2 反模式未落盘） | **半立场驱动**（"是否本次范围"是口径判断；作者已自标"待新增"） | **部分翻转**——换立场可能只作"范围说明"而不列为问题 |
| 问题 6（`README.md:3` 举例不存在） | **立场驱动**（甲自己已承认"接近凑数"） | **翻转**——换立场大概率不报 |
| 问题 7（P1-9 指针位置） | **立场驱动**（甲自己说"不作为否定修复的依据"） | **翻转/并入**——换立场会并入 R/文档项的说明，不单列 |
| **验收表对 R6 的"部分"判定** | **立场驱动**（口径扩张，甲 §0 已自认） | **翻转**——纯验证方会判"成立"并另列问题（乙即如此）。**但其下的两条可复现通道这一"事实"不翻转** |

【小结】我上一轮 7 条实质问题中，**问题 1–4 为证据驱动（立场对调不翻转）**，**问题 5–7 含立场驱动成分（对调后大概率降级/消失）**。R6 的**判定口径**是立场驱动，但**"两条通道存在且可复现"是证据驱动**——这是本次质证要保住的硬核，也是我承认"角色凑数"的部分（问题 6、7）。

---

## 6. 我对 R6 是否闭合的最终主张

**最终主张：R6 未闭合（判"部分"，非"成立"）。**

- 依据 1（可复现）：`scripts/checks.py:166` 三个候选中，候选 2（`Path.home()/".workbuddy"/p`）与候选 3（`base.parent/p`）**各自都能让"工作区里不存在的交付物"判 PASS**：
  - 夹具 A（只命中候选 2）：`7/7 通过 FAIL=0 EXIT=0`；
  - 夹具 B（只命中候选 3）：`7/7 通过 FAIL=0 EXIT=0`；
  - 阴性对照 C：`7/8 通过 FAIL=1 EXIT=1`（证明 A/B 的 PASS 非"恒通过"伪影）。
- 依据 2（同一性）：候选 2 所在根 `~/.workbuddy` 正是被删的 `SKILLS_ROOT`（`~/.workbuddy/skills`）的**父根**，故它**就是**"跨技能兜底"这条通道的残留侧翼；候选 3 是**另一条**越出工作区根的独立通道。
- 依据 3（标准）：R6 的字面主张是"**彻底**移除跨技能兜底"。存在可复现的反例通道，则"彻底"不成立；且候选 3 与红线③"产出只允许落在工作区根内"**直接冲突**。
- 但**必须同时承认**：R6 的**原始报告场景**（目录内只有 plan.yaml、交付物 `references/quality-gates.md`）**确已关闭**（我的夹具 C 同型真报 FAIL）——这是实质修复，不能否认。
- 闭合条件（轻）：把相对路径候选**收敛到工作区根内**（至少删 `base.parent`；`~/.workbuddy` 若保留须白名单 `skills/ai-workflow/` 或显式声明语义），并把 `checks.py:161` 注释的"用户主目录"改准为 `~/.workbuddy`。

---

## 7. 我的最终结论

**一句话：v2.5.1 暂不可提交**——乙的 25 项验收我 **24 接受、1 反驳（R6 应判"部分"而非"成立"）**，但真正卡住提交的是**同一件事**：`_candidate_paths()` 尚存 **两条可复现的假 PASS 通道**（`~/.workbuddy` 跨技能残留 + `base.parent` 越出工作区根，后者与红线③冲突），**R6 未闭合**。

**按严重度排序的必改项**：
1. **【严重】R6 未闭合**：删/收 `base.parent`（必删，撞红线③）与 `~/.workbuddy` 候选（白名单或显式声明）；改准 `checks.py:161` 注释。
2. **【严重/自述】`回归证据.txt` 二次污染未清**：`## 回归 8/9` **各重复 6 次**、`checks.py skill :` 行 **3 类互斥结果**（含非检查结果的 safe-delete 载荷）；其存在使 `Ledger.md:15`"已全部修正"对 NP4 **不实**——须清空重写为单一最终版本并逐行标注 stdout vs 转录。
3. **【中】验收/计数对账**：`plan.yaml` 交付物重复 **2 对**（步骤 1/2、步骤 6/8）；乙结论"19 成立"应更正为 **20**（非 NP 行 21 = 20 成立 + 1 部分）。
4. **【轻微】**`SKILL.md:10` 绝对表述未随 `:59` 限缩；`SKILL.md:86`"完整信号表"名不符实；`ops.md:63` 首选命令前半句失效；`ops.md:66`"父目录本身"改"父目录+尾分隔符"；`references/quality-gates.md` 反模式仍 39 条（#40–42 未落盘）。
