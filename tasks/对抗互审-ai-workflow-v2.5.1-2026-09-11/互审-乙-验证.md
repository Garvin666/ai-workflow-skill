# 互审报告（乙 · 验证方）

> 审核者：互审子代理「乙」（验证方，未参与本次修复）｜日期：2026-09-11
> 被审对象：ai-workflow v2.5.1 修复包（**10 个已修改文件的未提交工作区状态**，git HEAD = `e89a005` = v2.5.0）
> 方法：**以文件实际内容与亲跑输出为准**，不采信 `ops.md` 变更日志、`回归证据.txt`、`Ledger.md` 的任何自述；R5/R6 亲手复现；对 v2.5.0 旧版（`git show HEAD:scripts/checks.py`）做对照。
> 解释器一律 `C:\Users\26717\.workbuddy\binaries\python\envs\ai-workflow\Scripts\python.exe`（实测 `Python 3.13.14`）；`--base` 一律字面 Windows 路径（**未**用 `$(pwd)`）。
> **未对技能目录或 venv 做任何改名/删除/写入**（遵循环境硬约束）。临时产物全部落 `~/.workbuddy/cache/ai-workflow/yi-verify/`，R5/R6 夹具自建。

---

## 0. 立场自查

我**受到了**"验证方"立场的影响，风险点有三处，先摊开：

1. **我倾向于把"声称 20 条 → 实测 20 条落地"读成"修复成立"**。为对冲，我对每条都另找一个"作者没被点名、但同类"的点做全库检索（§3-#2、#6），并**故意在同一份代码里找反例**而不是只看被点名处。
2. **三处我"没验到"而非"验过了"就放行的地方**，我已全部记入 §5，不计入"已验证"：① `setup_env.ps1` **从未整脚本真跑**（真跑会触发 pip 安装 = 对 venv 写入，属明令禁止），R5 结论建立在"从原文逐字提取的等价 harness + 机制复现 + 静态覆盖核对"上；② 未做 venv 改名实验（禁止），"真实缺失"用"指向不存在目录"模拟；③ 未验证 git 提交后的行为（工作区未提交）。
3. **我最初预期 `run_stage.ps1` 在本机会静默 no-op（`ops.md` 如此描述），实测相反**：它在 PowerShell 工具通道下**真的执行了**子进程并传回 `rc=0`。我把这个"预期被推翻"如实记为观察（§4），没有为了让叙事自洽而删掉。

---

## 1. 逐条验收表

> 判定列取值：成立 / 不成立 / 部分 / 无法确认。**"成立"= 我亲眼看到代码或输出成立**。

| 编号 | 声称的修法 | 我实测看到什么 | 证据（文件:行 / 命令原文） | 判定 |
| --- | --- | --- | --- | --- |
| **R5** | `setup_env.ps1` 依赖自检：双表 `$pkgDir`(6)+`$cliDir`(1)，未映射显式 FAIL | `$deps`=7 项、`$pkgDir`=6 项、`$cliDir`=1 项；**并集 == `$deps` 精确相等**，无空标记值、两表无交集；未映射分支 `[FAIL]`+`$failedList += $d`；`:68` `exit 1`。行为：正常清单 7 项全 OK；注入假依赖 → `[FAIL] definitely-not-real-xyz has no marker`；把 duckdb 标记指向不存在目录 → `[FAIL] duckdb not found` | `scripts/setup_env.ps1:8,42,46,50-51,56-59,68`；命令见 §2-#1/#3/#4 | **成立**（带 §5-1 说明） |
| **R6** | `checks.py` `_candidate_paths()` 彻底移除跨技能兜底 | 新版只 3 个候选；**旧版 7/7 假 PASS → 新版 7/8 FAIL=1**，同一夹具同一命令 | `scripts/checks.py:156-166`；命令与原始输出见 §2-#2 | **成立** |
| **R1** | 红线①改"禁止执行**处理类**脚本"，豁免只读检查 | `SKILL.md:59` 原文成立；阶段 0 第 5 条（`:54`）用 `status` 不再与红线冲突 | `SKILL.md:59`、`:54` | **成立** |
| **R2** | `tasks/` 口径统一为"一律建在工作区根下" | `:27`「任务目录一律建在工作区根下的 `tasks/`…技能目录的 `tasks/` 只记录技能自身演进类的任务」；`:34` 例外表限缩；`:85` 阶段 3 同步。全库无相反口径 | `SKILL.md:27,34,85` | **成立** |
| **R3** | 信号清单补"数据分析类" | `:86` 补「数据分析类＝交叉校验脚本（总额对账／行数一致／空值率）+ 抽样复算」并指向 quality-gates；**被指向的表真的存在**该行 | `SKILL.md:86`；`references/quality-gates.md:27`（`| 数据分析 | 交叉校验脚本… |`） | **成立** |
| **R4** | 独立审核补优先级 + 批次规则 | 第 7 条优先级（豁免仅在未命中四类时适用）、第 8 条批次规则（≤5 文件、不得以合并减少覆盖面）均在；与第 6 条豁免无矛盾 | `SKILL.md:113,114` | **成立** |
| **R7** | `report-template.md` 补「工作区根」行 | 第 6 行已补，含红线③依据 | `assets/report-template.md:14` | **成立** |
| **收口-1** | 阶段 4 非 Office 跳过 | 引用块已加，且保留报告模板指针 | `SKILL.md:125` | **成立** |
| **收口-2** | `excel-write` 只吃 JSON | SKILL 主干与排障表两处都写明"CSV → `excel-read --fmt json` → `excel-write`"，并注明直接喂会抛 `JSONDecodeError` | `SKILL.md:128`；`references/ops.md:105` | **成立** |
| **收口-3** | L1 排除 `_` 开头 + 注明需阶段 0 后确认 | 两项都在；示例路径 `assets/templates/_示例-数据分析.yaml` 经 `_resolve` 可解析（我的全库扫描 35 条引用中它是"已解析"项） | `SKILL.md:17` | **成立** |
| **收口-4** | 阶段 0 列本地 skill 给做法 | 给了 `Glob ~/.workbuddy/skills/*/SKILL.md` + `.workbuddy/skills/*/SKILL.md` + `find-skills` 指向 | `SKILL.md:51` | **成立** |
| **收口-5** | 阶段 5 对账补 `--base` | 命令含 `--base <工作区根>` 并注明"非当前目录时必须带" | `SKILL.md:135` | **成立** |
| **收口-6** | 阶段 2 非工程类维度 + playbook 数据分析 | `:78` 非工程类维度已补；playbook 加了"通用数据分析类也参照本节" | `SKILL.md:78`；`references/playbook.md:10` | **成立** |
| **收口-7** | `run_stage.ps1` 加执行后自检 | 逻辑为"先清 `$LASTEXITCODE`，调用后仍为空 ⇒ `[FAIL]` 退出 1"。**我真跑了**它：子进程执行、`rc=0`、`19/19`，**未误判** | `scripts/run_stage.ps1:27-36`；实测见 §2-#5 | **成立** |
| **收口-8** | ops.md §一 首选直调 venv 声明 | `:18` 明确"首选 venv 绝对路径 + 脚本；ps1 仅作交互式终端备选"，并说明宿主差异 | `references/ops.md:18` | **成立** |
| **收口-9** | ops.md §四 import 探测命令 | 命令存在且**可执行**（见 §3-#4 的例外） | `references/ops.md:58-63`；实测见 §2-#6 | **成立** |
| **收口-10** | 排障表新增行（前轮 NP2 称"+6 行"不实） | 实为 `:103~:110` **8 行**新增；`ops.md:159` 现自称 **+8 行**，与文件事实一致 | `references/ops.md:103-110`；`references/ops.md:159` | **成立**（NP2 已修） |
| **收口-11** | `--check-links` 均值改区间 | SOP 处改"正常条 ~150-260ms，遇超时条可达数秒——不要把它当固定基线"，v2.3 日志处同步"不可当基线引用" | `references/ops.md:131-132,216` | **成立** |
| **收口-12** | `office_io.py` 按子命令前置依赖检查 | `_NEED` 覆盖全部 6 子命令；我 monkeypatch 模拟缺 `openpyxl` 实测：`[ERROR]`+2×`[HINT]`+`SystemExit code = 2`（不再是裸 traceback） | `scripts/office_io.py:206-221,266-276`；实测见 §2-#7 | **成立** |
| **收口-13** | `data_query.py` 提示改走 stdout | 实测：`2>/dev/null` 时命中 1 次，`1>/dev/null` 时命中 0 次 → 确在 stdout | `scripts/data_query.py:140-142`；实测见 §2-#8 | **成立** |
| **收口-14** | 报告模板指针出现在**阶段 3**交付物处 | 指针落在**阶段 5 第 4 条**（`SKILL.md:137`），阶段 3（`:81-91`）**无**该指针。作者在 `ops.md:159` **如实写明**是"阶段 5" | `SKILL.md:137`；`references/ops.md:159` | **部分**（位置与原要求不符，但自述诚实） |
| **NP1（前轮遗留）** | `ops.md` 仍写"skill 内相对路径会逐技能目录尝试" | **已修**：`:81` 现为"⚠️ **v2.5.1 起不再跨技能目录兜底**"。全库检索 `逐技能目录尝试` **在技能正文 0 命中** | `references/ops.md:81`；检索见 §2-#9 | **成立**（已修） |
| **NP3（前轮遗留）** | `plan.yaml` 步骤 3 仍写"兜底**收窄**" | **已修**：`plan.yaml:25` 现为"checks.py 跨技能兜底**彻底移除**" | `tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/plan.yaml:25` | **成立**（已修） |
| **NP4/转录保真度** | 回归证据里"原始输出"实为 harness 转录 | **未修也不影响结论**，但文件里仍无"转录自 harness"的标注（仅回归 5 有一处） | `回归证据.txt:57-61`（仅回归 5 标注） | **部分** |
| **NP5（前轮遗留）** | `SKILL.md:22` 环境预检只列 5 个依赖，缺 duckdb/ast-grep-cli | **已修**：`:22` 现含 `duckdb` 且含 `Scripts/ast-grep.exe` 检查 | `SKILL.md:22` | **成立**（已修） |

---

## 2. 我独立复现的硬证据（含命令与原始输出）

### #1 R5 根因机制（`Join-Path` 遇空子路径）——纯 PowerShell，未碰 venv
```
parent      = C:\Users\26717\.workbuddy\binaries\python\envs\ai-workflow\Lib\site-packages
empty child = []
Join-Path   = [C:\Users\26717\.workbuddy\binaries\python\envs\ai-workflow\Lib\site-packages\]
equals parent ? False
Test-Path joined = True
Test-Path parent = True
IsNullOrEmpty('') = True
```
→ 空映射 ⇒ 标记为空 ⇒ `Join-Path` 退化为父目录 ⇒ `Test-Path` 恒真。**R5 根因成立**。
（精确说：返回父目录**+尾分隔符**，字符串不完全等于父目录，但 `Test-Path` 仍为 `True` —— 见 §3-#3。）

### #2 R6 新旧版对照（自建夹具 `cache/ai-workflow/yi-verify/r6-empty/`，**目录内只有 plan.yaml**）
命令：`"<PY>" "<SK>/scripts/checks.py" plan "C:\...\r6-empty\plan.yaml" --base "C:\Users\26717\.workbuddy\cache\ai-workflow\yi-verify\r6-empty"`
```
=== ai-workflow 计划对账 ===
[FAIL] 步骤 1 交付物缺失 — references/quality-gates.md
[ OK ] Anti-drop 对账 — 已完成步骤 1 个，交付物确认 0 个
=== 结果：7/8 通过，FAIL=1 ===      EXIT=1
```
旧版对照（`git show HEAD:scripts/checks.py` 导出后同命令、同夹具）：
```
[ OK ] Anti-drop 对账 — 已完成步骤 1 个，交付物确认 1 个
=== 结果：7/7 通过，FAIL=0 ===      EXIT_OLD=0
```
→ **R6 确证关闭**：旧版把别的技能目录里的 `references/quality-gates.md` 当命中（假 PASS）。

### #3 R5 静态覆盖 + 行为等价 harness（从 `setup_env.ps1` **原文**提取 `$deps`/`$pkgDir`/`$cliDir` 与第 4 步循环体，`Write-Host` 原样、以 `*>&1` 捕获）
```
deps      = ['requests','openpyxl','python-docx','pypdf','pyyaml','duckdb','ast-grep-cli']   N = 7
pkgDir    = {'requests':'requests','openpyxl':'openpyxl','python-docx':'docx','pypdf':'pypdf','pyyaml':'yaml','duckdb':'duckdb'}  N = 6
cliDir    = {'ast-grep-cli':'Scripts\\ast-grep.exe'}  N = 1
union N = 7 ; unmapped = [] ; extra = [] ; pkgDir∩cliDir = [] ; empty marker values: []
guard IsNullOrEmpty present: True ; deps coverage exact: True
```
行为：
```
===== (a) normal deps =====
[ OK ] requests … [ OK ] ast-grep-cli          （7 项全 OK）
===== (b) inject unmapped fake dep =====
[FAIL] definitely-not-real-xyz has no marker in pkgDir/cliDir (add one; silent pass is forbidden)
===== (c) duckdb 标记指向不存在目录 =====
[FAIL] duckdb not found: …\Lib\site-packages\duckdb_DOES_NOT_EXIST_xyz
```
→ 未映射项**不再静默通过**，缺失项**真报 FAIL**。

### #4 R6 残留通道探针（**同类的其他未清扫处**，见 §3-#2）
夹具 `r6b-residual/`（目录内只有 `plan.yaml`），`交付物: "skills/ai-workflow/references/quality-gates.md"`：
```
[ OK ] Anti-drop 对账 — 已完成步骤 1 个，交付物确认 1 个
=== 结果：7/7 通过，FAIL=0 ===      EXIT=0
```
而 `ls` 显示工作区内**无**该文件；命中来自 `Path.home()/".workbuddy"/p` 候选。

### #5 `run_stage.ps1` 真跑（考它新加的执行后自检，通道 stdout 落盘后转码）
```
=== AI Workflow status check ===
[ OK ] venv: …\Scripts\python.exe
[ OK ] script: …\scripts\checks.py
[ -- ] AI_API_KEY (only needed for AI calls)
=== ai-workflow 技能自检 ===
… （中文经本通道出现 mojibake，属宿主通道现象）
=== 结果：19/19 通过，FAIL=0 ===
rc=0
```
→ **与我的预期相反**（`ops.md` 说本机该通道 ps1 内子进程常不执行）：本次子进程**执行了**、退出码正常、后置自检**未误判**。

### #6 ops.md 的探测命令
```
--- (1) import probe ---              deps OK                        rc=0
--- (2) python -m ast_grep_cli --version ---  No module named ast_grep_cli   rc=1
--- (3) subprocess fallback ---       ast-grep 0.45.3                rc=0
```

### #7 `office_io.py` 依赖守卫（monkeypatch 模拟缺 `openpyxl`）
```
[ERROR] 缺少依赖：openpyxl
[HINT] pip install openpyxl（或先跑 scripts/setup_env.ps1 初始化 venv）
[HINT] 或用 venv 解释器：C:\Users\26717\.workbuddy\binaries\python\envs\ai-workflow\Scripts\python.exe
SystemExit code = 2
```

### #8 `data_query.py files` 的 `[提示]` 流归属
```
stdout only  -> 1
stderr only  -> 0
```

### #9 全库同类检索（不只看被点名处）
```
$ grep -rn "SKILLS_ROOT" scripts/            -> (0 matches)
$ 全库 35 条反引号文件引用解析：
  UNRESOLVED: Scripts/ast-grep.exe（venv 相对路径，存在）
              plan.yaml（业务白名单）
              sg.exe（venv 内存在，已核）
              回归证据.txt / 复现基线.txt（任务内文件，未给路径）
              数据分析.yaml（templates/README.md:3，示例性写法，实际不存在——见 §3-#6）
$ 技能正文（SKILL.md + references/ + assets/ + scripts/）内 "逐技能目录尝试" 命中 = 0
```

### #10 改动范围与硬约束
```
git status --short  -> 恰好 10 个已改文件（M Ledger.md / SKILL.md / assets/report-template.md /
   references/ops.md / references/playbook.md / scripts/{checks,data_query,office_io,run_stage,setup_env}）
setup_env.ps1: total=3565 bytes, non-ASCII=0
run_stage.ps1: total=2008 bytes, non-ASCII=0
```

---

## 3. 问题清单

| # | 严重度 | 问题 | 证据 | 是否可复现 | 建议 |
| --- | --- | --- | --- | --- | --- |
| 1 | **严重** | **`回归证据.txt` 自相矛盾、含非结果记录，且重复 5 次**。同一份"原始证据"里，唯一的首要信号 `checks.py skill` 出现**互相矛盾**的记录：`19/19 通过，FAIL=0`（`:137,165,197`）／`(no stdout, rc=1)`（`:181`）／`[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":137,…}`（`:120,151`，这甚至**不是检查结果**）。"回归 8/9"整段被**追加了 5 遍**（`:109`起、`:136`起、`:150`起、`:180`起、`:196`起）。这是一份**关键产出**（回归证据），其自身记录不可信 | `回归证据.txt:109-127 / 136-143 / 150-157 / 180-187 / 196-203` | 是（读该文件即可） | 清空重写：只保留**最终一次**、且标注每行是"脚本原样 stdout"还是"等价 harness 转录"；`safe-delete`/空输出行须注明是宿主拦截而非检查结果 |
| 2 | **中** | **"彻底移除跨技能兜底"未完全成立 —— 同类残留通道仍在**。`_candidate_paths()` 仍含 `Path.home()/".workbuddy"/p`；交付物写 `skills/<任意技能>/<相对路径>` 时，工作区里**不存在**的文件仍会被 `~/.workbuddy` 候选**救活**（实测 7/7 FAIL=0）。且 `checks.py:161-166` 注释把该候选写成"**用户主目录**"，而代码实际是 `~/.workbuddy`（`~/p` 与 `~/.workbuddy/p` 不是一回事）——**正是作者反复翻车的"描述与实现不一致"** | `checks.py:161-166`（注释）与 `:166`（代码）；`ops.md:110`；实测见 §2-#4 | 是 | ① 注释/`ops.md` 把候选名改为 `~/.workbuddy`；② 要么在该根下再加"仅允许 `skills/ai-workflow/`"的白名单，要么显式声明"该候选本身就是技能内部文件的入口，不保证不跨技能" |
| 3 | 轻微 | `ops.md:66` 与 `checks.py` 注解说 `Join-Path` 遇空子路径"返回**父目录本身**"；实测返回父目录**+尾分隔符**（`equals parent = False`）。对 `Test-Path` 结论无影响，但属不精确 | 实测 §2-#1；`ops.md:66`；`checks.py:163-164` | 是 | 改为"返回父目录（带尾分隔符）" |
| 4 | 轻微 | `ops.md:63` 的**首选**探测命令 `"$PY" -m ast_grep_cli --version` **永不成立**（`No module named ast_grep_cli`），仅靠 `\|\|` 兜底成功。读者照抄前半句会以为装坏了 | 实测 §2-#6；`ops.md:63` | 是 | 把 `-m ast_grep_cli` 删除或降为注释，直接给 `Scripts\ast-grep.exe --version` |
| 5 | 轻微 | `技能增强-ai-workflow-v2.5.1/plan.yaml:65` 步骤 8（"交付并向用户请求验收；履历追加一行"）的 `交付物` 填的是 `回归证据.txt`——与步骤 6 **重复**，而其真实产物（Ledger 行/验收确认）未声明。使对账的"交付物确认 8 个"含水分 | `plan.yaml:65` vs `:51`；实测三份 plan 均 7/7（对账**不会**因此 FAIL，故属"报得虚高"而非"报得漏"） | 是 | 步骤 8 交付物改填 `Ledger.md` 或"验收确认记录" |
| 6 | 轻微 | `checks.py skill` 的"文档引用完整性"只扫 `SKILL.md` + `references/*.md`，**不覆盖 `assets/**`**：`assets/templates/README.md:3` 的 `数据分析.yaml` 实际不存在却检出为绿。另 `checks.py:139-141` 对 `__pycache__` 递归删除，规模大时会触发宿主 `safe-delete`（作者回归 9 的记录即此形态），使首要信号**看去像失败** | `checks.py:98`（docs 列表）、`checks.py:139-141`；全库扫描 §2-#9；作者记录 `回归证据.txt:120` | 是 | ① 把 `assets/**/*.md` 纳入引用检查；② `__pycache__` 清理改为 `py_compile` 前删一次单文件、或用 `-B` 不生成字节码，避免批量删除触发拦截 |
| 7 | 轻微 | P2 反模式（#40 用不对口实验反驳、#41 推断当观察、#42 改一处忘一处 + #19 追加"标识非空 + 负向测试"）**未落地**，`quality-gates.md` 仍 39 条 | `quality-gates.md:89`（#19 未追加新句）、池尾 `:109`（止于 39） | 是 | 不属本次声称范围；但 §四 已列，建议下一迭代补，勿长期悬空 |

---

## 4. 原有信号实测结果

| 信号 | 我实测 | 作者自述 | 一致？ |
| --- | --- | --- | --- |
| `checks.py skill` | `=== 结果：19/19 通过，FAIL=0 ===`，`EXIT=0`（含"检查 25 条引用"、8 个模板各 `9/9`、5 个 `py_compile`） | 19/19 FAIL=0 | 一致 |
| `plan 技能增强-ai-workflow-v2.5.1-2026-09-10 --base <技能目录>` | `7/7 通过，FAIL=0`，`EXIT=0`（8 步/确认 8） | 7/7 | 一致 |
| `plan 体验评估-ai-workflow-v2.5.0-2026-09-10 --base <技能目录>` | `7/7 通过，FAIL=0`，`EXIT=0`（6 步/确认 6） | 7/7 | 一致 |
| `plan 技能增强-ai-workflow-v2.4.4-2026-09-10 --base <技能目录>` | `7/7 通过，FAIL=0`，`EXIT=0`（6 步/确认 6） | 7/7 | 一致 |
| `scripts/setup_env.ps1` 非 ASCII 字节数 | **0** / 总 3565 字节 | 0 | 一致 |
| `scripts/run_stage.ps1` 非 ASCII 字节数 | **0** / 总 2008 字节 | 0 | 一致 |
| `SKILLS_ROOT` 在 `scripts/*.py` | **0 处** | 0 | 一致 |

→ **原有验证信号未回退**。三份 plan 均未因"移除跨技能兜底"被误伤（技能内部相对路径交付物由 `base/p` 正常解析）。

---

## 5. 我无法确认的项（**重点，不得留空**）

1. **`setup_env.ps1` 从未整脚本真跑**（R5 的最大盲区）。真跑会执行 `pip install`（对 venv 写入 + 联网），**违反环境硬约束**，故我只做：机制复现（§2-#1）+ 从原文逐字提取 `$deps`/`$pkgDir`/`$cliDir`/第 4 步循环体的等价 harness（§2-#3）+ 静态覆盖核对。**风险**：若提取范围遗漏了某个副作用行（`$ErrorActionPreference`、`$sitePkgs` 的 `Join-Path` 若失败等），等价 harness 的行为可能与整脚本真跑不同。**该条不达标为"已验证"**。
2. **未做 venv 改名实验**（禁止）。"真实缺失"我用"把 duckdb 标记指向不存在目录"模拟，**未**用真实目录缺失/改名；两者的失败分支相同，但严格意义上不等价。
3. **未验证 git 提交后的行为**：工作区未提交，`git HEAD` 仍是 v2.5.0；提交动作本身（`.gitattributes` 的 CRLF/LF 转换，见 `git status` 警告：`SKILL.md`/`checks.py` 将 CRLF→LF、两个 `.ps1` 将 LF→CRLF）**可能改变文件字节**，从而影响 `.ps1` 的非 ASCII 判定与 `py_compile`。我未做提交后复测。
4. **未在 v2.5.0 旧树跑旧版 `checks.py skill`**，故作者"18→19 项"的切换点我**只核对到机制**（模板 7→8 个），未复算旧树计数。
5. **未逐条复算 `ops.md` 中 v2.5.1 之前的历史条目**（本次范围为 v2.5.1 改动）。
6. **`safe-delete` 拦截是否稳定复现未确认**：它依赖 `__pycache__` 规模（阈值 50），我本次运行时目录为空、未触发；无法确认在用户日常环境下的触发概率。
7. **未评估"pkgDir/cliDir 双表"在 `ast-grep-cli` 升级为带可导入模块的版本后是否仍正确**（`Scripts/ast-grep.exe` 作标记的语义适应性）。当前 venv 实测 `ast-grep 0.45.3` 正常。
8. **第二次 PowerShell 通道调用的第一次异常（exit 1 / 无输出文件）未被定性**；我的做法是落盘 + 转码规避，但该通道同时存在"吞 stdout""首调异常"两种现象，我未能分离归因。

---

## 6. 待验证（我复现不了的疑点）

1. **`回归证据.txt:181` 的 `(no stdout, rc=1)` 到底是什么**：是 `safe-delete` 拦截导致的空输出，还是 checks.py 真的返回 1？作者未说明，我无法重建当时工作区。
2. **`回归证据.txt:120` 的 `{"count":137}`**：`__pycache__` 内 137 个文件从何而来（正常应是 ~5-10 个）？若是并发写入（另一代理）造成，则两次审核期间的证据可能被第三方扰动——**本次任务已知有并发代理在跑**，这条值得主代理留意。
3. **`Ledger.md:14` 写「skill 19/19」而 `Ledger.md:13`／v2.5.0 任务写「18/18」**：时间线上 19 出现在 v2.5.0 评估期，与"UI组件库搭建.yaml 是评估期新增"吻合，但我未核对每条 Ledger 行的当时口径。
4. **`assets/templates/README.md:3` 的 `数据分析.yaml`** 是有意留作示例（可能指 `_示例-数据分析.yaml` 的省略写法），还是笔误？我按"示例性写法"记为轻微，未获作者确认。
5. **`--check-links` 的"150-260ms"区间**是否为新实测、还是沿用 v2.3 的 12 条小样本重述？`ops.md:216` 自陈是 v2.3 数据，我未重测。

---

## 7. 结论

**一句话裁决：不可提交。** 两处真缺陷 R5/R6 **确实关闭且我已亲手复现**（R6 旧 7/7 假 PASS → 新 7/8 FAIL=1；R5 未映射与缺失项均真报 FAIL，覆盖 7 键精确无残余），20 条声称项中 **19 条成立、1 条部分**（报告模板指针落在阶段 5 而非阶段 3，但作者已如实标注），原有信号**未回退**；扣在 **1 条严重（`回归证据.txt` 自相矛盾且重复 5 次）+ 1 条中（"彻底移除跨技能兜底"仍有 `~/.workbuddy` 残留通道，且注释把候选名写成"用户主目录"）**——后者恰是作者屡犯的"描述与实现不一致"，前者使修复的官方证据失去可信度。

**一旦提交的具体后果**：
1. **回归证据永失真**：`回归证据.txt` 会作为 v2.5.1 的官方回归记录进入仓库。后人看到的是"19/19"与"(no stdout, rc=1)"与"safe-delete 载荷"并存、"回归 8/9"重复 5 次——**无法据此判断究竟跑了几次、跑成没有**；而本技能的核心纪律正是"以跑出来的输出为准"。后续任何一个按规则"不采信自述、只看证据"的复核者，都会**先卡在这份证据上**，反而无法抵达我这次已经验证过的"代码层确实修好了"的结论。
2. **残留通道被措辞掩盖**：`ops.md:110`「彻底移除跨技能兜底」+ `checks.py:161-166`「用户主目录」会让人相信"工作区外无任何救活路径"。实际交付物写 `skills/<任意技能>/相对路径` 时仍会被判存在——**下一次"检查器骗人"可能就从这个缝里出来**，而文档不会提示它有缝。
3. 次要：步骤 8 的重复交付物会让"交付物确认 8 个"继续虚高；`ops.md:63` 的失效首选命令会被反复照抄。

**放行条件（都很轻，半天内可完成，且不涉及重新验证代码逻辑）**：① 清空重写 `回归证据.txt`（只留最终一次，逐行标注 stdout vs harness 转录，剔除 safe-delete/空输出噪声）；② 把 `~/.workbuddy` 候选的命名与语义在 `checks.py` 注释与 `ops.md:110` 中改准，或补 `skills/ai-workflow/` 白名单并说明；③ 修 `plan.yaml:65` 步骤 8 交付物、`ops.md:63` 首选命令、`ops.md:66`"父目录本身"三处措辞。
