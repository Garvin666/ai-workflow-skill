# 互审质证稿（乙 · 验证方 → 回应甲）

> 质证者：互审子代理「乙」（验证方，未参与 v2.5.1 修复）｜日期：2026-09-11（第二轮：互相质证）
> 对手：甲（证伪方）｜对手报告：`tasks/对抗互审-ai-workflow-v2.5.1-2026-09-11/互审-甲-证伪.md`
> 被审对象：ai-workflow v2.5.1 未提交工作区（10 文件）
> 解释器一律 `C:\Users\26717\.workbuddy\binaries\python\envs\ai-workflow\Scripts\python.exe`；`--base` 一律**字面 Windows 路径**（未用 `$(pwd)`）
> **本轮纪律遵守**：① 未对技能目录/venv 做任何改名/删除/写入（`scripts/` 目录复核前后一致，无 `__pycache__` 残留，见 §2-D3 末尾）；② **未向 `回归证据.txt` 追加任何内容**（只读核对：203 行，`md5=172faf60c29cbbb64f6242d8e48b0fc6`）；③ 所有夹具建在 `C:\Users\26717\.workbuddy\cache\ai-workflow\yi-settle\`；④ 未把 `safe-delete` 载荷写进任何文件（它只作为**被审证据的原文**引用，见 §2-D2）。
> 环境怪癖：本通道每条 Bash 回执都带 `crashpad ... CreateFile (0x2)` 的 stderr 噪声（宿主产物，非命令输出）；本轮**未遇到命令被拦截**，未使用 PowerShell 通道。

---

## 1. 我对甲每条指控的三态判定

甲的问题清单共 **7 条**；另有「验收表判定」（R6=部分）1 条、§4「文档与代码不一致清单」8 行。逐条判定如下。

| 甲的指控 | 我的判定 | 证据 | 是否动摇我的第一轮裁决 |
| --- | --- | --- | --- |
| **问题 1（严重）实体**：R6 的缺陷类别未完全关闭——`_candidate_paths()` 保留 `~/.workbuddy` 与 `base.parent` 两条候选，仍能制造同型假 PASS；`base.parent` 与红线③矛盾 | **接受** | 我自建两个**单变量隔离夹具**独立复现（§2-D3）：夹具 H 只存在 `~/.workbuddy` 候选 → `7/7 通过，FAIL=0`；夹具 P 只存在 `base.parent` 候选 → `7/7 通过，FAIL=0`；对照 C 无任何候选 → `7/8 通过，FAIL=1`。代码原文 `scripts/checks.py:166` | **是**（R6 由「成立」改为「部分」，见 §3/§6） |
| **问题 1 的措辞与建议**：称 `~/.workbuddy` 候选「与 R6 **同型**」，并建议「至少删掉 `base.parent`」即可收敛 | **反驳**（仅此两点；实体指控已接受） | 【观察】`checks.py:166` 的 `~/.workbuddy/p` 只在 `p` **以 `skills/` 开头**时才可能命中（如 `skills/ai-workflow/SKILL.md`）；而 R6 的原始复现场景用的是 `references/quality-gates.md`——我实测 `C:\Users\26717\.workbuddy\references\quality-gates.md` **ABSENT**，故该候选对 R6 原场景**不生效**。旧 sweep（`SKILLS_ROOT.glob("*/")`）+ 相对路径与本候选是**两种不同机制**，不能称「同型」。【推断】因此甲的建议「只删 `base.parent`」**不完备**：删掉后 `skills/<技能>/…` 形态仍被 `~/.workbuddy` 救活（我夹具 H 即为此形态）。 | 否（不影响 R6 判「部分」的结论，只修正措辞与修法） |
| **问题 2（严重）**：① `回归证据.txt` 同一信号 `checks.py skill` 有 3 种互斥结果、回归 8/9 整段重复 6 次；② `Ledger.md:15`「已全部修正」不实（NP4 点名的「等价转录格式」「错误算式 7 + 6 + 1」原样未动） | **接受** | ① 我用 4 条独立计数命令重数：`^## 回归 8` = **6**、`^## 回归 9` = **6**、`checks\.py skill *:` = 6 行，去重后 = **3 种形态 / 4 种逐字内容**（§2-D1/D2）；② `回归证据.txt:64` 仍为「依赖数=7 pkgDir 条目=6 cliDir 条目=1 （7 + 6 + 1 = 全覆盖…）」；`:27/33/51` 仍为 `[ OK ] requests  (requests)` / `(ast_grep_cli*.dist-info)`，而**真实脚本** `scripts/setup_env.ps1:62` 只打印 `[ OK ] $d`（§2-D8） | **是**（我原报「重复 5 次」→ 应为 6，我错；新增 `Ledger.md:15` 自述一致性行；NP4 行由「部分」改为「不成立」） |
| **问题 3（轻微）**：`SKILL.md:10` 三红线概括仍是绝对表述「① 用户确认前不执行」，未同步 `:59` 的「**处理类**脚本」限定 | **接受** | `SKILL.md:10` 原文确为「① **用户确认前不执行**」无任何限定；`:59` 原文为「**红线①：未确认前禁止执行处理类脚本或产出文件**（…只读检查不受限…）」。**这是我第一轮的漏项**（我的问题清单未列此条） | 否（但须新增一行；R1 的「细则落地」判定不变——`:59` 本身成立） |
| **问题 4（轻微）**：`SKILL.md:86` 称「**完整**信号表见 `quality-gates.md`」，但该表只有 5 类，缺 SKILL 自列的 Office／调研／改造／性能，两份清单是**交叉**非包含 | **接受** | `SKILL.md:86` 自列 6 类（代码／Office／数据分析／调研／改造／性能）；`references/quality-gates.md:26-30` §二表仅 5 行（写代码·改代码／数据分析／报告·文档／爬取·数据抓取／自动化·编排）。**缺 Office／调研／改造／性能 4 类；反向缺 SKILL 未列的「爬取／自动化」2 类 → 确为交叉**。**这是我第一轮的漏项** | 否（R3「补数据分析类」判定不变——`:86` 确有该行且 `quality-gates.md:27` 也确有；「完整」是另一处措辞问题） |
| **问题 5（轻微）**：P2 反模式 #40/#41/#42 未落盘，`quality-gates.md` 仍止于 39 条 | **接受** | `quality-gates.md` 池尾为 `39. 缺陷类任务未复现就改…`；`grep -nE '#(40|41|42)'` → 未找到。**与我第一轮问题 #7 一致**（甲加了一条我更正的事实：作者在 `Ledger.md:20` 已自标「待新增」，故**非虚述**） | 否（我原判轻微成立，严重度不变） |
| **问题 6（轻微，非本次引入）**：`assets/templates/README.md:3` 举例 `数据分析.yaml` 不存在 | **接受** | `README.md:3` 原文「文件名 = 任务类型（如 `数据分析.yaml`、`技术调研.yaml`）」；`ls assets/templates/` → 有 `技术调研.yaml`、`_示例-数据分析.yaml`，**无 `数据分析.yaml`**。**与我第一轮问题 #6 一致**（我另发现根因：`checks.py:98` 的 docs 列表只含 `SKILL.md` + `references/*.md`，**不扫 `assets/**`**，故该断链检不出——甲未提这点，可算我方的补充） | 否（我原判轻微成立，严重度不变） |
| **问题 7（轻微）**：P1-9 第二半（报告模板指针在**阶段 3** 交付物处）未落实，实际落在阶段 5（`:137`） | **接受** | `可优化方向.md:53`「P1-9｜阶段 4 加"非 Office 交付跳过本阶段"；**报告模板指针在阶段 3 交付物处也出现**」；`SKILL.md:81-91`（阶段 3）**无**该指针，`:125`（阶段 4）与 `:137`（阶段 5 第 4 条）各有。**与我第一轮 `收口-14` 判「部分」一致**；甲标轻微、并认可「保持现状可接受」 | 否（我原判即为「部分」，甲与我的记录一致，见 §2-D5） |
| **验收表判定**：R6 应记 **「部分」**（甲：16 成立 / 1 部分 / 0 不成立） | **接受** | 同问题 1 的两组夹具（§2-D3） | **是**——我原判「成立」，现改「部分」（详见 §6） |
| **§4 不一致清单 8 行**（Ledger:15 不符、回归证据行不符、回归证据:62-64 不符、SKILL:10 不符、SKILL:86 不符、README:3 不符；另 3 行为「相符 ✅」：`ops.md:81` 已清、`checks.py:161-165` 注释、`setup_env.ps1:52-55` 注释） | **接受 ×8** | 前 6 行我逐条复核一致（证据同上）；后 3 行「相符」我亦认可——`ops.md:81` 现为「⚠️ **v2.5.1 起不再跨技能目录兜底**」 | 否（其中 `ops.md:81` 一行印证我第一轮 NP1 判「成立（已修）」正确） |

**三态统计（甲指控共 16 个判定单元：问题 7 + R6 判定 1 + 不一致清单 8）**：
`接受 15` / `反驳 1`（仅问题 1 的「同型」措辞与「只删 base.parent」建议不完备） / `无法判断 0`。
**注意**：`无法判断 0` 不等于「甲全对」——我在 §4 承认了**自己**的 2 处漏项（问题 3、问题 4）与 **3 处计数/判定错误**（D1、D4、结论笔误）。

---

## 2. 事实点 settle（D1-D8）

### D1 —— `## 回归 8` / `## 回归 9` 各出现几次

**争议点**：我报 5 次，甲报 6 次。

**命令与原始输出**（只读）：
```
$ cd .../tasks/技能增强-ai-workflow-v2.5.1-2026-09-10
$ wc -l 回归证据.txt
203 回归证据.txt
$ md5sum 回归证据.txt
172faf60c29cbbb64f6242d8e48b0fc6 *回归证据.txt
$ grep -c '^## 回归 8' 回归证据.txt
6
$ grep -c '^## 回归 9' 回归证据.txt
6
```
逐行定位（Grep 工具）：回归 8 → `:111, :128, :145, :159, :173, :189`；回归 9 → `:119, :136, :150, :164, :180, :196`。

**我的裁定**：各 **6 次**（＝"回归 8/9"成对出现 6 组）。
**谁对**：**甲对，我错。** 我第一轮写「重复 5 次」并只列了 5 个行号（漏了 `:128` 起的那一组）。这是**计数错误**，属我第一轮报告问题 #1 的**细节失实**（结论"该文件自相矛盾"不受影响，因为 6 次与 5 次同样构成重复，但证据数字必须改准）。

---

### D2 —— `checks.py skill :` 一行共有几种互斥版本

**争议点**：我报 2 种，甲报 3 种。

**命令与原始输出**（只读；`sed` 处仅做**计数归一**以便去重，不改原意）：
```
$ grep -c 'checks\.py skill *:' 回归证据.txt
6
$ grep -o 'checks\.py skill *:.*' 回归证据.txt | sed 's/{"count":[0-9]*/{"count":N/' | sort | uniq -c | sort -rn
      3 checks.py skill : === 结果：19/19 通过，FAIL=0 ===
      2 checks.py skill : [safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":N,"threshold":50,"scope":"turn","targets":["C:\\Users\\26717\\.workbuddy\\skills\\ai-workflow\\scripts\\__pycache__"],"targetCount":1}
      1 checks.py skill : (no stdout, rc=1)
```
逐条原文（3 种互斥形态，共 4 种逐字内容）：
1. `:120` → `checks.py skill : [safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":137,…}`
2. `:137 / :165 / :197` → `checks.py skill : === 结果：19/19 通过，FAIL=0 ===`（3 次，逐字相同）
3. `:151` → `checks.py skill : [safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":142,…}`（与 1 同形态、载荷 `count` 不同）
4. `:181` → `checks.py skill : (no stdout, rc=1)`

**我的裁定**：按「**互斥结果**」定义——`{safe-delete 拦截}` / `{19/19 通过}` / `{(no stdout, rc=1)}` = **3 种**（甲的口径）；按「逐字内容」= **4 种**（safe-delete 的 `count:137` 与 `count:142` 逐字不同）。
**谁对**：**甲对，我错。** 我第一轮把 `safe-delete` 那一类贬为"不是检查结果"，从而实质上按 **2 种**处理——但若把它剔除，也就同时剔除了"同一信号存在互斥记录"这一指控本身。正确读法是：`safe-delete` 载荷与 `19/19`、`(no stdout, rc=1)` **三者互斥**，故为 3 种。**我错在口径选择**（为了让"2 种"成立而剔除了一个形态）。

---

### D3 —— `base.parent` 与 `~/.workbuddy` 是"同一条通道的不同侧面"还是"两条独立通道"？R6 应判「已闭合」还是「未闭合」？

**争议点**：甲主张 `base.parent` 候选让**工作区根之外**的现存文件也 PASS（与红线③冲突）；我主张 `~/.workbuddy` 候选仍能救活 `skills/<技能>/…`。两者是什么关系？

**命令与原始输出**（自建单变量隔离夹具，全部在 `cache/ai-workflow/yi-settle/`；`--base` 为字面 Windows 路径）：

先证明「工作区内确实不存在」且「每个夹具只有一条候选能命中」：
```
wsHome/skills/ai-workflow/SKILL.md : ABSENT      （夹具H 的 base 候选）
夹具H 的 base.parent 侧 (yi-settle/skills/ai-workflow/SKILL.md): ABSENT
~/.workbuddy/skills/ai-workflow/SKILL.md : EXISTS （夹具H 的唯一命中源）
proj/sub/outsidesettle.md        : ABSENT        （夹具P 的 base 候选）
夹具P 的 ~ 侧  (~/.workbuddy/outsidesettle.md):     ABSENT
proj/outsidesettle.md (parent)   : EXISTS        （夹具P 的唯一命中源）
```
三条命令与输出：
```
########## 夹具 H（交付物 "skills/ai-workflow/SKILL.md"，隔离 ~/.workbuddy 通道）
[ OK ] steps 非空 — 1 步
[ OK ] Anti-drop 对账 — 已完成步骤 1 个，交付物确认 1 个
=== 结果：7/7 通过，FAIL=0 ===      EXIT=0

########## 夹具 P（base=...\proj\sub，交付物 "outsidesettle.md" 只存在于 base 的上级）
[ OK ] Anti-drop 对账 — 已完成步骤 1 个，交付物确认 1 个
=== 结果：7/7 通过，FAIL=0 ===      EXIT=0

########## 对照 C（任何候选均不存在）
[FAIL] 步骤 1 交付物缺失 — references/nothing-here-settle.md
[ OK ] Anti-drop 对账 — 已完成步骤 1 个，交付物确认 0 个
=== 结果：7/8 通过，FAIL=1 ===      EXIT=1
```
代码原文：
```
$ sed -n '166p' scripts/checks.py
    return [base / p, Path.home() / ".workbuddy" / p, base.parent / p]
```

**我的裁定（三问三答）**：
1. **关系**：**两条独立通道**，不是同一条通道的不同侧面。判据是"单变量隔离"：夹具 H 中 `base`／`base.parent` 候选路径均 ABSENT、只有 `~/.workbuddy` 命中；夹具 P 中 `base`／`~` 候选均 ABSENT、只有 `base.parent` 命中。**每条候选各自独立足以产生假 PASS**，且**触发前提不同**：`~` 通道要求 `p` 以 `skills/` 开头（点位回到技能树），`base.parent` 通道对**任意**相对路径都生效（只要恰好在上一级存在同名文件）。二者归为**同一缺陷类别**（"工作区根之外的兜底"），但机制与锚点不同。
2. **`base.parent` 与红线③**：**冲突成立**。`SKILL.md:29` 明确「越界（根目录之外的任何路径）：先停下来说明…」，红线③（`:10`）为「读写与产出只允许落在本次任务指定的工作区根目录内」；而对账把**上一级目录**的文件判为本任务交付物，等于承认"产出可落在根之外"。
3. **R6 最终主张**：**未闭合**。R6 的**字面主张**（移除跨技能目录 sweep）**成立**（`SKILL.md`/`references`/`assets`/`scripts` 内 `SKILLS_ROOT` 命中 0；原始场景 7/8 FAIL=1）；但 R6 所指向的**"假 PASS 类别"**仍有两条独立通道存活 → R6 应判 **「部分」**。

**谁对**：**甲的实体主张成立**（假 PASS 通道仍在、`base.parent` 与红线③冲突），**我的实体主张也成立**（`~` 通道仍在）——我们各自验证了**不同的一条**通道，**不存在互相否证**；我在第一轮把 R6 判「成立」是因为**只测了一条通道就收手**（见 §5），这是验证覆盖不足。

**附带（环境合规举证）**：跑完上述夹具后复核技能目录，`scripts/` 与第一轮完全一致（8 个文件、时间戳未变、**无 `__pycache__`**），未对该目录写入/删除。

---

### D4 —— 逐条验收表项数：交叉点名

**争议点**：我自报 25 行，甲自报 17 行。

**重数结果**（读两份报告逐行数）：
- **我的 25 行**：`R5/R6/R1/R2/R3/R4/R7`（7） + `收口-1…收口-14`（14） + `NP1/NP3/NP4/NP5`（4） = 25。
- **甲的 17 行**：`R5/R6/R1/R2/R3/R4/R7`（7） + `文档 1…文档 10`（10） = 17。

**映射与交叉点名**：
| 甲的行 | 对应的我的行 |
| --- | --- |
| R5/R6/R1/R2/R3/R4/R7 | 同名 7 行（一一对应） |
| 文档 1（阶段 4 跳过 + 报告模板指针） | `收口-1`（阶段 4 跳过）；其"指针"半句对应我的 `收口-14` |
| 文档 2 / 3 / 4 / 5 / 6 / 7 | `收口-2 / -3 / -4 / -5 / -6 / -7` |
| 文档 8（ops §一首选 + §四 import + 排障 +8 行） | **我拆成 3 行**：`收口-8` / `收口-9` / `收口-10` |
| 文档 9 | `收口-11` |
| 文档 10（office_io 自检 + data_query 提示） | **我拆成 2 行**：`收口-12` / `收口-13` |

- **甲有而我没有的项**：**0 项**（甲 17 行全部映射到我的 25 行）。
- **我有而甲没有的项**：**5 项** —— `收口-14`（报告模板指针落**阶段 3** 的要求未落实，我判「部分」；甲只在**问题 7**里提，未在验收表单列）、`NP1`、`NP3`、`NP4`、`NP5`（前几轮遗留项）。另**粒度差异 2 处**：甲的「文档 8」= 我的 `收口-8/9/10`；甲的「文档 10」= 我的 `收口-12/13`。

**诚实补报（验收表之外的漏项）**：若把**问题清单**也算进来，则甲有而我**确实漏掉**的有 **2 条**——甲问题 3（`SKILL.md:10` 绝对表述）与甲问题 4（`SKILL.md:86`「完整信号表」名不符实）。这 2 条已按质证纪律补入我的修正表（§3）。

**谁对**：**行数上双方各自的自报都不算错**（我们用了不同粒度：我把 ops 三件与 office/data_query 两件拆开，甲合并）；但**甲的 17 行覆盖面 = 我的 25 行覆盖面**，我并**没有**"因为 25 > 17 而覆盖更全"的优越性——反而在**问题清单**层面漏了 2 条。

---

### D5 —— 「部分成立」该记在哪一项？

**争议点**：我记的是「报告模板指针放阶段 3」，甲记的是 R6。

**读甲原文**：甲 §0 立场自查写明「R6 的字面主张…**证据上完全成立**；我把它升级为"缺陷**类别**未关闭"属**口径扩张**」，§1 表中 R6 判定「**部分**」，§3 问题 1 标注「（**注**：R6 字面主张"移除跨技能兜底"证据上成立，本条属口径扩张，见 §0）」。→ 甲记的「部分」是 **R6（假 PASS 类别未关闭）**。
我记的「部分」是 **`收口-14`（P1-9 第二半：报告模板指针应出现在阶段 3 交付物处，实际落在阶段 5）**，依据是 `可优化方向.md:53` 与 `SKILL.md:81-91/137`。

**我的裁定**：**不是同一件事**——两条指向完全不同的对象（一个是 `checks.py` 的路径候选逻辑，一个是 `SKILL.md` 的文档指针位置）。
**谁对**：**两者都不算错，但"原清单口径下的『部分成立』"应当记在我这一条**：
- 历史独立审核者 `tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/复核意见-回归.md:126` 对 N4/P1-9 **明确写**：「`可优化方向.md:53` 字面要求"阶段 **3**"，作者落在阶段 **5** 且如实注明…我按**字面要求**从严判为**部分解决**」。→ 这是**既有审核口径**下的「部分」，与我的记录**同一项**、同一判定。
- 甲的 R6「部分」是**甲在本轮新增**的判定（甲自承"口径扩张"），在历史口径里 R6 从未出现过「部分」。
**结论**：**同一槽位（"本次修复的部分成立项"）应记在我这一条（P1-9/收口-14）**；甲对 R6 的「部分」是一条**额外的、新增的**判定，它**也成立**（§2-D3 已证），但**不是对既有「部分」槽位的占用**。即：双方各有一条「部分」，我这条承接历史口径，甲那条是本轮新发现——**"部分成立"该记在我的项上，甲记 R6 也不算错，两者不冲突**。

---

### D6 —— `plan.yaml` 的 `交付物` 字段是否有重复？全部重复对

**争议点**：我只点了「步骤 8」。请数清全部。

**命令与原始输出**（用技能自身 `checks` 的同口径函数 token 化后统计；脚本在 `cache/ai-workflow/yi-settle/dup_check.py`）：
```
步骤 1: 交付物 = 'tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/复现基线.txt'
步骤 2: 交付物 = 'tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/复现基线.txt'
步骤 3: 交付物 = 'scripts/setup_env.ps1;scripts/checks.py'
步骤 4: 交付物 = 'SKILL.md'
步骤 5: 交付物 = 'assets/report-template.md;references/ops.md;scripts/run_stage.ps1;references/playbook.md'
步骤 6: 交付物 = 'tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/回归证据.txt'
步骤 7: 交付物 = 'tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/独立审核意见.md'
步骤 8: 交付物 = 'tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/回归证据.txt'

=== 重复的交付物 token（被 2 个及以上步骤声明）===
tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/复现基线.txt  <-- 步骤 [1, 2]
tasks/技能增强-ai-workflow-v2.5.1-2026-09-10/回归证据.txt  <-- 步骤 [6, 8]

重复 token 个数 = 2
```

**我的裁定**：**有重复，共 2 对**：
1. **步骤 1 ↔ 步骤 2**（`复现基线.txt`）——**我第一轮漏了**；
2. **步骤 6 ↔ 步骤 8**（`回归证据.txt`）——我第一轮点了。

**谁对**：**甲未提此点（这是本轮新增的 settle 题，非甲的指控）**；就我自己的旧判而言：**我的"只步骤 8"不完整，我错**。修正：问题 #5 应写「2 对重复」，且对账"交付物确认 8 个"的**虚高来自 2 处**而非 1 处。

---

### D7 —— `references/ops.md` §四那条被称作「首选」的 import 探测命令，实际能跑通吗？

**争议点**：该命令是 `ops.md:63` 的 `"$PY" -m ast_grep_cli --version 2>/dev/null || <subprocess 调 ast-grep.exe>`。

**命令与原始输出**（只读，安全）：
```
=== probe 1 (ops.md:62) ===
deps OK
rc1=0

=== probe 2 first half (ops.md:63) ===
C:\...\Scripts\python.exe: No module named ast_grep_cli
rc_m=1

=== probe 2 fallback ===
ast-grep 0.45.3
rc_fb=0

=== 佐证：site-packages 里只有 dist-info，无可导入模块 ===
Lib/site-packages/ast_grep_cli-0.45.3.dist-info
=== Scripts 目录 ===
ast-grep.exe   (51400704 bytes)
sg.exe         (51400704 bytes)
=== 再试 -m ast_grep ===
No module named ast_grep    rc=1
```

**我的裁定**：**跑不通（指首选的那半条）**。`ast-grep-cli` 是 **CLI-only 包**——venv 里只有 `ast_grep_cli-0.45.3.dist-info`，**没有可导入的 `ast_grep_cli` / `ast_grep` 模块**，故 `-m ast_grep_cli` **永不成立**；整行之所以"能跑通"完全靠 `||` 后面的兜底。该行作为**"推荐先跑这条"的首选命令**具有误导性：读者照抄前半句会以为依赖装坏了。
**正确写法**（建议）：直接调可执行文件——
```bash
"$PY" -c "import subprocess;print(subprocess.run([r'C:/Users/26717/.workbuddy/binaries/python/envs/ai-workflow/Scripts/ast-grep.exe','--version'],capture_output=True,text=True).stdout.strip())"
# 或（bash 下）直接：
"C:/Users/26717/.workbuddy/binaries/python/envs/ai-workflow/Scripts/ast-grep.exe" --version
```
**谁对**：此点由我第一轮提出（甲报告未涉及），本轮**我复核维持**：`ops.md:63` 的 `-m ast_grep_cli` 应删或降为注释。

---

### D8 —— 甲指控 `Ledger.md:15` 称「第二轮 5 条新问题已全部修正」不实（点名「等价转录格式」「错误算式 7 + 6 + 1」原样未动）。成立吗？

**争议点**：我上一轮**没有提这一条**（我提过 NP4 的转录问题，但没把它与 `Ledger.md:15` 的"已全部修正"挂钩）。

**命令与原始输出**（只读）：
```
=== setup_env.ps1 真实打印（第 4 步循环体）===
scripts/setup_env.ps1:62:        Write-Host "[ OK ] $d"
scripts/setup_env.ps1:64:        Write-Host "[FAIL] $d not found: $marker"
=== 回归证据.txt 实际写的 ===
:27:      [ OK ] requests  (requests)
:33:      [ OK ] ast-grep-cli  (ast_grep_cli*.dist-info)
:51:      [FAIL] duckdb 缺失 (duckdb)
:64:  依赖数=7  pkgDir 条目=6  cliDir 条目=1  （7 + 6 + 1 = 全覆盖，无未映射项）
:23:  脚本内 $pkgDir: $pkgDir = @{ … "ast-grep-cli" = "ast_grep_cli*.dist-info" }   ← 已作废的单表写法
=== 转录标注只出现在回归 5 ===
:57-61  ⚠️ 证据形态说明（如实标注）：回归 4/5 的输出由回归脚本转录…   ← 但回归 4（:21-54）自身无标注
=== Ledger.md:15 原文节选 ===
「返修后第二轮仍判不通过（2 条部分解决 + 5 条新问题…）；已全部修正并补做全库同类检索（命中 0，唯一命中为合法历史说明）」
```
对照第二轮独立审核的 NP4 原文 `复核意见-回归.md:118`：「`回归证据.txt:26-53`（回归 4）、`:60-98`（回归 5）的输出形如 `[ OK ] requests  (requests)` / `[ OK ] duckdb  -> C:\...\duckdb`，而**真实 `setup_env.ps1` 只打印 `[ OK ] $d`**…另 `:59` 的"7 + 6 + 1 = 全覆盖"算式把"依赖数"与"标记数"相加，表述不当」。

**我的裁定**：**甲成立**。理由：(a) 真实脚本 `setup_env.ps1:62` 只打印 `[ OK ] $d`，而 `回归证据.txt:27-33/36-42/46-52` 是 `[ OK ] requests  (requests)` 形态的**harness 转录**，且**回归 4 段（`:21-54`）没有转录标注**；(b) `:64` 的错误算式 `7 + 6 + 1 = 全覆盖` **原样在**；(c) `:23` 还是**已作废的单表** `$pkgDir`（与 `:62` 的双表矛盾）。因此 `Ledger.md:15` 的「**已全部修正**」对 NP4 **不实**（NP4 的两处点名均未清）。

**这确实是我是上一轮的漏判**：我第一轮把 NP4 单列一行判「部分（未修也不影响结论）」，**没有**去核 `Ledger.md` 的「已全部修正」是否与之矛盾——即漏了**"自述一致性"这一层**（讽刺的是，我第一轮报告开头还写着"不采信 `Ledger.md` 的任何自述"，实际却没去核这一句）。
**修正方式**：见 §3 —— ① `NP4/转录保真度` 行由「部分」改为「**不成立（未修）**」；② **新增一行**`Ledger.md:15 自述一致性` → 「**不成立**」；③ 问题清单新增一条（严重度：中／接受甲的"严重"定级需说明，见下）。

---

## 3. 我第一轮验收表的修正（原判 → 现判）

| 我的行 | 原判 | 现判 | 理由与证据 |
| --- | --- | --- | --- |
| **R6**（彻底移除跨技能兜底） | 成立 | **部分** | 修正（原判「成立」，现改为「部分」，理由：`checks.py:166` 的 `~/.workbuddy` 与 `base.parent` 两条候选各自独立可致假 PASS，§2-D3 两组隔离夹具均 `7/7 通过 FAIL=0`，其中 `base.parent` 与红线③冲突）。R6 字面主张仍成立，故不降为「不成立」 |
| **NP4 / 转录保真度** | 部分 | **不成立（未修）** | 修正（原判「部分」，现改为「不成立」，理由：`:27-33` 的转录格式与 `:64` 的错误算式**原样未动**，且回归 4 段无转录标注；`Ledger.md:15` 的「已全部修正」与之矛盾，§2-D8） |
| **新增：`Ledger.md:15` 自述一致性** | （无此行） | **不成立** | 新增（甲问题 2 的②成立；§2-D8） |
| **R1**（红线①改"处理类"） | 成立 | **成立**（不变） | `SKILL.md:59` 原文落地；`:10` 的绝对概括是**另一处**措辞问题（新增行，见下） |
| **R3**（信号清单补"数据分析类"） | 成立 | **成立**（不变） | `SKILL.md:86` 与 `quality-gates.md:27` 均有；`86` 的「完整」措辞是**另一处**问题（新增行，见下） |
| **收口-14**（报告模板指针在阶段 3） | 部分 | **部分**（不变） | 维持从严口径（历史审核者同判断，§2-D5）；甲亦认可"保持现状可接受"，但原清单位置确未落实 |
| R5、R2、R4、R7、收口-1…收口-13、NP1、NP3、NP5 | 成立 | **成立**（不变） | 甲与我的证据一致，无新反证 |
| **新增：`SKILL.md:10` 三红线绝对表述** | （无此行） | **不成立（轻微）** | 新增（甲问题 3；`SKILL.md:10` vs `:59`，§1） |
| **新增：`SKILL.md:86`「完整信号表」名不符实** | （无此行） | **不成立（轻微）** | 新增（甲问题 4；`SKILL.md:86` vs `quality-gates.md:26-30`，§1） |

**从「不成立／部分」改回「成立」的**：**无**。
**计数修正**：我第一轮结论写「20 条声称项中 19 条成立、1 条部分」，但我的验收表**实为 25 行 / 23 成立 / 2 部分**——**这是笔误**，一并更正。
**度数修正**：我的问题 #2（跨技能残留）原定「中」→ 现上调为「**严重**」（与红线③直接矛盾，且两条独立通道）。
**问题 #5 修正**：重复交付物由「仅步骤 8」改为「**步骤 1↔2、步骤 6↔8 共 2 对**」（§2-D6）。

---

## 4. 我主动修正/撤回的条目（若有）

1. **撤回「`回归证据.txt` 重复 5 次」** → 改为 **6 次**（§2-D1）。**我错**。
2. **撤回「`checks.py skill` 有 2 种互斥版本」** → 改为 **3 种互斥形态（4 种逐字内容）**（§2-D2）。**我错**，且我错在**为了让"2 种"成立而把 `safe-delete` 形态剔除**——这正是本技能自己批判的"用不对口的口径反驳指控"。
3. **撤回「`plan.yaml` 仅步骤 8 交付物重复」** → 改为 **2 对重复**（§2-D6）。**我错（漏报步骤 1↔2）**。
4. **撤回结论里的「19 条成立 / 1 条部分」** → 改为 **23 成立 / 2 部分（共 25 行）**（§3）。**我错（笔误）**。
5. **承认漏项 2 条**：`SKILL.md:10` 绝对表述、`SKILL.md:86`「完整信号表」——**我第一轮未提**，已补入 §3。
6. **承认漏判 1 条**：`Ledger.md:15`「已全部修正」与 NP4 的矛盾——我提了 NP4 的**现象**却没核**自述**，已补（§2-D8、§3）。
7. **维持不改**：`ops.md:63` 首选探测命令失效（甲未提，我复核维持，§2-D7）；`checks.py:98` 不扫 `assets/**`（甲未提，我方补充）。
8. **撤回我第一轮的"立场自查"结论**：我原写"我受到了验证方立场影响，风险点有三处"，实际**风险点比我自己承认的更多**——漏在"只测一条候选通道就判 R6 成立"（见 §5）。

---

## 5. 立场对调稳健性（D9）

**方法**：假设把我与甲立场对调（我当证伪方、甲当验证方），逐条问"这条判定会不会变"。

| 我的条目/判定 | 证据驱动 or 立场驱动 | 立场对调后是否翻转 |
| --- | --- | --- |
| R1、R2、R3、R4、R7（改动是否落地） | **证据驱动**（读文件即得，甲与我都独立看到同一原文） | 否 |
| 收口-1…收口-13（10 项文档收口是否落地） | **证据驱动** | 否 |
| NP1、NP3、NP5（前轮遗留是否已修） | **证据驱动**（检索 0 命中） | 否 |
| R5（依赖自检双表 + 未映射 FAIL） | **证据驱动（代码层）**；但"整脚本真跑"是双方共同的**验证盲区** | 判定不变（甲也判成立），**边界需附注**：若立场收紧，应写「成立（静态+等价 harness）/ 未整脚本真跑」 |
| 收口-7（`run_stage.ps1` 后置自检） | **证据驱动（代码层）**，运行时只跑过一次 | 同上，附注而非翻转 |
| **R6** | **证据驱动**（我本轮新做了隔离夹具） | **已翻转**（成立→部分），但**这是证据驱动**，不是立场驱动 —— 我第一轮**只测了 `~` 一条通道**就判成立，**属"没验到就放行"**（准立场驱动：验证方倾向于"一条不成立就收手"）。这是 D9 要暴露的第一类条目 |
| 收口-14（指针落阶段 3） | **证据驱动**（阶段 3 确无指针） | 否 |
| **NP4 / 转录保真度** | **立场驱动**（我用"不影响结论"淡化为「部分」） | **是——会翻转**（部分 → 不成立）。换立场即判"未修"。**这是 D9 暴露的第二类条目** |
| **`Ledger.md:15` 自述一致性** | 第一轮**未纳入**（漏判） | 换立场会被首先抓到 —— 属"验证方视角盲区" |
| 我的问题清单 #1（回归证据自相矛盾） | **证据驱动** | 否（仅计数需修） |
| 我的问题清单 #2（跨技能残留） | **证据驱动**，但覆盖不足（只测 1/2 条通道） | 否（结论方向对，覆盖需扩） |
| 我的问题清单 #3/#4/#5/#6/#7（措辞/命令/重复/assets/反模式） | **证据驱动** | 否 |
| `SKILL.md:10`、`SKILL.md:86`（甲问题 3/4） | **证据驱动**，但**我第一轮没看** | 否（结论不变，但暴露"没看"的漏洞） |

**统计**：本轮**明确翻转 1 条**（NP4）；**已翻转 1 条**（R6，证据驱动）；**附条件不变 2 条**（R5、收口-7 的运行时边界）。
**自我评价**：我的立场依赖集中在**两处**——① 对**难以真跑**的项（R5/收口-7）接受"代码层成立＝成立"；② 对**"不影响结论"的瑕疵**主动降级（NP4）。两者方向一致：**倾向于放行**。这正是甲的证伪方视角在第二轮补上的价值。

---

## 6. 我对 R6 是否闭合的最终主张

**最终主张：R6 判「部分」——字面主张成立，缺陷类别未闭合。**

- **成立的半边**：`SKILLS_ROOT` 跨技能目录 sweep **确已删除**——全库 `scripts/*.py` 命中 0；R6 原始场景（目录内只有 `plan.yaml`、交付物 `references/quality-gates.md`）**现真报 FAIL**（`7/8 通过，FAIL=1`，`EXIT=1`）；我对照 C 夹具再次验证基线正确。
- **未闭合的半边**：`scripts/checks.py:166` 的三候选里，`Path.home()/".workbuddy"/p` 与 `base.parent/p` **各自独立**能判定"工作区内不存在的交付物为存在"（夹具 H、夹具 P 均 `7/7 通过 FAIL=0`）；其中 `base.parent` 把**工作区根之外**的文件判为本任务交付物，与红线③（`SKILL.md:10`/`:29`）**直接矛盾**。
- **判「部分」而非「不成立」**：R6 的字面交付（删 sweep）**真实完成且可复现**，我拒绝把它降为「不成立」——那是把甲自己承认的"口径扩张"当作事实。
- **判「部分」而非「成立」**：我撤回第一轮的「成立」，因为"假 PASS 通道关闭"这一**类别主张**在证据上不成立——我用**两条单变量隔离夹具**证明了它。

**我是否改变判断**：**是。原判 R6「成立」，现判「部分」。** 依据是 §2-D3 的两组夹具与 `checks.py:166` 原文，不是甲的措辞。

---

## 7. 我的最终结论

**一句话：v2.5.1 仍不可提交**——但比第一轮的"不可提交"**更接近可提交**：甲的指控**实体全部成立**（我仅反驳其"同型"措辞与"只删 `base.parent`"的建议不完备），我因此**下调 1 条（R6→部分）、新增 3 条不成立**（NP4、`Ledger.md:15`、`SKILL.md:10/86` 中 2 条轻微）），**上调 1 条严重度**（跨技能残留：中→严重）。

**必须修（按严重度排序）**：

| 序 | 严重度 | 必须修 | 依据 |
| --- | --- | --- | --- |
| 1 | **严重** | `scripts/checks.py:166` 的 `base.parent/p` **必须删**（与红线③直接矛盾）；`~/.workbuddy/p` 要么再加 `skills/ai-workflow/` 白名单、要么在 `ops.md` 与红线③处**显式声明"对账允许跨出工作区根"**。**注意：只删 `base.parent` 不完备**（`skills/…` 形态仍被救活） | §2-D3 |
| 2 | **严重** | `回归证据.txt` **清空重写为单一最终版**：只留最后一次跑；逐行标注「脚本原样 stdout」vs「等价 harness 转录」；`safe-delete`/空输出行须注明是**宿主拦截**而非检查结果；删除错误算式 `7 + 6 + 1` 与已作废的单表 `$pkgDir`；**同时**把 `Ledger.md:15` 的「**已全部修正**」改为逐条列明（NP1✓/NP2✓/NP3✓/ **NP4 未清** /NP5✓） | §2-D1/D2/D8 |
| 3 | **中** | `SKILL.md:10` 改为「用户确认前不执行**处理类**脚本（只读检查除外）」，与 `:59` 对齐 | §1-问题3 |
| 4 | **轻微** | `SKILL.md:86` 改「完整信号表」为「信号表（与本节互补）」，或把 6 类合并进 `quality-gates.md` §二 | §1-问题4 |
| 5 | **轻微** | `ops.md:63` 删掉永不成立的 `-m ast_grep_cli`；`plan.yaml` 步骤 1/2 与 6/8 的重复交付物改填真实产物；`assets/templates/README.md:3` 的 `数据分析.yaml` 改为 `_示例-数据分析.yaml`；`checks.py:98` 把 `assets/**/*.md` 纳入引用检查 | §2-D6/D7 |

**放行条件**：第 1、2 项为**提交前必须**（第 1 项是代码级红线冲突，第 2 项是官方证据可信度）；第 3-5 项可与提交同批收口。

---

### 附：本轮我实际执行的命令清单（可复现）

```
① grep -c '^## 回归 8' 回归证据.txt            → 6
② grep -c '^## 回归 9' 回归证据.txt            → 6
③ grep -c 'checks\.py skill *:' 回归证据.txt   → 6
④ grep -o 'checks\.py skill *:.*' 回归证据.txt | sed … | uniq -c   → 3 形态 / 4 逐字
⑤ <venv>/python.exe <skill>/scripts/checks.py plan "…\wsHome\plan.yaml"  --base "…\wsHome"     → 7/7 FAIL=0 EXIT=0
⑥ <venv>/python.exe <skill>/scripts/checks.py plan "…\proj\sub\plan.yaml" --base "…\proj\sub"  → 7/7 FAIL=0 EXIT=0
⑦ <venv>/python.exe <skill>/scripts/checks.py plan "…\wsCtl\plan.yaml"    --base "…\wsCtl"     → 7/8 FAIL=1 EXIT=1
⑧ <venv>/python.exe cache/.../dup_check.py    → 重复对 = [1,2] 与 [6,8]
⑨ "$PY" -m ast_grep_cli --version             → No module named ast_grep_cli, rc=1
⑩ "$PY" -c "…subprocess…ast-grep.exe --version" → ast-grep 0.45.3, rc=0
⑪ md5sum 回归证据.txt → 172faf60c29cbbb64f6242d8e48b0fc6（203 行，未被本轮改动）
⑫ ls -la <skill>/scripts/ → 与第一轮一致，无 __pycache__（未写入技能目录）
```
