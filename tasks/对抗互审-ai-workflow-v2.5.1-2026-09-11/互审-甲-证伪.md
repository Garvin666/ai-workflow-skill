# 互审报告（甲 · 证伪方）

> 审查者：对抗式互审「甲」（未参与 v2.5.1 修复）｜日期：2026-09-11
> 被审对象：`~/.workbuddy/skills/ai-workflow/` 工作区（未提交，`git diff HEAD` 恰好 10 个文件）
> 方法：以代码/文件实际内容与**独立复现**为准，不采信 `ops.md` 变更日志、`Ledger.md`、`回归证据.txt` 的任何自述
> 环境：Windows + Git Bash；脚本一律用 `C:\Users\26717\.workbuddy\binaries\python\envs\ai-workflow\Scripts\python.exe`
> 证据纪律遵守说明：本报告**未对技能目录与 venv 做任何改名/删除/写入**；R5 的"目标缺失"用**自建临时假 venv**制造，R6 的"文件不存在"用**自建临时目录**制造；临时文件全部在 `~/.workbuddy/cache/ai-workflow/jia-repro/`

---

## 0. 立场自查

我是"证伪方"，需要主动承认以下条目存在立场驱动成分：

| 条目 | 立场风险 | 我的处理 |
| --- | --- | --- |
| 「R6 残留假 PASS 通道」（问题 1） | R6 的字面主张是"**移除跨技能兜底**"，该主张**证据上完全成立**；我把它升级为"缺陷**类别**未关闭"属**口径扩张** | 已显式拆开：验收表判 R6「部分」，并在问题 1 里写明"字面主张成立、类别未关闭"，读者可自行按严格口径降级为「轻微/不成立」 |
| `templates/README.md:3` 举例文件名不存在（问题 6） | **既有**、非本次引入、且是"如 …"式举例 | 已标 轻微 + 非本次引入；我承认它接近凑数，列出的唯一理由是本次验收第 5 条明确要求"全库同类检索" |
| P1-9 指针位置（问题 7） | 作者**已如实注明**改放阶段 5，且该项不在本次声称的 10 项收口清单内 | 已标 轻微 + "原清单要求位置未落实"，不作为否定修复的依据 |

**我未受立场影响的判定**：R5 我给出 4 组独立复现、**全部按预期**；10 项文档收口我逐条核过、**全部落地**；上一轮未收口点 `ops.md:81` 我确认**已修**。这些我都如实写进第 5 节。

---

## 1. 逐条验收表

| 编号 | 声称的修法 | 我实测看到什么 | 证据（文件:行 / 命令原文） | 判定 |
| --- | --- | --- | --- | --- |
| **R5** | `setup_env.ps1` 双表 `$pkgDir`(6)+`$cliDir`(1)，未映射显式 `[FAIL]` | 代码确为双表；我逐字提取第 4 步块 + 自建假 venv 跑 4 组：(A) 全在→失败 0；(B) 缺 duckdb→`[FAIL] duckdb`，失败 1；(C) 加未映射假依赖→`[FAIL] ... no marker in pkgDir/cliDir`，失败 1；(D) 缺 `Scripts\ast-grep.exe`→`[FAIL] ast-grep-cli`，失败 1。**未映射不再静默通过** | `scripts/setup_env.ps1:42`（6 键）、`:46`（1 键）、`:50-51`（`if/elseif` 分支）、`:56-60`（空 marker→FAIL）、`:68`（`exit 1`）；我的输出见 §2-③ | **成立** |
| **R6** | "彻底移除跨技能兜底" | 跨技能目录 sweep（旧 `SKILLS_ROOT.glob("*/")`）**确已删除**，全库 `scripts/*.py` 命中 0；报告的原始场景（目录内只有 plan.yaml、交付物 `references/quality-gates.md`）**现在真报 FAIL**（7/8，EXIT=1）。**但** `_candidate_paths()` 仍保留 `Path.home()/".workbuddy"/p` 与 `base.parent/p` 两条候选，我构造出**同型假 PASS**（工作区内不存在也判通过），见 §2-② | `scripts/checks.py:166` `return [base / p, Path.home() / ".workbuddy" / p, base.parent / p]`；我的输出见 §2-①② | **部分**（报告场景成立；缺陷类别未关闭） |
| **R1** | 红线①限定"处理类脚本"，豁免只读检查 | `:59` 已改为"未确认前禁止执行**处理类**脚本或产出文件（`checks.py status`／`skill`、`Read`/`Grep`/`Glob`…只读检查不受限）"。**但** `:10` 三红线概括仍是绝对的"① 用户确认前不执行"，未带限定（见问题 3） | `SKILL.md:59`；反例 `SKILL.md:10` | **成立**（细则落地；概括文案不一致另计 轻微） |
| **R2** | 任务目录一律建在工作区根下 `tasks/`；技能目录 `tasks/` 仅技能自身演进 | `:27` 新增该条；`:34` 例外表改为"**仅"技能自身演进"类任务**才可写 `tasks/` 记录"；`:85`（阶段 3 第 1 条）同步为"`<工作区根>/tasks/…`（**目录必须建在工作区根下**…）" | `SKILL.md:27`、`:34`、`:85` | **成立** |
| **R3** | 信号清单补"数据分析类" | `:86` 已插入"**数据分析类＝交叉校验脚本（总额对账／行数一致／空值率）+ 抽样复算**" | `SKILL.md:86` | **成立** |
| **R4** | 独立审核补优先级条款 + 批次规则 | `:113` 第 7 条优先级、`:114` 第 8 条批次规则（≤5 文件、不得减少覆盖面）均在；与第 6 条（豁免）是收窄关系、**无自相矛盾** | `SKILL.md:112-114` | **成立** |
| **R7** | `report-template.md` 补「工作区根」 | 确认表已补第 6 行，且与 SKILL.md 阶段 1 六要素第 ⑥ 项一致 | `assets/report-template.md:14`；`SKILL.md:65` | **成立** |
| 文档 1 | 阶段 4 非 Office 跳过 + 报告模板指针 | `:125` 加 blockquote"非 Office 交付…跳过本阶段；但报告类交付仍须参照 `assets/report-template.md`"；`:137`（阶段 5）也给了指针 | `SKILL.md:125`、`:137` | **成立** |
| 文档 2 | `excel-write` 只吃 JSON | `:128` 写"`excel-write` **只接受 `--rows` 的 JSON**——CSV 必须先 `excel-read <csv> --fmt json > rows.json`"；我实跑 `office_io.py excel-read in.csv --fmt json` → stdout 输出 `[["a","b"],["1","2"]]`，rc=0（可重定向，说法成立） | `SKILL.md:128`；§2-⑤ | **成立** |
| 文档 3 | L1 排除 `_` 开头文件 | `:17` 已写"（**排除 `_` 开头的示例／字段说明文件**，如 `assets/templates/_示例-数据分析.yaml`）；**L1 需待阶段 0 查过模板后才能确认**" | `SKILL.md:17` | **成立** |
| 文档 4 | 阶段 0 列本地 skill 给做法 | `:51` 已给 `Glob ~/.workbuddy/skills/*/SKILL.md` + 读 frontmatter + `find-skills` | `SKILL.md:51` | **成立** |
| 文档 5 | 阶段 5 对账补 `--base` | `:135` 已写 `checks.py plan <plan.yaml> --base <工作区根>` 并注明"非当前目录时必须带 `--base`" | `SKILL.md:135` | **成立** |
| 文档 6 | 阶段 2 非工程类维度 | `:78` 已补"**非工程类任务**（数据分析／文档／调研）可换用 口径选择／样本范围／结论稳健性／可复算性 等维度" | `SKILL.md:78` | **成立** |
| 文档 7 | `run_stage.ps1` 加执行后自检 | `:27-34` 已写 `$global:LASTEXITCODE = $null` → 调用 → `if ($null -eq $code) {[FAIL]…; exit 1}`；纯 ASCII（0 非 ASCII 字节）。**运行时行为未整脚本真跑**（理由见 §6-1） | `scripts/run_stage.ps1:24-36` | **成立**（代码层；运行时见待验证） |
| 文档 8 | ops §一 首选声明 + §四 import 探测 + 排障表新增行 | `:18` 首选声明在；`:58-64` import 探测命令在；排障表本文件新增 **8 行**（`:103-110`），与变更日志"**+8 行**"一致 | `references/ops.md:18`、`:56-66`、`:103-110`、`:159` | **成立** |
| 文档 9 | `--check-links` 均值改区间 | `:131-132` 改为"正常条 ~150-260ms，遇超时条可达数秒——不要把它当固定基线"；v2.3 历史条目 `:216` 同步改为区间并注"不可当基线引用" | `references/ops.md:131-132`、`:216` | **成立** |
| 文档 10 | `office_io.py` 按子命令前置依赖检查 / `data_query.py` 提示改走 stdout | (a) `_NEED` 6 键与 6 个 `add_parser` 名称**逐一对应**；我用 `PYTHONPATH` 屏蔽 `openpyxl` 实跑 `excel-read` → `[ERROR] 缺少依赖：openpyxl` + `[HINT]`，**rc=2**；依赖正常时 rc=0、不误报。(b) `data_query.py files` 的 `[提示]` 实测**只出现在 stdout**，stderr 落盘文件为空 | `scripts/office_io.py:206-221`、`:266-276`；`scripts/data_query.py:140-142`；§2-④⑤ | **成立** |

**判定汇总**：成立 16 条 / 部分 1 条（R6）/ 不成立 0 条 / 无法确认 0 条。

---

## 2. 我独立复现的硬证据

### ① R6 报告的原始场景 —— 现已真报 FAIL（**我复现，非读文件**）

夹具 `C:\Users\26717\.workbuddy\cache\ai-workflow\jia-repro\r6only\`：**目录内只有 `plan.yaml`**（无 `references/`），步骤 `状态: 完成`、`交付物: "references/quality-gates.md"`。

```
命令：<venv>\python.exe <skill>\scripts\checks.py plan "…\r6only\plan.yaml" --base "C:\Users\26717\.workbuddy\cache\ai-workflow\jia-repro\r6only"
=== ai-workflow 计划对账 ===
[ OK ] plan.yaml 存在 — C:\Users\26717\.workbuddy\cache\ai-workflow\jia-repro\r6only\plan.yaml
[ OK ] plan.yaml 可解析
[ OK ] meta.任务
[ OK ] meta.验证信号
[ OK ] meta.工作区根
[ OK ] steps 非空 — 1 步
[FAIL] 步骤 1 交付物缺失 — references/quality-gates.md
[ OK ] Anti-drop 对账 — 已完成步骤 1 个，交付物确认 0 个
=== 结果：7/8 通过，FAIL=1 ===
EXIT=1
```

→ R6 报告场景**确已关闭**（与作者/前两轮审核结论一致）。

### ② **反证：同类假 PASS 通道仍在**（我构造，两条均可复现）

**夹具 A**（`~/.workbuddy` 通道）：workspace `jia-repro\ws2\` 内**只有 plan.yaml**，交付物写 `"skills/ai-workflow/SKILL.md"`（工作区内不存在）：

```
命令：…checks.py plan "…\ws2\plan.yaml" --base "C:\Users\26717\.workbuddy\cache\ai-workflow\jia-repro\ws2"
=== 结果：7/7 通过，FAIL=0 ===
EXIT=0
```
（命中来源：`Path.home()/".workbuddy"/"skills/ai-workflow/SKILL.md"` 存在 —— 与 R6 的"被别的技能目录同名文件救活"**同型**：交付物不在工作区却判存在）

**夹具 B**（`base.parent` 通道，**落在工作区根之外**）：workspace 根 = `jia-repro\proj\sub\`，交付物 `"outsideprobe.md"`，该文件**只存在于工作区根的上级** `jia-repro\proj\outsideprobe.md`：

```
命令：…checks.py plan "…\proj\sub\plan.yaml" --base "C:\Users\26717\.workbuddy\cache\ai-workflow\jia-repro\proj\sub"
=== 结果：7/7 通过，FAIL=0 ===
EXIT=0
```
→ 对账把**工作区根之外**的现存文件当作本任务交付物判"通过"，与红线③"读写与产出只允许落在本次任务指定的工作区根目录内"**自相矛盾**。

### ③ R5 依赖自检 —— 4 组（我逐字提取 `setup_env.ps1:39-68` 的代码块，用**自建假 venv** 执行，只把末尾 `exit 1` 换成打印失败清单）

```
===== A：7 个 marker 全在 =====        ===== B：缺 site-packages\duckdb =====
[ OK ] requests                        [ OK ] requests … [ OK ] pyyaml
[ OK ] openpyxl                        [FAIL] duckdb not found: …\venvB\Lib\site-packages\duckdb
[ OK ] python-docx                     [ OK ] ast-grep-cli
[ OK ] pypdf                           R5FAILED_COUNT=1   R5FAILED_LIST=duckdb
[ OK ] pyyaml
[ OK ] duckdb                          ===== C：$deps 追加未映射假依赖 =====
[ OK ] ast-grep-cli                    …[ OK ] ast-grep-cli
R5FAILED_COUNT=0   R5FAILED_LIST=      [FAIL] definitely-not-real-xyz has no marker in pkgDir/cliDir (add one; silent pass is forbidden)
                                       R5FAILED_COUNT=1   R5FAILED_LIST=definitely-not-real-xyz
===== D：缺 Scripts\ast-grep.exe（验证新增 $cliDir 真被检查）=====
…[ OK ] duckdb
[FAIL] ast-grep-cli not found: …\venvD\Scripts\ast-grep.exe
R5FAILED_COUNT=1   R5FAILED_LIST=ast-grep-cli
```

→ R5 **确证关闭**；且新增的 `$cliDir` 标记（可执行文件本体，而非 dist-info 残留）**真的参与判定**。

### ④ `office_io.py` 依赖守卫真触发（我实跑）

```
命令：PYTHONPATH=<屏蔽目录含 openpyxl.py: raise ImportError> …\python.exe office_io.py excel-write out.xlsx --rows rows.json
[ERROR] 缺少依赖：openpyxl
[HINT] pip install openpyxl（或先跑 scripts/setup_env.ps1 初始化 venv）
[HINT] 或用 venv 解释器：C:\Users\26717\.workbuddy\binaries\python\envs\ai-workflow\Scripts\python.exe
rc=2
（去掉屏蔽后）命令：…office_io.py excel-read in.csv --fmt json  → [["a", "b"], ["1", "2"]]   rc=0
```

### ⑤ `data_query.py` 提示改走 stdout（我分流复现）

```
命令：…\python.exe data_query.py files <dir> 2>dq_stderr.txt | tail -5
stdout 末行：[提示] 结构化(JSON/CSV/Parquet)优先用 `sql` 子命令直查；自由文本用 `find`；原生 Grep 仍适合小目录
stderr 文件：（空）
```

### ⑥ 原有信号未回退 + 硬约束

```
命令：…\python.exe checks.py skill --skill-dir "C:/Users/26717/.workbuddy/skills/ai-workflow"
=== 结果：19/19 通过，FAIL=0 ===   EXIT=0   （含"文档引用完整性 — 检查 25 条引用"、8 个模板 schema 各 9/9、5 个 py_compile OK）

逐字节：scripts/setup_env.ps1 total=3565 nonASCII=0 ；scripts/run_stage.ps1 total=2008 nonASCII=0
```

---

## 3. 问题清单

| # | 严重度 | 问题 | 证据 | 是否可复现 | 建议 |
| --- | --- | --- | --- | --- | --- |
| 1 | **严重** | **R6 的缺陷类别未完全关闭**：`_candidate_paths()` 删除了跨技能目录 sweep，但保留了 `Path.home()/".workbuddy"/p` 与 `base.parent/p` 两条候选。前者可让 `skills/…` 型交付物被 `~/.workbuddy` 下同名文件"救活"（与 R6 同型）；后者把**工作区根之外**的现存文件判为交付物，与红线③直接矛盾。（**注**：R6 字面主张"移除跨技能兜底"证据上成立，本条属口径扩张，见 §0） | `scripts/checks.py:166`；§2-②两条夹具原始输出 | 是（两条夹具） | 相对路径候选应**收敛到工作区根内**（至少删掉 `base.parent`）；若必须保留兼容，请在 `ops.md` 与红线③处显式声明"对账允许跨出工作区根"，消除自相矛盾 |
| 2 | **严重** | **"声称的证据"自相矛盾 + "已全部修正"不实**：① `回归证据.txt` 同一信号 `checks.py skill` 出现 **3 种互斥结果**（`[safe-delete]…`×2、`19/19 通过`×3、`(no stdout, rc=1)`×1），且**回归 8/9 被整段重复 6 次**；② `Ledger.md:15` 称第二轮 5 条新问题"**已全部修正**"，但第二轮 NP4 明指的两处**仍在原样**——`回归证据.txt:27/33/51` 仍是等价 harness 的转录格式（`[ OK ] requests  (requests)` / `(ast_grep_cli*.dist-info)`，真实脚本只打印 `[ OK ] $d`），`:64` 仍是错误算式"`7 + 6 + 1 = 全覆盖`" | `回归证据.txt:120/137/151/165/181/197`、`:23-33`、`:64`、`:200`(文件 203 行) ；`Ledger.md:15`；对照 `复核意见-回归.md:118`(NP4) | 是 | 清空重写 `回归证据.txt` 为单一最终版本（保留失败记录的成因说明）；把 `Ledger.md:15` 的"已全部修正"改为逐条列明（NP1✓/NP2✓/NP3✓/NP4 未清/ NP5✓） |
| 3 | 轻微 | `SKILL.md:10` 三红线概括仍是绝对表述"① **用户确认前不执行**"，未同步 `:59` 的"**处理类**脚本"限定。R1 要解决的"红线① vs 阶段 0 跑 `checks.py status`"冲突，在**概括行 vs `:54`** 之间字面仍存在 | `SKILL.md:10` vs `:54`、`:59` | 是（读文件即可） | `:10` 改为"用户确认前不执行**处理类**脚本（只读检查除外）" |
| 4 | 轻微 | `SKILL.md:86` 称"**完整**信号表见 `references/quality-gates.md`（含未列出的任务类型）"，但该表（§二）只有 写代码/数据分析/报告文档/爬取/自动化编排 5 类，缺 SKILL 自己列的 **Office 类、调研类、改造类、性能类**——两份清单是**交叉**而非包含关系，"完整"名不符实 | `SKILL.md:86`；`references/quality-gates.md:24-30` | 是 | 二选一：把 6 类合并进 `quality-gates.md` §二，或把 SKILL 的措辞改为"信号表（与本节互补）" |
| 5 | 轻微 | 本次验收第 5 条要求的"全库同类清扫"在 **P2 反模式沉淀**上未执行：`quality-gates.md` 未被本次 10 文件变更覆盖，反模式清单仍止于 **39 条**，#40/#41/#42 未落盘（作者在 `Ledger.md:20` 自标"**待新增**"，故**非虚述**，仅是范围缺口） | `references/quality-gates.md:109`（39 条结尾）；`git diff HEAD --stat` 无该文件；`Ledger.md:20` | 是 | 若 `可优化方向.md` §六 的执行顺序（…→P2 反模式→回归）仍有效，则应补做；否则在 `Ledger.md` 写明"P2 顺延至下一迭代" |
| 6 | 轻微（**非本次引入**） | `assets/templates/README.md:3` 举例"文件名 = 任务类型（如 `数据分析.yaml`、`技术调研.yaml`）"，但目录内**不存在** `数据分析.yaml`（实际为 `_示例-数据分析.yaml`，同文件 `:37` 又正确引用了后者） | `assets/templates/README.md:3`、`:37`；目录清单 | 是 | 把 `:3` 的举例改为 `_示例-数据分析.yaml`，或直接删掉该例 |
| 7 | 轻微 | P1-9 第二半（"报告模板指针在**阶段 3 交付物处**也出现"）未按原清单位置落实，实际落在阶段 5（`:137`）。作者**已如实注明**，且 `SKILL.md:81-91`（阶段 3）确认无该指针 | `SKILL.md:81-91`、`:137`；`可优化方向.md:53`；`references/ops.md:159` | 是 | 保持现状可接受（意图已覆盖）；若要严格对齐原清单，在阶段 3 交付物说明处补一行指针 |

**实质问题数：严重 2 / 轻微 5 / 致命 0。**

---

## 4. 文档与代码不一致清单

| 位置 | 文档怎么说 | 实际是什么 | 判定 |
| --- | --- | --- | --- |
| `Ledger.md:15` | 第二轮 5 条新问题"**已全部修正**" | NP1/NP2/NP3/NP5 已修；**NP4 未修**（`回归证据.txt` 仍为等价转录 + 错误算式 `7 + 6 + 1`） | **不符**（=问题 2） |
| `回归证据.txt:120/137/151/165/181/197` | 单一"全部信号复跑"记录 | 同一信号 3 种互斥结果，含未解释的 `(no stdout, rc=1)` | **不符**（=问题 2） |
| `回归证据.txt:62-64` | `$pkgDir` 6 + `$cliDir` 1（最终实现） | 同文件 `:23-33`（回归 4）仍描述**已作废**的单表 + `ast_grep_cli*.dist-info`，且 `:64` 算式把"依赖数"与"标记数"相加 | **不符**（同文件内自相矛盾） |
| `SKILL.md:10` | 红线①"用户确认前不执行"（绝对） | `:59` 已限缩为"处理类脚本"，只读检查不受限 | **字面不一致**（=问题 3） |
| `SKILL.md:86` | "**完整**信号表见 `quality-gates.md`" | 该表非超集，缺 4 类 | **不符**（=问题 4） |
| `assets/templates/README.md:3` | 举例 `数据分析.yaml` | 该文件不存在 | **不符**（=问题 6，既有） |
| `references/ops.md:81`（上一轮未收口点） | 旧文"skill 内相对路径会**逐技能目录尝试**" | **本次已修**为"v2.5.1 起**不再跨技能目录兜底**" | **相符（已清）** ✅ |
| `scripts/checks.py:161-165` 注释 | "交付物只允许在「本任务基准／用户主目录／基准的上级」内解析" | 与 `:166` 代码三候选一致 | 相符 ✅ |
| `scripts/setup_env.ps1:52-55` 注释 | 说明"空子路径致 `Join-Path` 返回父目录 → 恒真" | 与修复前的 bug 及 `:50-51` 守卫一致 | 相符 ✅ |

---

## 5. 我确认成立的部分

1. **R5 完全关闭**（4 组独立复现：正常 0 失败 / 缺 duckdb FAIL / 未映射 FAIL / 缺 `ast-grep.exe` FAIL）。双表 6+1 与 `$deps` 7 项**完全覆盖**，`if/elseif` 无重复判定；`$cliDir` 用**可执行文件本体**作标记，比 dist-info 更可信——这条改进是**实质性**的。
2. **R6 报告的原始场景关闭**（§2-①：7/8、FAIL=1、EXIT=1）。
3. **10 项文档收口逐条落地**，我一条条在文件里找到了对应文本，无一项是"只写在变更日志里"。
4. **R1/R2/R3/R4/R7 的代码/文本改动全部落地**，且 R4 的第 7、8 条与第 6 条（豁免）是**收窄关系**，未产生新矛盾。
5. **上一轮的未收口点已清扫**：`ops.md:81`（"逐技能目录尝试"）已改写；`ops.md:159` 的"排障表 +8 行"与实际新增 8 行一致（第二轮指出的 +6 计数不实已修）。**这正是作者前两轮翻车的那一类，本次做对了。**
6. **删除 `SKILLS_ROOT` 后无残留引用、无断链**：`scripts/*.py` 命中 0；我另做了一遍**比 `checks.py` 更宽**的引用扫描（84 条反引号文件型引用，覆盖 json/csv/exe/html 等扩展名），未解析项均为"venv 内可执行文件/命令行串/任务目录内文件"等合法形态，**无新引入的断链**。
7. **硬约束满足**：两个 `.ps1` 非 ASCII 字节 = 0。
8. **原有信号未回退**：`checks.py skill` 19/19、FAIL=0、EXIT=0。

---

## 6. 待验证（我无法复现的疑点，**不计入问题**）

1. **`run_stage.ps1` 执行后自检在真宿主下的行为**：我只做了静态核对（`$global:LASTEXITCODE = $null` → 判空即 FAIL 的逻辑自洽）。**未整脚本真跑**——其 `setup_env.ps1` 会在技能目录 `scripts/` 下写 `pip_debug.log`，违反本次"禁止写入技能目录"的硬约束；且本通道吞 stdout。故"宿主下能否真判 FAIL"我**无法确认**（前两轮审核同样以等价 harness 代替）。
2. **`setup_env.ps1` 端到端真跑**：同上理由（pip 副作用 + 写文件），我用"逐字提取 `:39-68` 代码块 + 自建假 venv"代替。提取的是**原文**而非重写，但严格意义上非"同一进程跑同一文件"。
3. **`回归证据.txt:181` 的 `checks.py skill : (no stdout, rc=1)`**：该瞬间我无法重建（工作区未提交、时间窗已过）。它是"宿主吞 stdout"还是"当时的真实 FAIL"，无从判定——**这正是问题 2 的根源**：这条未解释的失败记录本应被作者清掉或说明。
4. **作者"修复前 18/19 FAIL=1"的来源**：前两轮审核已记录无法重建，我亦无法重建；本轮不再追。
5. **`_candidate_paths` 的 `~/.workbuddy` 通道是否曾被有意用于"技能内交付物"**：我在 `ops.md`/`SKILL.md` 中未找到任何要求写 `skills/…` 相对路径的指示，故倾向判定它为**历史残留**而非设计——但这是我的**推断**，未找到作者的原始设计说明，故只列待验证。

---

## 7. 结论

**v2.5.1 建议"有条件通过、暂不可直接提交"**：R5/R6 报告场景与全部 17 项声称修复我已独立复现、证据上成立（16 成立 / 1 部分 / 0 不成立），但仍有 **2 个严重问题**需先处理——(1) `_candidate_paths()` 残留的 `base.parent`／`~/.workbuddy` 两条候选仍在制造同类假 PASS，其中 `base.parent` 与红线③自相矛盾；(2) `回归证据.txt` 自相矛盾且 `Ledger.md:15` 的"已全部修正"对 NP4 不实。前者属**代码级**、后者属**自述一致性**，恰是作者反复翻车的两类；`SKILL.md:10`、`:86` 等 5 处轻微问题可一并收口。
