# 返修复查 · 独立复核意见（v3.0.0 收尾）

> 复核者：**未参与 v3.0.0 改造、也未参与返修**的独立子代理（第二轮独立审核）
> 复核方式：全程只读 —— 技能目录零改动（复核前后 `git status --porcelain` 的改动面完全一致），全部测试数据写在 `%TEMP%\aw-review\`
> 解释器：`C:\Users\26717\.workbuddy\binaries\python\envs\ai-workflow\Scripts\python.exe`
> 上一轮审核意见：`tasks/技能增强-ai-workflow-v3.0-2026-09-15/独立审核意见.md`（5 个问题）
> 本次复核对象：返修后的工作区状态（未提交）

---

## 0. 复核动作一览（全部亲手复现）

| # | 动作 | 结果 |
| --- | --- | --- |
| 1 | 范围内 grep `mark --revise`（`SKILL.md` / `references/` / `scripts/` / `assets/`） | 仅 1 处，为**描述该 bug 本身**的历史说明，非调用指引 → P1 已修复 |
| 2 | `decide` 缺 `--choice` / 缺 `--capability` | 均 argparse 报错 RC=2 → P2 已修复 |
| 3 | 照 SKILL.md 四参数用法 `decide` + `plan` 复跑 | `10/10 通过，FAIL=0` → P2 已修复 |
| 4 | 范围内 grep `决策依据` + 模板字段核查 | 无字段名残留（命中项均为自然语言短语） → P3 已修复 |
| 5 | 空 `能力类` / 缺必填键 / 非列表 三条 fixture 跑 `plan` | 全部 FAIL，文案与 `DECISION_KEYS` 一致 → P4 已修复 |
| 6 | `ops.md` 中 decide/revise 与 v3.0.0 条目 | 均有 → P5 已修复 |
| 7 | `checks.py skill` | **19/19 通过，FAIL=0**（引用 35 条） |
| 8 | 新旧 `checks.py` 逐 plan 对照（23 个 `plan*.yaml`） | 总 FAIL 8 → 8，**FAIL 集合不一致的 plan 数 = 0** |
| 9 | 红线段落 / 熔断机制 / 熔断常量与函数 逐字节对照备份 | 全部**逐字节相同** |
| 10 | 熔断门禁、`mark`、`revise`、`status` 子命令回归 | 全部正常（含置熔断后 `plan` 判 FAIL） |
| 11 | 回滚清单 §五 覆盖核查 | 已覆盖 `references/ops.md` |
| 12 | 改动对照表字节/行数口径复核 | 12 个数字**全部可复算一致** |

---

## 1. 逐条核验 P1–P5

### P1（主要）`checks.py mark --revise` 文档不存在 —— **已修复**

范围内 grep（排除 `tasks/` 历史档案与 `_backup-v3.0/`）：

```
$ grep -rn -- "--revise" SKILL.md references/ scripts/ assets/
references/ops.md:171:  - **独立审核（未参与改造的子代理）发现 5 项描述↔实现脱节，全部返修**：① 文档写
  `checks.py mark --revise`——该参数**不存在**（实为独立子命令 `checks.py revise`）；② …
```

**唯一命中项是"说明该 bug 曾存在"的历史记述**（`ops.md` 的 v3.0.0 变更日志条目，用于记录返修项本身），**不是调用指引**，不构成残留。两处调用点均已在返修中改正：

```
$ grep -n "revise" SKILL.md
45:| 计划修订 | `meta.计划修订` | `checks.py revise`（或手工追加） | 计划为什么变了 |
285:核心命令：…｜`checks.py revise <plan> --trigger R<n> --change …`（**计划修订留痕**）…

$ grep -n "revise" references/adaptive-planning.md
102:| **计划修订** | `plan.yaml` → `meta.计划修订` | `checks.py revise` 或手工追加 | 每次重规划：触发编号／变化／未变部分 |
```

实现侧确为独立子命令（非 `mark` 的参数）：

```
$ grep -n "add_parser(\"revise\"\|add_parser(\"mark\"" scripts/checks.py
736:    p = sub.add_parser("mark", help="更新 plan.yaml 的步骤状态与/或 meta 熔断状态（替代手工编辑）")
751:    p = sub.add_parser("revise", help="追加一条计划修订到 meta.计划修订（重规划留痕）")
```

**判定：已修复。**

---

### P2（主要）`decide` 文档用法产出被自家门禁判 FAIL 的记录 —— **已修复**

`--capability` 与 `--choice` 均已 `required=True`：

```
$ grep -n "required=True" scripts/checks.py
746:    p.add_argument("--point", required=True, …)
747:    p.add_argument("--basis", required=True, …)
748:    p.add_argument("--capability", required=True, choices=VALID_CAPABILITY, help="能力类：知识/算力/事实/手脚")
749:    p.add_argument("--choice", required=True, help="选择：实际引入的具体手段")
```

实测缺参（此前的"静默写入坏记录"路径已关闭）：

```
$ python checks.py decide <tmp>/p.yaml --point "缺外部事实" --basis "本机推不出" --capability 事实
usage: checks.py decide [-h] --point POINT --basis BASIS
                        --capability {知识,算力,事实,手脚} --choice CHOICE
                        plan
checks.py decide: error: the following arguments are required: --choice
RC=2

$ python checks.py decide <tmp>/p.yaml --point "缺外部事实" --basis "本机推不出" --choice "gh api repos/x"
checks.py decide: error: the following arguments are required: --capability
RC=2
```

**照 SKILL.md:285 所写的最简用法执行**（四参数全给），并在**最小合法计划**上跑门禁：

```
$ python checks.py decide <tmp>/valid.yaml --point "缺外部事实" --basis "本机推不出" \
      --capability 事实 --choice "gh api repos/x"
[ OK ] 决策记录已追加 — 能力类=事实；决策点=缺外部事实
RC=0

$ python checks.py plan <tmp>/valid.yaml --base <tmp>/ws
[ OK ] 决策记录 — 1 条
[ OK ] 计划修订 — 未使用（未发生重规划）
[ OK ] 熔断状态 — 正常（未声明）
=== 结果：10/10 通过，FAIL=0 ===
```

（同文件**无决策记录**时亦为 `10/10，FAIL=0`，证明新增记录不会引入任何 FAIL。）

`SKILL.md:285` 已补全四参数并注明"均必填"：

```
$ checks.py decide <plan> --point … --basis … --capability <类> --choice <手段>
  （**能力决策留痕**，四个参数均必填）
```

**判定：已修复。**（写入侧与门禁侧口径已一致，不再存在"照文档执行 → 被自家门禁判 FAIL"的路径。）

---

### P3（主要）模板 `steps[].决策依据` 在代码中零引用 —— **已修复（字段已删除）**

```
$ grep -rn "决策依据" scripts/ assets/ SKILL.md references/
references/capability-routing.md:74:| "先动手，依据事后补" | **决策依据无法事后补造**——留痕的价值就在它是决策当时写的 |
references/ops.md:171:  …③ 模板新增 `steps[].决策依据` 在代码中**零引用**（已**删除该字段**…）
references/ops.md:177:  - **决策依据**：每任务对每个关键产出另起干净上下文送审，是稳定发生的双倍人力税…
references/quality-gates.md:9:| 标记 | 含义 | 可否作为决策依据 |
references/quality-gates.md:80:12. 引用二手 star 数或营销倍数当决策依据。
```

**5 处命中全部是自然语言短语**（"决策依据无法事后补造"／"可否作为决策依据"／"…当决策依据"），**无一处是字段名**；`scripts/` 与 `assets/` **零命中**。模板步骤段现无该字段：

```
$ grep -n "决策依据\|模型档位\|决策记录\|计划修订" assets/plan-template.yaml assets/templates/*.yaml
assets/plan-template.yaml:19:  决策记录: []          # 【可选】自主决策层的"能力决策留痕"…
assets/plan-template.yaml:23:  计划修订: []          # 【可选】自适应计划的"重规划留痕"…
assets/plan-template.yaml:36:    模型档位: ""        # 本步模型档位：strong/mid/cheap…
```

原验收标准的更正已按机制留痕——任务 `plan.yaml` 的 `meta.计划修订` 存在 R2 条目：

```yaml
  计划修订:
    - 版本: "v1 → v2"
      触发: "R2"
      时间: "2026-09-15 17:32"
      变化: "验证信号『阴性对照缺决策依据必 FAIL』对 steps 级不成立（独立审核 P3）；
             该字段已删除，阴性对照口径改为 meta.决策记录/计划修订 的必填键、枚举与上限"
      未变: "步骤 1-8 的结论与交付物全部有效；步骤 9-10 不变"
```

且 `meta.验证信号` 已改为只对 `meta` 级字段提阴性对照（不再声称 steps 级"缺决策依据必 FAIL"）。该 R2 条目本身**通过门禁**（`plan` 对 v3.0 任务 plan 判 `10/10，FAIL=0`）。

**判定：已修复。**

---

### P4（轻微）门禁 FAIL 文案与 `DECISION_KEYS` 不一致；空 `能力类` 放行 —— **已修复**

常量已并轨为**单一事实源**，且补齐"能力类"：

```
$ grep -n "DECISION_KEYS\|REVISION_KEYS\|VALID_CAPABILITY" scripts/checks.py
72:VALID_CAPABILITY = ("知识", "算力", "事实", "手脚")
76:DECISION_KEYS = ("决策点", "能力类", "依据", "选择")
77:REVISION_KEYS = ("触发", "变化", "时间")
504:        fail("决策记录格式", f"应为列表（每项必含 {'/'.join(DECISION_KEYS)}；时间由工具自动填）")
511:            miss = [k for k in DECISION_KEYS if not str(d.get(k, "") or "").strip()]
523:        fail("计划修订格式", f"应为列表（每项必含 {'/'.join(REVISION_KEYS)}）")
530:            miss = [k for k in REVISION_KEYS if not str(r.get(k, "") or "").strip()]
```

文案由常量 `'/'.join(...)` 生成 **4 项**（不再是 5 项空话）；`能力类` 已进入必填判定（`:511`），**空值不再放行**。三条 fixture 实测：

```
===== 空能力类 =====
[FAIL] 决策记录 — 第1项缺 能力类
=== 结果：9/10 通过，FAIL=1 ===

===== 缺键（只写 决策点/时间）=====
[FAIL] 决策记录 — 第1项缺 能力类、依据、选择
=== 结果：9/10 通过，FAIL=1 ===

===== 非列表 =====
[FAIL] 决策记录格式 — 应为列表（每项必含 决策点/能力类/依据/选择；时间由工具自动填）
=== 结果：9/10 通过，FAIL=1 ===
```

文案所列字段（决策点/能力类/依据/选择）与 `DECISION_KEYS` **逐字一致**，与代码强制项**完全一致**。

**判定：已修复。**

---

### P5（轻微）`ops.md` 未收录 decide/revise 与 v3.0.0 变更日志 —— **已修复**

```
$ grep -n "decide\|revise" references/ops.md
24:| **checks.py** | …；**`decide` 追加一条能力决策记录到 meta.决策记录**（`--point --basis --capability --choice`，
    四参数均必填）；**`revise` 追加一条计划修订到 meta.计划修订**（`--trigger R1–R6 --change [--unchanged]`，
    上限 3 条，超限即 FAIL） | `checks.py plan tasks/x/plan.yaml --base "E:/ChatGPT/工作流"`<br>
    `checks.py decide tasks/x/plan.yaml --point "缺外部事实" --basis "本机推不出" --capability 事实 --choice "gh api repos/…"` |
165:  - **三层留痕…**：`steps[].状态`（`checks.py mark`）／`meta.决策记录`（`checks.py decide`）／`meta.计划修订`（`checks.py revise`）…
166:  - **`checks.py` 新增 `decide` / `revise` 子命令**：…`decide` 四参数…**均必填**；`revise` 在第 4 次追加时 FAIL（上限 3）。

$ grep -nE "^\- \*\*v[0-9]+\.[0-9]+\.[0-9]+" references/ops.md | head -3
162:- **v3.0.0（2026-09-15）新增「自主决策层」：能力路由 + 自适应计划 + 决策留痕**（由
     `tasks/技能增强-ai-workflow-v3.0-2026-09-15/` 驱动…）：
173:- **v2.5.3（2026-09-11）「独立审核」由强制门禁降级为抽查制**…

$ ls -la references/ops.md
-rw-r--r-- 1 26717 197609 63273 Sep 15 17:30 references/ops.md   ← 已在返修阶段被改动
```

§二 脚本速查含用途、参数、上限与示例；§七 顶部**新增 v3.0.0 条目**（且该条目内含本次返修的完整记录）。`SKILL.md` 的两处索引（"脚本用法、故障排查、变更日志见 `references/ops.md`"与"完整参数见 `references/ops.md`"）**现已成立**。

**判定：已修复。**

---

## 2. 返修是否引入新问题

### 2.1 原有验证信号重跑 —— **无回归**

**（a）技能自检**

```
$ python scripts/checks.py skill
[ OK ] 文档引用完整性 — 检查 35 条引用
[ OK ] plan-template 必填键 — 齐全
[ OK ] py_compile：ai_call.py / checks.py / data_query.py / http_fetch.py / office_io.py
=== 结果：19/19 通过，FAIL=0 ===
```

**（b）零回归（新旧 `checks.py` 逐 plan 对照，含 `tmp/`、`toy/` 夹具共 23 个 `plan*.yaml`）**

```
plan*.yaml 项数 = 23
基线（_backup-v3.0/checks.py）总 FAIL = 8；0 FAIL 项 = 15
改造后（当前 checks.py）总 FAIL = 8；0 FAIL 项 = 15
FAIL 数发生变化的项：0
```

> 口径说明：`回归证据.txt §三` 记录的"基线 9 → 改造后 8、变化项 1"是**基线采样时点**（17:05，v3.0 任务 step1 交付物尚未生成）的快照；复核时点该交付物已生成，故两边同为 8、变化项 0。两种时点下结论一致：**返修未使任何历史 plan 新增 FAIL**。

**（c）2026-09-10 / 09-11 历史 plan 抽样单跑（原始输出）**

```
===== UI组件库搭建-2026-09-10 =====
[ OK ] 决策记录 — 未使用（无外部辅助引入，属正常）
[ OK ] 计划修订 — 未使用（未发生重规划）
=== 结果：10/10 通过，FAIL=0 ===
===== 估值快照源接入-2026-09-10 =====
=== 结果：10/10 通过，FAIL=0 ===
===== 技能增强-ai-workflow-v2.5.3-2026-09-11 =====
=== 结果：10/10 通过，FAIL=0 ===
```

**（d）返修触及 `main()` 分派（`mark` 由 `else` 改显式 `elif`），专项回归子命令**：

```
$ python scripts/checks.py mark <tmp>/fuse.yaml --fuse 已熔断
[ OK ] meta.熔断状态已更新 — → 已熔断          RC=0
$ python scripts/checks.py plan <tmp>/fuse.yaml --base <tmp>/ws
[FAIL] 已熔断：不得作为可交付物 — meta.熔断状态=已熔断 —— 自动化已停止。…
[FAIL] 熔断报告存在 — 缺失 …\熔断报告.md
=== 结果：9/11 通过，FAIL=2 ===
$ python scripts/checks.py mark <tmp>/fuse.yaml 1 进行中
[ OK ] 步骤状态已更新 — 1 个：1: 状态: "完成" → 状态: 进行中     RC=0
$ python scripts/checks.py revise <tmp>/fuse.yaml --trigger R2 --change "返修复查抽样"
[ OK ] 计划修订已追加 — 第 1/3 条；触发=R2                     RC=0
$ python scripts/checks.py status --workspace <技能目录>
[ OK ] 任务总览 — 14 个任务目录
```

熔断门禁仍生效、复位两步路径完好、四个子命令分派无回归。

### 2.2 另一类：改动对照表数字可复算性

`改动对照表-v3.0.md §一` 的 12 个数字（改前/改后的字节与行数）**全部可复算一致**（口径为 LF 行尾，仓库 `.gitattributes` 为 `eol=lf`；磁盘值是 CRLF，`磁盘字节 = LF化字节 + 行数`）：

| 文件 | 表记（改前→改后，LF 化） | 实测 |
| --- | --- | --- |
| `SKILL.md` | 33,310/253 → 36,559/285 | 磁盘 36,844 − 285 = **36,559**、285 行 ✓（备份 33,563−253=33,310 ✓） |
| `assets/plan-template.yaml` | 2,399/35 → 3,351/43 | 3,394−43=**3,351**、43 行 ✓（备份 2,434−35=2,399 ✓） |
| `scripts/checks.py` | 28,592/593 → 39,276/798 | 40,074−798=**39,276**、798 行 ✓（备份 29,185−593=28,592 ✓） |
| `references/ops.md` | 57,453/272 → 62,990/283 | 63,273−283=**62,990**、283 行 ✓（HEAD 版 57,453/272 ✓） |

### 2.3 结论

**返修引入的回归问题：无。**（原有验证信号全绿，且熔断门禁与子命令分派经专项复跑无回归。）
**返修引入的其它问题：1 项，见 §3 的 N2。**

---

## 3. 新发现的问题（只列影响正确性者）

### N1（轻微）`references/adaptive-planning.md:101` 把必填字段写成「候选」，实际字段为「能力类」——照该表手写记录会被自家门禁判 FAIL

```
$ grep -n "候选" references/adaptive-planning.md
101:| **能力决策** | `plan.yaml` → `meta.决策记录` | `checks.py decide` | 每次实际引入外部辅助：决策点／候选／依据／选择 |
```

该行"记录内容"四格中，`决策点`／`依据`／`选择` 与 `DECISION_KEYS = ("决策点","能力类","依据","选择")` 逐字相同，唯独第 2 格写成 **`候选`** —— 该字段名**不存在**。后一列 `checks.py decide` 指向的命令本身是正确的（四参数必填），但按本表**手工追加**记录会写成 `候选:`，被 `plan` 判 `[FAIL] 决策记录 — 第1项缺 能力类`。

**性质**：与 P2/P4 同型的"描述↔实现脱节"；**非本次返修引入**（返修在该文件只改了第 102 行，本行未动），属返修**未覆盖**的同类残留。全库（含 `tasks/` 档案）grep `候选` 在该语义下**零其它命中**，"能力类"则在模板注释、fixture、日志中口径统一 —— 可确认为**一次性笔误**而非另一套口径。

**影响**：仅在使用者按本表**手工**写记录时触发；使用 `checks.py decide` 命令则不受影响。

### N2（轻微）`回归证据.txt §一「改造后文件指纹」`在返修中被重新生成，但仍未收录返修新增的改动面 `references/ops.md`

`回归证据.txt §一` 的五个 sha256[:32] **已等于返修后文件**（即该节在返修时被重新生成）：

```
$ python -c "打印各文件 sha256[:32]"
5fd68765197cacd4f2c8c161b16fdbe9  SKILL.md
a0fbf8eb302160a33f51bcaa3a8308a8  references/capability-routing.md
35c4d12e79168f98f747ce290d847cdd  references/adaptive-planning.md
c0cebe9e6f623fd3b81fa3decedff774  assets/plan-template.yaml
9d1ed1dafb7f9ac9e8fd71b866734090  scripts/checks.py
ff37cd47ee751b45054adbc817382493  references/ops.md        ← 未收录

$ sed -n '5,12p' tasks/…v3.0…/回归证据.txt
## 一、改造后文件指纹
  5fd68765197cacd4f2c8c161b16fdbe9  SKILL.md
  a0fbf8eb302160a33f51bcaa3a8308a8  references/capability-routing.md
  35c4d12e79168f98f747ce290d847cdd  references/adaptive-planning.md
  c0cebe9e6f623fd3b81fa3decedff774  assets/plan-template.yaml
  9d1ed1dafb7f9ac9e8fd71b866734090  scripts/checks.py

$ grep -n "ops.md" tasks/…v3.0…/回归证据.txt
（无任何提及）
```

返修后改动面为 **4 改 + 2 新**（`SKILL.md` / `plan-template.yaml` / `checks.py` / **`ops.md`** + 两个新 reference），`改动对照表 §五` 的回滚注释亦写明"4 改 + 2 新"并已覆盖 `ops.md`；但 `回归证据.txt §一` 只列 5 个文件，`references/ops.md` 成为**全部改动文件中唯一没有版本指纹锚点的一个**（该文件亦未提交，无法由 git 提供锚点）。后果：日后若有人只按 §一 校验"交付版本是否即被验证版本"，`ops.md` 的改动**不可被发现**。

**性质**：返修引入的记录完整性缺口（返修把 `ops.md` 加进改动面，却未同步扩展指纹清单）。**不影响任何机器门禁或技能行为**。

### 已核查、**不构成问题**的项（备查，不计入清单）

- `references/ops.md:171` 出现 `` `checks.py mark --revise` `` —— 位于 v3.0.0 变更日志中**描述该 bug 本身**（"该参数不存在"）的句子里，属历史记述，非调用指引。
- `tasks/技能增强-ai-workflow-v3.0-2026-09-15/plan.yaml` 的 step 5 / 6 / 7 文字仍提到 `steps[].决策依据`（"steps 增加可选「决策依据」"等）—— 该字段的删除已由 `meta.计划修订` 的 R2 条目显式留痕（含"该字段已删除"），正是"重规划留痕"机制的预期用法；步骤的**交付物与结论**未受影响。属历史执行记录，非现行口径。
- `references/capability-routing.md:74`／`references/quality-gates.md:9,80`／`references/ops.md:177` 中的"决策依据"均为自然语言短语，非字段名。

---

## 4. 返修是否只改了该改的地方 —— 三条红线与熔断机制**逐字节未改**

对照基线：`_backup-v3.0/`（已核验为 v2.6.2 原件 —— 备份 sha256[:16] 与 `基线固证.txt §1` 记录的 `349060370ad7fe7a`／`a3569016f55cf71e`／`99ce457724dc79d5` **逐一相符**）。

```
===== 红线段落（SKILL.md「三条红线（本流程内不可豁免）」整段）=====
  3 /tmp/rl_old.txt     3 /tmp/rl_new.txt
红线：逐字节相同（diff 无输出）

===== 熔断机制章节 =====
   59 /tmp/fu_old.txt   59 /tmp/fu_new.txt
熔断机制：逐字节相同（diff 无输出）

===== checks.py 熔断相关常量（去行号对照）=====
diff <(grep -E "^(VALID_FUSE|FUSE_REPORT|FUSE_SECTIONS) =" 备份) <(… 当前)  → 无输出
→ 常量逐字节相同

===== _check_fuse 函数体（备份 433-476 vs 当前 448-491，各 44 行）=====
→ _check_fuse 逐字节相同
===== _set_meta_fuse 函数体 =====
→ _set_meta_fuse 逐字节相同
```

`SKILL.md` 相对备份的全量 `diff -u` 仅 8 处（frontmatter `version`/`description`、标题、适用范围行加注、新增「自主决策层」章节、阶段 3 加指针、手册索引 +2 行、核心命令 +2 命令），**红线三条内容与熔断机制文字均不在其中**。返修的 4 处改动（P1×2 / P2 / P3 / P4 / P5）全部落在上述"应改"范围内，**无数值/行为面的越界改动**。

---

## 5. 回滚清单是否完整 —— **已覆盖 `ops.md`**

`改动对照表-v3.0.md §五` 已把返修新增的改动面纳入：

```
# 全量回滚（改动面：4 改 + 2 新）
cp _backup-v3.0/SKILL.md SKILL.md
cp _backup-v3.0/plan-template.yaml assets/plan-template.yaml
cp _backup-v3.0/checks.py scripts/checks.py
# ⚠️ references/ops.md 在**返修阶段**被改动（补 decide/revise 用法 + v3.0.0 变更日志），
#    它不在 _backup-v3.0/ 快照里 → 用 git 回滚（HEAD 7301a95 即 v2.6.2 状态）
git checkout -- references/ops.md
rm references/capability-routing.md references/adaptive-planning.md
# 校验回滚成功
"C:/Users/26717/.workbuddy/binaries/python/envs/ai-workflow/Scripts/python.exe" \
    scripts/checks.py skill          # 应回到 19/19（文档引用数回到 29）
```

核查：
- `references/ops.md` **已单列**并给出正确的回滚手段（`git checkout`，因其不在 `_backup-v3.0/` 快照内）—— 返修确未把它误列进 `cp` 分支；
- 两项 `rm`（新 reference）与三项 `cp`、一项 `git checkout` 覆盖了全部 6 个改动文件；
- 回滚校验行已按上一轮审核的提示改用**技能 venv 解释器**，并注明裸 `python` 会因缺 `pyyaml` 以 RC=2 中止（非"回滚失败"）；
- 备用单行回滚（`git checkout -- SKILL.md assets/plan-template.yaml scripts/checks.py references/ops.md` + 删两个新文件）同样覆盖 `ops.md`。

**判定：回滚清单完整。**

---

## 6. 复核结论

| 项 | 判定 |
| --- | --- |
| P1 `mark --revise` 文档不存在 | **已修复** |
| P2 `decide` 用法产出被自家门禁判 FAIL | **已修复** |
| P3 模板 `steps[].决策依据` 零引用 | **已修复**（字段已删 + R2 留痕 + 验收标准更正） |
| P4 FAIL 文案与 `DECISION_KEYS` 不一致；空 `能力类` 放行 | **已修复**（常量单一事实源 + `能力类` 入必填） |
| P5 `ops.md` 未收录 decide/revise 与 v3.0.0 | **已修复** |
| 返修是否引入新问题（回归信号） | **无**（19/19 FAIL=0；23 个 plan 逐项零回归；子命令与熔断门禁专项复跑无回归） |
| 返修是否引入新问题（其它） | **有 1 项**（N2，轻微，记录完整性） |
| 三条红线与熔断机制是否被改 | **逐字节未改** |
| 改动是否只落在应改处 | **是** |
| 回滚清单是否完整（含 `ops.md`） | **完整** |

另有 1 项为返修**未覆盖**的同类残留（N1，轻微）——非本次返修引入，但与 P2/P4 同型，建议一并收口。

> 说明：N1 与 N2 均**不触及任何机器门禁、技能行为或红线**；N1 的权威口径（`plan-template.yaml` 注释、`checks.py` 常量、`ops.md §二`）均正确，仅 `adaptive-planning.md` 一处汇总表笔误；N2 仅影响 `ops.md` 的版本可追溯性。
>
> 若采用更宽松的口径（N1 视为"记录内容"的语义描述而非字段规格、N2 视为 `§一` 只覆盖"改造期文件"），两条均可判为非阻塞。

**复核结论：不通过（2 个问题）**
