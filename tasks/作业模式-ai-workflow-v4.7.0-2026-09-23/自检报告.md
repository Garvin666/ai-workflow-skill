# 交付前自检报告 —— 作业模式-ai-workflow-v4.7.0（2026-09-23）

> 本报告只登记**判定结果**与**证据出处**；原始命中行全部落在 `tmp/`。

## 一、交付物清单（含 sha256 前 16 位）

### 新增（3）

| 文件 | 角色 | sha256(前16) |
| --- | --- | --- |
| `references/homework-mode.md` | 作业模式**判据语义源**（C1–C3 / H1–H5 / 三类题型 / I1–I4） | `aec9b0c4e45ba283` |
| `references/homework-judge.md` | **第四份 judge 契约**（模式选用·快判） | `86a34a9a5a17c254` |
| `assets/templates/作业解题.yaml` | L1 快速通道模板（schema 9/9 通过） | `e54df86e7f36b552` |

### 修改（16）

`SKILL.md`(`fae464e0ee38570f`) ／ `scripts/checks_core.py`(`4e3077cf63e9a693`) ／ `scripts/checks_parity.py`(`06e39cc5aac2481b`) ／ `scripts/checks_judges.py`(`501884f472b8da5d`) ／ `scripts/checks.py`(`75685cc7d8b19934`) ／ `scripts/laya_client.py`(`8e563d7ef76b6830`) ／ `scripts/judges.json`(`e856c05ff6809cbf`) ／ `assets/plan-template.yaml`(`754b188e18fa30d2`) ／ `references/data-model.md`(`5b6aa076a5ae9519`) ／ `references/decision-tables.md`(`f5172a9251639db9`) ／ `references/playbook.md` ／ `references/quality-gates.md` ／ `references/ops.md`(`272a0483139a7884`) ／ `references/laya-backend.md`(`9b5a56cefb2f53ee`) ／ `references/self-judge.md` ／ `references/method-judge.md` ／ `references/retrieval-judge.md` ／ `_archive/changelog.md` ／ `Ledger.md`(`9fb4ee0d4e676a91`)

## 二、验收证据（真跑，原始输出留档）

| # | 判据 | 期望 | 实得 | 证据 |
| --- | --- | --- | --- | --- |
| 1 | `checks.py skill --skill-dir <技能根>` | FAIL=0 | **通过 52/52，FAIL=0，SKIP=0，WARN=0**（v4.6.1 为 49/49，**+3**） | `tmp/门禁输出.txt` |
| 2 | 离线自证 `_offline_check.py`（6 节） | FAILS=0 | **FAILS=0** | `tmp/自证输出.txt` |
| 3 | 阴性对照 `_negative_controls.py` | FAILS=0 | **FAILS=0**（见下） | `tmp/自证输出-阴性对照.txt` |
| 4 | `checks.py plan <本 plan> --base <技能根>` | FAIL=0 | **通过 17/22，FAIL=0，SKIP=3，WARN=2** | `tmp/门禁输出.txt` |
| 5 | `guard_constants.py` | 漂移 0 | **7 组，OK 7 / DRIFT 0 / MISSING 0 / ERROR 0** | `tmp/门禁输出.txt` |
| 6 | 关键文件零漂移（独立进程前后快照） | 前后 sha256 全同 | **16/16 全同** | `tmp/_hash_before.txt` ／ `tmp/_hash_after.txt` |

### 判据 3 的两组对照（**证明判据有牙，不是空跑**）

**① 口径守卫「作业模式」**：把 `scripts/judges.json` 的一个 mode 取值注入漂移（`讲解题` → `讲解`）→ 该行报

```
[FAIL] 口径守卫：作业模式 — …解析得 作业题/讲解/讲解题/非作业 ≠ 代码常量 作业题/讲解题/非作业
```

整体 rc=1；写回原字节后 sha256 与注入前**逐位一致**（`e856c05ff6809cbf…`），再跑复绿 `FAIL=0`。

**② `meta.作业判定` 结构判据（5/5 符合预期）**：合法（作业题 + 派生自洽）→ OK；mode 越界（`作业模式`）→ FAIL；**派生矛盾（`mode=讲解题` 但 `needs_homework=true`）→ FAIL**；distribution 和 ≠ 1（1.22）→ FAIL；缺 `dimensions` → FAIL。

**③ 代理旁路**：对不可达本地端口，异常必须是**连接类**（`URLError`）而非 `HTTPError 502` —— 实测符合（`自证输出.txt` 第 5 节）。

### 判据 4 的两个 WARN（如实登记，非失败）
`meta.方法选用` / `meta.检索判定` 未记录 —— 本任务执行期未逐步产 method-judge / retrieval-judge 留痕，两条均为**只提示不阻断**的抽样项，**不是门禁**。SKIP=3 = 用户放行 / 自研工具链接「待推送」/ 反思重试未配置。

## 三、八项「不得读成绿」（如实登记，不粉饰）

1. **作业识别是启发式的**，无机器判据（误判两条路都不致命：漏判少一层结构，误判输出偏重）。
2. **`meta.作业判定` 整段缺省只 WARN**（不追认历史计划）—— **不得宣称它是门禁**。
3. **I1–I4 是纪律项**，`checks.py` 不校验 —— **不得宣称「已自动拦截」**。
4. **检查项 19 只指路**，机器不代替人判定；且只说「一致率」，**不声称「准确率」**。
5. **口径守卫是并集模型**：多源取并集 ⇒ **"多写会 FAIL、少写不会"**，与既有 9 项共有该覆盖边界。
6. **「两套三态枚举不得合并」无机器判据** —— 守卫只保证三源取值一致，**不保证二者不被合并**；防合并靠显式声明 + 人审。
7. **`PINNED_MODEL_BY_KIND` 的 `homework_judge: multilingual` 未经真机复核**（本轮服务未运行，只跑了"不可达 → exit 3 降级"路径）。首次真跑时**必须读服务端 `routing.reason` 复核**。
8. **两份物理拷贝靠人工双写**：技能 `scripts/judges.json` 与 `~/.workbuddy/laya/app/` 下的同名文件**无机器判据保证持续一致**。

## 四、本轮拦下的两处真实问题（均未流到交付）

| # | 问题 | 检出方式 | 处置 |
| --- | --- | --- | --- |
| 1 | **本机 `HTTP_PROXY` 把本地回环请求答成 502** → 服务明明活着却被判不可用 → **静默降级回 Realization A**（且不留显式错误） | 接入 Laya 时 `--selfcheck` 实测 | 客户端 `_opener()` 对回环走 `ProxyHandler({})`；配连接类异常阴性对照；登记 `laya-backend.md` §4.1 |
| 2 | **`homework-mode.md` 变更记录声称"`quality-gates.md` §二 补一行指路"，实际未落**（grep 零命中） | 写完变更记录后回查原文 | 已补该行（验证信号表新增「作业/习题」行）；属本技能记录过 3 次的「描述与实现不符」病灶 |

另有一处**表述冲突**在落地中被发现：`SKILL.md` 与 `homework-mode.md` 都写「本模块零新增：不加 plan 字段、不加判据」，而同批评定层确实新增了 —— 已加 **scope 分界注记**（"本模块" vs "判定层"），**改的是表述边界，不是把判据撤掉**。

## 五、已知边界（本轮未改变，登记备查）

- **任务档案含本机用户名**：`tasks/` 下**既有 171 份**文件含 `C:\Users\<具体用户名>` 形态路径（非本轮引入）。若 `tasks/` 进入推送面，`outbound_scan.py` 的第 4 项判据会 FAIL —— 该状态**先于本轮存在**，本轮未扩大也未清理（清理属独立决定）。本轮**新增**的门禁/自证输出已按 `<用户名>` 脱敏（`tmp/门禁输出.txt` 文件头有声明）。
- **`_offline_check.py` 的输出文案**沿用既有写法（`[ OK ] <失败文案>`，"断言失败时会怎样"作标签），读时须按"OK = 断言成立"理解，勿按字面读成"OK 了失败"。
- 本轮**未 commit、未 push**；Laya 开机自启仍待用户以管理员身份执行。

## 六、待用户拍板

1. **是否推送两仓**（`ai-workflow-skill` 本体 + `ai-workflow-tools`）—— 属不可逆对外动作，须过四道门并由用户单独确认；本轮**未执行**。
2. 若要正式启用 Realization B 为主判据，前提（温度拟合 + ≥50 条 A/B 一致率阈值）**仍未闭环**。
