# 确认表 —— 模式选用聚合模型（ai-workflow v4.7.0 补强）

| 项 | 值 |
| --- | --- |
| **任务** | 为 `homework-judge` 补上缺失的**聚合**一段：`scripts/homework_model.py`（Realization A 的可执行半边）+ 契约 §5.1／§5.2 |
| **工作区根** | `~/.workbuddy/skills/ai-workflow` |
| **任务层级** | L2 |
| **入口判定** | `category = code`（次类 content）；来源分型 = 正式需求 |
| **作业判定** | `mode = 非作业`（本任务是**给作业模式建模型**，不是作业题本身） |
| **创建时间** | 2026-09-23 22:10 |
| **计划** | `tasks/模式选用模型-ai-workflow-v4.7.0-2026-09-23/plan.yaml`（7 步，全部完成） |

## 一、触发依据（用户授权）

用户原话：**「如果需要的话请建立一个模型」** —— 条件式授权。判断为"需要"，依据是**客观缺口**（非偏好）：

1. `references/homework-judge.md` §5 的阈值挂在 `confidence` 上，但**「W1–W5 → 分布」这一段没有定义**（黑箱，藏在 Laya 内部或现场即兴）；
2. 后果是 A 侧判定**不可复现** ⇒ §7.3 的出口条件「≥50 条 A/B 一致率」**结构上无法被满足**（该「未闭环」是上一版契约自己登记在案的）。

## 二、越界授权登记

| 路径 | 操作 | 依据 |
| --- | --- | --- |
| `~/.workbuddy/skills/ai-workflow/` | 写：新增 1 脚本 + 新建 1 任务目录 + 改 4 文件 | 技能生成例外（`SKILL.md` 工作区边界「基础设施例外」表首行）；本任务即技能自身演进类 |

**不可逆副作用**：无（未 commit、未 push、未删任何既有文件、未改任何既有判据的取值或阈值）。

## 三、交付物清单

| # | 文件 | 角色 |
| --- | --- | --- |
| 1 | `scripts/homework_model.py`（新建） | 聚合内核 + 契约装配 + A/B 一致率 + selftest + CLI |
| 2 | `references/homework-judge.md`（改） | +§5.1 聚合规则（唯一口径源）、+§5.2 一致率出口条件、§7.3 时点注记、变更记录 v1.1 |
| 3 | `references/ops.md`（改） | 脚本速查表 +1 行 |
| 4 | `SKILL.md`（改） | 作业模式节 +「聚合与一致率」段；核心命令 +1 行 |
| 5 | `references/laya-backend.md`（改） | §5 影子期出口条件 + 时点注记 |
| 6 | 任务档案 | `plan.yaml`、`确认表.md`、`自检报告.md`、`trace.jsonl`、`_metrics.jsonl`、`tmp/` |

## 四、验收清单（机器实测，原始输出见 `tmp/`）

| # | 验收项 | 命令 | 结论 |
| --- | --- | --- | --- |
| 1 | 模型自证 | `homework_model.py selftest` | **FAILS=0（18 项）** |
| 2 | 阴性对照①：一致率有牙 | selftest 第 8 组 | 注入分歧 → `exact=0.0` 且进分歧清单 |
| 3 | 阴性对照②：锚例断言非空跑 | selftest 第 9 组 | 破坏锚点 → 命中 **8/8 → 1/8** |
| 4 | 阴性对照③：不静默 | selftest 第 10 组 | 缺维度 / 越界 → 均抛 `ValueError` |
| 5 | 判定块可过门禁 | `checks.py plan <本任务 plan>` | **FAIL=0**；`meta.作业判定 合法` = OK |
| 6 | 技能自检 | `checks.py skill --skill-dir <技能根>` | **53/53 FAIL=0**（52→53 = 新增脚本的 py_compile 项） |
| 7 | 未污染自证 | `tmp/_hash_sources.py before/after`（两次独立进程） | 关键文件哈希一致 |
| 8 | 留痕 | `checks.py trace` ×7 + `metrics --finalize` | `trace.jsonl` 7 行；`_metrics.jsonl` 已写 |

## 五、不得读成绿（四条）

1. `selftest` 全绿只证明 **实现与公式自洽**，不证明**判得对**；
2. 锚例 8/8 命中用的 W 值是**本模型推定**，**不是真实标注**；
3. 一致率工具**不产生样本** —— 真机 Laya 输出 0 条时仍无数据可测；
4. `ANCHORS` / `TAU` 是**未拟合先验**，`distribution` / `confidence` 只可用于对照与回归。

## 六、待用户拍板

| # | 事项 | 说明 |
| --- | --- | --- |
| 1 | 一致率阈值 `--min-agree` 取多少 | 契约 §7.3 记为"未定值"；脚本**刻意不给默认值** |
| 2 | 是否拟合 `ANCHORS` / `TAU` | 需先有 A 侧独立标注样本（可用 `--anchors-file` / `--tau` 替换） |
| 3 | 是否推送（两仓） | **不可逆对外动作**，本轮未执行 |

## 七、用户确认

- [ ] 认可本次交付
- [ ] 认可"模型只做聚合、不做识别"的边界
- [ ] 同意/不同意推送远端（若不勾选，保持本地未推送状态）
