# 任务确认表 —— 技能增强-ai-workflow-v4.6.0（2026-09-22）

## 用户原话（逐字保留）

> 我需要你在ai-workflow工作流中添加项目Git开发规范：
> 1. main分支：存放稳定可运行版本，禁止直接在main开发新功能。
> 2. 所有新功能、实验调试、新增模块（Jeff/Jev、DAG工作流、Skill），全部在dev分支开发；
> 多个并行实验，新建独立exp-xxx分支。
> 3. 提交commit注释规范：
> feat: 新增功能
> fix: 修复bug
> refactor: 代码重构，功能不变
> docs: 更新文档注释
> 4. 禁止提交：模型权重、数据集、虚拟环境目录、日志、密钥.env文件，使用.gitignore过滤。
> 5. 开发流程：dev分支编码→本地测试→commit提交→推远程dev；验证稳定后再合并到main。
> 后续编写代码、修改方案、生成commit信息时，请严格遵守这套规则。

## 任务类型与分层

| 项 | 值 |
| --- | --- |
| 入口类型 | 正式需求（带明确规则条目与落点要求） |
| 任务层级 | **L2 全流程** |
| 主类 / 次类 | `content`（规范手册）/ `code`（校验脚本与守卫） |

## 工作区根与越界判定

- **工作区根**：`C:/Users/<用户名>/.workbuddy/skills/ai-workflow`
  （⚠️ 本文件会进入 public 仓，故本机主目录路径按出站脱敏口径写作 `<用户名>` 占位符；其余内容逐字保留）
- **性质**：技能自身演进类任务 —— 落在 SKILL.md「工作区边界」的基础设施例外路径内（`~/.workbuddy/skills/ai-workflow/` 允许「改技能本体」，且**仅"技能自身演进"类任务**才可写该目录下的 `tasks/`）。**不构成越界，无需申请授权。**
- 本任务**不触碰**技能目录之外的任何路径（不读 `E:\ChatGPT\ui库`，不读 JEV 原始设计文档）。

## 落地口径（用户 5 条 → 技能内落点）

| 用户规则 | 落点 |
| --- | --- |
| ① main 稳定 / 禁止直开 | `references/git-conventions.md` §2 分支模型；`git_check.py branch` 机器检查 |
| ② dev 开发 / exp-xxx 并行实验 | 同上 §2.2–2.3 分支命名与生命周期 |
| ③ commit 注释规范（4 类） | 同上 §3；`git_check.py msg` 机器校验；**纳入口径守卫**（手册 ↔ `COMMIT_TYPES` 常量同源） |
| ④ 禁止提交 5 类 + .gitignore | 同上 §4；`assets/gitignore-template.txt`；`git_check.py ignore` 覆盖率检查 |
| ⑤ 开发流程五步 | 同上 §5；与 `push-routing.md` 的推送口径做职责分界（§6） |

## 范围边界（本轮不做什么）

- **不**改写 `push-routing.md` 的推送机制（只做分界声明，避免两处定义同一件事）。
- **不**为 ai-workflow 技能本体自身执行分支迁移（技能仓当前只有 `main`；迁移到 dev/exp 属**不可逆的对外动作**，须用户单独确认，见交付说明「待确认项」）。
- **不**扩展用户给定的 4 类 commit 类型（如需新增 chore/perf 等，须用户确认后同步手册 + 常量 + 守卫三处）。

## 验收标准

1. `checks.py skill`（从工作区根调用）→ FAIL=0。
2. `git_check.py` 阳性对照绿、阴性对照红，各贴原始输出。
3. 口径守卫新增项「提交类型」做一次故意改错 → 必须 FAIL，随后回退并复绿。
4. 新增文件（`references/git-conventions.md`、`assets/gitignore-template.txt`、`scripts/git_check.py`）均被真实引用，引用完整性检查通过。
5. 用户 5 条规则逐条可在规范文件中定位到对应小节。
