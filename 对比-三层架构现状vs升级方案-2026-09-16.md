# 对照分析：三层架构（现状） vs 升级方案 v2.0

> 日期：2026-09-16 ｜ 类型：对照分析
> **对比对象**
> - **现状**：面向支付／开户场景的 Agent 三层架构 —— 来自用户提供的三张设计说明材料
> - **升级后**：`Agent三层架构升级方案-2026-09-16.md`（v2.0）
>
> **口径声明**：现状侧的描述全部来自输入材料原文；升级侧的数值均为**设计目标值**，非实测数据。两者不作同口径数值比较，只作**机制层面**的对照。

---

## 0. 速览：六个维度的对照

| 维度 | 现状 | 升级后 | 变化性质 |
|---|---|---|---|
| **结构形态** | 3 层串行直链 | 3 层 + **L0 贯穿平面** | 补观测面 |
| **分型方式** | 强／弱二分类，**无判据** | 状态强度 × 业务风险**二维矩阵 + 打分阈值** | 判据从"无"到"可复现计算" |
| **控制点密度** | 9 个功能点，**0 个定值参数** | 约 17 个节点，**30 个定值参数** | 从声明到可执行 |
| **异常闭环** | **3 处断头**（挂起无唤醒／失败无补偿／拦截无出口） | **0 处断头** | 补闭环 |
| **变更治理** | 无版本、无灰度、无审计 | 规则与 FSM 版本化 + 影子模式 + 审计流 | 变更从"直接生效"到"可量化放量" |
| **可验证性** | 无法回答"这次是哪层出的错" | 全链路 trace，任意请求可回溯 | 从"相信它可控"到"证明它可控" |

---

## 1. 整体结构对比

### 1.1 结构形态

**现状** —— 三层是**串行叠加**关系，每层只对下一层负责：

```
用户请求 → L1 任务分级 → L2 执行链路兜底 → L3 架构层优化 → 业务系统
```

问题不在串行本身，而在于：**三层之间没有任何横向的联系机制**。L1 的分型结果 L2 不知道，L2 的失败信号 L1 收不到（无法触发重分类），L3 的规则拦截不产生可追溯的链路记录。出问题时，三层的日志各自为政。

**升级后** —— 三层仍保持串行主链（这点刻意不变），但增加了 L0 平面：

```
                    ┌──── L0 观测与治理平面（贯穿 L1–L3）────┐
                    │  Trace │ 指标 │ 审计 │ 规则/FSM 版本中心  │
                    └───────────────────────────────────────┘
用户请求 → L1 任务分型与状态编排 → L2 执行韧性链路 → L3 上下文与规则治理 → 业务系统
```

L0 同时充当三条**反向通路**：
- **L2 → L1**：失败信号回传，触发动态重分类
- **L3 → L2**：规则判定结果前置到工具调用之前
- **L1/L2/L3 → L0**：全部决策依据落审计

### 1.2 规模对比

| 项目 | 现状 | 升级后 | 说明 |
|---|---|---|---|
| 层数 | 3 | 3 + 1（L0） | 主链层数不变 |
| 功能节点 | 约 9 | 约 17 | 新增：分型评估、风险分级、动态重分类、看门狗、补偿器、保真校验、规则引擎、人工工单等 |
| 有默认定值的参数 | **0** | **30** | 现状所有阈值（超时多长、几轮记忆）均未定义 |
| 已闭环的异常路径 | 0 / 3 | 3 / 3 | 见 §3 差异 D9/D10/D17 |
| 可度量的质量指标 | 0 | 11 | 见升级方案 §6.2 |

---

## 2. 逐层步骤对比

### 2.1 L1 任务分级

| | 现状步骤 | 升级后步骤 |
|---|---|---|
| 1 | 接收请求 | 接收请求 |
| 2 | 判断强状态／弱状态（**判据未定义**） | **状态强度评估**（S0／S1／S2，按打分公式） |
| 3 | 分流：强 → 状态机；弱 → 原生对话 | **风险分级**（R0／R1／R2／R3） |
| 4 | 强状态：每步持久化 + 每轮读状态节点 | **策略矩阵查表**（S×R → 4 类策略） |
| 5 | — | 分流：按矩阵进入对应路径 |
| 6 | — | **运行期信号监控**（失败数／风险标签／轮次／空闲） |
| 7 | — | **动态重分类**（触发即升级到强状态，原子回填快照） |
| 8 | — | 终态**归档降级**（释放状态机槽位） |

**关键差别**：现状的决策发生在**第 2 步一次**，之后不再改变；升级后的决策在第 6–7 步**持续可纠偏**。
**第二处关键差别**：现状**风险维度完全不参与分型** —— "单轮支付"在现状下可能被判为弱状态，只走模型记忆。升级后 R2/R3 可独立于状态维度触发强制管控。

### 2.2 L2 执行链路兜底与校验

| | 现状步骤 | 升级后步骤 |
|---|---|---|
| 1 | 工具调用请求 | 工具调用请求 |
| 2 | 检查 `task_id` 是否调用过 | **计算复合幂等键** `sha256(task_id‖step_id‖tool‖canonical_args)` |
| 3 | 命中 → 直接返回缓存结果 | **在途锁检查**（写工具）／**缓存查询**（只读工具） |
| 4 | 未命中 → 执行 | **规则引擎前置校验**（allow／audit／review／block） |
| 5 | 结果持久化 | 执行 + **心跳续租**（10s） |
| 6 | 超时 → 挂起 + 保留现场 | 结果落库 + **副作用标记**（PENDING／COMMITTED／ROLLED_BACK） |
| 7 | **（无后续）** | 失败 → **退避重试** → **熔断判定** → **死信** |
| 8 | — | 需要时 **Saga 逆序补偿**（补偿本身幂等） |
| 9 | — | 看门狗扫描 → **退避唤醒** → 超限转人工 |

**最关键差别**：现状第 2 步的 `task_id` 键会让**同一任务内不同参数**的工具调用（例如先查余额 A 再查余额 B）被判定为重复，直接返回 A 的结果。**这是数据正确性问题，不是效率问题。**
**最关键断头**：现状第 6 步"挂起"之后**没有第 7 步** —— 任务永久停在挂起态。

### 2.3 L3 架构层优化

| | 现状步骤 | 升级后步骤 |
|---|---|---|
| 1 | 工具返回原始长数据 | 工具返回原始长数据 |
| 2 | 精简预处理（**策略未定义**） | 判断有无 `response_schema` → **通道 A 结构化抽取** / **通道 B 语义摘要** |
| 3 | 结果入上下文 | **保真校验**（数字／ID／否定词回查） |
| 4 | — | 校验失败 → **回退「首尾截断 + 省略标记」** |
| 5 | 记忆切面：固定保留最近 **3 轮** + 全局任务状态 | 按 **Token 预算**组装三层（LF 10% / TS 30% / WM 60%） |
| 6 | — | **裁剪优先级排序**（高风险交互与计划引用参数不可裁） |
| 7 | 高风险业务加规则校验，不符合直接拦截修正 | **独立规则引擎**：决策表 → 四类动作 |
| 8 | — | 拦截产出**可解释证据**；变更走**影子模式→active** |

**关键差别**：现状第 2 步和第 5 步都是**静态**的（固定精简策略、固定 3 轮）；升级后两者都是**按数据与预算动态决定**的。
**关键差别（安全相关）**：现状的"精简预处理"可能把"**未**扣款成功"压缩为"扣款成功"而无人知晓；升级后第 3 步专门校验这类损失，失败即回退。

### 2.4 L0 观测与治理平面

| | 现状 | 升级后 |
|---|---|---|
| 全链路 trace | 无 | `trace_id` 贯穿 L1–L3，逐节点 span |
| 运行指标 | 无 | 11 项（含红线指标"重复副作用次数 = 0"） |
| 审计日志 | 无（或散落各处） | append-only，含 `ruleset_version` / `fsm_version` / 证据字段 |
| 版本治理 | 无（规则改了无从追溯） | 规则与 FSM 版本中心 + 热更新 + 灰度放量 |

---

## 3. 关键差异清单（18 条）

| # | 差异维度 | 现状 | 升级后 | 性质 |
|---|---|---|---|---|
| D1 | 分型维度 | 1 维（状态强度，隐含） | 2 维（状态强度 × 业务风险） | 能力扩展 |
| D2 | 分型判据 | 无 | 打分公式 + 阈值 4 | **可复现性** |
| D3 | 分型时机 | 一次定终身 | 运行期持续可纠偏 | **新增能力** |
| D4 | 状态机规格 | 有名字，无状态集／守卫／迁移定义 | 完整规格 + 版本冻结 | **可实现性** |
| D5 | 状态机生命周期 | 进入即常驻 | 终态归档降级 | 防膨胀 |
| D6 | 幂等键 | 1 元组 `task_id` | 4 元组复合哈希 | **正确性修复** |
| D7 | 幂等分级 | 一刀切 | 三级（缓存／在途锁／副作用标记） | 读写分流 |
| D8 | 缓存失效 | 无 TTL、无失效 | TTL + 显式失效 + 事件失效 | **正确性修复** |
| D9 | 挂起后续 | **无唤醒**（断头） | 租约 + 心跳 + 看门狗 + 退避 | **补闭环** |
| D10 | 失败恢复 | **无补偿**（断头） | Saga 逆序补偿 + 补偿幂等 | **补闭环** |
| D11 | 指令优先级 | 2 层 + 附加 | 4 层 + 冲突矩阵 + 抢占队列 | 可执行化 |
| D12 | 不可逆动作保护 | 无 | 可撤销性裁决（不可逆不允许抢占） | **新增保护** |
| D13 | 韧性机制 | 无 | 重试退避 + 熔断 + 死信 | **新增能力** |
| D14 | 摘要质量 | 无策略、无校验 | 双通道 + 保真校验 + 回退截断 | **可监控** |
| D15 | 记忆窗口 | 固定 3 轮 | Token 预算自适应（N ∈ [2,8]）+ 裁剪优先级 | 静态 → 动态 |
| D16 | 规则承载 | 场景清单（无版本、无解释） | 独立规则引擎（版本化／影子／可解释／fail-closed） | **工程化** |
| D17 | 卡住出口 | **无**（断头） | 人机协同工单（附完整快照） | **补闭环** |
| D18 | 观测能力 | 无 | L0 平面 | **新增基础设施** |

---

## 4. 优势分析

### 4.1 正确性：修的是事故，不是效率

D6 + D8 是这份升级里**唯一必须排在第一位**的项。理由：现状的 `task_id` 幂等键在支付场景下会产生**错误的业务结果**（返回别人／别次查询的数据），而不是"慢一点"。同类问题还有 D8（无失效策略 → 余额类查询返回陈旧值）。

### 4.2 闭环性：三处断头全部接上

| 断头 | 现状后果 | 升级后 |
|---|---|---|
| 挂起无唤醒（D9） | 任务永久停在挂起态，用户侧表现为"卡住" | 看门狗退避唤醒，超限转人工 |
| 失败无补偿（D10） | 有副作用的步骤无法收敛，状态可能不一致 | Saga 逆序补偿 |
| 拦截无出口（D17） | 规则误拦 = 业务中断且无申诉通道 | 人工工单 + 决议回写 FSM |

### 4.3 可度量性：从"相信"到"证明"

现状**没有任何一项可量化的质量指标** —— 这意味着"可控性优先"这个设计意图**无法被验证，也无法被质疑**。
升级后 11 项指标让判断变成事实，其中"同一 `idem_key` 重复副作用次数 = 0"是可直接作为验收门槛的红线。

### 4.4 可演进性：变更不再"直接生效"

现状改一条规则就是全量生效，误拦即事故。
升级后：影子模式先量化误拦率 → 达标才 active → 出问题按 `ruleset_version` 定位到具体版本回滚。

### 4.5 成本：不是"所有任务都变重"

一个常见误解是"升级 = 处处加管控"。实际上升级后按风险分化：
- **S0 × R0**（只读、无状态）：走原生对话，**不触发**状态机、**不触发**规则引擎
- 只有高风险路径才付出完整管控成本

但需注意：**幂等键计算与 L0 埋点对每个请求都生效**，这部分是固定开销（见 §5.2）。

### 4.6 意图保持

升级方案附录 B 做了逐条对应（原设计表述 → 升级后如何承载 → 意图是否保持），16 条原设计表述全部标记为"保持"，其中 12 条标注"保持并加强/补齐"。**唯一一处主动修正**是 D5（终态归档降级），已在方案中显式标注为修正项而非隐含变更。

---

## 5. 潜在影响与代价

> 这一节是本次对照的重点。任何升级都有代价，不说清代价的方案不可信。

### 5.1 复杂度成本（最直接）

| 项目 | 现状 | 升级后 | 倍数 |
|---|---|---|---|
| 功能节点 | ~9 | ~17 | ~1.9× |
| 需维护的参数 | 0 | 30 | — |
| 需维护的层 | 3 | 4 | 1.33× |

17 个节点、30 个参数、1 个新平台 —— 这对团队的工程能力有实际要求。**小团队（1–3 人）落地全量升级，维护负担可能超过收益。**

### 5.2 延迟成本

每个请求新增的串行步骤：
- 幂等键计算（`canonical_json` 归一 + 哈希）—— 微秒级，可忽略
- 规则引擎前置校验 —— 一次内存/DB 查表，**毫秒级**
- L0 埋点与审计落盘 —— 取决于是否异步，**同步写会是最大延迟来源**
- 高风险路径额外：保真校验（正则抽数 + 回查）

**建议**：审计落盘必须异步化，否则会成为 P95 延迟的主要贡献者。

### 5.3 新引入的失败模式（最需要警惕）

| 新风险 | 说明 | 缓解 |
|---|---|---|
| **规则引擎故障主动阻断业务** | `fail_mode = closed` 意味着规则引擎挂掉时 R2/R3 全部拒绝 —— 这是**用可用性换安全性**的设计选择 | 必须由业务方明确认账；保留限时 `fail_open` 白名单开关 |
| **保真校验失败率过高** | 若回退率高，上下文质量可能反不如"直接摘要" | 监控 `digest_lossy` 比例；回退率 > 20% 时重新评估摘要策略 |
| **看门狗误判导致双写** | 租约接管判断错误 → 两个执行者同时写 | CAS 校验 + 被接管进程立即自杀；上线初期关闭自动唤醒，改人工触发 |
| **动态性削弱可复现性** | 预算自适应记忆使**同一任务在不同时刻的窗口可能不同** → 问题复现变难 | 把 `wm_rounds`、裁剪决策一并写入 trace，使"当时为什么这么裁"可回溯 |

### 5.4 Saga 补偿的能力边界（重要澄清）

Saga **不是万能的**。它能工作的前提是：每个写操作都有语义上成立的逆操作。现实中存在两类做不到的情况：

| 类型 | 例子 | Saga 能做到什么 |
|---|---|---|
| 有逆操作 | 冻结额度 → 解冻 | 真回滚 |
| **无逆操作（R3 不可逆）** | 资金已划出、账户已开立、监管数据已上报 | **只能标记 + 人工介入，做不到自动回滚** |

> 因此升级方案里的"补偿覆盖率 100%"应理解为**注册覆盖率** 100%（每个写步骤都登记了补偿动作），而非"回滚成功率" 100%。这个区别必须在给业务方讲方案时讲清楚，否则会被误解为"有了 Saga 就能自动撤销一切"。

### 5.5 组织与纪律成本

规则引擎的核心约束是"**LLM 不能修改规则、不能绕过规则、不能解释掉一次拦截**"。这是代码层约束 + 评审纪律的组合，实践中很容易被"这次是特例，先放行"逐步侵蚀。**需要把这条写进代码评审清单，而不只是写在文档里。**

### 5.6 迁移成本

| 迁移项 | 影响 |
|---|---|
| 幂等键升级 | 需双写过渡（新旧键并存一个版本周期），期间存储翻倍 |
| 存量挂起任务 | 需要迁移到新状态机，或按旧逻辑消费完再切换 |
| 规则迁移 | 现有硬编码规则需改写为决策表形式 |

### 5.7 过度管控的摩擦

对**低频、小额、个人**场景（如给自己账户间转账），完整的规则校验 + 复核流程可能带来不必要的摩擦。建议按 `risk_class` 与金额分层设定校验强度，而不是 R2 一刀切。

---

## 6. 结论与落地优先级

### 6.1 分档判断

| 档位 | 项目 | 判断依据 |
|---|---|---|
| **必做**（不做会出事故或无法验收） | D6、D7、D8（幂等三人组）；D9、D10（唤醒 + 补偿）；D16（规则引擎）；**D18 的最小可用子集** | 前两项修的是**正确性事故**；D9/D10 修的是**断头**；D16 是"可控性优先"的落点；D18 见 6.2 |
| **强烈建议**（高收益、低成本） | D1、D2、D3（二维分型 + 动态升降级）；D4（FSM 规格）；D11、D12（抢占 + 不可逆保护）；D14（摘要保真校验） | 实现成本可控，直接提升分型准确性与安全性 |
| **按需**（有明确代价，需业务方认账） | `fail_mode = closed`（拿可用性换安全）；D15 预算自适应记忆（动态性代价）；D13 熔断（低频业务可能过度） | 代价真实存在，需业务方而非技术方单方面决定 |

### 6.2 对 v2.0 排期的一处修正建议

升级方案 v2.0 把 L0 观测平面整体排在 **P2**。对照分析后我认为这里需要修正：

**L0 是其余所有项验收的前提，不能等到 P2。**

具体地说，P0 的验收标准"同一 `idem_key` 重复副作用次数 = 0"，这个指标本身就是 L0 产生的 —— 没有 L0，P0 就无法验收。

**建议**：把 L0 拆成两个子集

| 子集 | 内容 | 排期 |
|---|---|---|
| **L0-min**（最小可用） | `trace_id` 贯通 + 重复副作用计数器 + 审计日志落盘 | **提前到 P0** |
| **L0-full**（完整平台） | 11 项指标看板 + 版本中心 + 灰度能力 | 保持 P2 |

### 6.3 一句话总结

> 现状与升级后的差别，不是"多做了几件事"，而是**同一个设计意图（可控性优先）从"被声明"变成了"被证明"**。
> 升级的收益集中在正确性与闭环性（这两项不做会出事故），代价集中在复杂度与运维负担（可通过分档落地摊薄）。
> **最需要提防的不是"升级做不完"，而是"升级了但没人认账它的代价"** —— 尤其是 `fail_mode = closed` 这类拿可用性换安全的选择，必须由业务方明确拍板。
