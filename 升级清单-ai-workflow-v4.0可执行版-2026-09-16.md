# ai-workflow v4.0 升级清单（可执行版）

> 日期：2026-09-16 ｜ 类型：施工清单
> **上游依据**：`对比-ai-workflow现状vs-v4.0升级提案-2026-09-16.md`（10 项提案 U1–U10 的原始定义）
> **对象**：`ai-workflow` 技能 v3.2.1（技能根 `~/.workbuddy/skills/ai-workflow/`）
> **本文件性质**：**清单，仍不修改技能本体**。每项给出「改哪个文件的哪一处 + 怎么验收 + 前置依赖 + 在什么范围内生效」，使其可直接作为施工单使用。
>
> **行号口径**：所有行号均为 **2026-09-16 核对 v3.2.1 实测值**（`checks.py` 901 行 / `SKILL.md` 464 行 / `plan-template.yaml` 49 行）。施工前若技能已变动，**行号须重核**——本工作区已有"文档与实现脱节"的前车之鉴。

---

## 0. 怎么用这份清单

三条前提纪律，缺任何一条都不要开工：

| # | 纪律 | 为什么 |
|---|---|---|
| **D1** | **先夹具，后门禁** | 本工作区门禁出问题**绝大多数是误判而非漏判**（引用完整性扁平匹配、`[自研工具]` 字符串假阳性，同一个病）。没有阴性对照夹具，每加一个门禁就是往流程里加一个随机 FAIL 源。**批次 1 必须先于所有升级项完成。** |
| **D2** | **每批独立验证，不混批** | 一次 commit 混入多项改动 → FAIL 时无法定位是哪项引入的。**每批结束必须单独跑一次全量夹具 + `checks.py skill`，全绿才进下一批。** |
| **D3** | **「改一处要同步几处」的清单（§5）逐条对照** | `plan-template.yaml` 的任一字段改动会同时波及 `checks.py` 的两个元组、一处必填键检查与 SKILL.md 正文（§6.4 已识别）。这是本工作区最贵的复发模式。 |

**关于"只取机制不取强制"（§6.1 的结论）**：本清单全部 10 项**不改变技能入口的调用判断**（要不要走本流程仍由模型按最高准则自判），只收紧**技能内部的执行纪律**。SKILL.md 第 16 行已明确"一旦决定走本流程，下面的红线在本流程内不可豁免"——既然不可豁免，就不该只靠模型自律。

---

## 1. 升级项总表

| # | 升级项 | 目的（一句话） | 适用范围 | 落地位置 | 优先级 |
|---|---|---|---|---|---|
| **U1** | 红线③ 机器化 | 交付物越出工作区根 → 机器 FAIL，不再只靠人审 | 凡产出 `plan.yaml` 的任务（L1/L2）；L0 无 plan 不适用 | `checks.py:208-219`、`755-809`；`plan-template.yaml:11` | **P0** |
| **U5** | 不可逆副作用登记 | 熔断时能立刻回答"已经发生了什么不可逆的事" | **仅**含推送/发布/删除/外部写的任务必填；纯本地任务留空 | `plan-template.yaml:9-32`；`SKILL.md:194-203` | **P0** |
| **U2** | 红线① 留痕 | 从"能证明我问了"到"能证明用户放行了" | 全部 L2；L1 可省 | `plan-template.yaml:9-32`；`checks.py:773-775` | P1（**非门禁**，见 §2.3） |
| **U3** | 执行 trace | 复盘四问从"回忆"变"查账" | L2 全记；L1 只记 metrics 不记 trace | `checks.py` 新增子命令 + `SKILL.md:125` | P1 |
| **U4** | 步骤级幂等指纹 | 中断重启不重复烧外部资源 | **仅**标记为昂贵/不可逆的步骤；普通步骤不启用 | `plan-template.yaml:34-49`；`SKILL.md:123` | P1 |
| **U6** | 子代理结论保真校验 | 堵住"约束在浓缩中静默丢失" | **仅**派子代理时适用 | `references/orchestration.md`；`SKILL.md:146-148` | P1 |
| **U10** | 门禁失效时的行为 | 不留空白：不可逆动作 fail-closed，只读放行 | 全部走流程的任务 | `SKILL.md` 新增节；`checks.py` 异常分支 | P1 |
| **U7** | 上下文用量度量 | 自检表出现数字而非"未超量" | L2 阶段 5 自检；**估算值，不作门禁** | `checks.py` 新增子命令；`SKILL.md:199` | P2 |
| **U8** | 任务级指标沉淀 | 复盘四问能用数据回答 | L1 + L2（成本极低）；L0 不适用 | `checks.py`；`SKILL.md:205-214` | P2 |
| **U9** | L1→L2 运行期升级通道 | 层级不再"开工时定一次就锁死" | L1 命中升级条件时 | `SKILL.md:20-30` | P2 |

**依赖关系一览**（施工排期用）：

```
批次 0 边界口径议定 ──► 批次 1 夹具 ──┬──► 批次 2 U1（单独）
                                     └──► 批次 3 U2+U5（同改 meta）
批次 4 U3+U8（共用 jsonl 写入设施）  ← 独立
批次 5 U6+U9+U10（纯文档/SKILL.md）  ← 独立
批次 6 U7                            ← 最低价值，可延后
```

---

## 2. 逐项详案

### 2.1 U1 · 红线③ 机器化（P0）

**目的**：把 SKILL.md 第 18/192 行的"逐条确认落在工作区根内"从**人审要求**变成**机器判据**。这是本清单唯一一项"技能自己承诺过红线不可豁免、代码却没实现"的补课。

**适用范围**：凡产出 `plan.yaml` 的任务（L1/L2）；L0 不建任务目录故不适用。**不影响**技能目录、venv、cache 三个基础设施例外路径。

**落地位置（本次核对新增的精确锚点）**

| 位置 | 现状 | 要改什么 |
|---|---|---|
| `checks.py:208-219` `_candidate_paths()` | `if p.is_absolute(): return [p]`（**绝对路径直接放行，零边界校验**）；相对路径 `return [base / p]` | **两条通道都要补根内校验** |
| `checks.py:252-271` `audit_step()` | 只做 `c.exists()` | 新增"越界"分类，与 `缺失` 并列 |
| `checks.py:795-802` 调用处 | 只遍历 `a["缺失"]` | 新增遍历 `a["越界"]` → `fail(...)` |
| `plan-template.yaml:11` `工作区根` | 已有该字段 | 加注释说明它是越界判定的依据 |
| `SKILL.md:62-76` 工作区边界节 | 纯文字 | 补一句"该约束由 `checks.py plan` 机器校验" |

**⚠️ 本次核对的重要更正**：上一轮描述"只判断存在性、不判断是否越界"是对的，但**不够具体**。实测发现 `_candidate_paths` 已经在 v2.5.2 移除了两条兜底通道（注释原文：「使交付物落在工作区根之外也判 PASS，与红线③冲突」）——**设计者已经意识到了这个问题，但只修了一半**。当前**两条通道都还是漏的**：
- **绝对路径**：`return [p]` 无条件放行 → 交付物写 `D:\别处\x.md` 且文件存在 → 判 OK
- **相对路径**：`base / "../x.md"` → 归一化后逃出根 → 同样判 OK

所以 U1 **不是新增一个判断，而是补完一次未完成的修改**。这个定性影响施工：改动应当**贴合 v2.5.2 注释已表达的意图**，而不是另起一套判定逻辑。

**改动内容**

```
越界判定 = 交付物解析为绝对路径并 resolve() 后
           ∉ 工作区根（须为前缀，且用 is_relative_to 而非字符串 startswith）
           且 ∉ 基础设施例外白名单（见下）
           且 ∉ meta.越界授权 的当次登记项
```

**例外白名单必须由代码做唯一事实源**：

```python
# checks.py 新增常量（与 SKILL.md:68-74 的例外表同源）
WORKSPACE_EXEMPT_PREFIXES = (
    Path.home() / ".workbuddy" / "skills" / "ai-workflow",
    Path.home() / ".workbuddy" / "binaries" / "python" / "envs" / "ai-workflow",
    Path.home() / ".workbuddy" / "cache" / "ai-workflow",
)
```

> 这不是可选的美化。**技能库卫生第 4 条**（`SKILL.md:382`）明确要求"同一物理量的判据必须跨模块同源，且配一条跨模块守卫测试（含阴性对照：把旧实现回滚验一次，确认守卫会红）"。例外表散落在文档与代码两处，就是这个约定要防的分叉。故 SKILL.md 例外表应改为注明"以 `checks.py:WORKSPACE_EXEMPT_PREFIXES` 为准"。

**验收信号（含阴性对照，缺一不可）**

| 类型 | 案例 | 期望 |
|---|---|---|
| **阳性** | 交付物写 `D:\临时\out.md` 且文件**存在** | `checks.py plan` → **FAIL**（当前会 PASS） |
| **阳性** | 交付物写 `../out.md` 且文件存在（相对逃逸） | **FAIL**（当前会 PASS） |
| **阴性** | 交付物写 `tasks/<任务>/报告.md`（根内相对） | **OK**，不误红 |
| **阴性** | 交付物是例外路径下的文件（如技能本体 `.py`） | **OK**，不误红 |
| **阴性** | `meta.越界授权` 已登记该项 + 用户原话摘要 | **OK**（合法越界放行） |
| **阴性** | Windows 大小写混写、`/` 与 `\` 混用 | **OK**，不误红 |

**前置依赖**：批次 0（边界口径议定）+ 批次 1（夹具）。**必须单独一批施工**（见 §3 理由）。

**成本/风险**：实现量**小**；风险**中**——唯一有假阳性风险的项，全部风险集中在边界处理（见 §4）。

---

### 2.2 U5 · 不可逆副作用登记（P0）

**目的**：现状只有"执行前单独确认"（`SKILL.md:202`），**没有"执行后清单"**。熔断五步的第 2 步是"冻"（`SKILL.md:420`），但熔断时回答不了"**已经发生了什么不可逆的事**"——而这恰恰是决定"能不能回滚、要不要通知对方"的唯一依据。

**适用范围**：**仅**含推送远端 / 发布上线 / 删改数据 / 写生产配置 / 对外发送的任务**必填**；纯本地读写任务该字段留空（**不强制填空**——与 `meta.决策记录` 同口径，强制填空会退化成走过场）。

**落地位置**：`plan-template.yaml:9-32`（meta 区新增 `不可逆副作用: []`）+ `SKILL.md:194-203`（阶段 5 交付前核对）。

**改动内容**

```yaml
  不可逆副作用: []      # 【可选，含不可逆动作的任务必填】执行后追加，追加不覆盖
                        #   每项：{时间, 动作, 目标, 可否回滚, 回滚方式}
                        #   「可否回滚」只允许 可回滚/不可回滚/未知 —— 未知必须显式写，
                        #   不得用空值表示"应该没问题"
```

**校验口径**：字段**可选**，一旦填写必须合法（沿用 `_check_autonomy` / `_check_selftools` 的既有口径：不强制填空，填了就不能糊弄）。**不得**把它做成"非空才 PASS"的门禁——那会逼模型编造。

**验收信号**

| 类型 | 案例 | 期望 |
|---|---|---|
| 阳性 | 含推送的任务，`不可逆副作用` 为空 | **SKIP**（提示需人工确认，不 FAIL——无法机器判断"是否该有副作用"） |
| 阳性 | 登记项缺 `可否回滚` | **FAIL** |
| 阴性 | 纯本地任务该字段留空 | **OK** |

**前置依赖**：批次 0/1（夹具）。与 U2 同批（同改 meta 区）。

**成本/风险**：**低**。无假阳性风险（判定对象是自己填的字段格式，不是路径匹配）。

---

### 2.3 U2 · 红线① 留痕（P1，**非门禁**）

**目的**：现状能证明"我提问了"，**不能证明"用户放行了"**——`PLAN_META_REQUIRED`（`checks.py:71`）只有 `("任务", "验证信号", "工作区根")` 三项，**没有确认状态字段**（这是本轮核对的原话）。

**适用范围**：L2 全量；L1 可省（`SKILL.md:25` 已规定 L1 命中模板即跳过追问循环，无独立放行环节）。

**⚠️ 必须诚实标注（不可省）**：`用户放行` 字段**做不到真正的机器强制** —— 模型可以自己填。它只能提高伪造成本（要求写入**用户原话摘要** + 时间），属"纪律项 + 低成本留痕"。**不得宣称它是门禁。**

这条要求的依据在技能自己身上：`SKILL.md:393` 写着「**写在提示里的熔断不是熔断，是建议**」。同理，一个可自填的字段不是门禁。**字段注释里必须原样写下这句话**，否则就是自欺。

**落地位置**：`plan-template.yaml:9-32`；`checks.py:773-775`（若要加非空校验）。

**验收信号**：字段非空（**仅此而已**，不得声称更多）。若选择在 `check_plan` 里加非空校验，须同时接受"模型可自填"这一事实，并在 SKILL.md 写明其非门禁性质。

**前置依赖**：批次 1。与 U5 同批。

**成本/风险**：**低**（实现）。**中**（语义）——若标注不诚实，会造出"看起来有门禁其实没有"的假安全感，比不做更糟。

---

### 2.4 U3 · 执行 trace（P1）

**目的**：`plan.yaml` 记的是**离散状态点**（做到哪了 / 为什么引入 / 为什么变了），**不含时序**。导致阶段 6 的复盘四问（`SKILL.md:211`"哪个阶段返工最多"）只能靠回忆。

**适用范围**：**L2 全记；L1 不记 trace**（§6.3 的缓解措施：短任务记 trace 是纯开销）；L0 不适用。

**落地位置**：`checks.py`（新子命令 + 写入函数，注册在 `812-856` 的 argparse 区、分发在 `858-878`）+ `SKILL.md:125`（阶段 3 第 3 条"逐步执行"）。

**改动内容**：`tasks/<任务>/trace.jsonl`，每步一行：

```json
{"ts":"2026-09-16 11:20","step":"3","action":"...","cmd":"...","result":"...","elapsed_s":42}
```

新增 `checks.py trace <plan> --step <id> --action ... --cmd ... --result ...`，与 `mark`/`decide` 同风格（**追加不覆盖**，避免手工编辑）。

**⚠️ 与 `_candidate_paths` 的交互**：`trace.jsonl` 会被 plan.yaml 的 `交付物` 字段引用吗？**不应引用**——它是留痕不是交付物。若引用，`_is_file_like()`（`checks.py:222-223`，匹配 `\.[A-Za-z0-9]{1,5}$`）会命中 `.jsonl`，然后按存在性判定。**约定：trace/metrics 不进 `交付物` 字段**，须写进 SKILL.md 防误用。

**验收信号**：任务完成后可**回放出完整时序**（阴性对照：缺 trace 的任务，`checks.py trace --replay` 应明确报"无记录"而非静默通过）。

**前置依赖**：批次 1。与 U8 同批（共用 jsonl 追加写入设施）。

**成本/风险**：实现量**中**（新子命令 + 明确的写入时机）。风险**低**。

---

### 2.5 U4 · 步骤级幂等指纹（P1）

**目的**：现状幂等**只在工具层**（`publish_tools.py init`、`push --apply` 内容一致则 SKIP）。**步骤层没有**：会话中断重启后，若某步被误判为未完成而重跑，会重复调外部 API、重复生成文件、重复推送。

**适用范围**：**仅**显式标记为"昂贵/不可逆"的步骤启用（按需，**不是每步**）。理由：给每步算指纹会给普通读写步骤平白增加开销，而它本来就便宜到重跑无所谓。

**落地位置**：`plan-template.yaml:34-49`（steps 区新增两个可选键）+ `SKILL.md:123`（阶段 3 第 1 条）。

**改动内容**

```yaml
    输入指纹: ""        # 【可选，仅昂贵/不可逆步骤填】sha256(输入文件摘要集合 ‖ 规范化参数)
    产物指纹: ""        # 【可选】sha256(交付物内容)；重跑时两者皆一致 → SKIP 并在 trace 记 [SKIP] 指纹一致
```

**关键设计约束**：指纹的**真正价值在"输入"**，不在产物。仅比对产物指纹是危险的——**同样内容的产物可能由不同输入产生**。所以两者**必须成对**：只比产物 → 判 SKIP 可能吞掉一次本该发生的执行。

**验收信号**：中断重启后同步骤**不重复调外部资源**（阴性对照：故意改动输入文件 → 指纹变化 → **必须不 SKIP**，确认不会误跳过）。

**前置依赖**：批次 1。与 U2/U5 同批改 `plan-template.yaml`（**避免同一文件改两轮**，见 §5）。

**成本/风险**：实现量**中**（指纹算法须按步骤类型定义）；风险**中**（"误 SKIP"会静默吞掉本该执行的动作，属危险方向）。

---

### 2.6 U6 · 子代理结论保真校验（P1）

**目的**：`SKILL.md:152` 要求子代理产出"未经核实不得进入下一环节（**优先抽查可复现硬数据**）"。但核实对象是**硬数据**，**不覆盖"关键约束是否在浓缩中丢失"**——子代理回传时漏掉一句"仅限 X 目录"，主代理就会基于不完整信息决策，**且没有任何机制会发现**。

**适用范围**：**仅**派子代理时适用（`SKILL.md:146-148` "独立才并行"的场景）。

**落地位置**：`references/orchestration.md`（Handoff 六字段 → 七字段）+ `SKILL.md:146-148`（子智能体编排节）。

**改动内容**：Handoff 增 `must_keep`——主代理在派活时**显式指定**必须原样保留的约束/数字/路径（如"工作区根""不得越界""阈值 0.05"）；回传后主代理**逐项校验**。校验不过 → 退回重跑（或按返修复查流程）。

**验收信号（阳性/阴性成对）**

| 类型 | 案例 | 期望 |
|---|---|---|
| 阳性 | 故意让子代理丢一个 `must_keep` 项 | **必须被发现** |
| 阴性 | 子代理完整保留全部 `must_keep` 项 | **不误报** |

**前置依赖**：无（纯文档 + 流程）。可与 U9/U10 同批。

**成本/风险**：**低**。

---

### 2.7 U10 · 门禁失效时的行为定义（P1）

**目的**：`checks.py` **异常时无任何规定**。现状 `_require_yaml()`（`226-236`）在缺依赖时以退出码 2 退出并给指引——但**没有规定"门禁跑不起来时任务该怎么办"**。这是 fail-open 的隐患：门禁不可用 = 没人拦。

**适用范围**：全部走流程的任务。

**落地位置**：`SKILL.md` 新增小节（建议并入"熔断机制"章节之前）；`checks.py` 异常分支（可选加固）。

**改动内容**（一句话原则，按动作类型分开）：

```
门禁不可用时：
  ├─ 不可逆动作（推送/发布/删除/外部写）→ fail-closed，拒绝执行，升级用户
  └─ 只读动作 → 放行，但须在交付说明中显式标注"本次未经 <门禁> 校验"
```

**关键约束**：**必须显式写出，不留空白**。留白的默认行为是 fail-open（继续干），而 fail-open 在不可逆动作上等于没有门禁。同时**必须与既有口径一致**：`SKILL.md:415` 已列出"不构成熔断"的情形（单次工具报错未耗尽重试），U10 **不得**与之冲突——门禁不可用若属"环境类"且已耗尽重试，则按 **F3**（`SKILL.md:411`）触发熔断，而非另立一套。

**验收信号**：文档中存在明确条文；**阴性对照**：模拟 `checks.py` 不可用（venv 缺 pyyaml）→ 含推送的任务**不得**静默继续。

**前置依赖**：无。可与 U6/U9 同批。**但若 U1/U3 已落地，U10 的条文须覆盖新门禁**——故建议排在其后。

**成本/风险**：**低**。

---

### 2.8 U7 · 上下文用量度量（P2）

**目的**：`SKILL.md:199` 自检项有"references 按需加载（未预读全量 / 主上下文未超量）"，但**靠人审**；`SKILL.md:128` 的"压缩 40-60%"是**目标值不是度量**。

**适用范围**：L2 阶段 5 自检。**估算值，不得作为门禁。**

**落地位置**：`checks.py` 新增轻量估算子命令；`SKILL.md:199` 自检表。

**改动内容**：按**字符数折算**估算 token（中英分别计权），写进阶段 5 自检表。

**⚠️ 诚实标注**：折算误差显著，**它解决的是"从无数字到有数字"，不是"精确控制"**。文档里必须写"估算值"。**不得**用它做门禁（估算误差 + 门禁 = 假阳性源，与 D1 冲突）。

**验收信号**：自检表出现数字而非"未超量"。

**前置依赖**：无。**价值最低，可延后或不做**（见 §6）。

---

### 2.9 U8 · 任务级指标沉淀（P2）

**目的**：现状**没有任何任务级统计**——返修轮次分布、熔断触发频次、抽查豁免比例、门禁 FAIL 率、阶段耗时，一个都没有。导致**技能自身演进缺数据依据**（工作区那份《工作流瓶颈分析与优化方案》就是人工翻 10 个 plan.yaml 写出来的）。

**适用范围**：L1 + L2（写入成本极低）；L0 不适用。

**落地位置**：`checks.py`（可与 U3 共用 jsonl 追加设施）；`SKILL.md:205-214`（阶段 6 第 5 条之后）。

**改动内容**：`tasks/_metrics.jsonl`，每任务一行：

```json
{"task":"...","level":"L2","date":"2026-09-16","revisions":1,"fused":false,
 "gate_fail":0,"audit_exempt":true,"steps":7,"elapsed_s":3600}
```

**位置选择的理由**：放 `tasks/_metrics.jsonl` 而非工作区根——`cmd_status`（`checks.py:287`）已按 `not d.name.startswith("_")` 跳过下划线开头的**目录**，与 `_archive/` 同层的下划线文件语义一致，且不会被误当任务目录。**注意它是文件不是目录**，故 `status` 的目录过滤对它无影响。

**验收信号**：阶段 6 复盘四问能用数据回答（**阴性对照**：无 metrics 的旧任务，统计脚本应报"无记录"而非算作 0）。

**前置依赖**：批次 1。与 U3 同批。

**成本/风险**：**低**。

---

### 2.10 U9 · L1→L2 运行期升级通道（P2）

**目的**：`SKILL.md:20-30` 的任务分层是**开工时判定一次就锁死**。L1 命中模板开工后，若实际产出规模超出模板预期（如"改一个字段"变成跨 8 个文件），现状**没有回退到 L2 的机制**。

**适用范围**：L1 命中以下任一升级条件时：① 模板不适用 ② 产出规模超预期 ③ 出现跨文件改造。

**落地位置**：`SKILL.md:20-30`（任务分层节）。

**改动内容**：命中条件 → 升级为 L2、**补建** `plan.yaml`、留痕升级事件。

**⚠️ 留痕载体须施工时议定**：升级事件应记入 `meta.计划修订`（`checks.py:844-848` 的 `revise`），但其 `触发` 字段受 `VALID_TRIGGER = ("R1"…"R6")`（`checks.py:79`）约束——**具体用哪个 R 编号，施工时必须对照 `references/adaptive-planning.md` 的 R1–R6 定义选定，不得自行编号**（本轮未读该手册，不预设）。若无匹配项，则需新增编号并在 `references/adaptive-planning.md` 与 `checks.py:79` **同步**（又一处"改一处同步两处"）。

**验收信号**：升级事件在 `plan.yaml` 中可见。

**前置依赖**：无。可与 U6/U10 同批。

**成本/风险**：**低**（文档 + 流程）。

---

## 3. 建议执行顺序

### 3.1 排期原则（为什么不是简单按 P0→P2）

1. **夹具是前置，不是配套** —— 缺它则 U1 的边界处理无法验证，而 U1 恰是唯一有假阳性风险的项（§7.3 的纪律）。
2. **U1 单独一批** —— 它是唯一会在 FAIL 时引起"是越界判定错了，还是别处引入的"归因歧义的项。与其他改动混批 = 自己给自己制造排查困难。
3. **同文件同批** —— U2/U5/U4 都改 `plan-template.yaml`；U3/U8 都改 `checks.py` 并共用 jsonl 写入。**分两轮改同一文件是"改一处忘一处"的高发场景**。
4. **纯文档项后置** —— SKILL.md 类改动不阻塞代码项，且 U10 的条文需要覆盖 U1/U3 的新门禁，应先让代码定型。
5. **`checks.py skill` 每批必跑** —— 它含 `py_compile` 与「plan-template 必填键」（`checks.py:174-178`），能自动抓到字段改动引发的连带 FAIL。

### 3.2 批次表

| 批次 | 内容 | 出口条件（全绿才进下批） |
|---|---|---|
| **0** | **边界口径议定**（§4 六项逐条拍板）+ 确认例外白名单由代码做唯一事实源 | §4 全部有结论，无"待定" |
| **1** | **最小对照夹具**：`tasks/_fixtures/` 下 5 个 plan.yaml（阳性 2 / 阴性 3）+ 1 个跑批脚本（零依赖，直接调 `checks.py plan`）+ 一个"全绿/有红"判定输出 | 夹具**当前**跑出的结果与预期一致（即：U1 的两条阳性案例现在**故意是绿的**——这正是它存在的意义） |
| **2** | **U1 单独**：`_candidate_paths` 两分支补边界校验 + `audit_step` 增"越界"分类 + `WORKSPACE_EXEMPT_PREFIXES` 常量 + SKILL.md 例外表改为指向代码 | ① `checks.py skill` 全绿 ② 夹具中 U1 两条阳性**转红**、三条阴性**保持绿** ③ 旧任务 plan.yaml **零回归**（拿真实历史 plan 重跑，不得凭空新增 FAIL） |
| **3** | **U5 + U2 + U4**（同改 `plan-template.yaml`，一次改完） | `checks.py skill` 全绿；§5 的四处同步点逐条核对；U4 阴性对照（改输入 → 不 SKIP）通过 |
| **4** | **U3 + U8**（同改 `checks.py`，共用 jsonl 追加设施） | trace 可回放；旧任务 `status` 输出不变；`.jsonl` 不出现在 `交付物` 字段的误用检查通过 |
| **5** | **U6 + U9 + U10**（纯文档/SKILL.md） | `checks.py skill` 的「文档引用完整性」不得新增 FAIL（**新增任何被反引号写的路径必须真实存在**）；SKILL.md 未超量膨胀（见 §6 末） |
| **6** | **U7**（可选） | 自检表出现数字；文档明确标注"估算值、非门禁" |

### 3.3 每批的固定动作（不可省）

```
1. 施工前：git status 确认工作区干净（避免把无关改动混进本批）
2. 施工中：只做本批范围内的改动，不顺手改别的
3. 施工后：
   a. checks.py skill                    ← 技能自检（含 py_compile + 必填键）
   b. 全量夹具（含历史阴性对照）
   c. 拿 2-3 个真实历史 plan.yaml 重跑 checks.py plan  ← 防"新门禁误伤旧任务"
   d. §5 影响面矩阵逐条对照
4. 本批未全绿 → 停下修，不得带 FAIL 进入下一批
```

**（c）是本清单新增的一条纪律**，不在原始提案里：**新门禁最可能的破坏方式不是漏判新案例，而是误伤历史任务**。夹具覆盖不到真实数据的多样性（相对/绝对路径混写、带括号注释、花括号批量、历史遗留写法），只有真跑旧 plan 才暴露。

---

## 4. U1 施工前必须议定的边界口径

上一轮已标注这是"最容易出假阳性的地方"。以下是**必须逐条拍板**的清单——任何一条留作"施工时再说"，都会变成 §6.2 所述的假阳性源：

| # | 边界情形 | 待定问题 | 建议口径 |
|---|---|---|---|
| **B1** | Windows 大小写不敏感 | `E:\Work` 与 `e:\work` 是否算同一根？ | 归一化后**大小写不敏感比较**（Windows 实情），但 Linux 上须敏感 |
| **B2** | 分隔符混用 | `E:/ChatGPT/工作流` vs `E:\ChatGPT\工作流` | `Path.resolve()` 归一化后比较，**不做字符串比较** |
| **B3** | `..` 逃逸 | `base / "../x.md"` | `resolve()` 后判定 → **认定为越界**（当前会漏） |
| **B4** | 符号链接 | 根内的 symlink 指向根外 | **须 resolve 后判定**，否则 symlink 是绕过通道 |
| **B5** | 基础设施例外 | 三个例外路径 | 由 `WORKSPACE_EXEMPT_PREFIXES` 常量做**唯一事实源**；SKILL.md 指向它 |
| **B6** | 历史遗留写法 | 已存在的 plan.yaml 里各种路径写法 | **兼容**：不得因新门禁让历史任务凭空 FAIL（§3.3-c 验的就是这条） |
| **B7** | 根即是盘的根 | `工作区根` = `E:\` | 前缀判定须处理"根本身就是根"的退化情形 |
| **B8** | 空/未填 `工作区根` | `PLAN_META_REQUIRED`（`checks.py:71`）已强制非空，故不会出现 | 但须确认**判定时不再兜底为 cwd**——否则会拿运行目录当根，产生随机结果 |

**B8 值得单独强调**：`checks.py:821` 的 `--base` 默认值是 `Path.cwd()`。若越界判定依赖它而调用方忘传 `--base`，判定基准会退化成"运行时所在目录"→ **结果随机**（在根内跑一次绿、在根外跑一次红）。**这是最阴险的一类假阳性：同一份 plan.yaml 因运行位置不同而结论不同。**

缓解：越界判定**必须读 `meta.工作区根`**，`--base` 只用于相对路径解析；两者不一致时**显式警告**，不静默取其一。

---

## 5. 影响面矩阵（改一处要同步几处）

`plan-template.yaml` 的字段不是孤立数据——它被代码与文档共 4 处引用：

| 触发改动 | 必须同步的 4 处 | 漏改的后果 |
|---|---|---|
| 新增 meta 字段（U2/U5） | ① `plan-template.yaml:9-32` ② `checks.py:71` `PLAN_META_REQUIRED`（若须强制） ③ 新 `_check_*` 函数 + 在 `checks.py:806-808` 注册 ④ `SKILL.md` 阶段 3 第 1 条（`123` 行） | 字段不被校验（②③漏）或文档描述与模板不一致（④漏） |
| 新增 step 字段（U4） | ① `plan-template.yaml:34-49` ② `checks.py:72` `PLAN_STEP_REQUIRED` ③ 校验函数 ④ `SKILL.md:123` | 同上 |
| 新增技能内文件（新 `.py`/`.md`） | ① 文件真实落地 ② 若被 SKILL.md/references 反引号引用 → **必须存在**，否则「文档引用完整性」FAIL（`checks.py:145-163`）③ 若是自研工具 → `meta.自研工具` 登记 + 标注头 + 推送 | 技能自检 FAIL |
| 例外白名单改动（U1/B5） | ① `checks.py:WORKSPACE_EXEMPT_PREFIXES` ② `SKILL.md:68-74` 例外表 ③ 守卫测试（技能库卫生第 4 条） | 文档与实现分叉 → 静默放大 |

**⚠️ 一条反直觉的注意**：`checks.py:174-178` 检查的是 plan-template **是否包含** `("任务", "验证信号", "步骤", "交付物")` **四个字符串**（`k not in text`，**子串包含判定，不是键名解析**）。这意味着**新增字段不会导致此检查失败**（宽松），但也意味着**它抓不到"字段写错位置"**。所以它不能作为 U2/U4 改动的验收依据——仍需人工对照 §5 第 ①②③④ 四条。

---

## 6. 明确不做的事（防过度工程）

| 不做 | 理由 |
|---|---|
| **不照搬三层架构方案的强制式机制**（`fail_mode=closed` 全量、规则引擎、Saga） | `SKILL.md:41` 已明确"只取机制、不取强制"；且 ai-workflow 的管控对象是**一个项目**（失败可重来），不是**一次请求**（资金损失）。v4.0 全部 10 项**不改变入口调用判断** |
| **不动熔断机制** | `_check_fuse`（`checks.py:474-517` 区段）已是本技能工程质量最高部分：fail-closed、防"写进代码块绕过"、防"前缀粘连绕过"、复位两步缺一不可。**没有改进空间，改动只会有损害风险** |
| **不动独立审核 / 证据分级 / 能力路由 / 自研工具留痕** | 这四项**已经超过**三层架构方案（§3.2 的公正对比）。"升级"它们＝退化 |
| **不给 U7 做精确 token 计数** | 精确计数需引入 tokenizer 依赖 → 违反技能"免费优先/零新依赖"倾向，且收益仅是一个自检数字 |
| **不让 U2 变成门禁** | 可自填的字段不是门禁（`SKILL.md:393` 自己的原则）。假装它是，比不做更糟 |
| **不扩写 SKILL.md 正文承载全部细则** | SKILL.md 已 464 行且每轮注入。**新增内容默认写进 `references/`，SKILL.md 只加一句指针**——这是既有惯例（`SKILL.md:34`"两份手册按需加载，勿预读"） |

---

## 7. 总判断与建议的下一步

### 7.1 一句话定性

**v4.0 的 10 项，本质是同一件事：「把已有的红线从被声明变成被机器判定」。** 措辞不重要，重要的是 U1 这一项——SKILL.md 第 18 行写了"三条红线在本流程内不可豁免"，而其中两条（①③）目前**零行代码**。既然写了"不可豁免"，代码就不该缺席。

### 7.2 不夸大的收益预期

| 能拿到 | 拿不到 |
|---|---|
| 红线③ 越界可机器 FAIL | 红线① 的真机器强制（**做不到**，只能留痕） |
| 中断重启不重复烧外部资源 | 所有任务都自动变轻（**升级只在高风险路径付成本**） |
| 熔断时能立刻答"已发生什么不可逆" | 自动回滚（无 Saga，不可逆动作仍只能标记 + 人工） |
| 复盘从"回忆"变"查账" | 精确实时 token 计量（只有估算） |

### 7.3 建议的下一步（按顺序）

1. **先做批次 0**：把 §4 的 B1–B8 逐条拍板——这是唯一需要用户参与的决策点；其余批次可自主施工。
2. **再做批次 1**：夹具必须**先于 U1** 建立，且第一个动作是"确认 U1 的两条阳性案例当前是绿的"——**先把缺口钉死在证据上，再修**。这个顺序不能反：反过来就是"改了代码再找案例证明它对"，无法排除自证偏差。
3. **本清单不构成施工授权**：按 `SKILL.md:16` 与红线①②，改动技能本体属独立任务——须先落 `plan.yaml`（含工作区根、六要素、验收信号），走完阶段 0–6，**并把本清单当作阶段 2 的候选方案之一**，而非既成决议。
4. **施工属于"技能生成所需的越界"**（`USER.md` 硬规矩第 2 条豁免）：改 `~/.workbuddy/skills/ai-workflow/` 是功能实现的必要条件，**无需另申请越界授权**；但若涉及删改既有文件/批量递归，仍按高风险先确认。

### 7.4 交付物与免责

**本文件是清单，未修改技能本体。** 文中所有结论均来自 **2026-09-16 对 v3.2.1 源码的实测核对**（`checks.py` 901 行 / `SKILL.md` 464 行 / `plan-template.yaml` 49 行），行号已随文标注供复核。

**上游**：`对比-ai-workflow现状vs-v4.0升级提案-2026-09-16.md`（提案定义与代价分析）
**附件**：`Agent三层架构升级方案-2026-09-16.md`（v2.0，三轮前的对象，与本清单**无技术继承关系**——§6 已说明为何不照搬）
